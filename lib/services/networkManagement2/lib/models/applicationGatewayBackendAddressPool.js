/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApplicationGatewayBackendAddressPool class.
 * @constructor
 * Backend Address Pool of application gateway
 * @member {string} [name] Gets name of the resource that is unique within a
 * resource group. This name can be used to access the resource
 * 
 * @member {string} [etag] A unique read-only string that changes whenever the
 * resource is updated
 * 
 * @member {array} [backendIPConfigurations] Gets or sets
 * backendIPConfiguration of application gateway
 * 
 * @member {array} [backendAddresses] Gets or sets the backend addresses
 * 
 * @member {string} [provisioningState] Gets or sets Provisioning state of the
 * backend address pool resource Updating/Deleting/Failed
 * 
 */
function ApplicationGatewayBackendAddressPool(parameters) {
  ApplicationGatewayBackendAddressPool['super_'].call(this, parameters);
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.etag !== undefined) {
      this.etag = parameters.etag;
    }
    if (parameters.backendIPConfigurations) {
      var tempParametersbackendIPConfigurations = [];
      parameters.backendIPConfigurations.forEach(function(element) {
        if (element) {
          element = new models['SubResource'](element);
        }
        tempParametersbackendIPConfigurations.push(element);
      });
      this.backendIPConfigurations = tempParametersbackendIPConfigurations;
    }
    if (parameters.backendAddresses) {
      var tempParametersbackendAddresses = [];
      parameters.backendAddresses.forEach(function(element1) {
        if (element1) {
          element1 = new models['ApplicationGatewayBackendAddress'](element1);
        }
        tempParametersbackendAddresses.push(element1);
      });
      this.backendAddresses = tempParametersbackendAddresses;
    }
    if (parameters.provisioningState !== undefined) {
      this.provisioningState = parameters.provisioningState;
    }
  }    
}

util.inherits(ApplicationGatewayBackendAddressPool, models['SubResource']);

/**
 * Validate the payload against the ApplicationGatewayBackendAddressPool schema
 *
 * @param {JSON} payload
 *
 */
ApplicationGatewayBackendAddressPool.prototype.serialize = function () {
  var payload = ApplicationGatewayBackendAddressPool['super_'].prototype.serialize.call(this);
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['name'] = this['name'];
  }

  if (this['etag'] !== null && this['etag'] !== undefined) {
    if (typeof this['etag'].valueOf() !== 'string') {
      throw new Error('this[\'etag\'] must be of type string.');
    }
    payload['etag'] = this['etag'];
  }

  if (util.isArray(this['backendIPConfigurations'])) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['backendIPConfigurations'] = [];
    for (var i = 0; i < this['backendIPConfigurations'].length; i++) {
      if (this['backendIPConfigurations'][i]) {
        payload['properties']['backendIPConfigurations'][i] = this['backendIPConfigurations'][i].serialize();
      }
    }
  }

  if (util.isArray(this['backendAddresses'])) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['backendAddresses'] = [];
    for (var i1 = 0; i1 < this['backendAddresses'].length; i1++) {
      if (this['backendAddresses'][i1]) {
        payload['properties']['backendAddresses'][i1] = this['backendAddresses'][i1].serialize();
      }
    }
  }

  if (this['provisioningState'] !== null && this['provisioningState'] !== undefined) {
    if (typeof this['provisioningState'].valueOf() !== 'string') {
      throw new Error('this[\'provisioningState\'] must be of type string.');
    }
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['provisioningState'] = this['provisioningState'];
  }

  return payload;
};

/**
 * Deserialize the instance to ApplicationGatewayBackendAddressPool schema
 *
 * @param {JSON} instance
 *
 */
ApplicationGatewayBackendAddressPool.prototype.deserialize = function (instance) {
  ApplicationGatewayBackendAddressPool['super_'].prototype.deserialize.call(this, instance);
  if (instance) {
    if (instance['name'] !== undefined) {
      this['name'] = instance['name'];
    }

    if (instance['etag'] !== undefined) {
      this['etag'] = instance['etag'];
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['backendIPConfigurations']) {
        var tempInstancepropertiesbackendIPConfigurations = [];
        instance['properties']['backendIPConfigurations'].forEach(function(element2) {
          if (element2) {
            element2 = new models['SubResource']().deserialize(element2);
          }
          tempInstancepropertiesbackendIPConfigurations.push(element2);
        });
        this['backendIPConfigurations'] = tempInstancepropertiesbackendIPConfigurations;
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['backendAddresses']) {
        var tempInstancepropertiesbackendAddresses = [];
        instance['properties']['backendAddresses'].forEach(function(element3) {
          if (element3) {
            element3 = new models['ApplicationGatewayBackendAddress']().deserialize(element3);
          }
          tempInstancepropertiesbackendAddresses.push(element3);
        });
        this['backendAddresses'] = tempInstancepropertiesbackendAddresses;
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['provisioningState'] !== undefined) {
        this['provisioningState'] = instance['properties']['provisioningState'];
      }
    }
  }

  return this;
};

module.exports = ApplicationGatewayBackendAddressPool;
