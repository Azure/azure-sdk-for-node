/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApplicationGatewayUrlPathMap class.
 * @constructor
 * UrlPathMap of application gateway
 * @member {string} [name] Gets name of the resource that is unique within a
 * resource group. This name can be used to access the resource
 * 
 * @member {string} [etag] A unique read-only string that changes whenever the
 * resource is updated
 * 
 * @member {object} [defaultBackendAddressPool] Gets or sets default backend
 * address pool resource of URL path map
 * 
 * @member {string} [defaultBackendAddressPool.id] Resource Id
 * 
 * @member {object} [defaultBackendHttpSettings] Gets or sets default backend
 * http settings resource of URL path map
 * 
 * @member {string} [defaultBackendHttpSettings.id] Resource Id
 * 
 * @member {array} [pathRules] Gets or sets path rule of URL path map resource
 * 
 * @member {string} [provisioningState] Gets or sets Provisioning state of the
 * backend http settings resource Updating/Deleting/Failed
 * 
 */
function ApplicationGatewayUrlPathMap(parameters) {
  ApplicationGatewayUrlPathMap['super_'].call(this, parameters);
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.etag !== undefined) {
      this.etag = parameters.etag;
    }
    if (parameters.defaultBackendAddressPool) {
      this.defaultBackendAddressPool = new models['SubResource'](parameters.defaultBackendAddressPool);
    }
    if (parameters.defaultBackendHttpSettings) {
      this.defaultBackendHttpSettings = new models['SubResource'](parameters.defaultBackendHttpSettings);
    }
    if (parameters.pathRules) {
      var tempParameterspathRules = [];
      parameters.pathRules.forEach(function(element) {
        if (element) {
          element = new models['ApplicationGatewayPathRule'](element);
        }
        tempParameterspathRules.push(element);
      });
      this.pathRules = tempParameterspathRules;
    }
    if (parameters.provisioningState !== undefined) {
      this.provisioningState = parameters.provisioningState;
    }
  }    
}

util.inherits(ApplicationGatewayUrlPathMap, models['SubResource']);

/**
 * Validate the payload against the ApplicationGatewayUrlPathMap schema
 *
 * @param {JSON} payload
 *
 */
ApplicationGatewayUrlPathMap.prototype.serialize = function () {
  var payload = ApplicationGatewayUrlPathMap['super_'].prototype.serialize.call(this);
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['name'] = this['name'];
  }

  if (this['etag'] !== null && this['etag'] !== undefined) {
    if (typeof this['etag'].valueOf() !== 'string') {
      throw new Error('this[\'etag\'] must be of type string.');
    }
    payload['etag'] = this['etag'];
  }

  if (this['defaultBackendAddressPool']) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['defaultBackendAddressPool'] = this['defaultBackendAddressPool'].serialize();
  }

  if (this['defaultBackendHttpSettings']) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['defaultBackendHttpSettings'] = this['defaultBackendHttpSettings'].serialize();
  }

  if (util.isArray(this['pathRules'])) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['pathRules'] = [];
    for (var i = 0; i < this['pathRules'].length; i++) {
      if (this['pathRules'][i]) {
        payload['properties']['pathRules'][i] = this['pathRules'][i].serialize();
      }
    }
  }

  if (this['provisioningState'] !== null && this['provisioningState'] !== undefined) {
    if (typeof this['provisioningState'].valueOf() !== 'string') {
      throw new Error('this[\'provisioningState\'] must be of type string.');
    }
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['provisioningState'] = this['provisioningState'];
  }

  return payload;
};

/**
 * Deserialize the instance to ApplicationGatewayUrlPathMap schema
 *
 * @param {JSON} instance
 *
 */
ApplicationGatewayUrlPathMap.prototype.deserialize = function (instance) {
  ApplicationGatewayUrlPathMap['super_'].prototype.deserialize.call(this, instance);
  if (instance) {
    if (instance['name'] !== undefined) {
      this['name'] = instance['name'];
    }

    if (instance['etag'] !== undefined) {
      this['etag'] = instance['etag'];
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['defaultBackendAddressPool']) {
        this['defaultBackendAddressPool'] = new models['SubResource']().deserialize(instance['properties']['defaultBackendAddressPool']);
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['defaultBackendHttpSettings']) {
        this['defaultBackendHttpSettings'] = new models['SubResource']().deserialize(instance['properties']['defaultBackendHttpSettings']);
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['pathRules']) {
        var tempInstancepropertiespathRules = [];
        instance['properties']['pathRules'].forEach(function(element1) {
          if (element1) {
            element1 = new models['ApplicationGatewayPathRule']().deserialize(element1);
          }
          tempInstancepropertiespathRules.push(element1);
        });
        this['pathRules'] = tempInstancepropertiespathRules;
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['provisioningState'] !== undefined) {
        this['provisioningState'] = instance['properties']['provisioningState'];
      }
    }
  }

  return this;
};

module.exports = ApplicationGatewayUrlPathMap;
