/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var util = require('util');

/**
 * @class
 * Initializes a new instance of the NetworkInterfaceDnsSettings class.
 * @constructor
 * Dns Settings of a network interface
 * @member {array} [dnsServers] Gets or sets list of DNS servers IP addresses
 * 
 * @member {array} [appliedDnsServers] Gets or sets list of Applied DNS
 * servers IP addresses
 * 
 * @member {string} [internalDnsNameLabel] Gets or sets the Internal DNS name
 * 
 * @member {string} [internalFqdn] Gets or sets full IDNS name in the form,
 * DnsName.VnetId.ZoneId.TopleveSuffix. This is set when the NIC is
 * associated to a VM
 * 
 */
function NetworkInterfaceDnsSettings(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.dnsServers) {
      var tempParametersdnsServers = [];
      parameters.dnsServers.forEach(function(element) {
        if (element !== undefined) {
          element = element;
        }
        tempParametersdnsServers.push(element);
      });
      this.dnsServers = tempParametersdnsServers;
    }
    if (parameters.appliedDnsServers) {
      var tempParametersappliedDnsServers = [];
      parameters.appliedDnsServers.forEach(function(element1) {
        if (element1 !== undefined) {
          element1 = element1;
        }
        tempParametersappliedDnsServers.push(element1);
      });
      this.appliedDnsServers = tempParametersappliedDnsServers;
    }
    if (parameters.internalDnsNameLabel !== undefined) {
      this.internalDnsNameLabel = parameters.internalDnsNameLabel;
    }
    if (parameters.internalFqdn !== undefined) {
      this.internalFqdn = parameters.internalFqdn;
    }
  }    
}


/**
 * Validate the payload against the NetworkInterfaceDnsSettings schema
 *
 * @param {JSON} payload
 *
 */
NetworkInterfaceDnsSettings.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['dnsServers'])) {
    payload['dnsServers'] = [];
    for (var i = 0; i < this['dnsServers'].length; i++) {
      if (this['dnsServers'][i] !== null && this['dnsServers'][i] !== undefined) {
        if (typeof this['dnsServers'][i].valueOf() !== 'string') {
          throw new Error('this[\'dnsServers\'][i] must be of type string.');
        }
        if (payload['dnsServers'] === null || payload['dnsServers'] === undefined) {
          payload['dnsServers'] = {};
        }
        payload['dnsServers'][i] = this['dnsServers'][i];
      }
    }
  }

  if (util.isArray(this['appliedDnsServers'])) {
    payload['appliedDnsServers'] = [];
    for (var i1 = 0; i1 < this['appliedDnsServers'].length; i1++) {
      if (this['appliedDnsServers'][i1] !== null && this['appliedDnsServers'][i1] !== undefined) {
        if (typeof this['appliedDnsServers'][i1].valueOf() !== 'string') {
          throw new Error('this[\'appliedDnsServers\'][i1] must be of type string.');
        }
        if (payload['appliedDnsServers'] === null || payload['appliedDnsServers'] === undefined) {
          payload['appliedDnsServers'] = {};
        }
        payload['appliedDnsServers'][i1] = this['appliedDnsServers'][i1];
      }
    }
  }

  if (this['internalDnsNameLabel'] !== null && this['internalDnsNameLabel'] !== undefined) {
    if (typeof this['internalDnsNameLabel'].valueOf() !== 'string') {
      throw new Error('this[\'internalDnsNameLabel\'] must be of type string.');
    }
    payload['internalDnsNameLabel'] = this['internalDnsNameLabel'];
  }

  if (this['internalFqdn'] !== null && this['internalFqdn'] !== undefined) {
    if (typeof this['internalFqdn'].valueOf() !== 'string') {
      throw new Error('this[\'internalFqdn\'] must be of type string.');
    }
    payload['internalFqdn'] = this['internalFqdn'];
  }

  return payload;
};

/**
 * Deserialize the instance to NetworkInterfaceDnsSettings schema
 *
 * @param {JSON} instance
 *
 */
NetworkInterfaceDnsSettings.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['dnsServers']) {
      var tempInstancednsServers = [];
      instance['dnsServers'].forEach(function(element2) {
        if (element2 !== undefined) {
          element2 = element2;
        }
        tempInstancednsServers.push(element2);
      });
      this['dnsServers'] = tempInstancednsServers;
    }

    if (instance['appliedDnsServers']) {
      var tempInstanceappliedDnsServers = [];
      instance['appliedDnsServers'].forEach(function(element3) {
        if (element3 !== undefined) {
          element3 = element3;
        }
        tempInstanceappliedDnsServers.push(element3);
      });
      this['appliedDnsServers'] = tempInstanceappliedDnsServers;
    }

    if (instance['internalDnsNameLabel'] !== undefined) {
      this['internalDnsNameLabel'] = instance['internalDnsNameLabel'];
    }

    if (instance['internalFqdn'] !== undefined) {
      this['internalFqdn'] = instance['internalFqdn'];
    }
  }

  return this;
};

module.exports = NetworkInterfaceDnsSettings;
