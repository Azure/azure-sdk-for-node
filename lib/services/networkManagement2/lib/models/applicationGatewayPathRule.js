/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApplicationGatewayPathRule class.
 * @constructor
 * Path rule of URL path map of application gateway
 * @member {string} [name] Gets name of the resource that is unique within a
 * resource group. This name can be used to access the resource
 * 
 * @member {string} [etag] A unique read-only string that changes whenever the
 * resource is updated
 * 
 * @member {array} [paths] Gets or sets the path rules of URL path map
 * 
 * @member {object} [backendAddressPool] Gets or sets backend address pool
 * resource of URL path map
 * 
 * @member {string} [backendAddressPool.id] Resource Id
 * 
 * @member {object} [backendHttpSettings] Gets or sets backend http settings
 * resource of URL path map
 * 
 * @member {string} [backendHttpSettings.id] Resource Id
 * 
 * @member {string} [provisioningState] Gets or sets path rule of URL path map
 * resource Updating/Deleting/Failed
 * 
 */
function ApplicationGatewayPathRule(parameters) {
  ApplicationGatewayPathRule['super_'].call(this, parameters);
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.etag !== undefined) {
      this.etag = parameters.etag;
    }
    if (parameters.paths) {
      var tempParameterspaths = [];
      parameters.paths.forEach(function(element) {
        if (element !== undefined) {
          element = element;
        }
        tempParameterspaths.push(element);
      });
      this.paths = tempParameterspaths;
    }
    if (parameters.backendAddressPool) {
      this.backendAddressPool = new models['SubResource'](parameters.backendAddressPool);
    }
    if (parameters.backendHttpSettings) {
      this.backendHttpSettings = new models['SubResource'](parameters.backendHttpSettings);
    }
    if (parameters.provisioningState !== undefined) {
      this.provisioningState = parameters.provisioningState;
    }
  }    
}

util.inherits(ApplicationGatewayPathRule, models['SubResource']);

/**
 * Validate the payload against the ApplicationGatewayPathRule schema
 *
 * @param {JSON} payload
 *
 */
ApplicationGatewayPathRule.prototype.serialize = function () {
  var payload = ApplicationGatewayPathRule['super_'].prototype.serialize.call(this);
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['name'] = this['name'];
  }

  if (this['etag'] !== null && this['etag'] !== undefined) {
    if (typeof this['etag'].valueOf() !== 'string') {
      throw new Error('this[\'etag\'] must be of type string.');
    }
    payload['etag'] = this['etag'];
  }

  if (util.isArray(this['paths'])) {
    payload['properties']['paths'] = [];
    for (var i = 0; i < this['paths'].length; i++) {
      if (this['paths'][i] !== null && this['paths'][i] !== undefined) {
        if (typeof this['paths'][i].valueOf() !== 'string') {
          throw new Error('this[\'paths\'][i] must be of type string.');
        }
        if (payload['properties'] === null || payload['properties'] === undefined) {
          payload['properties'] = {};
        }
        payload['properties']['paths'][i] = this['paths'][i];
      }
    }
  }

  if (this['backendAddressPool']) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['backendAddressPool'] = this['backendAddressPool'].serialize();
  }

  if (this['backendHttpSettings']) {
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['backendHttpSettings'] = this['backendHttpSettings'].serialize();
  }

  if (this['provisioningState'] !== null && this['provisioningState'] !== undefined) {
    if (typeof this['provisioningState'].valueOf() !== 'string') {
      throw new Error('this[\'provisioningState\'] must be of type string.');
    }
    if (payload['properties'] === null || payload['properties'] === undefined) {
      payload['properties'] = {};
    }
    payload['properties']['provisioningState'] = this['provisioningState'];
  }

  return payload;
};

/**
 * Deserialize the instance to ApplicationGatewayPathRule schema
 *
 * @param {JSON} instance
 *
 */
ApplicationGatewayPathRule.prototype.deserialize = function (instance) {
  ApplicationGatewayPathRule['super_'].prototype.deserialize.call(this, instance);
  if (instance) {
    if (instance['name'] !== undefined) {
      this['name'] = instance['name'];
    }

    if (instance['etag'] !== undefined) {
      this['etag'] = instance['etag'];
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['paths']) {
        var tempInstancepropertiespaths = [];
        instance['properties']['paths'].forEach(function(element1) {
          if (element1 !== undefined) {
            element1 = element1;
          }
          tempInstancepropertiespaths.push(element1);
        });
        this['paths'] = tempInstancepropertiespaths;
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['backendAddressPool']) {
        this['backendAddressPool'] = new models['SubResource']().deserialize(instance['properties']['backendAddressPool']);
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['backendHttpSettings']) {
        this['backendHttpSettings'] = new models['SubResource']().deserialize(instance['properties']['backendHttpSettings']);
      }
    }

    if (instance['properties'] !== null && instance['properties'] !== undefined) {
      if (instance['properties']['provisioningState'] !== undefined) {
        this['provisioningState'] = instance['properties']['provisioningState'];
      }
    }
  }

  return this;
};

module.exports = ApplicationGatewayPathRule;
