/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

/**
 * @class
 * Initializes a new instance of the VirtualMachineScaleSetSku class.
 * @constructor
 * Describes an available virtual machine scale set sku.
 * @member {string} [resourceType] Gets the type of resource the sku applies
 * to.
 * 
 * @member {object} [sku] Gets the Sku.
 * 
 * @member {string} [sku.name] Gets or sets the sku name.
 * 
 * @member {string} [sku.tier] Gets or sets the sku tier.
 * 
 * @member {number} [sku.capacity] Gets or sets the sku capacity.
 * 
 * @member {object} [capacity] Gets available scaling information.
 * 
 * @member {number} [capacity.minimum] Gets the minimum capacity.
 * 
 * @member {number} [capacity.maximum] Gets the maximum capacity that can be
 * set.
 * 
 * @member {number} [capacity.defaultCapacity] Gets the default capacity.
 * 
 * @member {string} [capacity.scaleType] Gets the scale type applicable to the
 * sku. Possible values for this property include: 'Automatic', 'None'.
 * 
 */
function VirtualMachineScaleSetSku(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.resourceType !== undefined) {
      this.resourceType = parameters.resourceType;
    }
    if (parameters.sku) {
      this.sku = new models['Sku'](parameters.sku);
    }
    if (parameters.capacity) {
      this.capacity = new models['VirtualMachineScaleSetSkuCapacity'](parameters.capacity);
    }
  }    
}


/**
 * Validate the payload against the VirtualMachineScaleSetSku schema
 *
 * @param {JSON} payload
 *
 */
VirtualMachineScaleSetSku.prototype.serialize = function () {
  var payload = {};
  if (this['resourceType'] !== null && this['resourceType'] !== undefined) {
    if (typeof this['resourceType'].valueOf() !== 'string') {
      throw new Error('this[\'resourceType\'] must be of type string.');
    }
    payload['resourceType'] = this['resourceType'];
  }

  if (this['sku']) {
    payload['sku'] = this['sku'].serialize();
  }

  if (this['capacity']) {
    payload['capacity'] = this['capacity'].serialize();
  }

  return payload;
};

/**
 * Deserialize the instance to VirtualMachineScaleSetSku schema
 *
 * @param {JSON} instance
 *
 */
VirtualMachineScaleSetSku.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['resourceType'] !== undefined) {
      this['resourceType'] = instance['resourceType'];
    }

    if (instance['sku']) {
      this['sku'] = new models['Sku']().deserialize(instance['sku']);
    }

    if (instance['capacity']) {
      this['capacity'] = new models['VirtualMachineScaleSetSkuCapacity']().deserialize(instance['capacity']);
    }
  }

  return this;
};

module.exports = VirtualMachineScaleSetSku;
