/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

/**
 * @class
 * Initializes a new instance of the DiagnosticsProfile class.
 * @constructor
 * Describes a diagnostics profile.
 * @member {object} [bootDiagnostics] Gets or sets the boot diagnostics.
 * 
 * @member {boolean} [bootDiagnostics.enabled] Gets or sets whether VM Agent
 * should be provisioned on the Virtual Machine.
 * 
 * @member {string} [bootDiagnostics.storageUri] Gets or sets the boot
 * diagnostics storage Uri. It should be a valid Uri
 * 
 */
function DiagnosticsProfile(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.bootDiagnostics) {
      this.bootDiagnostics = new models['BootDiagnostics'](parameters.bootDiagnostics);
    }
  }    
}


/**
 * Validate the payload against the DiagnosticsProfile schema
 *
 * @param {JSON} payload
 *
 */
DiagnosticsProfile.prototype.serialize = function () {
  var payload = {};
  if (this['bootDiagnostics']) {
    payload['bootDiagnostics'] = this['bootDiagnostics'].serialize();
  }

  return payload;
};

/**
 * Deserialize the instance to DiagnosticsProfile schema
 *
 * @param {JSON} instance
 *
 */
DiagnosticsProfile.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['bootDiagnostics']) {
      this['bootDiagnostics'] = new models['BootDiagnostics']().deserialize(instance['bootDiagnostics']);
    }
  }

  return this;
};

module.exports = DiagnosticsProfile;
