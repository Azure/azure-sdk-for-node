/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the DiskInstanceView class.
 * @constructor
 * The instance view of the disk.
 * @member {string} [name] Gets or sets the disk name.
 * 
 * @member {array} [statuses] Gets or sets the resource status information.
 * 
 */
function DiskInstanceView(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.statuses) {
      var tempParametersstatuses = [];
      parameters.statuses.forEach(function(element) {
        if (element) {
          element = new models['InstanceViewStatus'](element);
        }
        tempParametersstatuses.push(element);
      });
      this.statuses = tempParametersstatuses;
    }
  }    
}


/**
 * Validate the payload against the DiskInstanceView schema
 *
 * @param {JSON} payload
 *
 */
DiskInstanceView.prototype.serialize = function () {
  var payload = {};
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['name'] = this['name'];
  }

  if (util.isArray(this['statuses'])) {
    payload['statuses'] = [];
    for (var i = 0; i < this['statuses'].length; i++) {
      if (this['statuses'][i]) {
        payload['statuses'][i] = this['statuses'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to DiskInstanceView schema
 *
 * @param {JSON} instance
 *
 */
DiskInstanceView.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['name'] !== undefined) {
      this['name'] = instance['name'];
    }

    if (instance['statuses']) {
      var tempInstancestatuses = [];
      instance['statuses'].forEach(function(element1) {
        if (element1) {
          element1 = new models['InstanceViewStatus']().deserialize(element1);
        }
        tempInstancestatuses.push(element1);
      });
      this['statuses'] = tempInstancestatuses;
    }
  }

  return this;
};

module.exports = DiskInstanceView;
