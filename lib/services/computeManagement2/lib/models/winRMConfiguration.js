/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the WinRMConfiguration class.
 * @constructor
 * Describes Windows Remote Management configuration of the VM
 * @member {array} [listeners] Gets or sets the list of Windows Remote
 * Management listeners
 * 
 */
function WinRMConfiguration(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.listeners) {
      var tempParameterslisteners = [];
      parameters.listeners.forEach(function(element) {
        if (element) {
          element = new models['WinRMListener'](element);
        }
        tempParameterslisteners.push(element);
      });
      this.listeners = tempParameterslisteners;
    }
  }    
}


/**
 * Validate the payload against the WinRMConfiguration schema
 *
 * @param {JSON} payload
 *
 */
WinRMConfiguration.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['listeners'])) {
    payload['listeners'] = [];
    for (var i = 0; i < this['listeners'].length; i++) {
      if (this['listeners'][i]) {
        payload['listeners'][i] = this['listeners'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to WinRMConfiguration schema
 *
 * @param {JSON} instance
 *
 */
WinRMConfiguration.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['listeners']) {
      var tempInstancelisteners = [];
      instance['listeners'].forEach(function(element1) {
        if (element1) {
          element1 = new models['WinRMListener']().deserialize(element1);
        }
        tempInstancelisteners.push(element1);
      });
      this['listeners'] = tempInstancelisteners;
    }
  }

  return this;
};

module.exports = WinRMConfiguration;
