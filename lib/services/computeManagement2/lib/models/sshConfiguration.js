/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the SshConfiguration class.
 * @constructor
 * SSH configuration for Linux based VMs running on Azure
 * @member {array} [publicKeys] Gets or sets the list of SSH public keys used
 * to authenticate with linux based VMs
 * 
 */
function SshConfiguration(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.publicKeys) {
      var tempParameterspublicKeys = [];
      parameters.publicKeys.forEach(function(element) {
        if (element) {
          element = new models['SshPublicKey'](element);
        }
        tempParameterspublicKeys.push(element);
      });
      this.publicKeys = tempParameterspublicKeys;
    }
  }    
}


/**
 * Validate the payload against the SshConfiguration schema
 *
 * @param {JSON} payload
 *
 */
SshConfiguration.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['publicKeys'])) {
    payload['publicKeys'] = [];
    for (var i = 0; i < this['publicKeys'].length; i++) {
      if (this['publicKeys'][i]) {
        payload['publicKeys'][i] = this['publicKeys'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to SshConfiguration schema
 *
 * @param {JSON} instance
 *
 */
SshConfiguration.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['publicKeys']) {
      var tempInstancepublicKeys = [];
      instance['publicKeys'].forEach(function(element1) {
        if (element1) {
          element1 = new models['SshPublicKey']().deserialize(element1);
        }
        tempInstancepublicKeys.push(element1);
      });
      this['publicKeys'] = tempInstancepublicKeys;
    }
  }

  return this;
};

module.exports = SshConfiguration;
