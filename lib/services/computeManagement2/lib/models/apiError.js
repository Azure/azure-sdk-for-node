/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ApiError class.
 * @constructor
 * Api error.
 * @member {array} [details] Gets or sets the Api error details
 * 
 * @member {object} [innererror] Gets or sets the Api inner error
 * 
 * @member {string} [innererror.exceptiontype] Gets or sets the exception type.
 * 
 * @member {string} [innererror.errordetail] Gets or sets the internal error
 * message or exception dump.
 * 
 * @member {string} [code] Gets or sets the error code.
 * 
 * @member {string} [target] Gets or sets the target of the particular error.
 * 
 * @member {string} [message] Gets or sets the error message.
 * 
 */
function ApiError(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.details) {
      var tempParametersdetails = [];
      parameters.details.forEach(function(element) {
        if (element) {
          element = new models['ApiErrorBase'](element);
        }
        tempParametersdetails.push(element);
      });
      this.details = tempParametersdetails;
    }
    if (parameters.innererror) {
      this.innererror = new models['InnerError'](parameters.innererror);
    }
    if (parameters.code !== undefined) {
      this.code = parameters.code;
    }
    if (parameters.target !== undefined) {
      this.target = parameters.target;
    }
    if (parameters.message !== undefined) {
      this.message = parameters.message;
    }
  }    
}


/**
 * Validate the payload against the ApiError schema
 *
 * @param {JSON} payload
 *
 */
ApiError.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['details'])) {
    payload['details'] = [];
    for (var i = 0; i < this['details'].length; i++) {
      if (this['details'][i]) {
        payload['details'][i] = this['details'][i].serialize();
      }
    }
  }

  if (this['innererror']) {
    payload['innererror'] = this['innererror'].serialize();
  }

  if (this['code'] !== null && this['code'] !== undefined) {
    if (typeof this['code'].valueOf() !== 'string') {
      throw new Error('this[\'code\'] must be of type string.');
    }
    payload['code'] = this['code'];
  }

  if (this['target'] !== null && this['target'] !== undefined) {
    if (typeof this['target'].valueOf() !== 'string') {
      throw new Error('this[\'target\'] must be of type string.');
    }
    payload['target'] = this['target'];
  }

  if (this['message'] !== null && this['message'] !== undefined) {
    if (typeof this['message'].valueOf() !== 'string') {
      throw new Error('this[\'message\'] must be of type string.');
    }
    payload['message'] = this['message'];
  }

  return payload;
};

/**
 * Deserialize the instance to ApiError schema
 *
 * @param {JSON} instance
 *
 */
ApiError.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['details']) {
      var tempInstancedetails = [];
      instance['details'].forEach(function(element1) {
        if (element1) {
          element1 = new models['ApiErrorBase']().deserialize(element1);
        }
        tempInstancedetails.push(element1);
      });
      this['details'] = tempInstancedetails;
    }

    if (instance['innererror']) {
      this['innererror'] = new models['InnerError']().deserialize(instance['innererror']);
    }

    if (instance['code'] !== undefined) {
      this['code'] = instance['code'];
    }

    if (instance['target'] !== undefined) {
      this['target'] = instance['target'];
    }

    if (instance['message'] !== undefined) {
      this['message'] = instance['message'];
    }
  }

  return this;
};

module.exports = ApiError;
