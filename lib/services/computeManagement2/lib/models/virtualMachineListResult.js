/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.13.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the VirtualMachineListResult class.
 * @constructor
 * The List Virtual Machine operation response.
 * @member {array} [value] Gets or sets the list of virtual machines.
 * 
 * @member {string} [nextLink] Gets or sets the uri to fetch the next page of
 * VMs. Call ListNext() with this to fetch the next page of Virtual Machines.
 * 
 */
function VirtualMachineListResult(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.value) {
      var tempParametersvalue = [];
      parameters.value.forEach(function(element) {
        if (element) {
          element = new models['VirtualMachine'](element);
        }
        tempParametersvalue.push(element);
      });
      this.value = tempParametersvalue;
    }
    if (parameters.nextLink !== undefined) {
      this.nextLink = parameters.nextLink;
    }
  }    
}


/**
 * Validate the payload against the VirtualMachineListResult schema
 *
 * @param {JSON} payload
 *
 */
VirtualMachineListResult.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['value'])) {
    payload['value'] = [];
    for (var i = 0; i < this['value'].length; i++) {
      if (this['value'][i]) {
        if (payload['value'] === null || payload['value'] === undefined) {
          payload['value'] = {};
        }
        payload['value'][i] = this['value'][i].serialize();
      }
    }
  }

  if (this['nextLink'] !== null && this['nextLink'] !== undefined) {
    if (typeof this['nextLink'].valueOf() !== 'string') {
      throw new Error('this[\'nextLink\'] must be of type string.');
    }
    payload['nextLink'] = this['nextLink'];
  }

  return payload;
};

/**
 * Deserialize the instance to VirtualMachineListResult schema
 *
 * @param {JSON} instance
 *
 */
VirtualMachineListResult.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['value']) {
      var tempInstancevalue = [];
      instance['value'].forEach(function(element1) {
        if (element1) {
          element1 = new models['VirtualMachine']().deserialize(element1);
        }
        tempInstancevalue.push(element1);
      });
      this['value'] = tempInstancevalue;
    }

    if (instance['nextLink'] !== undefined) {
      this['nextLink'] = instance['nextLink'];
    }
  }

  return this;
};

module.exports = VirtualMachineListResult;
