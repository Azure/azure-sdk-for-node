/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.12.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ListUsagesResult class.
 * @constructor
 * The List Usages operation response.
 * @member {array} [value] Gets or sets the list Compute Resource Usages.
 * 
 */
function ListUsagesResult(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.value) {
      var tempParametersvalue = [];
      parameters.value.forEach(function(element) {
        if (element) {
          element = new models['Usage'](element);
        }
        tempParametersvalue.push(element);
      });
      this.value = tempParametersvalue;
    }
  }    
}


/**
 * Validate the payload against the ListUsagesResult schema
 *
 * @param {JSON} payload
 *
 */
ListUsagesResult.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['value'])) {
    payload['value'] = [];
    for (var i = 0; i < this['value'].length; i++) {
      if (this['value'][i]) {
        if (payload['value'] === null || payload['value'] === undefined) {
          payload['value'] = {};
        }
        payload['value'][i] = this['value'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to ListUsagesResult schema
 *
 * @param {JSON} instance
 *
 */
ListUsagesResult.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['value']) {
      var tempInstancevalue = [];
      instance['value'].forEach(function(element1) {
        if (element1) {
          element1 = new models['Usage']().deserialize(element1);
        }
        tempInstancevalue.push(element1);
      });
      this['value'] = tempInstancevalue;
    }
  }

  return this;
};

module.exports = ListUsagesResult;
