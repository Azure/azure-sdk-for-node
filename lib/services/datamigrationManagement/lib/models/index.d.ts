/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

import { BaseResource } from 'ms-rest-azure';
import { CloudError } from 'ms-rest-azure';
import * as moment from 'moment';

export { BaseResource } from 'ms-rest-azure';
export { CloudError } from 'ms-rest-azure';


/**
 * @class
 * Initializes a new instance of the Resource class.
 * @constructor
 * ARM resource.
 *
 * @property {string} [id] Resource ID.
 * @property {string} [name] Resource name.
 * @property {string} [type] Resource type.
 */
export interface Resource extends BaseResource {
  readonly id?: string;
  readonly name?: string;
  readonly type?: string;
}

/**
 * @class
 * Initializes a new instance of the TrackedResource class.
 * @constructor
 * ARM tracked top level resource.
 *
 * @property {object} [tags] Resource tags.
 * @property {string} location Resource location.
 */
export interface TrackedResource extends Resource {
  tags?: { [propertyName: string]: string };
  location: string;
}

/**
 * @class
 * Initializes a new instance of the ProjectFileProperties class.
 * @constructor
 * Base class for file properties.
 *
 * @property {string} [extension] Optional File extension. If submitted it
 * should not have a leading period and must match the extension from filePath.
 * @property {string} [filePath] Relative path of this file resource. This
 * property can be set when creating or updating the file resource.
 * @property {date} [lastModified] Modification DateTime.
 * @property {string} [mediaType] File content type. This propery can be
 * modified to reflect the file content type.
 * @property {number} [size] File size.
 */
export interface ProjectFileProperties {
  extension?: string;
  filePath?: string;
  readonly lastModified?: Date;
  mediaType?: string;
  readonly size?: number;
}

/**
 * @class
 * Initializes a new instance of the ProjectFile class.
 * @constructor
 * A file resource
 *
 * @property {string} [etag] HTTP strong entity tag value. This is ignored if
 * submitted.
 * @property {object} [properties] Custom file properties
 * @property {string} [properties.extension] Optional File extension. If
 * submitted it should not have a leading period and must match the extension
 * from filePath.
 * @property {string} [properties.filePath] Relative path of this file
 * resource. This property can be set when creating or updating the file
 * resource.
 * @property {date} [properties.lastModified] Modification DateTime.
 * @property {string} [properties.mediaType] File content type. This propery
 * can be modified to reflect the file content type.
 * @property {number} [properties.size] File size.
 */
export interface ProjectFile extends Resource {
  etag?: string;
  properties?: ProjectFileProperties;
}

/**
 * @class
 * Initializes a new instance of the ODataError class.
 * @constructor
 * Error information in OData format.
 *
 * @property {string} [code] The machine-readable description of the error,
 * such as 'InvalidRequest' or 'InternalServerError'
 * @property {string} [message] The human-readable description of the error
 * @property {array} [details] Inner errors that caused this error
 */
export interface ODataError {
  code?: string;
  message?: string;
  details?: ODataError[];
}

/**
 * @class
 * Initializes a new instance of the ReportableException class.
 * @constructor
 * Exception object for all custom exceptions
 *
 * @property {string} [message] Error message
 * @property {string} [actionableMessage] Actionable steps for this exception
 * @property {string} [filePath] The path to the file where exception occurred
 * @property {string} [lineNumber] The line number where exception occurred
 * @property {number} [hResult] Coded numerical value that is assigned to a
 * specific exception
 * @property {string} [stackTrace] Stack trace
 */
export interface ReportableException {
  message?: string;
  actionableMessage?: string;
  filePath?: string;
  lineNumber?: string;
  hResult?: number;
  stackTrace?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSyncCompleteCommandOutput class.
 * @constructor
 * Output for command that completes sync migration for a database.
 *
 * @property {array} [errors] List of errors that happened during the command
 * execution
 */
export interface MigrateSyncCompleteCommandOutput {
  errors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSyncCompleteCommandInput class.
 * @constructor
 * Input for command that completes sync migration for a database.
 *
 * @property {string} databaseName Name of database
 * @property {date} [commitTimeStamp] Time stamp to complete
 */
export interface MigrateSyncCompleteCommandInput {
  databaseName: string;
  commitTimeStamp?: Date;
}

/**
 * @class
 * Initializes a new instance of the CommandProperties class.
 * @constructor
 * Base class for all types of DMS command properties. If command is not
 * supported by current client, this object is returned.
 *
 * @property {array} [errors] Array of errors. This is ignored if submitted.
 * @property {string} [state] The state of the command. This is ignored if
 * submitted. Possible values include: 'Unknown', 'Accepted', 'Running',
 * 'Succeeded', 'Failed'
 * @property {string} commandType Polymorphic Discriminator
 */
export interface CommandProperties {
  readonly errors?: ODataError[];
  readonly state?: string;
  commandType: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSyncCompleteCommandProperties class.
 * @constructor
 * Properties for the command that completes sync migration for a database.
 *
 * @property {object} [input] Command input
 * @property {string} [input.databaseName] Name of database
 * @property {date} [input.commitTimeStamp] Time stamp to complete
 * @property {object} [output] Command output. This is ignored if submitted.
 * @property {array} [output.errors] List of errors that happened during the
 * command execution
 */
export interface MigrateSyncCompleteCommandProperties extends CommandProperties {
  input?: MigrateSyncCompleteCommandInput;
  readonly output?: MigrateSyncCompleteCommandOutput;
}

/**
 * @class
 * Initializes a new instance of the GetTdeCertificatesSqlTaskOutput class.
 * @constructor
 * Output of the task that gets TDE certificates in Base64 encoded format.
 *
 * @property {object} [base64EncodedCertificates] Mapping from certificate name
 * to base 64 encoded format.
 * @property {array} [validationErrors] Validation errors
 */
export interface GetTdeCertificatesSqlTaskOutput {
  readonly base64EncodedCertificates?: { [propertyName: string]: string[] };
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the SelectedCertificateInput class.
 * @constructor
 * Info for ertificate to be exported for TDE enabled databases.
 *
 * @property {string} certificateName Name of certificate to be exported.
 * @property {string} password Password to use for encrypting the exported
 * certificate.
 */
export interface SelectedCertificateInput {
  certificateName: string;
  password: string;
}

/**
 * @class
 * Initializes a new instance of the FileShare class.
 * @constructor
 * File share information with Path, Username, and Password.
 *
 * @property {string} [userName] User name credential to connect to the share
 * location
 * @property {string} [password] Password credential used to connect to the
 * share location.
 * @property {string} path The folder path for this share.
 */
export interface FileShare {
  userName?: string;
  password?: string;
  path: string;
}

/**
 * @class
 * Initializes a new instance of the ConnectionInfo class.
 * @constructor
 * Defines the connection properties of a server
 *
 * @property {string} [userName] User name
 * @property {string} [password] Password credential.
 * @property {string} type Polymorphic Discriminator
 */
export interface ConnectionInfo {
  userName?: string;
  password?: string;
  type: string;
}

/**
 * @class
 * Initializes a new instance of the PostgreSqlConnectionInfo class.
 * @constructor
 * Information for connecting to PostgreSQL server
 *
 * @property {string} serverName Name of the server
 * @property {string} [databaseName] Name of the database
 * @property {number} port Port for Server
 */
export interface PostgreSqlConnectionInfo extends ConnectionInfo {
  serverName: string;
  databaseName?: string;
  port: number;
}

/**
 * @class
 * Initializes a new instance of the MySqlConnectionInfo class.
 * @constructor
 * Information for connecting to MySQL server
 *
 * @property {string} serverName Name of the server
 * @property {number} port Port for Server
 */
export interface MySqlConnectionInfo extends ConnectionInfo {
  serverName: string;
  port: number;
}

/**
 * @class
 * Initializes a new instance of the MongoDbConnectionInfo class.
 * @constructor
 * Describes a connection to a MongoDB data source
 *
 * @property {string} connectionString A MongoDB connection string or blob
 * container URL. The user name and password can be specified here or in the
 * userName and password properties
 */
export interface MongoDbConnectionInfo extends ConnectionInfo {
  connectionString: string;
}

/**
 * @class
 * Initializes a new instance of the SqlConnectionInfo class.
 * @constructor
 * Information for connecting to SQL database server
 *
 * @property {string} dataSource Data source in the format
 * Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [authentication] Authentication type to use for
 * connection. Possible values include: 'None', 'WindowsAuthentication',
 * 'SqlAuthentication', 'ActiveDirectoryIntegrated', 'ActiveDirectoryPassword'
 * @property {boolean} [encryptConnection] Whether to encrypt the connection.
 * Default value: true .
 * @property {string} [additionalSettings] Additional connection settings
 * @property {boolean} [trustServerCertificate] Whether to trust the server
 * certificate. Default value: false .
 * @property {string} [platform] Server platform type for connection. Possible
 * values include: 'SqlOnPrem'
 */
export interface SqlConnectionInfo extends ConnectionInfo {
  dataSource: string;
  authentication?: string;
  encryptConnection?: boolean;
  additionalSettings?: string;
  trustServerCertificate?: boolean;
  platform?: string;
}

/**
 * @class
 * Initializes a new instance of the GetTdeCertificatesSqlTaskInput class.
 * @constructor
 * Input for the task that gets TDE certificates in Base64 encoded format.
 *
 * @property {object} connectionInfo Connection information for SQL Server
 * @property {string} [connectionInfo.dataSource] Data source in the format
 * Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [connectionInfo.authentication] Authentication type to
 * use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [connectionInfo.encryptConnection] Whether to encrypt
 * the connection
 * @property {string} [connectionInfo.additionalSettings] Additional connection
 * settings
 * @property {boolean} [connectionInfo.trustServerCertificate] Whether to trust
 * the server certificate
 * @property {string} [connectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} backupFileShare Backup file share information for file
 * share to be used for temporarily storing files.
 * @property {string} [backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [backupFileShare.password] Password credential used to
 * connect to the share location.
 * @property {string} [backupFileShare.path] The folder path for this share.
 * @property {array} selectedCertificates List containing certificate names and
 * corresponding password to use for encrypting the exported certificate.
 */
export interface GetTdeCertificatesSqlTaskInput {
  connectionInfo: SqlConnectionInfo;
  backupFileShare: FileShare;
  selectedCertificates: SelectedCertificateInput[];
}

/**
 * @class
 * Initializes a new instance of the ProjectTaskProperties class.
 * @constructor
 * Base class for all types of DMS task properties. If task is not supported by
 * current client, this object is returned.
 *
 * @property {array} [errors] Array of errors. This is ignored if submitted.
 * @property {string} [state] The state of the task. This is ignored if
 * submitted. Possible values include: 'Unknown', 'Queued', 'Running',
 * 'Canceled', 'Succeeded', 'Failed', 'FailedInputValidation', 'Faulted'
 * @property {array} [commands] Array of command properties.
 * @property {string} taskType Polymorphic Discriminator
 */
export interface ProjectTaskProperties {
  readonly errors?: ODataError[];
  readonly state?: string;
  readonly commands?: CommandProperties[];
  taskType: string;
}

/**
 * @class
 * Initializes a new instance of the GetTdeCertificatesSqlTaskProperties class.
 * @constructor
 * Properties for the task that gets TDE certificates in Base64 encoded format.
 *
 * @property {object} [input] Task input
 * @property {object} [input.connectionInfo] Connection information for SQL
 * Server
 * @property {string} [input.connectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.connectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.connectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [input.connectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [input.connectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [input.connectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} [input.backupFileShare] Backup file share information for
 * file share to be used for temporarily storing files.
 * @property {string} [input.backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [input.backupFileShare.password] Password credential used
 * to connect to the share location.
 * @property {string} [input.backupFileShare.path] The folder path for this
 * share.
 * @property {array} [input.selectedCertificates] List containing certificate
 * names and corresponding password to use for encrypting the exported
 * certificate.
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface GetTdeCertificatesSqlTaskProperties extends ProjectTaskProperties {
  input?: GetTdeCertificatesSqlTaskInput;
  readonly output?: GetTdeCertificatesSqlTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MongoDbError class.
 * @constructor
 * Describes an error or warning that occurred during a MongoDB migration
 *
 * @property {string} [code] The non-localized, machine-readable code that
 * describes the error or warning
 * @property {number} [count] The number of times the error or warning has
 * occurred
 * @property {string} [message] The localized, human-readable message that
 * describes the error or warning
 * @property {string} [type] The type of error or warning. Possible values
 * include: 'Error', 'ValidationError', 'Warning'
 */
export interface MongoDbError {
  code?: string;
  count?: number;
  message?: string;
  type?: string;
}

/**
 * @class
 * Initializes a new instance of the MongoDbProgress class.
 * @constructor
 * Base class for MongoDB migration outputs
 *
 * @property {number} bytesCopied The number of document bytes copied during
 * the Copying stage
 * @property {number} documentsCopied The number of documents copied during the
 * Copying stage
 * @property {string} elapsedTime The elapsed time in the format
 * [ddd.]hh:mm:ss[.fffffff] (i.e. TimeSpan format)
 * @property {object} errors The errors and warnings that have occurred for the
 * current object. The keys are the error codes.
 * @property {number} eventsPending The number of oplog events awaiting replay
 * @property {number} eventsReplayed The number of oplog events replayed so far
 * @property {date} [lastEventTime] The timestamp of the last oplog event
 * received, or null if no oplog event has been received yet
 * @property {date} [lastReplayTime] The timestamp of the last oplog event
 * replayed, or null if no oplog event has been replayed yet
 * @property {string} [name] The name of the progress object. For a collection,
 * this is the unqualified collection name. For a database, this is the
 * database name. For the overall migration, this is null.
 * @property {string} [qualifiedName] The qualified name of the progress
 * object. For a collection, this is the database-qualified name. For a
 * database, this is the database name. For the overall migration, this is
 * null.
 * @property {string} resultType The type of progress object. Possible values
 * include: 'Migration', 'Database', 'Collection'
 * @property {string} state Possible values include: 'NotStarted',
 * 'ValidatingInput', 'Initializing', 'Restarting', 'Copying', 'InitialReplay',
 * 'Replaying', 'Finalizing', 'Complete', 'Canceled', 'Failed'
 * @property {number} totalBytes The total number of document bytes on the
 * source at the beginning of the Copying stage, or -1 if the total size was
 * unknown
 * @property {number} totalDocuments The total number of documents on the
 * source at the beginning of the Copying stage, or -1 if the total count was
 * unknown
 */
export interface MongoDbProgress {
  bytesCopied: number;
  documentsCopied: number;
  elapsedTime: string;
  errors: { [propertyName: string]: MongoDbError };
  eventsPending: number;
  eventsReplayed: number;
  lastEventTime?: Date;
  lastReplayTime?: Date;
  name?: string;
  qualifiedName?: string;
  resultType: string;
  state: string;
  totalBytes: number;
  totalDocuments: number;
}

/**
 * @class
 * Initializes a new instance of the MongoDbCollectionProgress class.
 * @constructor
 * Describes the progress of a collection
 *
 */
export interface MongoDbCollectionProgress extends MongoDbProgress {
}

/**
 * @class
 * Initializes a new instance of the MongoDbDatabaseProgress class.
 * @constructor
 * Describes the progress of a database
 *
 * @property {object} [collections] The progress of the collections in the
 * database. The keys are the unqualified names of the collections
 */
export interface MongoDbDatabaseProgress extends MongoDbProgress {
  collections?: { [propertyName: string]: MongoDbCollectionProgress };
}

/**
 * @class
 * Initializes a new instance of the MongoDbMigrationProgress class.
 * @constructor
 * Describes the progress of the overall migration
 *
 * @property {object} [databases] The progress of the databases in the
 * migration. The keys are the names of the databases
 */
export interface MongoDbMigrationProgress extends MongoDbProgress {
  databases?: { [propertyName: string]: MongoDbDatabaseProgress };
}

/**
 * @class
 * Initializes a new instance of the MongoDbThrottlingSettings class.
 * @constructor
 * Specifies resource limits for the migration
 *
 * @property {number} [minFreeCpu] The percentage of CPU time that the migrator
 * will try to avoid using, from 0 to 100
 * @property {number} [minFreeMemoryMb] The number of megabytes of RAM that the
 * migrator will try to avoid using
 * @property {number} [maxParallelism] The maximum number of work items (e.g.
 * collection copies) that will be processed in parallel
 */
export interface MongoDbThrottlingSettings {
  minFreeCpu?: number;
  minFreeMemoryMb?: number;
  maxParallelism?: number;
}

/**
 * @class
 * Initializes a new instance of the MongoDbShardKeyField class.
 * @constructor
 * Describes a field reference within a MongoDB shard key
 *
 * @property {string} name The name of the field
 * @property {string} order The field ordering. Possible values include:
 * 'Forward', 'Reverse', 'Hashed'
 */
export interface MongoDbShardKeyField {
  name: string;
  order: string;
}

/**
 * @class
 * Initializes a new instance of the MongoDbShardKeySetting class.
 * @constructor
 * Describes a MongoDB shard key
 *
 * @property {array} fields The fields within the shard key
 * @property {boolean} isUnique Whether the shard key is unique
 */
export interface MongoDbShardKeySetting {
  fields: MongoDbShardKeyField[];
  isUnique: boolean;
}

/**
 * @class
 * Initializes a new instance of the MongoDbCollectionSettings class.
 * @constructor
 * Describes how an individual MongoDB collection should be migrated
 *
 * @property {boolean} [canDelete] Whether the migrator is allowed to drop the
 * target collection in the course of performing a migration. The default is
 * true.
 * @property {object} [shardKey]
 * @property {array} [shardKey.fields] The fields within the shard key
 * @property {boolean} [shardKey.isUnique] Whether the shard key is unique
 * @property {number} [targetRUs] The RUs that should be configured on a
 * CosmosDB target, or null to use the default. This has no effect on
 * non-CosmosDB targets.
 */
export interface MongoDbCollectionSettings {
  canDelete?: boolean;
  shardKey?: MongoDbShardKeySetting;
  targetRUs?: number;
}

/**
 * @class
 * Initializes a new instance of the MongoDbDatabaseSettings class.
 * @constructor
 * Describes how an individual MongoDB database should be migrated
 *
 * @property {object} collections The collections on the source database to
 * migrate to the target. The keys are the unqualified names of the
 * collections.
 * @property {number} [targetRUs] The RUs that should be configured on a
 * CosmosDB target, or null to use the default, or 0 if throughput should not
 * be provisioned for the database. This has no effect on non-CosmosDB targets.
 */
export interface MongoDbDatabaseSettings {
  collections: { [propertyName: string]: MongoDbCollectionSettings };
  targetRUs?: number;
}

/**
 * @class
 * Initializes a new instance of the MongoDbMigrationSettings class.
 * @constructor
 * Describes how a MongoDB data migration should be performed
 *
 * @property {number} [boostRUs] The RU limit on a CosmosDB target that
 * collections will be temporarily increased to (if lower) during the initial
 * copy of a migration, from 10,000 to 1,000,000, or 0 to use the default boost
 * (which is generally the maximum), or null to not boost the RUs. This setting
 * has no effect on non-CosmosDB targets.
 * @property {object} databases The databases on the source cluster to migrate
 * to the target. The keys are the names of the databases.
 * @property {string} [replication] Describes how changes will be replicated
 * from the source to the target. The default is OneTime. Possible values
 * include: 'Disabled', 'OneTime', 'Continuous'
 * @property {object} source Settings used to connect to the source cluster
 * @property {string} [source.connectionString] A MongoDB connection string or
 * blob container URL. The user name and password can be specified here or in
 * the userName and password properties
 * @property {object} target Settings used to connect to the target cluster
 * @property {string} [target.connectionString] A MongoDB connection string or
 * blob container URL. The user name and password can be specified here or in
 * the userName and password properties
 * @property {object} [throttling] Settings used to limit the resource usage of
 * the migration
 * @property {number} [throttling.minFreeCpu] The percentage of CPU time that
 * the migrator will try to avoid using, from 0 to 100
 * @property {number} [throttling.minFreeMemoryMb] The number of megabytes of
 * RAM that the migrator will try to avoid using
 * @property {number} [throttling.maxParallelism] The maximum number of work
 * items (e.g. collection copies) that will be processed in parallel
 */
export interface MongoDbMigrationSettings {
  boostRUs?: number;
  databases: { [propertyName: string]: MongoDbDatabaseSettings };
  replication?: string;
  source: MongoDbConnectionInfo;
  target: MongoDbConnectionInfo;
  throttling?: MongoDbThrottlingSettings;
}

/**
 * @class
 * Initializes a new instance of the ValidateMongoDbTaskProperties class.
 * @constructor
 * Properties for the task that validates a migration between MongoDB data
 * sources
 *
 * @property {object} [input]
 * @property {number} [input.boostRUs] The RU limit on a CosmosDB target that
 * collections will be temporarily increased to (if lower) during the initial
 * copy of a migration, from 10,000 to 1,000,000, or 0 to use the default boost
 * (which is generally the maximum), or null to not boost the RUs. This setting
 * has no effect on non-CosmosDB targets.
 * @property {object} [input.databases] The databases on the source cluster to
 * migrate to the target. The keys are the names of the databases.
 * @property {string} [input.replication] Describes how changes will be
 * replicated from the source to the target. The default is OneTime. Possible
 * values include: 'Disabled', 'OneTime', 'Continuous'
 * @property {object} [input.source] Settings used to connect to the source
 * cluster
 * @property {string} [input.source.connectionString] A MongoDB connection
 * string or blob container URL. The user name and password can be specified
 * here or in the userName and password properties
 * @property {object} [input.target] Settings used to connect to the target
 * cluster
 * @property {string} [input.target.connectionString] A MongoDB connection
 * string or blob container URL. The user name and password can be specified
 * here or in the userName and password properties
 * @property {object} [input.throttling] Settings used to limit the resource
 * usage of the migration
 * @property {number} [input.throttling.minFreeCpu] The percentage of CPU time
 * that the migrator will try to avoid using, from 0 to 100
 * @property {number} [input.throttling.minFreeMemoryMb] The number of
 * megabytes of RAM that the migrator will try to avoid using
 * @property {number} [input.throttling.maxParallelism] The maximum number of
 * work items (e.g. collection copies) that will be processed in parallel
 * @property {array} [output] An array containing a single
 * MongoDbMigrationProgress object
 */
export interface ValidateMongoDbTaskProperties extends ProjectTaskProperties {
  input?: MongoDbMigrationSettings;
  readonly output?: MongoDbMigrationProgress[];
}

/**
 * @class
 * Initializes a new instance of the DatabaseBackupInfo class.
 * @constructor
 * Information about backup files when existing backup mode is used.
 *
 * @property {string} [databaseName] Database name.
 * @property {string} [backupType] Backup Type. Possible values include:
 * 'Database', 'TransactionLog', 'File', 'DifferentialDatabase',
 * 'DifferentialFile', 'Partial', 'DifferentialPartial'
 * @property {array} [backupFiles] The list of backup files for the current
 * database.
 * @property {number} [position] Position of current database backup in the
 * file.
 * @property {boolean} [isDamaged] Database was damaged when backed up, but the
 * backup operation was requested to continue despite errors.
 * @property {boolean} [isCompressed] Whether the backup set is compressed
 * @property {number} [familyCount] Number of files in the backup set.
 * @property {date} [backupFinishDate] Date and time when the backup operation
 * finished.
 */
export interface DatabaseBackupInfo {
  readonly databaseName?: string;
  readonly backupType?: string;
  readonly backupFiles?: string[];
  readonly position?: number;
  readonly isDamaged?: boolean;
  readonly isCompressed?: boolean;
  readonly familyCount?: number;
  readonly backupFinishDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ValidateMigrationInputSqlServerSqlMITaskOutput class.
 * @constructor
 * Output for task that validates migration input for SQL to Azure SQL Managed
 * Instance migrations
 *
 * @property {string} [id] Result identifier
 * @property {string} [name] Name of database
 * @property {array} [restoreDatabaseNameErrors] Errors associated with the
 * RestoreDatabaseName
 * @property {array} [backupFolderErrors] Errors associated with the
 * BackupFolder path
 * @property {array} [backupShareCredentialsErrors] Errors associated with
 * backup share user name and password credentials
 * @property {array} [backupStorageAccountErrors] Errors associated with the
 * storage account provided.
 * @property {array} [existingBackupErrors] Errors associated with existing
 * backup files.
 * @property {object} [databaseBackupInfo] Information about backup files when
 * existing backup mode is used.
 * @property {string} [databaseBackupInfo.databaseName] Database name.
 * @property {string} [databaseBackupInfo.backupType] Backup Type. Possible
 * values include: 'Database', 'TransactionLog', 'File',
 * 'DifferentialDatabase', 'DifferentialFile', 'Partial', 'DifferentialPartial'
 * @property {array} [databaseBackupInfo.backupFiles] The list of backup files
 * for the current database.
 * @property {number} [databaseBackupInfo.position] Position of current
 * database backup in the file.
 * @property {boolean} [databaseBackupInfo.isDamaged] Database was damaged when
 * backed up, but the backup operation was requested to continue despite
 * errors.
 * @property {boolean} [databaseBackupInfo.isCompressed] Whether the backup set
 * is compressed
 * @property {number} [databaseBackupInfo.familyCount] Number of files in the
 * backup set.
 * @property {date} [databaseBackupInfo.backupFinishDate] Date and time when
 * the backup operation finished.
 */
export interface ValidateMigrationInputSqlServerSqlMITaskOutput {
  readonly id?: string;
  readonly name?: string;
  readonly restoreDatabaseNameErrors?: ReportableException[];
  readonly backupFolderErrors?: ReportableException[];
  readonly backupShareCredentialsErrors?: ReportableException[];
  readonly backupStorageAccountErrors?: ReportableException[];
  readonly existingBackupErrors?: ReportableException[];
  databaseBackupInfo?: DatabaseBackupInfo;
}

/**
 * @class
 * Initializes a new instance of the BlobShare class.
 * @constructor
 * Blob container storage information.
 *
 * @property {string} sasUri SAS URI of Azure Storage Account Container.
 */
export interface BlobShare {
  sasUri: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMIDatabaseInput class.
 * @constructor
 * Database specific information for SQL to Azure SQL DB Managed Instance
 * migration task inputs
 *
 * @property {string} name Name of the database
 * @property {string} restoreDatabaseName Name of the database at destination
 * @property {object} [backupFileShare] Backup file share information for
 * backing up this database.
 * @property {string} [backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [backupFileShare.password] Password credential used to
 * connect to the share location.
 * @property {string} [backupFileShare.path] The folder path for this share.
 * @property {array} [backupFilePaths] The list of backup files to be used in
 * case of existing backups.
 */
export interface MigrateSqlServerSqlMIDatabaseInput {
  name: string;
  restoreDatabaseName: string;
  backupFileShare?: FileShare;
  backupFilePaths?: string[];
}

/**
 * @class
 * Initializes a new instance of the ValidateMigrationInputSqlServerSqlMITaskInput class.
 * @constructor
 * Input for task that validates migration input for SQL to Azure SQL Managed
 * Instance
 *
 * @property {object} sourceConnectionInfo Information for connecting to source
 * @property {string} [sourceConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [sourceConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [sourceConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [sourceConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [sourceConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [sourceConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} targetConnectionInfo Information for connecting to target
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {array} selectedDatabases Databases to migrate
 * @property {array} [selectedLogins] Logins to migrate
 * @property {object} [backupFileShare] Backup file share information for all
 * selected databases.
 * @property {string} [backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [backupFileShare.password] Password credential used to
 * connect to the share location.
 * @property {string} [backupFileShare.path] The folder path for this share.
 * @property {object} backupBlobShare SAS URI of Azure Storage Account
 * Container to be used for storing backup files.
 * @property {string} [backupBlobShare.sasUri] SAS URI of Azure Storage Account
 * Container.
 * @property {string} [backupMode] Backup Mode to specify whether to use
 * existing backup or create new backup. Possible values include:
 * 'CreateBackup', 'ExistingBackup'
 */
export interface ValidateMigrationInputSqlServerSqlMITaskInput {
  sourceConnectionInfo: SqlConnectionInfo;
  targetConnectionInfo: SqlConnectionInfo;
  selectedDatabases: MigrateSqlServerSqlMIDatabaseInput[];
  selectedLogins?: string[];
  backupFileShare?: FileShare;
  backupBlobShare: BlobShare;
  backupMode?: string;
}

/**
 * @class
 * Initializes a new instance of the ValidateMigrationInputSqlServerSqlMITaskProperties class.
 * @constructor
 * Properties for task that validates migration input for SQL to Azure SQL
 * Database Managed Instance
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Information for connecting
 * to source
 * @property {string} [input.sourceConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.sourceConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.sourceConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.sourceConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.sourceConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.sourceConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {object} [input.targetConnectionInfo] Information for connecting
 * to target
 * @property {string} [input.targetConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.targetConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.targetConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.targetConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.targetConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.targetConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {array} [input.selectedLogins] Logins to migrate
 * @property {object} [input.backupFileShare] Backup file share information for
 * all selected databases.
 * @property {string} [input.backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [input.backupFileShare.password] Password credential used
 * to connect to the share location.
 * @property {string} [input.backupFileShare.path] The folder path for this
 * share.
 * @property {object} [input.backupBlobShare] SAS URI of Azure Storage Account
 * Container to be used for storing backup files.
 * @property {string} [input.backupBlobShare.sasUri] SAS URI of Azure Storage
 * Account Container.
 * @property {string} [input.backupMode] Backup Mode to specify whether to use
 * existing backup or create new backup. Possible values include:
 * 'CreateBackup', 'ExistingBackup'
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ValidateMigrationInputSqlServerSqlMITaskProperties extends ProjectTaskProperties {
  input?: ValidateMigrationInputSqlServerSqlMITaskInput;
  readonly output?: ValidateMigrationInputSqlServerSqlMITaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the ValidateSyncMigrationInputSqlServerTaskOutput class.
 * @constructor
 * Output for task that validates migration input for SQL sync migrations
 *
 * @property {string} [id] Database identifier
 * @property {string} [name] Name of database
 * @property {array} [validationErrors] Errors associated with a selected
 * database object
 */
export interface ValidateSyncMigrationInputSqlServerTaskOutput {
  readonly id?: string;
  readonly name?: string;
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncDatabaseInput class.
 * @constructor
 * Database specific information for SQL to Azure SQL DB sync migration task
 * inputs
 *
 * @property {string} [id] Unique identifier for database
 * @property {string} [name] Name of database
 * @property {string} [targetDatabaseName] Target database name
 * @property {string} [schemaName] Schema name to be migrated
 * @property {object} [tableMap] Mapping of source to target tables
 * @property {object} [migrationSetting] Migration settings which tune the
 * migration behavior
 * @property {object} [sourceSetting] Source settings to tune source endpoint
 * migration behavior
 * @property {object} [targetSetting] Target settings to tune target endpoint
 * migration behavior
 */
export interface MigrateSqlServerSqlDbSyncDatabaseInput {
  id?: string;
  name?: string;
  targetDatabaseName?: string;
  schemaName?: string;
  tableMap?: { [propertyName: string]: string };
  migrationSetting?: { [propertyName: string]: string };
  sourceSetting?: { [propertyName: string]: string };
  targetSetting?: { [propertyName: string]: string };
}

/**
 * @class
 * Initializes a new instance of the ValidateSyncMigrationInputSqlServerTaskInput class.
 * @constructor
 * Input for task that validates migration input for SQL sync migrations
 *
 * @property {object} sourceConnectionInfo Information for connecting to source
 * SQL server
 * @property {string} [sourceConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [sourceConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [sourceConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [sourceConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [sourceConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [sourceConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} targetConnectionInfo Information for connecting to target
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {array} selectedDatabases Databases to migrate
 */
export interface ValidateSyncMigrationInputSqlServerTaskInput {
  sourceConnectionInfo: SqlConnectionInfo;
  targetConnectionInfo: SqlConnectionInfo;
  selectedDatabases: MigrateSqlServerSqlDbSyncDatabaseInput[];
}

/**
 * @class
 * Initializes a new instance of the ValidateMigrationInputSqlServerSqlDbSyncTaskProperties class.
 * @constructor
 * Properties for task that validates migration input for SQL to Azure SQL DB
 * sync migrations
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Information for connecting
 * to source SQL server
 * @property {string} [input.sourceConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.sourceConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.sourceConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.sourceConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.sourceConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.sourceConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {object} [input.targetConnectionInfo] Information for connecting
 * to target
 * @property {string} [input.targetConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.targetConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.targetConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.targetConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.targetConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.targetConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ValidateMigrationInputSqlServerSqlDbSyncTaskProperties extends ProjectTaskProperties {
  input?: ValidateSyncMigrationInputSqlServerTaskInput;
  readonly output?: ValidateSyncMigrationInputSqlServerTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the SyncMigrationDatabaseErrorEvent class.
 * @constructor
 * Database migration errors for online migration
 *
 * @property {string} [timestampString] String value of timestamp.
 * @property {string} [eventTypeString] Event type.
 * @property {string} [eventText] Event text.
 */
export interface SyncMigrationDatabaseErrorEvent {
  readonly timestampString?: string;
  readonly eventTypeString?: string;
  readonly eventText?: string;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput class.
 * @constructor
 * Output for the task that migrates PostgreSQL databases to Azure Database for
 * PostgreSQL for online migrations
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputDatabaseError class.
 * @constructor
 * @property {string} [errorMessage] Error message
 * @property {array} [events] List of error events.
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputDatabaseError extends MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput {
  errorMessage?: string;
  events?: SyncMigrationDatabaseErrorEvent[];
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputError class.
 * @constructor
 * @property {object} [error] Migration error
 * @property {string} [error.message] Error message
 * @property {string} [error.actionableMessage] Actionable steps for this
 * exception
 * @property {string} [error.filePath] The path to the file where exception
 * occurred
 * @property {string} [error.lineNumber] The line number where exception
 * occurred
 * @property {number} [error.hResult] Coded numerical value that is assigned to
 * a specific exception
 * @property {string} [error.stackTrace] Stack trace
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputError extends MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput {
  readonly error?: ReportableException;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputTableLevel class.
 * @constructor
 * @property {string} [tableName] Name of the table
 * @property {string} [databaseName] Name of the database
 * @property {number} [cdcInsertCounter] Number of applied inserts
 * @property {number} [cdcUpdateCounter] Number of applied updates
 * @property {number} [cdcDeleteCounter] Number of applied deletes
 * @property {date} [fullLoadEstFinishTime] Estimate to finish full load
 * @property {date} [fullLoadStartedOn] Full load start time
 * @property {date} [fullLoadEndedOn] Full load end time
 * @property {number} [fullLoadTotalRows] Number of rows applied in full load
 * @property {string} [state] Current state of the table migration. Possible
 * values include: 'BEFORE_LOAD', 'FULL_LOAD', 'COMPLETED', 'CANCELED',
 * 'ERROR', 'FAILED'
 * @property {number} [totalChangesApplied] Total number of applied changes
 * @property {number} [dataErrorsCounter] Number of data errors occurred
 * @property {date} [lastModifiedTime] Last modified time on target
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputTableLevel extends MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput {
  readonly tableName?: string;
  readonly databaseName?: string;
  readonly cdcInsertCounter?: number;
  readonly cdcUpdateCounter?: number;
  readonly cdcDeleteCounter?: number;
  readonly fullLoadEstFinishTime?: Date;
  readonly fullLoadStartedOn?: Date;
  readonly fullLoadEndedOn?: Date;
  readonly fullLoadTotalRows?: number;
  readonly state?: string;
  readonly totalChangesApplied?: number;
  readonly dataErrorsCounter?: number;
  readonly lastModifiedTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputDatabaseLevel class.
 * @constructor
 * @property {string} [databaseName] Name of the database
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [migrationState] Migration state that this database is
 * in. Possible values include: 'UNDEFINED', 'CONFIGURING', 'INITIALIAZING',
 * 'STARTING', 'RUNNING', 'READY_TO_COMPLETE', 'COMPLETING', 'COMPLETE',
 * 'CANCELLING', 'CANCELLED', 'FAILED'
 * @property {number} [incomingChanges] Number of incoming changes
 * @property {number} [appliedChanges] Number of applied changes
 * @property {number} [cdcInsertCounter] Number of cdc inserts
 * @property {number} [cdcDeleteCounter] Number of cdc deletes
 * @property {number} [cdcUpdateCounter] Number of cdc updates
 * @property {number} [fullLoadCompletedTables] Number of tables completed in
 * full load
 * @property {number} [fullLoadLoadingTables] Number of tables loading in full
 * load
 * @property {number} [fullLoadQueuedTables] Number of tables queued in full
 * load
 * @property {number} [fullLoadErroredTables] Number of tables errored in full
 * load
 * @property {boolean} [initializationCompleted] Indicates if initial load
 * (full load) has been completed
 * @property {number} [latency] CDC apply latency
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputDatabaseLevel extends MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput {
  readonly databaseName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly migrationState?: string;
  readonly incomingChanges?: number;
  readonly appliedChanges?: number;
  readonly cdcInsertCounter?: number;
  readonly cdcDeleteCounter?: number;
  readonly cdcUpdateCounter?: number;
  readonly fullLoadCompletedTables?: number;
  readonly fullLoadLoadingTables?: number;
  readonly fullLoadQueuedTables?: number;
  readonly fullLoadErroredTables?: number;
  readonly initializationCompleted?: boolean;
  readonly latency?: number;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputMigrationLevel class.
 * @constructor
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServer] Source server name
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServer] Target server name
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutputMigrationLevel extends MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput {
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly sourceServerVersion?: string;
  readonly sourceServer?: string;
  readonly targetServerVersion?: string;
  readonly targetServer?: string;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncDatabaseInput class.
 * @constructor
 * Database specific information for PostgreSQL to Azure Database for
 * PostgreSQL migration task inputs
 *
 * @property {string} [name] Name of the database
 * @property {string} [targetDatabaseName] Name of target database. Note:
 * Target database will be truncated before starting migration.
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncDatabaseInput {
  name?: string;
  targetDatabaseName?: string;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskInput class.
 * @constructor
 * Input for the task that migrates PostgreSQL databases to Azure Database for
 * PostgreSQL for online migrations
 *
 * @property {array} selectedDatabases Databases to migrate
 * @property {object} targetConnectionInfo Connection information for target
 * Azure Database for PostgreSQL
 * @property {string} [targetConnectionInfo.serverName] Name of the server
 * @property {string} [targetConnectionInfo.databaseName] Name of the database
 * @property {number} [targetConnectionInfo.port] Port for Server
 * @property {object} sourceConnectionInfo Connection information for source
 * PostgreSQL
 * @property {string} [sourceConnectionInfo.serverName] Name of the server
 * @property {string} [sourceConnectionInfo.databaseName] Name of the database
 * @property {number} [sourceConnectionInfo.port] Port for Server
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskInput {
  selectedDatabases: MigratePostgreSqlAzureDbForPostgreSqlSyncDatabaseInput[];
  targetConnectionInfo: PostgreSqlConnectionInfo;
  sourceConnectionInfo: PostgreSqlConnectionInfo;
}

/**
 * @class
 * Initializes a new instance of the MigratePostgreSqlAzureDbForPostgreSqlSyncTaskProperties class.
 * @constructor
 * Properties for the task that migrates PostgreSQL databases to Azure Database
 * for PostgreSQL for online migrations
 *
 * @property {object} [input] Task input
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {object} [input.targetConnectionInfo] Connection information for
 * target Azure Database for PostgreSQL
 * @property {string} [input.targetConnectionInfo.serverName] Name of the
 * server
 * @property {string} [input.targetConnectionInfo.databaseName] Name of the
 * database
 * @property {number} [input.targetConnectionInfo.port] Port for Server
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * source PostgreSQL
 * @property {string} [input.sourceConnectionInfo.serverName] Name of the
 * server
 * @property {string} [input.sourceConnectionInfo.databaseName] Name of the
 * database
 * @property {number} [input.sourceConnectionInfo.port] Port for Server
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface MigratePostgreSqlAzureDbForPostgreSqlSyncTaskProperties extends ProjectTaskProperties {
  input?: MigratePostgreSqlAzureDbForPostgreSqlSyncTaskInput;
  readonly output?: MigratePostgreSqlAzureDbForPostgreSqlSyncTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskOutput class.
 * @constructor
 * Output for the task that migrates MySQL databases to Azure Database for
 * MySQL for online migrations
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskOutputDatabaseError class.
 * @constructor
 * @property {string} [errorMessage] Error message
 * @property {array} [events] List of error events.
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskOutputDatabaseError extends MigrateMySqlAzureDbForMySqlSyncTaskOutput {
  errorMessage?: string;
  events?: SyncMigrationDatabaseErrorEvent[];
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskOutputError class.
 * @constructor
 * @property {object} [error] Migration error
 * @property {string} [error.message] Error message
 * @property {string} [error.actionableMessage] Actionable steps for this
 * exception
 * @property {string} [error.filePath] The path to the file where exception
 * occurred
 * @property {string} [error.lineNumber] The line number where exception
 * occurred
 * @property {number} [error.hResult] Coded numerical value that is assigned to
 * a specific exception
 * @property {string} [error.stackTrace] Stack trace
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskOutputError extends MigrateMySqlAzureDbForMySqlSyncTaskOutput {
  readonly error?: ReportableException;
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskOutputTableLevel class.
 * @constructor
 * @property {string} [tableName] Name of the table
 * @property {string} [databaseName] Name of the database
 * @property {string} [cdcInsertCounter] Number of applied inserts
 * @property {string} [cdcUpdateCounter] Number of applied updates
 * @property {string} [cdcDeleteCounter] Number of applied deletes
 * @property {date} [fullLoadEstFinishTime] Estimate to finish full load
 * @property {date} [fullLoadStartedOn] Full load start time
 * @property {date} [fullLoadEndedOn] Full load end time
 * @property {number} [fullLoadTotalRows] Number of rows applied in full load
 * @property {string} [state] Current state of the table migration. Possible
 * values include: 'BEFORE_LOAD', 'FULL_LOAD', 'COMPLETED', 'CANCELED',
 * 'ERROR', 'FAILED'
 * @property {number} [totalChangesApplied] Total number of applied changes
 * @property {number} [dataErrorsCounter] Number of data errors occurred
 * @property {date} [lastModifiedTime] Last modified time on target
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskOutputTableLevel extends MigrateMySqlAzureDbForMySqlSyncTaskOutput {
  readonly tableName?: string;
  readonly databaseName?: string;
  readonly cdcInsertCounter?: string;
  readonly cdcUpdateCounter?: string;
  readonly cdcDeleteCounter?: string;
  readonly fullLoadEstFinishTime?: Date;
  readonly fullLoadStartedOn?: Date;
  readonly fullLoadEndedOn?: Date;
  readonly fullLoadTotalRows?: number;
  readonly state?: string;
  readonly totalChangesApplied?: number;
  readonly dataErrorsCounter?: number;
  readonly lastModifiedTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskOutputDatabaseLevel class.
 * @constructor
 * @property {string} [databaseName] Name of the database
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [migrationState] Migration state that this database is
 * in. Possible values include: 'UNDEFINED', 'CONFIGURING', 'INITIALIAZING',
 * 'STARTING', 'RUNNING', 'READY_TO_COMPLETE', 'COMPLETING', 'COMPLETE',
 * 'CANCELLING', 'CANCELLED', 'FAILED'
 * @property {number} [incomingChanges] Number of incoming changes
 * @property {number} [appliedChanges] Number of applied changes
 * @property {number} [cdcInsertCounter] Number of cdc inserts
 * @property {number} [cdcDeleteCounter] Number of cdc deletes
 * @property {number} [cdcUpdateCounter] Number of cdc updates
 * @property {number} [fullLoadCompletedTables] Number of tables completed in
 * full load
 * @property {number} [fullLoadLoadingTables] Number of tables loading in full
 * load
 * @property {number} [fullLoadQueuedTables] Number of tables queued in full
 * load
 * @property {number} [fullLoadErroredTables] Number of tables errored in full
 * load
 * @property {boolean} [initializationCompleted] Indicates if initial load
 * (full load) has been completed
 * @property {number} [latency] CDC apply latency
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskOutputDatabaseLevel extends MigrateMySqlAzureDbForMySqlSyncTaskOutput {
  readonly databaseName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly migrationState?: string;
  readonly incomingChanges?: number;
  readonly appliedChanges?: number;
  readonly cdcInsertCounter?: number;
  readonly cdcDeleteCounter?: number;
  readonly cdcUpdateCounter?: number;
  readonly fullLoadCompletedTables?: number;
  readonly fullLoadLoadingTables?: number;
  readonly fullLoadQueuedTables?: number;
  readonly fullLoadErroredTables?: number;
  readonly initializationCompleted?: boolean;
  readonly latency?: number;
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskOutputMigrationLevel class.
 * @constructor
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServer] Source server name
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServer] Target server name
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskOutputMigrationLevel extends MigrateMySqlAzureDbForMySqlSyncTaskOutput {
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly sourceServerVersion?: string;
  readonly sourceServer?: string;
  readonly targetServerVersion?: string;
  readonly targetServer?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncDatabaseInput class.
 * @constructor
 * Database specific information for MySQL to Azure Database for MySQL
 * migration task inputs
 *
 * @property {string} [name] Name of the database
 * @property {string} [targetDatabaseName] Name of target database. Note:
 * Target database will be truncated before starting migration.
 */
export interface MigrateMySqlAzureDbForMySqlSyncDatabaseInput {
  name?: string;
  targetDatabaseName?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskInput class.
 * @constructor
 * Input for the task that migrates MySQL databases to Azure Database for MySQL
 * for online migrations
 *
 * @property {object} sourceConnectionInfo Connection information for source
 * MySQL
 * @property {string} [sourceConnectionInfo.serverName] Name of the server
 * @property {number} [sourceConnectionInfo.port] Port for Server
 * @property {object} targetConnectionInfo Connection information for target
 * Azure Database for MySQL
 * @property {string} [targetConnectionInfo.serverName] Name of the server
 * @property {number} [targetConnectionInfo.port] Port for Server
 * @property {array} selectedDatabases Databases to migrate
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskInput {
  sourceConnectionInfo: MySqlConnectionInfo;
  targetConnectionInfo: MySqlConnectionInfo;
  selectedDatabases: MigrateMySqlAzureDbForMySqlSyncDatabaseInput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateMySqlAzureDbForMySqlSyncTaskProperties class.
 * @constructor
 * Properties for the task that migrates MySQL databases to Azure Database for
 * MySQL for online migrations
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * source MySQL
 * @property {string} [input.sourceConnectionInfo.serverName] Name of the
 * server
 * @property {number} [input.sourceConnectionInfo.port] Port for Server
 * @property {object} [input.targetConnectionInfo] Connection information for
 * target Azure Database for MySQL
 * @property {string} [input.targetConnectionInfo.serverName] Name of the
 * server
 * @property {number} [input.targetConnectionInfo.port] Port for Server
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface MigrateMySqlAzureDbForMySqlSyncTaskProperties extends ProjectTaskProperties {
  input?: MigrateMySqlAzureDbForMySqlSyncTaskInput;
  readonly output?: MigrateMySqlAzureDbForMySqlSyncTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskOutput class.
 * @constructor
 * Output for the task that migrates on-prem SQL Server databases to Azure SQL
 * Database for online migrations
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface MigrateSqlServerSqlDbSyncTaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskOutputDatabaseError class.
 * @constructor
 * @property {string} [errorMessage] Error message
 * @property {array} [events] List of error events.
 */
export interface MigrateSqlServerSqlDbSyncTaskOutputDatabaseError extends MigrateSqlServerSqlDbSyncTaskOutput {
  errorMessage?: string;
  events?: SyncMigrationDatabaseErrorEvent[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskOutputError class.
 * @constructor
 * @property {object} [error] Migration error
 * @property {string} [error.message] Error message
 * @property {string} [error.actionableMessage] Actionable steps for this
 * exception
 * @property {string} [error.filePath] The path to the file where exception
 * occurred
 * @property {string} [error.lineNumber] The line number where exception
 * occurred
 * @property {number} [error.hResult] Coded numerical value that is assigned to
 * a specific exception
 * @property {string} [error.stackTrace] Stack trace
 */
export interface MigrateSqlServerSqlDbSyncTaskOutputError extends MigrateSqlServerSqlDbSyncTaskOutput {
  readonly error?: ReportableException;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskOutputTableLevel class.
 * @constructor
 * @property {string} [tableName] Name of the table
 * @property {string} [databaseName] Name of the database
 * @property {number} [cdcInsertCounter] Number of applied inserts
 * @property {number} [cdcUpdateCounter] Number of applied updates
 * @property {number} [cdcDeleteCounter] Number of applied deletes
 * @property {date} [fullLoadEstFinishTime] Estimate to finish full load
 * @property {date} [fullLoadStartedOn] Full load start time
 * @property {date} [fullLoadEndedOn] Full load end time
 * @property {number} [fullLoadTotalRows] Number of rows applied in full load
 * @property {string} [state] Current state of the table migration. Possible
 * values include: 'BEFORE_LOAD', 'FULL_LOAD', 'COMPLETED', 'CANCELED',
 * 'ERROR', 'FAILED'
 * @property {number} [totalChangesApplied] Total number of applied changes
 * @property {number} [dataErrorsCounter] Number of data errors occurred
 * @property {date} [lastModifiedTime] Last modified time on target
 */
export interface MigrateSqlServerSqlDbSyncTaskOutputTableLevel extends MigrateSqlServerSqlDbSyncTaskOutput {
  readonly tableName?: string;
  readonly databaseName?: string;
  readonly cdcInsertCounter?: number;
  readonly cdcUpdateCounter?: number;
  readonly cdcDeleteCounter?: number;
  readonly fullLoadEstFinishTime?: Date;
  readonly fullLoadStartedOn?: Date;
  readonly fullLoadEndedOn?: Date;
  readonly fullLoadTotalRows?: number;
  readonly state?: string;
  readonly totalChangesApplied?: number;
  readonly dataErrorsCounter?: number;
  readonly lastModifiedTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskOutputDatabaseLevel class.
 * @constructor
 * @property {string} [databaseName] Name of the database
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [migrationState] Migration state that this database is
 * in. Possible values include: 'UNDEFINED', 'CONFIGURING', 'INITIALIAZING',
 * 'STARTING', 'RUNNING', 'READY_TO_COMPLETE', 'COMPLETING', 'COMPLETE',
 * 'CANCELLING', 'CANCELLED', 'FAILED'
 * @property {number} [incomingChanges] Number of incoming changes
 * @property {number} [appliedChanges] Number of applied changes
 * @property {number} [cdcInsertCounter] Number of cdc inserts
 * @property {number} [cdcDeleteCounter] Number of cdc deletes
 * @property {number} [cdcUpdateCounter] Number of cdc updates
 * @property {number} [fullLoadCompletedTables] Number of tables completed in
 * full load
 * @property {number} [fullLoadLoadingTables] Number of tables loading in full
 * load
 * @property {number} [fullLoadQueuedTables] Number of tables queued in full
 * load
 * @property {number} [fullLoadErroredTables] Number of tables errored in full
 * load
 * @property {boolean} [initializationCompleted] Indicates if initial load
 * (full load) has been completed
 * @property {number} [latency] CDC apply latency
 */
export interface MigrateSqlServerSqlDbSyncTaskOutputDatabaseLevel extends MigrateSqlServerSqlDbSyncTaskOutput {
  readonly databaseName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly migrationState?: string;
  readonly incomingChanges?: number;
  readonly appliedChanges?: number;
  readonly cdcInsertCounter?: number;
  readonly cdcDeleteCounter?: number;
  readonly cdcUpdateCounter?: number;
  readonly fullLoadCompletedTables?: number;
  readonly fullLoadLoadingTables?: number;
  readonly fullLoadQueuedTables?: number;
  readonly fullLoadErroredTables?: number;
  readonly initializationCompleted?: boolean;
  readonly latency?: number;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskOutputMigrationLevel class.
 * @constructor
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServer] Source server name
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServer] Target server name
 * @property {number} [databaseCount] Count of databases
 */
export interface MigrateSqlServerSqlDbSyncTaskOutputMigrationLevel extends MigrateSqlServerSqlDbSyncTaskOutput {
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly sourceServerVersion?: string;
  readonly sourceServer?: string;
  readonly targetServerVersion?: string;
  readonly targetServer?: string;
  readonly databaseCount?: number;
}

/**
 * @class
 * Initializes a new instance of the SqlMigrationTaskInput class.
 * @constructor
 * Base class for migration task input
 *
 * @property {object} sourceConnectionInfo Information for connecting to source
 * @property {string} [sourceConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [sourceConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [sourceConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [sourceConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [sourceConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [sourceConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} targetConnectionInfo Information for connecting to target
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 */
export interface SqlMigrationTaskInput {
  sourceConnectionInfo: SqlConnectionInfo;
  targetConnectionInfo: SqlConnectionInfo;
}

/**
 * @class
 * Initializes a new instance of the MigrationValidationOptions class.
 * @constructor
 * Types of validations to run after the migration
 *
 * @property {boolean} [enableSchemaValidation] Allows to compare the schema
 * information between source and target.
 * @property {boolean} [enableDataIntegrityValidation] Allows to perform a
 * checksum based data integrity validation between source and target for the
 * selected database / tables .
 * @property {boolean} [enableQueryAnalysisValidation] Allows to perform a
 * quick and intelligent query analysis by retrieving queries from the source
 * database and executes them in the target. The result will have execution
 * statistics for executions in source and target databases for the extracted
 * queries.
 */
export interface MigrationValidationOptions {
  enableSchemaValidation?: boolean;
  enableDataIntegrityValidation?: boolean;
  enableQueryAnalysisValidation?: boolean;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskInput class.
 * @constructor
 * Input for the task that migrates on-prem SQL Server databases to Azure SQL
 * Database for online migrations
 *
 * @property {array} selectedDatabases Databases to migrate
 * @property {object} [validationOptions] Validation options
 * @property {boolean} [validationOptions.enableSchemaValidation] Allows to
 * compare the schema information between source and target.
 * @property {boolean} [validationOptions.enableDataIntegrityValidation] Allows
 * to perform a checksum based data integrity validation between source and
 * target for the selected database / tables .
 * @property {boolean} [validationOptions.enableQueryAnalysisValidation] Allows
 * to perform a quick and intelligent query analysis by retrieving queries from
 * the source database and executes them in the target. The result will have
 * execution statistics for executions in source and target databases for the
 * extracted queries.
 */
export interface MigrateSqlServerSqlDbSyncTaskInput extends SqlMigrationTaskInput {
  selectedDatabases: MigrateSqlServerSqlDbSyncDatabaseInput[];
  validationOptions?: MigrationValidationOptions;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbSyncTaskProperties class.
 * @constructor
 * Properties for the task that migrates on-prem SQL Server databases to Azure
 * SQL Database for online migrations
 *
 * @property {object} [input] Task input
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {object} [input.validationOptions] Validation options
 * @property {boolean} [input.validationOptions.enableSchemaValidation] Allows
 * to compare the schema information between source and target.
 * @property {boolean} [input.validationOptions.enableDataIntegrityValidation]
 * Allows to perform a checksum based data integrity validation between source
 * and target for the selected database / tables .
 * @property {boolean} [input.validationOptions.enableQueryAnalysisValidation]
 * Allows to perform a quick and intelligent query analysis by retrieving
 * queries from the source database and executes them in the target. The result
 * will have execution statistics for executions in source and target databases
 * for the extracted queries.
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface MigrateSqlServerSqlDbSyncTaskProperties extends ProjectTaskProperties {
  input?: MigrateSqlServerSqlDbSyncTaskInput;
  readonly output?: MigrateSqlServerSqlDbSyncTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the ValidationError class.
 * @constructor
 * Description about the errors happen while performing migration validation
 *
 * @property {string} [text] Error Text
 * @property {string} [severity] Severity of the error. Possible values
 * include: 'Message', 'Warning', 'Error'
 */
export interface ValidationError {
  text?: string;
  severity?: string;
}

/**
 * @class
 * Initializes a new instance of the WaitStatistics class.
 * @constructor
 * Wait statistics gathered during query batch execution
 *
 * @property {string} [waitType] Type of the Wait
 * @property {number} [waitTimeMs] Total wait time in millisecond(s) . Default
 * value: 0 .
 * @property {number} [waitCount] Total no. of waits
 */
export interface WaitStatistics {
  waitType?: string;
  waitTimeMs?: number;
  waitCount?: number;
}

/**
 * @class
 * Initializes a new instance of the ExecutionStatistics class.
 * @constructor
 * Description about the errors happen while performing migration validation
 *
 * @property {number} [executionCount] No. of query executions
 * @property {number} [cpuTimeMs] CPU Time in millisecond(s) for the query
 * execution
 * @property {number} [elapsedTimeMs] Time taken in millisecond(s) for
 * executing the query
 * @property {object} [waitStats] Dictionary of sql query execution wait types
 * and the respective statistics
 * @property {boolean} [hasErrors] Indicates whether the query resulted in an
 * error
 * @property {array} [sqlErrors] List of sql Errors
 */
export interface ExecutionStatistics {
  executionCount?: number;
  cpuTimeMs?: number;
  elapsedTimeMs?: number;
  waitStats?: { [propertyName: string]: WaitStatistics };
  hasErrors?: boolean;
  sqlErrors?: string[];
}

/**
 * @class
 * Initializes a new instance of the QueryExecutionResult class.
 * @constructor
 * Describes query analysis results for execution in source and target
 *
 * @property {string} [queryText] Query text retrieved from the source server
 * @property {number} [statementsInBatch] Total no. of statements in the batch
 * @property {object} [sourceResult] Query analysis result from the source
 * @property {number} [sourceResult.executionCount] No. of query executions
 * @property {number} [sourceResult.cpuTimeMs] CPU Time in millisecond(s) for
 * the query execution
 * @property {number} [sourceResult.elapsedTimeMs] Time taken in millisecond(s)
 * for executing the query
 * @property {object} [sourceResult.waitStats] Dictionary of sql query
 * execution wait types and the respective statistics
 * @property {boolean} [sourceResult.hasErrors] Indicates whether the query
 * resulted in an error
 * @property {array} [sourceResult.sqlErrors] List of sql Errors
 * @property {object} [targetResult] Query analysis result from the target
 * @property {number} [targetResult.executionCount] No. of query executions
 * @property {number} [targetResult.cpuTimeMs] CPU Time in millisecond(s) for
 * the query execution
 * @property {number} [targetResult.elapsedTimeMs] Time taken in millisecond(s)
 * for executing the query
 * @property {object} [targetResult.waitStats] Dictionary of sql query
 * execution wait types and the respective statistics
 * @property {boolean} [targetResult.hasErrors] Indicates whether the query
 * resulted in an error
 * @property {array} [targetResult.sqlErrors] List of sql Errors
 */
export interface QueryExecutionResult {
  queryText?: string;
  statementsInBatch?: number;
  sourceResult?: ExecutionStatistics;
  targetResult?: ExecutionStatistics;
}

/**
 * @class
 * Initializes a new instance of the QueryAnalysisValidationResult class.
 * @constructor
 * Results for query analysis comparison between the source and target
 *
 * @property {object} [queryResults] List of queries executed and it's
 * execution results in source and target
 * @property {string} [queryResults.queryText] Query text retrieved from the
 * source server
 * @property {number} [queryResults.statementsInBatch] Total no. of statements
 * in the batch
 * @property {object} [queryResults.sourceResult] Query analysis result from
 * the source
 * @property {number} [queryResults.sourceResult.executionCount] No. of query
 * executions
 * @property {number} [queryResults.sourceResult.cpuTimeMs] CPU Time in
 * millisecond(s) for the query execution
 * @property {number} [queryResults.sourceResult.elapsedTimeMs] Time taken in
 * millisecond(s) for executing the query
 * @property {object} [queryResults.sourceResult.waitStats] Dictionary of sql
 * query execution wait types and the respective statistics
 * @property {boolean} [queryResults.sourceResult.hasErrors] Indicates whether
 * the query resulted in an error
 * @property {array} [queryResults.sourceResult.sqlErrors] List of sql Errors
 * @property {object} [queryResults.targetResult] Query analysis result from
 * the target
 * @property {number} [queryResults.targetResult.executionCount] No. of query
 * executions
 * @property {number} [queryResults.targetResult.cpuTimeMs] CPU Time in
 * millisecond(s) for the query execution
 * @property {number} [queryResults.targetResult.elapsedTimeMs] Time taken in
 * millisecond(s) for executing the query
 * @property {object} [queryResults.targetResult.waitStats] Dictionary of sql
 * query execution wait types and the respective statistics
 * @property {boolean} [queryResults.targetResult.hasErrors] Indicates whether
 * the query resulted in an error
 * @property {array} [queryResults.targetResult.sqlErrors] List of sql Errors
 * @property {object} [validationErrors] Errors that are part of the execution
 * @property {string} [validationErrors.text] Error Text
 * @property {string} [validationErrors.severity] Severity of the error.
 * Possible values include: 'Message', 'Warning', 'Error'
 */
export interface QueryAnalysisValidationResult {
  queryResults?: QueryExecutionResult;
  validationErrors?: ValidationError;
}

/**
 * @class
 * Initializes a new instance of the SchemaComparisonValidationResultType class.
 * @constructor
 * Description about the errors happen while performing migration validation
 *
 * @property {string} [objectName] Name of the object that has the difference
 * @property {string} [objectType] Type of the object that has the difference.
 * e.g (Table/View/StoredProcedure). Possible values include:
 * 'StoredProcedures', 'Table', 'User', 'View', 'Function'
 * @property {string} [updateAction] Update action type with respect to target.
 * Possible values include: 'DeletedOnTarget', 'ChangedOnTarget',
 * 'AddedOnTarget'
 */
export interface SchemaComparisonValidationResultType {
  objectName?: string;
  objectType?: string;
  updateAction?: string;
}

/**
 * @class
 * Initializes a new instance of the SchemaComparisonValidationResult class.
 * @constructor
 * Results for schema comparison between the source and target
 *
 * @property {object} [schemaDifferences] List of schema differences between
 * the source and target databases
 * @property {string} [schemaDifferences.objectName] Name of the object that
 * has the difference
 * @property {string} [schemaDifferences.objectType] Type of the object that
 * has the difference. e.g (Table/View/StoredProcedure). Possible values
 * include: 'StoredProcedures', 'Table', 'User', 'View', 'Function'
 * @property {string} [schemaDifferences.updateAction] Update action type with
 * respect to target. Possible values include: 'DeletedOnTarget',
 * 'ChangedOnTarget', 'AddedOnTarget'
 * @property {object} [validationErrors] List of errors that happened while
 * performing schema compare validation
 * @property {string} [validationErrors.text] Error Text
 * @property {string} [validationErrors.severity] Severity of the error.
 * Possible values include: 'Message', 'Warning', 'Error'
 * @property {object} [sourceDatabaseObjectCount] Count of source database
 * objects
 * @property {object} [targetDatabaseObjectCount] Count of target database
 * objects
 */
export interface SchemaComparisonValidationResult {
  schemaDifferences?: SchemaComparisonValidationResultType;
  validationErrors?: ValidationError;
  sourceDatabaseObjectCount?: { [propertyName: string]: number };
  targetDatabaseObjectCount?: { [propertyName: string]: number };
}

/**
 * @class
 * Initializes a new instance of the DataIntegrityValidationResult class.
 * @constructor
 * Results for checksum based Data Integrity validation results
 *
 * @property {object} [failedObjects] List of failed table names of source and
 * target pair
 * @property {object} [validationErrors] List of errors that happened while
 * performing data integrity validation
 * @property {string} [validationErrors.text] Error Text
 * @property {string} [validationErrors.severity] Severity of the error.
 * Possible values include: 'Message', 'Warning', 'Error'
 */
export interface DataIntegrityValidationResult {
  failedObjects?: { [propertyName: string]: string };
  validationErrors?: ValidationError;
}

/**
 * @class
 * Initializes a new instance of the MigrationValidationDatabaseLevelResult class.
 * @constructor
 * Database level validation results
 *
 * @property {string} [id] Result identifier
 * @property {string} [migrationId] Migration Identifier
 * @property {string} [sourceDatabaseName] Name of the source database
 * @property {string} [targetDatabaseName] Name of the target database
 * @property {date} [startedOn] Validation start time
 * @property {date} [endedOn] Validation end time
 * @property {object} [dataIntegrityValidationResult] Provides data integrity
 * validation result between the source and target tables that are migrated.
 * @property {object} [dataIntegrityValidationResult.failedObjects] List of
 * failed table names of source and target pair
 * @property {object} [dataIntegrityValidationResult.validationErrors] List of
 * errors that happened while performing data integrity validation
 * @property {string} [dataIntegrityValidationResult.validationErrors.text]
 * Error Text
 * @property {string} [dataIntegrityValidationResult.validationErrors.severity]
 * Severity of the error. Possible values include: 'Message', 'Warning',
 * 'Error'
 * @property {object} [schemaValidationResult] Provides schema comparison
 * result between source and target database
 * @property {object} [schemaValidationResult.schemaDifferences] List of schema
 * differences between the source and target databases
 * @property {string} [schemaValidationResult.schemaDifferences.objectName]
 * Name of the object that has the difference
 * @property {string} [schemaValidationResult.schemaDifferences.objectType]
 * Type of the object that has the difference. e.g
 * (Table/View/StoredProcedure). Possible values include: 'StoredProcedures',
 * 'Table', 'User', 'View', 'Function'
 * @property {string} [schemaValidationResult.schemaDifferences.updateAction]
 * Update action type with respect to target. Possible values include:
 * 'DeletedOnTarget', 'ChangedOnTarget', 'AddedOnTarget'
 * @property {object} [schemaValidationResult.validationErrors] List of errors
 * that happened while performing schema compare validation
 * @property {string} [schemaValidationResult.validationErrors.text] Error Text
 * @property {string} [schemaValidationResult.validationErrors.severity]
 * Severity of the error. Possible values include: 'Message', 'Warning',
 * 'Error'
 * @property {object} [schemaValidationResult.sourceDatabaseObjectCount] Count
 * of source database objects
 * @property {object} [schemaValidationResult.targetDatabaseObjectCount] Count
 * of target database objects
 * @property {object} [queryAnalysisValidationResult] Results of some of the
 * query execution result between source and target database
 * @property {object} [queryAnalysisValidationResult.queryResults] List of
 * queries executed and it's execution results in source and target
 * @property {string} [queryAnalysisValidationResult.queryResults.queryText]
 * Query text retrieved from the source server
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.statementsInBatch] Total no. of
 * statements in the batch
 * @property {object} [queryAnalysisValidationResult.queryResults.sourceResult]
 * Query analysis result from the source
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.sourceResult.executionCount] No.
 * of query executions
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.sourceResult.cpuTimeMs] CPU Time
 * in millisecond(s) for the query execution
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.sourceResult.elapsedTimeMs] Time
 * taken in millisecond(s) for executing the query
 * @property {object}
 * [queryAnalysisValidationResult.queryResults.sourceResult.waitStats]
 * Dictionary of sql query execution wait types and the respective statistics
 * @property {boolean}
 * [queryAnalysisValidationResult.queryResults.sourceResult.hasErrors]
 * Indicates whether the query resulted in an error
 * @property {array}
 * [queryAnalysisValidationResult.queryResults.sourceResult.sqlErrors] List of
 * sql Errors
 * @property {object} [queryAnalysisValidationResult.queryResults.targetResult]
 * Query analysis result from the target
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.targetResult.executionCount] No.
 * of query executions
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.targetResult.cpuTimeMs] CPU Time
 * in millisecond(s) for the query execution
 * @property {number}
 * [queryAnalysisValidationResult.queryResults.targetResult.elapsedTimeMs] Time
 * taken in millisecond(s) for executing the query
 * @property {object}
 * [queryAnalysisValidationResult.queryResults.targetResult.waitStats]
 * Dictionary of sql query execution wait types and the respective statistics
 * @property {boolean}
 * [queryAnalysisValidationResult.queryResults.targetResult.hasErrors]
 * Indicates whether the query resulted in an error
 * @property {array}
 * [queryAnalysisValidationResult.queryResults.targetResult.sqlErrors] List of
 * sql Errors
 * @property {object} [queryAnalysisValidationResult.validationErrors] Errors
 * that are part of the execution
 * @property {string} [queryAnalysisValidationResult.validationErrors.text]
 * Error Text
 * @property {string} [queryAnalysisValidationResult.validationErrors.severity]
 * Severity of the error. Possible values include: 'Message', 'Warning',
 * 'Error'
 * @property {string} [status] Current status of validation at the database
 * level. Possible values include: 'Default', 'NotStarted', 'Initialized',
 * 'InProgress', 'Completed', 'CompletedWithIssues', 'Stopped', 'Failed'
 */
export interface MigrationValidationDatabaseLevelResult {
  readonly id?: string;
  readonly migrationId?: string;
  readonly sourceDatabaseName?: string;
  readonly targetDatabaseName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly dataIntegrityValidationResult?: DataIntegrityValidationResult;
  readonly schemaValidationResult?: SchemaComparisonValidationResult;
  readonly queryAnalysisValidationResult?: QueryAnalysisValidationResult;
  readonly status?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrationValidationDatabaseSummaryResult class.
 * @constructor
 * Migration Validation Database level summary result
 *
 * @property {string} [id] Result identifier
 * @property {string} [migrationId] Migration Identifier
 * @property {string} [sourceDatabaseName] Name of the source database
 * @property {string} [targetDatabaseName] Name of the target database
 * @property {date} [startedOn] Validation start time
 * @property {date} [endedOn] Validation end time
 * @property {string} [status] Current status of validation at the database
 * level. Possible values include: 'Default', 'NotStarted', 'Initialized',
 * 'InProgress', 'Completed', 'CompletedWithIssues', 'Stopped', 'Failed'
 */
export interface MigrationValidationDatabaseSummaryResult {
  readonly id?: string;
  readonly migrationId?: string;
  readonly sourceDatabaseName?: string;
  readonly targetDatabaseName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly status?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrationValidationResult class.
 * @constructor
 * Migration Validation Result
 *
 * @property {string} [id] Migration validation result identifier
 * @property {string} [migrationId] Migration Identifier
 * @property {object} [summaryResults] Validation summary results for each
 * database
 * @property {string} [status] Current status of validation at the migration
 * level. Status from the database validation result status will be aggregated
 * here. Possible values include: 'Default', 'NotStarted', 'Initialized',
 * 'InProgress', 'Completed', 'CompletedWithIssues', 'Stopped', 'Failed'
 */
export interface MigrationValidationResult {
  readonly id?: string;
  readonly migrationId?: string;
  summaryResults?: { [propertyName: string]: MigrationValidationDatabaseSummaryResult };
  readonly status?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskOutput class.
 * @constructor
 * Output for the task that migrates on-prem SQL Server databases to Azure SQL
 * Database
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface MigrateSqlServerSqlDbTaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskOutputError class.
 * @constructor
 * @property {object} [error] Migration error
 * @property {string} [error.message] Error message
 * @property {string} [error.actionableMessage] Actionable steps for this
 * exception
 * @property {string} [error.filePath] The path to the file where exception
 * occurred
 * @property {string} [error.lineNumber] The line number where exception
 * occurred
 * @property {number} [error.hResult] Coded numerical value that is assigned to
 * a specific exception
 * @property {string} [error.stackTrace] Stack trace
 */
export interface MigrateSqlServerSqlDbTaskOutputError extends MigrateSqlServerSqlDbTaskOutput {
  readonly error?: ReportableException;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskOutputTableLevel class.
 * @constructor
 * @property {string} [objectName] Name of the item
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {string} [statusMessage] Status message
 * @property {number} [itemsCount] Number of items
 * @property {number} [itemsCompletedCount] Number of successfully completed
 * items
 * @property {string} [errorPrefix] Wildcard string prefix to use for querying
 * all errors of the item
 * @property {string} [resultPrefix] Wildcard string prefix to use for querying
 * all sub-tem results of the item
 */
export interface MigrateSqlServerSqlDbTaskOutputTableLevel extends MigrateSqlServerSqlDbTaskOutput {
  readonly objectName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly state?: string;
  readonly statusMessage?: string;
  readonly itemsCount?: number;
  readonly itemsCompletedCount?: number;
  readonly errorPrefix?: string;
  readonly resultPrefix?: string;
}

/**
 * @class
 * Initializes a new instance of the DataItemMigrationSummaryResult class.
 * @constructor
 * Basic summary of a data item migration
 *
 * @property {string} [name] Name of the item
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {string} [statusMessage] Status message
 * @property {number} [itemsCount] Number of items
 * @property {number} [itemsCompletedCount] Number of successfully completed
 * items
 * @property {string} [errorPrefix] Wildcard string prefix to use for querying
 * all errors of the item
 * @property {string} [resultPrefix] Wildcard string prefix to use for querying
 * all sub-tem results of the item
 */
export interface DataItemMigrationSummaryResult {
  readonly name?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly state?: string;
  readonly statusMessage?: string;
  readonly itemsCount?: number;
  readonly itemsCompletedCount?: number;
  readonly errorPrefix?: string;
  readonly resultPrefix?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskOutputDatabaseLevel class.
 * @constructor
 * @property {string} [databaseName] Name of the item
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {string} [stage] Migration stage that this database is in.
 * Possible values include: 'None', 'Initialize', 'Backup', 'FileCopy',
 * 'Restore', 'Completed'
 * @property {string} [statusMessage] Status message
 * @property {string} [message] Migration progress message
 * @property {number} [numberOfObjects] Number of objects
 * @property {number} [numberOfObjectsCompleted] Number of successfully
 * completed objects
 * @property {number} [errorCount] Number of database/object errors.
 * @property {string} [errorPrefix] Wildcard string prefix to use for querying
 * all errors of the item
 * @property {string} [resultPrefix] Wildcard string prefix to use for querying
 * all sub-tem results of the item
 * @property {array} [exceptionsAndWarnings] Migration exceptions and warnings.
 * @property {object} [objectSummary] Summary of object results in the
 * migration
 */
export interface MigrateSqlServerSqlDbTaskOutputDatabaseLevel extends MigrateSqlServerSqlDbTaskOutput {
  readonly databaseName?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly state?: string;
  readonly stage?: string;
  readonly statusMessage?: string;
  readonly message?: string;
  readonly numberOfObjects?: number;
  readonly numberOfObjectsCompleted?: number;
  readonly errorCount?: number;
  readonly errorPrefix?: string;
  readonly resultPrefix?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
  readonly objectSummary?: { [propertyName: string]: DataItemMigrationSummaryResult };
}

/**
 * @class
 * Initializes a new instance of the MigrationReportResult class.
 * @constructor
 * Migration validation report result, contains the url for downloading the
 * generated report.
 *
 * @property {string} [id] Migration validation result identifier
 * @property {string} [reportUrl] The url of the report.
 */
export interface MigrationReportResult {
  id?: string;
  reportUrl?: string;
}

/**
 * @class
 * Initializes a new instance of the DatabaseSummaryResult class.
 * @constructor
 * Summary of database results in the migration
 *
 * @property {number} [sizeMB] Size of the database in megabytes
 */
export interface DatabaseSummaryResult extends DataItemMigrationSummaryResult {
  readonly sizeMB?: number;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskOutputMigrationLevel class.
 * @constructor
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {number} [durationInSeconds] Duration of task execution in
 * seconds.
 * @property {string} [status] Current status of migration. Possible values
 * include: 'Default', 'Connecting', 'SourceAndTargetSelected', 'SelectLogins',
 * 'Configured', 'Running', 'Error', 'Stopped', 'Completed',
 * 'CompletedWithWarnings'
 * @property {string} [statusMessage] Migration status message
 * @property {string} [message] Migration progress message
 * @property {object} [databases] Selected databases as a map from database
 * name to database id
 * @property {object} [databaseSummary] Summary of database results in the
 * migration
 * @property {object} [migrationValidationResult] Migration Validation Results
 * @property {string} [migrationValidationResult.id] Migration validation
 * result identifier
 * @property {string} [migrationValidationResult.migrationId] Migration
 * Identifier
 * @property {object} [migrationValidationResult.summaryResults] Validation
 * summary results for each database
 * @property {string} [migrationValidationResult.status] Current status of
 * validation at the migration level. Status from the database validation
 * result status will be aggregated here. Possible values include: 'Default',
 * 'NotStarted', 'Initialized', 'InProgress', 'Completed',
 * 'CompletedWithIssues', 'Stopped', 'Failed'
 * @property {object} [migrationReportResult] Migration Report Result, provides
 * unique url for downloading your migration report.
 * @property {string} [migrationReportResult.id] Migration validation result
 * identifier
 * @property {string} [migrationReportResult.reportUrl] The url of the report.
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServerBrandVersion] Source server brand version
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServerBrandVersion] Target server brand version
 * @property {array} [exceptionsAndWarnings] Migration exceptions and warnings.
 */
export interface MigrateSqlServerSqlDbTaskOutputMigrationLevel extends MigrateSqlServerSqlDbTaskOutput {
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly durationInSeconds?: number;
  readonly status?: string;
  readonly statusMessage?: string;
  readonly message?: string;
  readonly databases?: { [propertyName: string]: string };
  readonly databaseSummary?: { [propertyName: string]: DatabaseSummaryResult };
  migrationValidationResult?: MigrationValidationResult;
  migrationReportResult?: MigrationReportResult;
  readonly sourceServerVersion?: string;
  readonly sourceServerBrandVersion?: string;
  readonly targetServerVersion?: string;
  readonly targetServerBrandVersion?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbDatabaseInput class.
 * @constructor
 * Database specific information for SQL to Azure SQL DB migration task inputs
 *
 * @property {string} [name] Name of the database
 * @property {string} [targetDatabaseName] Name of target database. Note:
 * Target database will be truncated before starting migration.
 * @property {boolean} [makeSourceDbReadOnly] Whether to set database read only
 * before migration
 * @property {object} [tableMap] Mapping of source to target tables
 */
export interface MigrateSqlServerSqlDbDatabaseInput {
  name?: string;
  targetDatabaseName?: string;
  makeSourceDbReadOnly?: boolean;
  tableMap?: { [propertyName: string]: string };
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskInput class.
 * @constructor
 * Input for the task that migrates on-prem SQL Server databases to Azure SQL
 * Database
 *
 * @property {array} selectedDatabases Databases to migrate
 * @property {object} [validationOptions] Options for enabling various post
 * migration validations. Available options,
 * 1.) Data Integrity Check: Performs a checksum based comparison on source and
 * target tables after the migration to ensure the correctness of the data.
 * 2.) Schema Validation: Performs a thorough schema comparison between the
 * source and target tables and provides a list of differences between the
 * source and target database, 3.) Query Analysis: Executes a set of queries
 * picked up automatically either from the Query Plan Cache or Query Store and
 * execute them and compares the execution time between the source and target
 * database.
 * @property {boolean} [validationOptions.enableSchemaValidation] Allows to
 * compare the schema information between source and target.
 * @property {boolean} [validationOptions.enableDataIntegrityValidation] Allows
 * to perform a checksum based data integrity validation between source and
 * target for the selected database / tables .
 * @property {boolean} [validationOptions.enableQueryAnalysisValidation] Allows
 * to perform a quick and intelligent query analysis by retrieving queries from
 * the source database and executes them in the target. The result will have
 * execution statistics for executions in source and target databases for the
 * extracted queries.
 */
export interface MigrateSqlServerSqlDbTaskInput extends SqlMigrationTaskInput {
  selectedDatabases: MigrateSqlServerSqlDbDatabaseInput[];
  validationOptions?: MigrationValidationOptions;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlDbTaskProperties class.
 * @constructor
 * Properties for the task that migrates on-prem SQL Server databases to Azure
 * SQL Database
 *
 * @property {object} [input] Task input
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {object} [input.validationOptions] Options for enabling various
 * post migration validations. Available options,
 * 1.) Data Integrity Check: Performs a checksum based comparison on source and
 * target tables after the migration to ensure the correctness of the data.
 * 2.) Schema Validation: Performs a thorough schema comparison between the
 * source and target tables and provides a list of differences between the
 * source and target database, 3.) Query Analysis: Executes a set of queries
 * picked up automatically either from the Query Plan Cache or Query Store and
 * execute them and compares the execution time between the source and target
 * database.
 * @property {boolean} [input.validationOptions.enableSchemaValidation] Allows
 * to compare the schema information between source and target.
 * @property {boolean} [input.validationOptions.enableDataIntegrityValidation]
 * Allows to perform a checksum based data integrity validation between source
 * and target for the selected database / tables .
 * @property {boolean} [input.validationOptions.enableQueryAnalysisValidation]
 * Allows to perform a quick and intelligent query analysis by retrieving
 * queries from the source database and executes them in the target. The result
 * will have execution statistics for executions in source and target databases
 * for the extracted queries.
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface MigrateSqlServerSqlDbTaskProperties extends ProjectTaskProperties {
  input?: MigrateSqlServerSqlDbTaskInput;
  readonly output?: MigrateSqlServerSqlDbTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskOutput class.
 * @constructor
 * Output for task that migrates SQL Server databases to Azure SQL Database
 * Managed Instance.
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface MigrateSqlServerSqlMITaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskOutputError class.
 * @constructor
 * @property {object} [error] Migration error
 * @property {string} [error.message] Error message
 * @property {string} [error.actionableMessage] Actionable steps for this
 * exception
 * @property {string} [error.filePath] The path to the file where exception
 * occurred
 * @property {string} [error.lineNumber] The line number where exception
 * occurred
 * @property {number} [error.hResult] Coded numerical value that is assigned to
 * a specific exception
 * @property {string} [error.stackTrace] Stack trace
 */
export interface MigrateSqlServerSqlMITaskOutputError extends MigrateSqlServerSqlMITaskOutput {
  readonly error?: ReportableException;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskOutputLoginLevel class.
 * @constructor
 * @property {string} [loginName] Login name.
 * @property {string} [state] Current state of login. Possible values include:
 * 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped', 'Stopped'
 * @property {string} [stage] Current stage of login. Possible values include:
 * 'None', 'Initialize', 'LoginMigration', 'EstablishUserMapping',
 * 'AssignRoleMembership', 'AssignRoleOwnership', 'EstablishServerPermissions',
 * 'EstablishObjectPermissions', 'Completed'
 * @property {date} [startedOn] Login migration start time
 * @property {date} [endedOn] Login migration end time
 * @property {string} [message] Login migration progress message
 * @property {array} [exceptionsAndWarnings] Login migration errors and
 * warnings per login
 */
export interface MigrateSqlServerSqlMITaskOutputLoginLevel extends MigrateSqlServerSqlMITaskOutput {
  readonly loginName?: string;
  readonly state?: string;
  readonly stage?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly message?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskOutputAgentJobLevel class.
 * @constructor
 * @property {string} [name] Agent Job name.
 * @property {boolean} [isEnabled] The state of the original Agent Job.
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [message] Migration progress message
 * @property {array} [exceptionsAndWarnings] Migration errors and warnings per
 * job
 */
export interface MigrateSqlServerSqlMITaskOutputAgentJobLevel extends MigrateSqlServerSqlMITaskOutput {
  readonly name?: string;
  readonly isEnabled?: boolean;
  readonly state?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly message?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskOutputDatabaseLevel class.
 * @constructor
 * @property {string} [databaseName] Name of the database
 * @property {number} [sizeMB] Size of the database in megabytes
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {string} [stage] Current stage of migration. Possible values
 * include: 'None', 'Initialize', 'Backup', 'FileCopy', 'Restore', 'Completed'
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [message] Migration progress message
 * @property {array} [exceptionsAndWarnings] Migration exceptions and warnings
 */
export interface MigrateSqlServerSqlMITaskOutputDatabaseLevel extends MigrateSqlServerSqlMITaskOutput {
  readonly databaseName?: string;
  readonly sizeMB?: number;
  readonly state?: string;
  readonly stage?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly message?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the StartMigrationScenarioServerRoleResult class.
 * @constructor
 * Server role migration result
 *
 * @property {string} [name] Name of server role.
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {array} [exceptionsAndWarnings] Migration exceptions and warnings.
 */
export interface StartMigrationScenarioServerRoleResult {
  readonly name?: string;
  readonly state?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskOutputMigrationLevel class.
 * @constructor
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [status] Current status of migration. Possible values
 * include: 'Default', 'Connecting', 'SourceAndTargetSelected', 'SelectLogins',
 * 'Configured', 'Running', 'Error', 'Stopped', 'Completed',
 * 'CompletedWithWarnings'
 * @property {string} [state] Current state of migration. Possible values
 * include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed', 'Skipped',
 * 'Stopped'
 * @property {object} [agentJobs] Selected agent jobs as a map from name to id
 * @property {object} [logins] Selected logins as a map from name to id
 * @property {string} [message] Migration progress message
 * @property {object} [serverRoleResults] Map of server role migration results.
 * @property {object} [orphanedUsers] Map of users to database name of orphaned
 * users.
 * @property {object} [databases] Selected databases as a map from database
 * name to database id
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServerBrandVersion] Source server brand version
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServerBrandVersion] Target server brand version
 * @property {array} [exceptionsAndWarnings] Migration exceptions and warnings.
 */
export interface MigrateSqlServerSqlMITaskOutputMigrationLevel extends MigrateSqlServerSqlMITaskOutput {
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly status?: string;
  readonly state?: string;
  readonly agentJobs?: { [propertyName: string]: string };
  readonly logins?: { [propertyName: string]: string };
  readonly message?: string;
  readonly serverRoleResults?: { [propertyName: string]: StartMigrationScenarioServerRoleResult };
  readonly orphanedUsers?: { [propertyName: string]: string };
  readonly databases?: { [propertyName: string]: string };
  readonly sourceServerVersion?: string;
  readonly sourceServerBrandVersion?: string;
  readonly targetServerVersion?: string;
  readonly targetServerBrandVersion?: string;
  readonly exceptionsAndWarnings?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskInput class.
 * @constructor
 * Input for task that migrates SQL Server databases to Azure SQL Database
 * Managed Instance.
 *
 * @property {array} selectedDatabases Databases to migrate
 * @property {array} [selectedLogins] Logins to migrate.
 * @property {array} [selectedAgentJobs] Agent Jobs to migrate.
 * @property {object} [backupFileShare] Backup file share information for all
 * selected databases.
 * @property {string} [backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [backupFileShare.password] Password credential used to
 * connect to the share location.
 * @property {string} [backupFileShare.path] The folder path for this share.
 * @property {object} backupBlobShare SAS URI of Azure Storage Account
 * Container to be used for storing backup files.
 * @property {string} [backupBlobShare.sasUri] SAS URI of Azure Storage Account
 * Container.
 * @property {string} [backupMode] Backup Mode to specify whether to use
 * existing backup or create new backup. If using existing backups, backup file
 * paths are required to be provided in selectedDatabases. Possible values
 * include: 'CreateBackup', 'ExistingBackup'
 */
export interface MigrateSqlServerSqlMITaskInput extends SqlMigrationTaskInput {
  selectedDatabases: MigrateSqlServerSqlMIDatabaseInput[];
  selectedLogins?: string[];
  selectedAgentJobs?: string[];
  backupFileShare?: FileShare;
  backupBlobShare: BlobShare;
  backupMode?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlMITaskProperties class.
 * @constructor
 * Properties for task that migrates SQL Server databases to Azure SQL Database
 * Managed Instance
 *
 * @property {object} [input] Task input
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {array} [input.selectedLogins] Logins to migrate.
 * @property {array} [input.selectedAgentJobs] Agent Jobs to migrate.
 * @property {object} [input.backupFileShare] Backup file share information for
 * all selected databases.
 * @property {string} [input.backupFileShare.userName] User name credential to
 * connect to the share location
 * @property {string} [input.backupFileShare.password] Password credential used
 * to connect to the share location.
 * @property {string} [input.backupFileShare.path] The folder path for this
 * share.
 * @property {object} [input.backupBlobShare] SAS URI of Azure Storage Account
 * Container to be used for storing backup files.
 * @property {string} [input.backupBlobShare.sasUri] SAS URI of Azure Storage
 * Account Container.
 * @property {string} [input.backupMode] Backup Mode to specify whether to use
 * existing backup or create new backup. If using existing backups, backup file
 * paths are required to be provided in selectedDatabases. Possible values
 * include: 'CreateBackup', 'ExistingBackup'
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface MigrateSqlServerSqlMITaskProperties extends ProjectTaskProperties {
  input?: MigrateSqlServerSqlMITaskInput;
  readonly output?: MigrateSqlServerSqlMITaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateMongoDbTaskProperties class.
 * @constructor
 * Properties for the task that migrates data between MongoDB data sources
 *
 * @property {object} [input]
 * @property {number} [input.boostRUs] The RU limit on a CosmosDB target that
 * collections will be temporarily increased to (if lower) during the initial
 * copy of a migration, from 10,000 to 1,000,000, or 0 to use the default boost
 * (which is generally the maximum), or null to not boost the RUs. This setting
 * has no effect on non-CosmosDB targets.
 * @property {object} [input.databases] The databases on the source cluster to
 * migrate to the target. The keys are the names of the databases.
 * @property {string} [input.replication] Describes how changes will be
 * replicated from the source to the target. The default is OneTime. Possible
 * values include: 'Disabled', 'OneTime', 'Continuous'
 * @property {object} [input.source] Settings used to connect to the source
 * cluster
 * @property {string} [input.source.connectionString] A MongoDB connection
 * string or blob container URL. The user name and password can be specified
 * here or in the userName and password properties
 * @property {object} [input.target] Settings used to connect to the target
 * cluster
 * @property {string} [input.target.connectionString] A MongoDB connection
 * string or blob container URL. The user name and password can be specified
 * here or in the userName and password properties
 * @property {object} [input.throttling] Settings used to limit the resource
 * usage of the migration
 * @property {number} [input.throttling.minFreeCpu] The percentage of CPU time
 * that the migrator will try to avoid using, from 0 to 100
 * @property {number} [input.throttling.minFreeMemoryMb] The number of
 * megabytes of RAM that the migrator will try to avoid using
 * @property {number} [input.throttling.maxParallelism] The maximum number of
 * work items (e.g. collection copies) that will be processed in parallel
 * @property {array} [output]
 */
export interface MigrateMongoDbTaskProperties extends ProjectTaskProperties {
  input?: MongoDbMigrationSettings;
  readonly output?: MongoDbProgress[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetAzureDbForMySqlTaskOutput class.
 * @constructor
 * Output for the task that validates connection to Azure Database for MySQL
 * and target server requirements
 *
 * @property {string} [id] Result identifier
 * @property {string} [serverVersion] Version of the target server
 * @property {array} [databases] List of databases on target server
 * @property {string} [targetServerBrandVersion] Target server brand version
 * @property {array} [validationErrors] Validation errors associated with the
 * task
 */
export interface ConnectToTargetAzureDbForMySqlTaskOutput {
  readonly id?: string;
  readonly serverVersion?: string;
  readonly databases?: string[];
  readonly targetServerBrandVersion?: string;
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetAzureDbForMySqlTaskInput class.
 * @constructor
 * Input for the task that validates connection to Azure Database for MySQL and
 * target server requirements
 *
 * @property {object} sourceConnectionInfo Connection information for source
 * MySQL server
 * @property {string} [sourceConnectionInfo.serverName] Name of the server
 * @property {number} [sourceConnectionInfo.port] Port for Server
 * @property {object} targetConnectionInfo Connection information for target
 * Azure Database for MySQL server
 * @property {string} [targetConnectionInfo.serverName] Name of the server
 * @property {number} [targetConnectionInfo.port] Port for Server
 */
export interface ConnectToTargetAzureDbForMySqlTaskInput {
  sourceConnectionInfo: MySqlConnectionInfo;
  targetConnectionInfo: MySqlConnectionInfo;
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetAzureDbForMySqlTaskProperties class.
 * @constructor
 * Properties for the task that validates connection to Azure Database for
 * MySQL and target server requirements
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * source MySQL server
 * @property {string} [input.sourceConnectionInfo.serverName] Name of the
 * server
 * @property {number} [input.sourceConnectionInfo.port] Port for Server
 * @property {object} [input.targetConnectionInfo] Connection information for
 * target Azure Database for MySQL server
 * @property {string} [input.targetConnectionInfo.serverName] Name of the
 * server
 * @property {number} [input.targetConnectionInfo.port] Port for Server
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToTargetAzureDbForMySqlTaskProperties extends ProjectTaskProperties {
  input?: ConnectToTargetAzureDbForMySqlTaskInput;
  readonly output?: ConnectToTargetAzureDbForMySqlTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlMITaskOutput class.
 * @constructor
 * Output for the task that validates connection to Azure SQL Database Managed
 * Instance.
 *
 * @property {string} [id] Result identifier
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServerBrandVersion] Target server brand version
 * @property {array} [logins] List of logins on the target server.
 * @property {array} [agentJobs] List of agent jobs on the target server.
 * @property {array} [validationErrors] Validation errors
 */
export interface ConnectToTargetSqlMITaskOutput {
  readonly id?: string;
  readonly targetServerVersion?: string;
  readonly targetServerBrandVersion?: string;
  readonly logins?: string[];
  readonly agentJobs?: string[];
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlMITaskInput class.
 * @constructor
 * Input for the task that validates connection to Azure SQL Database Managed
 * Instance.
 *
 * @property {object} targetConnectionInfo Connection information for target
 * SQL Server
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 */
export interface ConnectToTargetSqlMITaskInput {
  targetConnectionInfo: SqlConnectionInfo;
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlMITaskProperties class.
 * @constructor
 * Properties for the task that validates connection to Azure SQL Database
 * Managed Instance
 *
 * @property {object} [input] Task input
 * @property {object} [input.targetConnectionInfo] Connection information for
 * target SQL Server
 * @property {string} [input.targetConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.targetConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.targetConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.targetConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.targetConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.targetConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToTargetSqlMITaskProperties extends ProjectTaskProperties {
  input?: ConnectToTargetSqlMITaskInput;
  readonly output?: ConnectToTargetSqlMITaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the DatabaseTable class.
 * @constructor
 * Table properties
 *
 * @property {boolean} [hasRows] Indicates whether table is empty or not
 * @property {string} [name] Schema-qualified name of the table
 */
export interface DatabaseTable {
  readonly hasRows?: boolean;
  readonly name?: string;
}

/**
 * @class
 * Initializes a new instance of the GetUserTablesSqlSyncTaskOutput class.
 * @constructor
 * Output of the task that collects user tables for the given list of databases
 *
 * @property {object} [databasesToSourceTables] Mapping from database name to
 * list of source tables
 * @property {object} [databasesToTargetTables] Mapping from database name to
 * list of target tables
 * @property {object} [tableValidationErrors] Mapping from database name to
 * list of validation errors
 * @property {array} [validationErrors] Validation errors
 */
export interface GetUserTablesSqlSyncTaskOutput {
  readonly databasesToSourceTables?: { [propertyName: string]: DatabaseTable[] };
  readonly databasesToTargetTables?: { [propertyName: string]: DatabaseTable[] };
  readonly tableValidationErrors?: { [propertyName: string]: string[] };
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the GetUserTablesSqlSyncTaskInput class.
 * @constructor
 * Input for the task that collects user tables for the given list of databases
 *
 * @property {object} sourceConnectionInfo Connection information for SQL
 * Server
 * @property {string} [sourceConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [sourceConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [sourceConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [sourceConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [sourceConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [sourceConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} targetConnectionInfo Connection information for SQL DB
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {array} selectedSourceDatabases List of source database names to
 * collect tables for
 * @property {array} selectedTargetDatabases List of target database names to
 * collect tables for
 */
export interface GetUserTablesSqlSyncTaskInput {
  sourceConnectionInfo: SqlConnectionInfo;
  targetConnectionInfo: SqlConnectionInfo;
  selectedSourceDatabases: string[];
  selectedTargetDatabases: string[];
}

/**
 * @class
 * Initializes a new instance of the GetUserTablesSqlSyncTaskProperties class.
 * @constructor
 * Properties for the task that collects user tables for the given list of
 * databases
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * SQL Server
 * @property {string} [input.sourceConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.sourceConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.sourceConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.sourceConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.sourceConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.sourceConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {object} [input.targetConnectionInfo] Connection information for
 * SQL DB
 * @property {string} [input.targetConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.targetConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.targetConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.targetConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.targetConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.targetConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {array} [input.selectedSourceDatabases] List of source database
 * names to collect tables for
 * @property {array} [input.selectedTargetDatabases] List of target database
 * names to collect tables for
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface GetUserTablesSqlSyncTaskProperties extends ProjectTaskProperties {
  input?: GetUserTablesSqlSyncTaskInput;
  readonly output?: GetUserTablesSqlSyncTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the GetUserTablesSqlTaskOutput class.
 * @constructor
 * Output of the task that collects user tables for the given list of databases
 *
 * @property {string} [id] Result identifier
 * @property {object} [databasesToTables] Mapping from database name to list of
 * tables
 * @property {array} [validationErrors] Validation errors
 */
export interface GetUserTablesSqlTaskOutput {
  readonly id?: string;
  readonly databasesToTables?: { [propertyName: string]: DatabaseTable[] };
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the GetUserTablesSqlTaskInput class.
 * @constructor
 * Input for the task that collects user tables for the given list of databases
 *
 * @property {object} connectionInfo Connection information for SQL Server
 * @property {string} [connectionInfo.dataSource] Data source in the format
 * Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [connectionInfo.authentication] Authentication type to
 * use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [connectionInfo.encryptConnection] Whether to encrypt
 * the connection
 * @property {string} [connectionInfo.additionalSettings] Additional connection
 * settings
 * @property {boolean} [connectionInfo.trustServerCertificate] Whether to trust
 * the server certificate
 * @property {string} [connectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {array} selectedDatabases List of database names to collect tables
 * for
 */
export interface GetUserTablesSqlTaskInput {
  connectionInfo: SqlConnectionInfo;
  selectedDatabases: string[];
}

/**
 * @class
 * Initializes a new instance of the GetUserTablesSqlTaskProperties class.
 * @constructor
 * Properties for the task that collects user tables for the given list of
 * databases
 *
 * @property {object} [input] Task input
 * @property {object} [input.connectionInfo] Connection information for SQL
 * Server
 * @property {string} [input.connectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.connectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.connectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [input.connectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [input.connectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [input.connectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {array} [input.selectedDatabases] List of database names to
 * collect tables for
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface GetUserTablesSqlTaskProperties extends ProjectTaskProperties {
  input?: GetUserTablesSqlTaskInput;
  readonly output?: GetUserTablesSqlTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlDbTaskOutput class.
 * @constructor
 * Output for the task that validates connection to SQL DB and target server
 * requirements
 *
 * @property {string} [id] Result identifier
 * @property {object} [databases] Source databases as a map from database name
 * to database id
 * @property {string} [targetServerVersion] Version of the target server
 * @property {string} [targetServerBrandVersion] Target server brand version
 */
export interface ConnectToTargetSqlDbTaskOutput {
  readonly id?: string;
  readonly databases?: { [propertyName: string]: string };
  readonly targetServerVersion?: string;
  readonly targetServerBrandVersion?: string;
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlSqlDbSyncTaskInput class.
 * @constructor
 * Input for the task that validates connection to Azure SQL DB and target
 * server requirements
 *
 * @property {object} sourceConnectionInfo Connection information for source
 * SQL Server
 * @property {string} [sourceConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [sourceConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [sourceConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [sourceConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [sourceConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [sourceConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {object} targetConnectionInfo Connection information for target
 * SQL DB
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 */
export interface ConnectToTargetSqlSqlDbSyncTaskInput {
  sourceConnectionInfo: SqlConnectionInfo;
  targetConnectionInfo: SqlConnectionInfo;
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlSqlDbSyncTaskProperties class.
 * @constructor
 * Properties for the task that validates connection to SQL DB and target
 * server requirements for online migration
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * source SQL Server
 * @property {string} [input.sourceConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.sourceConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.sourceConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.sourceConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.sourceConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.sourceConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {object} [input.targetConnectionInfo] Connection information for
 * target SQL DB
 * @property {string} [input.targetConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.targetConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.targetConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.targetConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.targetConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.targetConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToTargetSqlSqlDbSyncTaskProperties extends ProjectTaskProperties {
  input?: ConnectToTargetSqlSqlDbSyncTaskInput;
  readonly output?: ConnectToTargetSqlDbTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlDbTaskInput class.
 * @constructor
 * Input for the task that validates connection to SQL DB and target server
 * requirements
 *
 * @property {object} targetConnectionInfo Connection information for target
 * SQL DB
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 */
export interface ConnectToTargetSqlDbTaskInput {
  targetConnectionInfo: SqlConnectionInfo;
}

/**
 * @class
 * Initializes a new instance of the ConnectToTargetSqlDbTaskProperties class.
 * @constructor
 * Properties for the task that validates connection to SQL DB and target
 * server requirements
 *
 * @property {object} [input] Task input
 * @property {object} [input.targetConnectionInfo] Connection information for
 * target SQL DB
 * @property {string} [input.targetConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.targetConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.targetConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.targetConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.targetConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.targetConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToTargetSqlDbTaskProperties extends ProjectTaskProperties {
  input?: ConnectToTargetSqlDbTaskInput;
  readonly output?: ConnectToTargetSqlDbTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MigrationEligibilityInfo class.
 * @constructor
 * Information about migration eligibility of a server object
 *
 * @property {boolean} [isEligibileForMigration] Whether object is eligible for
 * migration or not.
 * @property {array} [validationMessages] Information about eligibility failure
 * for the server object.
 */
export interface MigrationEligibilityInfo {
  readonly isEligibileForMigration?: boolean;
  readonly validationMessages?: string[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskOutput class.
 * @constructor
 * Output for the task that validates connection to SQL Server and also
 * validates source server requirements
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface ConnectToSourceSqlServerTaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskOutputAgentJobLevel class.
 * @constructor
 * Agent Job level output for the task that validates connection to SQL Server
 * and also validates source server requirements
 *
 * @property {string} [name] Agent Job name
 * @property {string} [jobCategory] The type of Agent Job.
 * @property {boolean} [isEnabled] The state of the original Agent Job.
 * @property {string} [jobOwner] The owner of the Agent Job
 * @property {date} [lastExecutedOn] UTC Date and time when the Agent Job was
 * last executed.
 * @property {array} [validationErrors] Validation errors
 * @property {object} [migrationEligibility] Information about eligiblity of
 * agent job for migration.
 * @property {boolean} [migrationEligibility.isEligibileForMigration] Whether
 * object is eligible for migration or not.
 * @property {array} [migrationEligibility.validationMessages] Information
 * about eligibility failure for the server object.
 */
export interface ConnectToSourceSqlServerTaskOutputAgentJobLevel extends ConnectToSourceSqlServerTaskOutput {
  readonly name?: string;
  readonly jobCategory?: string;
  readonly isEnabled?: boolean;
  readonly jobOwner?: string;
  readonly lastExecutedOn?: Date;
  readonly validationErrors?: ReportableException[];
  readonly migrationEligibility?: MigrationEligibilityInfo;
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskOutputLoginLevel class.
 * @constructor
 * Login level output for the task that validates connection to SQL Server and
 * also validates source server requirements
 *
 * @property {string} [name] Login name.
 * @property {string} [loginType] The type of login. Possible values include:
 * 'WindowsUser', 'WindowsGroup', 'SqlLogin', 'Certificate', 'AsymmetricKey',
 * 'ExternalUser', 'ExternalGroup'
 * @property {string} [defaultDatabase] The default database for the login.
 * @property {boolean} [isEnabled] The state of the login.
 * @property {object} [migrationEligibility] Information about eligiblity of
 * login for migration.
 * @property {boolean} [migrationEligibility.isEligibileForMigration] Whether
 * object is eligible for migration or not.
 * @property {array} [migrationEligibility.validationMessages] Information
 * about eligibility failure for the server object.
 */
export interface ConnectToSourceSqlServerTaskOutputLoginLevel extends ConnectToSourceSqlServerTaskOutput {
  readonly name?: string;
  readonly loginType?: string;
  readonly defaultDatabase?: string;
  readonly isEnabled?: boolean;
  readonly migrationEligibility?: MigrationEligibilityInfo;
}

/**
 * @class
 * Initializes a new instance of the DatabaseFileInfo class.
 * @constructor
 * Database file specific information
 *
 * @property {string} [databaseName] Name of the database
 * @property {string} [id] Unique identifier for database file
 * @property {string} [logicalName] Logical name of the file
 * @property {string} [physicalFullName] Operating-system full path of the file
 * @property {string} [restoreFullName] Suggested full path of the file for
 * restoring
 * @property {string} [fileType] Database file type. Possible values include:
 * 'Rows', 'Log', 'Filestream', 'NotSupported', 'Fulltext'
 * @property {number} [sizeMB] Size of the file in megabytes
 */
export interface DatabaseFileInfo {
  databaseName?: string;
  id?: string;
  logicalName?: string;
  physicalFullName?: string;
  restoreFullName?: string;
  fileType?: string;
  sizeMB?: number;
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskOutputDatabaseLevel class.
 * @constructor
 * Database level output for the task that validates connection to SQL Server
 * and also validates source server requirements
 *
 * @property {string} [name] Database name
 * @property {number} [sizeMB] Size of the file in megabytes
 * @property {array} [databaseFiles] The list of database files
 * @property {string} [compatibilityLevel] SQL Server compatibility level of
 * database. Possible values include: 'CompatLevel80', 'CompatLevel90',
 * 'CompatLevel100', 'CompatLevel110', 'CompatLevel120', 'CompatLevel130',
 * 'CompatLevel140'
 * @property {string} [databaseState] State of the database. Possible values
 * include: 'Online', 'Restoring', 'Recovering', 'RecoveryPending', 'Suspect',
 * 'Emergency', 'Offline', 'Copying', 'OfflineSecondary'
 */
export interface ConnectToSourceSqlServerTaskOutputDatabaseLevel extends ConnectToSourceSqlServerTaskOutput {
  readonly name?: string;
  readonly sizeMB?: number;
  readonly databaseFiles?: DatabaseFileInfo[];
  readonly compatibilityLevel?: string;
  readonly databaseState?: string;
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskOutputTaskLevel class.
 * @constructor
 * Task level output for the task that validates connection to SQL Server and
 * also validates source server requirements
 *
 * @property {object} [databases] Source databases as a map from database name
 * to database id
 * @property {object} [logins] Source logins as a map from login name to login
 * id.
 * @property {object} [agentJobs] Source agent jobs as a map from agent job
 * name to id.
 * @property {object} [databaseTdeCertificateMapping] Mapping from database
 * name to TDE certificate name, if applicable
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServerBrandVersion] Source server brand version
 * @property {array} [validationErrors] Validation errors
 */
export interface ConnectToSourceSqlServerTaskOutputTaskLevel extends ConnectToSourceSqlServerTaskOutput {
  readonly databases?: { [propertyName: string]: string };
  readonly logins?: { [propertyName: string]: string };
  readonly agentJobs?: { [propertyName: string]: string };
  readonly databaseTdeCertificateMapping?: { [propertyName: string]: string };
  readonly sourceServerVersion?: string;
  readonly sourceServerBrandVersion?: string;
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskInput class.
 * @constructor
 * Input for the task that validates connection to SQL Server and also
 * validates source server requirements
 *
 * @property {object} sourceConnectionInfo Connection information for Source
 * SQL Server
 * @property {string} [sourceConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [sourceConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [sourceConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [sourceConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [sourceConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [sourceConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {string} [checkPermissionsGroup] Permission group for validations.
 * Possible values include: 'Default', 'MigrationFromSqlServerToAzureDB',
 * 'MigrationFromSqlServerToAzureMI', 'MigrationFromMySQLToAzureDBForMySQL'
 * @property {boolean} [collectLogins] Flag for whether to collect logins from
 * source server. Default value: false .
 * @property {boolean} [collectAgentJobs] Flag for whether to collect agent
 * jobs from source server. Default value: false .
 * @property {boolean} [collectTdeCertificateInfo] Flag for whether to collect
 * TDE Certificate names from source server. Default value: false .
 */
export interface ConnectToSourceSqlServerTaskInput {
  sourceConnectionInfo: SqlConnectionInfo;
  checkPermissionsGroup?: string;
  collectLogins?: boolean;
  collectAgentJobs?: boolean;
  collectTdeCertificateInfo?: boolean;
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerSyncTaskProperties class.
 * @constructor
 * Properties for the task that validates connection to SQL Server and source
 * server requirements for online migration
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * Source SQL Server
 * @property {string} [input.sourceConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.sourceConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.sourceConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.sourceConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.sourceConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.sourceConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {string} [input.checkPermissionsGroup] Permission group for
 * validations. Possible values include: 'Default',
 * 'MigrationFromSqlServerToAzureDB', 'MigrationFromSqlServerToAzureMI',
 * 'MigrationFromMySQLToAzureDBForMySQL'
 * @property {boolean} [input.collectLogins] Flag for whether to collect logins
 * from source server.
 * @property {boolean} [input.collectAgentJobs] Flag for whether to collect
 * agent jobs from source server.
 * @property {boolean} [input.collectTdeCertificateInfo] Flag for whether to
 * collect TDE Certificate names from source server.
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToSourceSqlServerSyncTaskProperties extends ProjectTaskProperties {
  input?: ConnectToSourceSqlServerTaskInput;
  readonly output?: ConnectToSourceSqlServerTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceSqlServerTaskProperties class.
 * @constructor
 * Properties for the task that validates connection to SQL Server and also
 * validates source server requirements
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Connection information for
 * Source SQL Server
 * @property {string} [input.sourceConnectionInfo.dataSource] Data source in
 * the format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [input.sourceConnectionInfo.authentication]
 * Authentication type to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [input.sourceConnectionInfo.encryptConnection] Whether
 * to encrypt the connection
 * @property {string} [input.sourceConnectionInfo.additionalSettings]
 * Additional connection settings
 * @property {boolean} [input.sourceConnectionInfo.trustServerCertificate]
 * Whether to trust the server certificate
 * @property {string} [input.sourceConnectionInfo.platform] Server platform
 * type for connection. Possible values include: 'SqlOnPrem'
 * @property {string} [input.checkPermissionsGroup] Permission group for
 * validations. Possible values include: 'Default',
 * 'MigrationFromSqlServerToAzureDB', 'MigrationFromSqlServerToAzureMI',
 * 'MigrationFromMySQLToAzureDBForMySQL'
 * @property {boolean} [input.collectLogins] Flag for whether to collect logins
 * from source server.
 * @property {boolean} [input.collectAgentJobs] Flag for whether to collect
 * agent jobs from source server.
 * @property {boolean} [input.collectTdeCertificateInfo] Flag for whether to
 * collect TDE Certificate names from source server.
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToSourceSqlServerTaskProperties extends ProjectTaskProperties {
  input?: ConnectToSourceSqlServerTaskInput;
  readonly output?: ConnectToSourceSqlServerTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MongoDbShardKeyInfo class.
 * @constructor
 * Describes a MongoDB shard key
 *
 * @property {array} fields The fields within the shard key
 * @property {boolean} isUnique Whether the shard key is unique
 */
export interface MongoDbShardKeyInfo {
  fields: MongoDbShardKeyField[];
  isUnique: boolean;
}

/**
 * @class
 * Initializes a new instance of the MongoDbObjectInfo class.
 * @constructor
 * Describes a database or collection within a MongoDB data source
 *
 * @property {number} averageDocumentSize The average document size, or -1 if
 * the average size is unknown
 * @property {number} dataSize The estimated total data size, in bytes, or -1
 * if the size is unknown.
 * @property {number} documentCount The estimated total number of documents, or
 * -1 if the document count is unknown
 * @property {string} name The unqualified name of the database or collection
 * @property {string} qualifiedName The qualified name of the database or
 * collection. For a collection, this is the database-qualified name.
 */
export interface MongoDbObjectInfo {
  averageDocumentSize: number;
  dataSize: number;
  documentCount: number;
  name: string;
  qualifiedName: string;
}

/**
 * @class
 * Initializes a new instance of the MongoDbCollectionInfo class.
 * @constructor
 * Describes a supported collection within a MongoDB database
 *
 * @property {string} databaseName The name of the database containing the
 * collection
 * @property {boolean} isCapped Whether the collection is a capped collection
 * (i.e. whether it has a fixed size and acts like a circular buffer)
 * @property {boolean} isSystemCollection Whether the collection is system
 * collection
 * @property {boolean} isView Whether the collection is a view of another
 * collection
 * @property {object} [shardKey] The shard key on the collection, or null if
 * the collection is not sharded
 * @property {array} [shardKey.fields] The fields within the shard key
 * @property {boolean} [shardKey.isUnique] Whether the shard key is unique
 * @property {boolean} supportsSharding Whether the database has sharding
 * enabled. Note that the migration task will enable sharding on the target if
 * necessary.
 * @property {string} [viewOf] The name of the collection that this is a view
 * of, if IsView is true
 */
export interface MongoDbCollectionInfo extends MongoDbObjectInfo {
  databaseName: string;
  isCapped: boolean;
  isSystemCollection: boolean;
  isView: boolean;
  shardKey?: MongoDbShardKeyInfo;
  supportsSharding: boolean;
  viewOf?: string;
}

/**
 * @class
 * Initializes a new instance of the MongoDbDatabaseInfo class.
 * @constructor
 * Describes a database within a MongoDB data source
 *
 * @property {array} collections A list of supported collections in a MongoDB
 * database
 * @property {boolean} supportsSharding Whether the database has sharding
 * enabled. Note that the migration task will enable sharding on the target if
 * necessary.
 */
export interface MongoDbDatabaseInfo extends MongoDbObjectInfo {
  collections: MongoDbCollectionInfo[];
  supportsSharding: boolean;
}

/**
 * @class
 * Initializes a new instance of the MongoDbClusterInfo class.
 * @constructor
 * Describes a MongoDB data source
 *
 * @property {array} databases A list of non-system databases in the cluster
 * @property {boolean} supportsSharding Whether the cluster supports sharded
 * collections
 * @property {string} type The type of data source. Possible values include:
 * 'BlobContainer', 'CosmosDb', 'MongoDb'
 * @property {string} version The version of the data source in the form x.y.z
 * (e.g. 3.6.7). Not used if Type is BlobContainer.
 */
export interface MongoDbClusterInfo {
  databases: MongoDbDatabaseInfo[];
  supportsSharding: boolean;
  type: string;
  version: string;
}

/**
 * @class
 * Initializes a new instance of the ConnectToMongoDbTaskProperties class.
 * @constructor
 * Properties for the task that validates the connection to and provides
 * information about a MongoDB server
 *
 * @property {object} [input]
 * @property {string} [input.connectionString] A MongoDB connection string or
 * blob container URL. The user name and password can be specified here or in
 * the userName and password properties
 * @property {array} [output] An array containing a single MongoDbClusterInfo
 * object
 */
export interface ConnectToMongoDbTaskProperties extends ProjectTaskProperties {
  input?: MongoDbConnectionInfo;
  readonly output?: MongoDbClusterInfo[];
}

/**
 * @class
 * Initializes a new instance of the ProjectTask class.
 * @constructor
 * A task resource
 *
 * @property {string} [etag] HTTP strong entity tag value. This is ignored if
 * submitted.
 * @property {object} [properties] Custom task properties
 * @property {array} [properties.errors] Array of errors. This is ignored if
 * submitted.
 * @property {string} [properties.state] The state of the task. This is ignored
 * if submitted. Possible values include: 'Unknown', 'Queued', 'Running',
 * 'Canceled', 'Succeeded', 'Failed', 'FailedInputValidation', 'Faulted'
 * @property {array} [properties.commands] Array of command properties.
 * @property {string} [properties.taskType] Polymorphic Discriminator
 */
export interface ProjectTask extends Resource {
  etag?: string;
  properties?: ProjectTaskProperties;
}

/**
 * @class
 * Initializes a new instance of the ServiceSku class.
 * @constructor
 * An Azure SKU instance
 *
 * @property {string} [name] The unique name of the SKU, such as 'P3'
 * @property {string} [tier] The tier of the SKU, such as 'Basic', 'General
 * Purpose', or 'Business Critical'
 * @property {string} [family] The SKU family, used when the service has
 * multiple performance classes within a tier, such as 'A', 'D', etc. for
 * virtual machines
 * @property {string} [size] The size of the SKU, used when the name alone does
 * not denote a service size or when a SKU has multiple performance classes
 * within a family, e.g. 'A1' for virtual machines
 * @property {number} [capacity] The capacity of the SKU, if it supports
 * scaling
 */
export interface ServiceSku {
  name?: string;
  tier?: string;
  family?: string;
  size?: string;
  capacity?: number;
}

/**
 * @class
 * Initializes a new instance of the DataMigrationService class.
 * @constructor
 * A Database Migration Service resource
 *
 * @property {string} [etag] HTTP strong entity tag value. Ignored if submitted
 * @property {string} [kind] The resource kind. Only 'vm' (the default) is
 * supported.
 * @property {string} [provisioningState] The resource's provisioning state.
 * Possible values include: 'Accepted', 'Deleting', 'Deploying', 'Stopped',
 * 'Stopping', 'Starting', 'FailedToStart', 'FailedToStop', 'Succeeded',
 * 'Failed'
 * @property {string} [publicKey] The public key of the service, used to
 * encrypt secrets sent to the service
 * @property {string} virtualSubnetId The ID of the
 * Microsoft.Network/virtualNetworks/subnets resource to which the service
 * should be joined
 * @property {object} [sku] Service SKU
 * @property {string} [sku.name] The unique name of the SKU, such as 'P3'
 * @property {string} [sku.tier] The tier of the SKU, such as 'Basic', 'General
 * Purpose', or 'Business Critical'
 * @property {string} [sku.family] The SKU family, used when the service has
 * multiple performance classes within a tier, such as 'A', 'D', etc. for
 * virtual machines
 * @property {string} [sku.size] The size of the SKU, used when the name alone
 * does not denote a service size or when a SKU has multiple performance
 * classes within a family, e.g. 'A1' for virtual machines
 * @property {number} [sku.capacity] The capacity of the SKU, if it supports
 * scaling
 */
export interface DataMigrationService extends TrackedResource {
  etag?: string;
  kind?: string;
  readonly provisioningState?: string;
  publicKey?: string;
  virtualSubnetId: string;
  sku?: ServiceSku;
}

/**
 * @class
 * Initializes a new instance of the NameAvailabilityRequest class.
 * @constructor
 * A resource type and proposed name
 *
 * @property {string} [name] The proposed resource name
 * @property {string} [type] The resource type chain (e.g.
 * virtualMachines/extensions)
 */
export interface NameAvailabilityRequest {
  name?: string;
  type?: string;
}

/**
 * @class
 * Initializes a new instance of the DatabaseInfo class.
 * @constructor
 * Project Database Details
 *
 * @property {string} sourceDatabaseName Name of the database
 */
export interface DatabaseInfo {
  sourceDatabaseName: string;
}

/**
 * @class
 * Initializes a new instance of the Project class.
 * @constructor
 * A project resource
 *
 * @property {string} sourcePlatform Source platform for the project. Possible
 * values include: 'SQL', 'MySQL', 'PostgreSql', 'MongoDb', 'Unknown'
 * @property {string} targetPlatform Target platform for the project. Possible
 * values include: 'SQLDB', 'SQLMI', 'AzureDbForMySql', 'AzureDbForPostgreSql',
 * 'MongoDb', 'Unknown'
 * @property {date} [creationTime] UTC Date and time when project was created
 * @property {object} [sourceConnectionInfo] Information for connecting to
 * source
 * @property {string} [sourceConnectionInfo.userName] User name
 * @property {string} [sourceConnectionInfo.password] Password credential.
 * @property {string} [sourceConnectionInfo.type] Polymorphic Discriminator
 * @property {object} [targetConnectionInfo] Information for connecting to
 * target
 * @property {string} [targetConnectionInfo.userName] User name
 * @property {string} [targetConnectionInfo.password] Password credential.
 * @property {string} [targetConnectionInfo.type] Polymorphic Discriminator
 * @property {array} [databasesInfo] List of DatabaseInfo
 * @property {string} [provisioningState] The project's provisioning state.
 * Possible values include: 'Deleting', 'Succeeded'
 */
export interface Project extends TrackedResource {
  sourcePlatform: string;
  targetPlatform: string;
  readonly creationTime?: Date;
  sourceConnectionInfo?: ConnectionInfo;
  targetConnectionInfo?: ConnectionInfo;
  databasesInfo?: DatabaseInfo[];
  readonly provisioningState?: string;
}

/**
 * @class
 * Initializes a new instance of the ApiError class.
 * @constructor
 * Error information.
 *
 * @property {object} [error] Error information in OData format
 * @property {string} [error.code] The machine-readable description of the
 * error, such as 'InvalidRequest' or 'InternalServerError'
 * @property {string} [error.message] The human-readable description of the
 * error
 * @property {array} [error.details] Inner errors that caused this error
 */
export interface ApiError {
  error?: ODataError;
}

/**
 * @class
 * Initializes a new instance of the FileStorageInfo class.
 * @constructor
 * File storage information.
 *
 * @property {string} [uri] A URI that can be used to access the file content.
 * @property {object} [headers]
 */
export interface FileStorageInfo {
  uri?: string;
  headers?: { [propertyName: string]: string };
}

/**
 * @class
 * Initializes a new instance of the ServiceOperationDisplay class.
 * @constructor
 * Localized display text
 *
 * @property {string} [provider] The localized resource provider name
 * @property {string} [resource] The localized resource type name
 * @property {string} [operation] The localized operation name
 * @property {string} [description] The localized operation description
 */
export interface ServiceOperationDisplay {
  provider?: string;
  resource?: string;
  operation?: string;
  description?: string;
}

/**
 * @class
 * Initializes a new instance of the ServiceOperation class.
 * @constructor
 * Description of an action supported by the Database Migration Service
 *
 * @property {string} [name] The fully qualified action name, e.g.
 * Microsoft.DataMigration/services/read
 * @property {object} [display] Localized display text
 * @property {string} [display.provider] The localized resource provider name
 * @property {string} [display.resource] The localized resource type name
 * @property {string} [display.operation] The localized operation name
 * @property {string} [display.description] The localized operation description
 */
export interface ServiceOperation {
  name?: string;
  display?: ServiceOperationDisplay;
}

/**
 * @class
 * Initializes a new instance of the QuotaName class.
 * @constructor
 * The name of the quota
 *
 * @property {string} [localizedValue] The localized name of the quota
 * @property {string} [value] The unlocalized name (or ID) of the quota
 */
export interface QuotaName {
  localizedValue?: string;
  value?: string;
}

/**
 * @class
 * Initializes a new instance of the Quota class.
 * @constructor
 * Describes a quota for or usage details about a resource
 *
 * @property {number} [currentValue] The current value of the quota. If null or
 * missing, the current value cannot be determined in the context of the
 * request.
 * @property {string} [id] The resource ID of the quota object
 * @property {number} [limit] The maximum value of the quota. If null or
 * missing, the quota has no maximum, in which case it merely tracks usage.
 * @property {object} [name] The name of the quota
 * @property {string} [name.localizedValue] The localized name of the quota
 * @property {string} [name.value] The unlocalized name (or ID) of the quota
 * @property {string} [unit] The unit for the quota, such as Count, Bytes,
 * BytesPerSecond, etc.
 */
export interface Quota {
  currentValue?: number;
  id?: string;
  limit?: number;
  name?: QuotaName;
  unit?: string;
}

/**
 * @class
 * Initializes a new instance of the NameAvailabilityResponse class.
 * @constructor
 * Indicates whether a proposed resource name is available
 *
 * @property {boolean} [nameAvailable] If true, the name is valid and
 * available. If false, 'reason' describes why not.
 * @property {string} [reason] The reason why the name is not available, if
 * nameAvailable is false. Possible values include: 'AlreadyExists', 'Invalid'
 * @property {string} [message] The localized reason why the name is not
 * available, if nameAvailable is false
 */
export interface NameAvailabilityResponse {
  nameAvailable?: boolean;
  reason?: string;
  message?: string;
}

/**
 * @class
 * Initializes a new instance of the AvailableServiceSkuSku class.
 * @constructor
 * SKU name, tier, etc.
 *
 * @property {string} [name] The name of the SKU
 * @property {string} [family] SKU family
 * @property {string} [size] SKU size
 * @property {string} [tier] The tier of the SKU, such as "Basic", "General
 * Purpose", or "Business Critical"
 */
export interface AvailableServiceSkuSku {
  name?: string;
  family?: string;
  size?: string;
  tier?: string;
}

/**
 * @class
 * Initializes a new instance of the AvailableServiceSkuCapacity class.
 * @constructor
 * A description of the scaling capacities of the SKU
 *
 * @property {number} [minimum] The minimum capacity, usually 0 or 1.
 * @property {number} [maximum] The maximum capacity
 * @property {number} [default] The default capacity
 * @property {string} [scaleType] The scalability approach. Possible values
 * include: 'none', 'manual', 'automatic'
 */
export interface AvailableServiceSkuCapacity {
  minimum?: number;
  maximum?: number;
  default?: number;
  scaleType?: string;
}

/**
 * @class
 * Initializes a new instance of the AvailableServiceSku class.
 * @constructor
 * Describes the available service SKU.
 *
 * @property {string} [resourceType] The resource type, including the provider
 * namespace
 * @property {object} [sku] SKU name, tier, etc.
 * @property {string} [sku.name] The name of the SKU
 * @property {string} [sku.family] SKU family
 * @property {string} [sku.size] SKU size
 * @property {string} [sku.tier] The tier of the SKU, such as "Basic", "General
 * Purpose", or "Business Critical"
 * @property {object} [capacity] A description of the scaling capacities of the
 * SKU
 * @property {number} [capacity.minimum] The minimum capacity, usually 0 or 1.
 * @property {number} [capacity.maximum] The maximum capacity
 * @property {number} [capacity.default] The default capacity
 * @property {string} [capacity.scaleType] The scalability approach. Possible
 * values include: 'none', 'manual', 'automatic'
 */
export interface AvailableServiceSku {
  resourceType?: string;
  sku?: AvailableServiceSkuSku;
  capacity?: AvailableServiceSkuCapacity;
}

/**
 * @class
 * Initializes a new instance of the DataMigrationServiceStatusResponse class.
 * @constructor
 * Service health status
 *
 * @property {string} [agentVersion] The DMS instance agent version
 * @property {string} [status] The machine-readable status, such as
 * 'Initializing', 'Offline', 'Online', 'Deploying', 'Deleting', 'Stopped',
 * 'Stopping', 'Starting', 'FailedToStart', 'FailedToStop' or 'Failed'
 * @property {string} [vmSize] The services virtual machine size, such as
 * 'Standard_D2_v2'
 * @property {array} [supportedTaskTypes] The list of supported task types
 */
export interface DataMigrationServiceStatusResponse {
  agentVersion?: string;
  status?: string;
  vmSize?: string;
  supportedTaskTypes?: string[];
}

/**
 * @class
 * Initializes a new instance of the ResourceSkuRestrictions class.
 * @constructor
 * Describes scaling information of a SKU.
 *
 * @property {string} [type] The type of restrictions. Possible values include:
 * 'location'
 * @property {array} [values] The value of restrictions. If the restriction
 * type is set to location. This would be different locations where the SKU is
 * restricted.
 * @property {string} [reasonCode] The reason code for restriction. Possible
 * values include: 'QuotaId', 'NotAvailableForSubscription'
 */
export interface ResourceSkuRestrictions {
  readonly type?: string;
  readonly values?: string[];
  readonly reasonCode?: string;
}

/**
 * @class
 * Initializes a new instance of the ResourceSkuCapabilities class.
 * @constructor
 * Describes The SKU capabilites object.
 *
 * @property {string} [name] An invariant to describe the feature.
 * @property {string} [value] An invariant if the feature is measured by
 * quantity.
 */
export interface ResourceSkuCapabilities {
  readonly name?: string;
  readonly value?: string;
}

/**
 * @class
 * Initializes a new instance of the ResourceSkuCosts class.
 * @constructor
 * Describes metadata for retrieving price info.
 *
 * @property {string} [meterID] Used for querying price from commerce.
 * @property {number} [quantity] The multiplier is needed to extend the base
 * metered cost.
 * @property {string} [extendedUnit] An invariant to show the extended unit.
 */
export interface ResourceSkuCosts {
  readonly meterID?: string;
  readonly quantity?: number;
  readonly extendedUnit?: string;
}

/**
 * @class
 * Initializes a new instance of the ResourceSkuCapacity class.
 * @constructor
 * Describes scaling information of a SKU.
 *
 * @property {number} [minimum] The minimum capacity.
 * @property {number} [maximum] The maximum capacity.
 * @property {number} [default] The default capacity.
 * @property {string} [scaleType] The scale type applicable to the SKU.
 * Possible values include: 'Automatic', 'Manual', 'None'
 */
export interface ResourceSkuCapacity {
  readonly minimum?: number;
  readonly maximum?: number;
  readonly default?: number;
  readonly scaleType?: string;
}

/**
 * @class
 * Initializes a new instance of the ResourceSku class.
 * @constructor
 * Describes an available DMS SKU.
 *
 * @property {string} [resourceType] The type of resource the SKU applies to.
 * @property {string} [name] The name of SKU.
 * @property {string} [tier] Specifies the tier of DMS in a scale set.
 * @property {string} [size] The Size of the SKU.
 * @property {string} [family] The Family of this particular SKU.
 * @property {string} [kind] The Kind of resources that are supported in this
 * SKU.
 * @property {object} [capacity] Not used.
 * @property {number} [capacity.minimum] The minimum capacity.
 * @property {number} [capacity.maximum] The maximum capacity.
 * @property {number} [capacity.default] The default capacity.
 * @property {string} [capacity.scaleType] The scale type applicable to the
 * SKU. Possible values include: 'Automatic', 'Manual', 'None'
 * @property {array} [locations] The set of locations that the SKU is
 * available.
 * @property {array} [apiVersions] The api versions that support this SKU.
 * @property {array} [costs] Metadata for retrieving price info.
 * @property {array} [capabilities] A name value pair to describe the
 * capability.
 * @property {array} [restrictions] The restrictions because of which SKU
 * cannot be used. This is empty if there are no restrictions.
 */
export interface ResourceSku {
  readonly resourceType?: string;
  readonly name?: string;
  readonly tier?: string;
  readonly size?: string;
  readonly family?: string;
  readonly kind?: string;
  readonly capacity?: ResourceSkuCapacity;
  readonly locations?: string[];
  readonly apiVersions?: string[];
  readonly costs?: ResourceSkuCosts[];
  readonly capabilities?: ResourceSkuCapabilities[];
  readonly restrictions?: ResourceSkuRestrictions[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceMySqlTaskInput class.
 * @constructor
 * Input for the task that validates MySQL database connection
 *
 * @property {object} sourceConnectionInfo Information for connecting to MySQL
 * source
 * @property {string} [sourceConnectionInfo.serverName] Name of the server
 * @property {number} [sourceConnectionInfo.port] Port for Server
 * @property {string} [targetPlatform] Target Platform for the migration.
 * Possible values include: 'AzureDbForMySQL'
 * @property {string} [checkPermissionsGroup] Permission group for validations.
 * Possible values include: 'Default', 'MigrationFromSqlServerToAzureDB',
 * 'MigrationFromSqlServerToAzureMI', 'MigrationFromMySQLToAzureDBForMySQL'
 */
export interface ConnectToSourceMySqlTaskInput {
  sourceConnectionInfo: MySqlConnectionInfo;
  targetPlatform?: string;
  checkPermissionsGroup?: string;
}

/**
 * @class
 * Initializes a new instance of the ServerProperties class.
 * @constructor
 * Server properties for Oracle, MySQL type source
 *
 * @property {string} [serverPlatform] Name of the server platform
 * @property {string} [serverName] Name of the server
 * @property {string} [serverVersion] Version of the database server
 * @property {string} [serverEdition] Edition of the database server
 * @property {string} [serverOperatingSystemVersion] Version of the operating
 * system
 * @property {number} [serverDatabaseCount] Number of databases in the server
 */
export interface ServerProperties {
  readonly serverPlatform?: string;
  readonly serverName?: string;
  readonly serverVersion?: string;
  readonly serverEdition?: string;
  readonly serverOperatingSystemVersion?: string;
  readonly serverDatabaseCount?: number;
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceNonSqlTaskOutput class.
 * @constructor
 * Output for connect to Oracle, MySQL type source
 *
 * @property {string} [id] Result identifier
 * @property {string} [sourceServerBrandVersion] Server brand version
 * @property {object} [serverProperties] Server properties
 * @property {string} [serverProperties.serverPlatform] Name of the server
 * platform
 * @property {string} [serverProperties.serverName] Name of the server
 * @property {string} [serverProperties.serverVersion] Version of the database
 * server
 * @property {string} [serverProperties.serverEdition] Edition of the database
 * server
 * @property {string} [serverProperties.serverOperatingSystemVersion] Version
 * of the operating system
 * @property {number} [serverProperties.serverDatabaseCount] Number of
 * databases in the server
 * @property {array} [databases] List of databases on the server
 * @property {array} [validationErrors] Validation errors associated with the
 * task
 */
export interface ConnectToSourceNonSqlTaskOutput {
  readonly id?: string;
  readonly sourceServerBrandVersion?: string;
  readonly serverProperties?: ServerProperties;
  readonly databases?: string[];
  readonly validationErrors?: ReportableException[];
}

/**
 * @class
 * Initializes a new instance of the ConnectToSourceMySqlTaskProperties class.
 * @constructor
 * Properties for the task that validates MySQL database connection
 *
 * @property {object} [input] Task input
 * @property {object} [input.sourceConnectionInfo] Information for connecting
 * to MySQL source
 * @property {string} [input.sourceConnectionInfo.serverName] Name of the
 * server
 * @property {number} [input.sourceConnectionInfo.port] Port for Server
 * @property {string} [input.targetPlatform] Target Platform for the migration.
 * Possible values include: 'AzureDbForMySQL'
 * @property {string} [input.checkPermissionsGroup] Permission group for
 * validations. Possible values include: 'Default',
 * 'MigrationFromSqlServerToAzureDB', 'MigrationFromSqlServerToAzureMI',
 * 'MigrationFromMySQLToAzureDBForMySQL'
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface ConnectToSourceMySqlTaskProperties extends ProjectTaskProperties {
  input?: ConnectToSourceMySqlTaskInput;
  readonly output?: ConnectToSourceNonSqlTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the SchemaMigrationSetting class.
 * @constructor
 * Settings for migrating schema from source to target
 *
 * @property {string} [schemaOption] Option on how to migrate the schema.
 * Possible values include: 'None', 'ExtractFromSource', 'UseStorageFile'
 * @property {string} [fileId] Resource Identifier of a file resource
 * containing the uploaded schema file
 */
export interface SchemaMigrationSetting {
  schemaOption?: string;
  fileId?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbDatabaseInput class.
 * @constructor
 * Database input for migrate schema Sql Server to Azure SQL Server scenario
 *
 * @property {string} [name] Name of source database
 * @property {string} [targetDatabaseName] Name of target database
 * @property {object} [schemaSetting] Database schema migration settings
 * @property {string} [schemaSetting.schemaOption] Option on how to migrate the
 * schema. Possible values include: 'None', 'ExtractFromSource',
 * 'UseStorageFile'
 * @property {string} [schemaSetting.fileId] Resource Identifier of a file
 * resource containing the uploaded schema file
 */
export interface MigrateSchemaSqlServerSqlDbDatabaseInput {
  name?: string;
  targetDatabaseName?: string;
  schemaSetting?: SchemaMigrationSetting;
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbTaskInput class.
 * @constructor
 * Input for task that migrates Schema for SQL Server databases to Azure SQL
 * databases
 *
 * @property {array} selectedDatabases Databases to migrate
 */
export interface MigrateSchemaSqlServerSqlDbTaskInput extends SqlMigrationTaskInput {
  selectedDatabases: MigrateSchemaSqlServerSqlDbDatabaseInput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbTaskOutput class.
 * @constructor
 * Output for the task that migrates Schema for SQL Server databases to Azure
 * SQL databases
 *
 * @property {string} [id] Result identifier
 * @property {string} resultType Polymorphic Discriminator
 */
export interface MigrateSchemaSqlServerSqlDbTaskOutput {
  readonly id?: string;
  resultType: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbTaskProperties class.
 * @constructor
 * Properties for task that migrates Schema for SQL Server databases to Azure
 * SQL databases
 *
 * @property {object} [input] Task input
 * @property {array} [input.selectedDatabases] Databases to migrate
 * @property {array} [output] Task output. This is ignored if submitted.
 */
export interface MigrateSchemaSqlServerSqlDbTaskProperties extends ProjectTaskProperties {
  input?: MigrateSchemaSqlServerSqlDbTaskInput;
  readonly output?: MigrateSchemaSqlServerSqlDbTaskOutput[];
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbTaskOutputMigrationLevel class.
 * @constructor
 * @property {string} [state] Overall state of the schema migration. Possible
 * values include: 'None', 'InProgress', 'Failed', 'Warning', 'Completed',
 * 'Skipped', 'Stopped'
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [sourceServerVersion] Source server version
 * @property {string} [sourceServerBrandVersion] Source server brand version
 * @property {string} [targetServerVersion] Target server version
 * @property {string} [targetServerBrandVersion] Target server brand version
 */
export interface MigrateSchemaSqlServerSqlDbTaskOutputMigrationLevel extends MigrateSchemaSqlServerSqlDbTaskOutput {
  readonly state?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly sourceServerVersion?: string;
  readonly sourceServerBrandVersion?: string;
  readonly targetServerVersion?: string;
  readonly targetServerBrandVersion?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbTaskOutputDatabaseLevel class.
 * @constructor
 * @property {string} [databaseName] The name of the database
 * @property {string} [state] State of the schema migration for this database.
 * Possible values include: 'None', 'InProgress', 'Failed', 'Warning',
 * 'Completed', 'Skipped', 'Stopped'
 * @property {string} [stage] Schema migration stage for this database.
 * Possible values include: 'NotStarted', 'ValidatingInputs',
 * 'CollectingObjects', 'DownloadingScript', 'GeneratingScript',
 * 'UploadingScript', 'DeployingSchema', 'Completed', 'CompletedWithWarnings',
 * 'Failed'
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [databaseErrorResultPrefix] Prefix string to use for
 * querying errors for this database
 * @property {string} [schemaErrorResultPrefix] Prefix string to use for
 * querying schema errors for this database
 * @property {number} [numberOfSuccessfulOperations] Number of successful
 * operations for this database
 * @property {number} [numberOfFailedOperations] Number of failed operations
 * for this database
 * @property {string} [fileId] Identifier for the file resource containing the
 * schema of this database
 */
export interface MigrateSchemaSqlServerSqlDbTaskOutputDatabaseLevel extends MigrateSchemaSqlServerSqlDbTaskOutput {
  readonly databaseName?: string;
  readonly state?: string;
  readonly stage?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly databaseErrorResultPrefix?: string;
  readonly schemaErrorResultPrefix?: string;
  readonly numberOfSuccessfulOperations?: number;
  readonly numberOfFailedOperations?: number;
  readonly fileId?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlServerSqlDbTaskOutputError class.
 * @constructor
 * @property {string} [commandText] Schema command which failed
 * @property {string} [errorText] Reason of failure
 */
export interface MigrateSchemaSqlServerSqlDbTaskOutputError extends MigrateSchemaSqlServerSqlDbTaskOutput {
  readonly commandText?: string;
  readonly errorText?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSchemaSqlTaskOutputError class.
 * @constructor
 * @property {object} [error] Migration error
 * @property {string} [error.message] Error message
 * @property {string} [error.actionableMessage] Actionable steps for this
 * exception
 * @property {string} [error.filePath] The path to the file where exception
 * occurred
 * @property {string} [error.lineNumber] The line number where exception
 * occurred
 * @property {number} [error.hResult] Coded numerical value that is assigned to
 * a specific exception
 * @property {string} [error.stackTrace] Stack trace
 */
export interface MigrateSchemaSqlTaskOutputError extends MigrateSchemaSqlServerSqlDbTaskOutput {
  readonly error?: ReportableException;
}

/**
 * @class
 * Initializes a new instance of the MongoDbCommandInput class.
 * @constructor
 * Describes the input to the 'cancel' and 'restart' MongoDB migration commands
 *
 * @property {string} [objectName] The qualified name of a database or
 * collection to act upon, or null to act upon the entire migration
 */
export interface MongoDbCommandInput {
  objectName?: string;
}

/**
 * @class
 * Initializes a new instance of the MongoDbCancelCommand class.
 * @constructor
 * Properties for the command that cancels a migration in whole or in part
 *
 * @property {object} [input] Command input
 * @property {string} [input.objectName] The qualified name of a database or
 * collection to act upon, or null to act upon the entire migration
 */
export interface MongoDbCancelCommand extends CommandProperties {
  input?: MongoDbCommandInput;
}

/**
 * @class
 * Initializes a new instance of the MongoDbFinishCommandInput class.
 * @constructor
 * Describes the input to the 'finish' MongoDB migration command
 *
 * @property {boolean} immediate If true, replication for the affected objects
 * will be stopped immediately. If false, the migrator will finish replaying
 * queued events before finishing the replication.
 */
export interface MongoDbFinishCommandInput extends MongoDbCommandInput {
  immediate: boolean;
}

/**
 * @class
 * Initializes a new instance of the MongoDbFinishCommand class.
 * @constructor
 * Properties for the command that finishes a migration in whole or in part
 *
 * @property {object} [input] Command input
 * @property {boolean} [input.immediate] If true, replication for the affected
 * objects will be stopped immediately. If false, the migrator will finish
 * replaying queued events before finishing the replication.
 */
export interface MongoDbFinishCommand extends CommandProperties {
  input?: MongoDbFinishCommandInput;
}

/**
 * @class
 * Initializes a new instance of the MongoDbRestartCommand class.
 * @constructor
 * Properties for the command that restarts a migration in whole or in part
 *
 * @property {object} [input] Command input
 * @property {string} [input.objectName] The qualified name of a database or
 * collection to act upon, or null to act upon the entire migration
 */
export interface MongoDbRestartCommand extends CommandProperties {
  input?: MongoDbCommandInput;
}

/**
 * @class
 * Initializes a new instance of the Database class.
 * @constructor
 * Information about a single database
 *
 * @property {string} [id] Unique identifier for the database
 * @property {string} [name] Name of the database
 * @property {string} [compatibilityLevel] SQL Server compatibility level of
 * database. Possible values include: 'CompatLevel80', 'CompatLevel90',
 * 'CompatLevel100', 'CompatLevel110', 'CompatLevel120', 'CompatLevel130',
 * 'CompatLevel140'
 * @property {string} [collation] Collation name of the database
 * @property {string} [serverName] Name of the server
 * @property {string} [fqdn] Fully qualified name
 * @property {string} [installId] Install id of the database
 * @property {string} [serverVersion] Version of the server
 * @property {string} [serverEdition] Edition of the server
 * @property {string} [serverLevel] Product level of the server (RTM, SP, CTP).
 * @property {string} [serverDefaultDataPath] Default path of the data files
 * @property {string} [serverDefaultLogPath] Default path of the log files
 * @property {string} [serverDefaultBackupPath] Default path of the backup
 * folder
 * @property {number} [serverCoreCount] Number of cores on the server
 * @property {number} [serverVisibleOnlineCoreCount] Number of cores on the
 * server that have VISIBLE ONLINE status
 * @property {string} [databaseState] State of the database. Possible values
 * include: 'Online', 'Restoring', 'Recovering', 'RecoveryPending', 'Suspect',
 * 'Emergency', 'Offline', 'Copying', 'OfflineSecondary'
 * @property {string} [serverId] The unique Server Id
 */
export interface Database {
  id?: string;
  name?: string;
  compatibilityLevel?: string;
  collation?: string;
  serverName?: string;
  fqdn?: string;
  installId?: string;
  serverVersion?: string;
  serverEdition?: string;
  serverLevel?: string;
  serverDefaultDataPath?: string;
  serverDefaultLogPath?: string;
  serverDefaultBackupPath?: string;
  serverCoreCount?: number;
  serverVisibleOnlineCoreCount?: number;
  databaseState?: string;
  serverId?: string;
}

/**
 * @class
 * Initializes a new instance of the DatabaseObjectName class.
 * @constructor
 * A representation of the name of an object in a database
 *
 * @property {string} [databaseName] The unescaped name of the database
 * containing the object
 * @property {string} [objectName] The unescaped name of the object
 * @property {string} [schemaName] The unescaped name of the schema containing
 * the object
 * @property {string} [objectType] Type of the object in the database. Possible
 * values include: 'StoredProcedures', 'Table', 'User', 'View', 'Function'
 */
export interface DatabaseObjectName {
  readonly databaseName?: string;
  readonly objectName?: string;
  readonly schemaName?: string;
  objectType?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrationTableMetadata class.
 * @constructor
 * Metadata for tables selected in migration project
 *
 * @property {string} [sourceTableName] Source table name
 * @property {string} [targetTableName] Target table name
 */
export interface MigrationTableMetadata {
  readonly sourceTableName?: string;
  readonly targetTableName?: string;
}

/**
 * @class
 * Initializes a new instance of the DataMigrationProjectMetadata class.
 * @constructor
 * Common metadata for migration projects
 *
 * @property {string} [sourceServerName] Source server name
 * @property {string} [sourceServerPort] Source server port number
 * @property {string} [sourceUsername] Source username
 * @property {string} [targetServerName] Target server name
 * @property {string} [targetUsername] Target username
 * @property {string} [targetDbName] Target database name
 * @property {boolean} [targetUsingWinAuth] Whether target connection is
 * Windows authentication
 * @property {array} [selectedMigrationTables] List of tables selected for
 * migration
 */
export interface DataMigrationProjectMetadata {
  readonly sourceServerName?: string;
  readonly sourceServerPort?: string;
  readonly sourceUsername?: string;
  readonly targetServerName?: string;
  readonly targetUsername?: string;
  readonly targetDbName?: string;
  readonly targetUsingWinAuth?: boolean;
  readonly selectedMigrationTables?: MigrationTableMetadata[];
}

/**
 * @class
 * Initializes a new instance of the GetProjectDetailsNonSqlTaskInput class.
 * @constructor
 * Input for the task that reads configuration from project artifacts
 *
 * @property {string} projectName Name of the migration project
 * @property {string} projectLocation A URL that points to the location to
 * access project artifacts
 */
export interface GetProjectDetailsNonSqlTaskInput {
  projectName: string;
  projectLocation: string;
}

/**
 * @class
 * Initializes a new instance of the NonSqlDataMigrationTable class.
 * @constructor
 * Defines metadata for table to be migrated
 *
 * @property {string} [sourceName] Source table name
 */
export interface NonSqlDataMigrationTable {
  sourceName?: string;
}

/**
 * @class
 * Initializes a new instance of the NonSqlMigrationTaskInput class.
 * @constructor
 * Base class for non sql migration task input
 *
 * @property {object} targetConnectionInfo Information for connecting to target
 * @property {string} [targetConnectionInfo.dataSource] Data source in the
 * format Protocol:MachineName\SQLServerInstanceName,PortNumber
 * @property {string} [targetConnectionInfo.authentication] Authentication type
 * to use for connection. Possible values include: 'None',
 * 'WindowsAuthentication', 'SqlAuthentication', 'ActiveDirectoryIntegrated',
 * 'ActiveDirectoryPassword'
 * @property {boolean} [targetConnectionInfo.encryptConnection] Whether to
 * encrypt the connection
 * @property {string} [targetConnectionInfo.additionalSettings] Additional
 * connection settings
 * @property {boolean} [targetConnectionInfo.trustServerCertificate] Whether to
 * trust the server certificate
 * @property {string} [targetConnectionInfo.platform] Server platform type for
 * connection. Possible values include: 'SqlOnPrem'
 * @property {string} targetDatabaseName Target database name
 * @property {string} projectName Name of the migration project
 * @property {string} projectLocation A URL that points to the drop location to
 * access project artifacts
 * @property {array} selectedTables Metadata of the tables selected for
 * migration
 */
export interface NonSqlMigrationTaskInput {
  targetConnectionInfo: SqlConnectionInfo;
  targetDatabaseName: string;
  projectName: string;
  projectLocation: string;
  selectedTables: NonSqlDataMigrationTable[];
}

/**
 * @class
 * Initializes a new instance of the DataMigrationError class.
 * @constructor
 * Migration Task errors
 *
 * @property {string} [message] Error description
 * @property {string} [type] Possible values include: 'Default', 'Warning',
 * 'Error'
 */
export interface DataMigrationError {
  readonly message?: string;
  type?: string;
}

/**
 * @class
 * Initializes a new instance of the NonSqlDataMigrationTableResult class.
 * @constructor
 * Object used to report the data migration results of a table
 *
 * @property {string} [resultCode] Result code of the data migration. Possible
 * values include: 'Initial', 'Completed', 'ObjectNotExistsInSource',
 * 'ObjectNotExistsInTarget', 'TargetObjectIsInaccessible', 'FatalError'
 * @property {string} [sourceName] Name of the source table
 * @property {string} [targetName] Name of the target table
 * @property {number} [sourceRowCount] Number of rows in the source table
 * @property {number} [targetRowCount] Number of rows in the target table
 * @property {number} [elapsedTimeInMiliseconds] Time taken to migrate the data
 * @property {array} [errors] List of errors, if any, during migration
 */
export interface NonSqlDataMigrationTableResult {
  readonly resultCode?: string;
  readonly sourceName?: string;
  readonly targetName?: string;
  readonly sourceRowCount?: number;
  readonly targetRowCount?: number;
  readonly elapsedTimeInMiliseconds?: number;
  readonly errors?: DataMigrationError[];
}

/**
 * @class
 * Initializes a new instance of the NonSqlMigrationTaskOutput class.
 * @constructor
 * Base class for non sql migration task output
 *
 * @property {string} [id] Result identifier
 * @property {date} [startedOn] Migration start time
 * @property {date} [endedOn] Migration end time
 * @property {string} [status] Current state of migration. Possible values
 * include: 'Default', 'Connecting', 'SourceAndTargetSelected', 'SelectLogins',
 * 'Configured', 'Running', 'Error', 'Stopped', 'Completed',
 * 'CompletedWithWarnings'
 * @property {object} [dataMigrationTableResults] Results of the migration. The
 * key contains the table name and the value the table result object
 * @property {string} [progressMessage] Message about the progress of the
 * migration
 * @property {string} [sourceServerName] Name of source server
 * @property {string} [targetServerName] Name of target server
 */
export interface NonSqlMigrationTaskOutput {
  readonly id?: string;
  readonly startedOn?: Date;
  readonly endedOn?: Date;
  readonly status?: string;
  readonly dataMigrationTableResults?: { [propertyName: string]: NonSqlDataMigrationTableResult };
  readonly progressMessage?: string;
  readonly sourceServerName?: string;
  readonly targetServerName?: string;
}

/**
 * @class
 * Initializes a new instance of the DatabaseFileInput class.
 * @constructor
 * Database file specific information for input
 *
 * @property {string} [id] Unique identifier for database file
 * @property {string} [logicalName] Logical name of the file
 * @property {string} [physicalFullName] Operating-system full path of the file
 * @property {string} [restoreFullName] Suggested full path of the file for
 * restoring
 * @property {string} [fileType] Database file type. Possible values include:
 * 'Rows', 'Log', 'Filestream', 'NotSupported', 'Fulltext'
 */
export interface DatabaseFileInput {
  id?: string;
  logicalName?: string;
  physicalFullName?: string;
  restoreFullName?: string;
  fileType?: string;
}

/**
 * @class
 * Initializes a new instance of the MigrateSqlServerSqlServerDatabaseInput class.
 * @constructor
 * Database specific information for SQL to SQL migration task inputs
 *
 * @property {string} [name] Name of the database
 * @property {string} [restoreDatabaseName] Name of the database at destination
 * @property {string} [backupAndRestoreFolder] The backup and restore folder
 * @property {array} [databaseFiles] The list of database files
 */
export interface MigrateSqlServerSqlServerDatabaseInput {
  name?: string;
  restoreDatabaseName?: string;
  backupAndRestoreFolder?: string;
  databaseFiles?: DatabaseFileInput[];
}


/**
 * @class
 * Initializes a new instance of the ResourceSkusResult class.
 * @constructor
 * The DMS List SKUs operation response.
 *
 * @property {string} [nextLink] The uri to fetch the next page of DMS SKUs.
 * Call ListNext() with this to fetch the next page of DMS SKUs.
 */
export interface ResourceSkusResult extends Array<ResourceSku> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the ServiceSkuList class.
 * @constructor
 * OData page of available SKUs
 *
 * @property {string} [nextLink] URL to load the next page of service SKUs
 */
export interface ServiceSkuList extends Array<AvailableServiceSku> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the DataMigrationServiceList class.
 * @constructor
 * OData page of service objects
 *
 * @property {string} [nextLink] URL to load the next page of services
 */
export interface DataMigrationServiceList extends Array<DataMigrationService> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the TaskList class.
 * @constructor
 * OData page of tasks
 *
 * @property {string} [nextLink] URL to load the next page of tasks
 */
export interface TaskList extends Array<ProjectTask> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the ProjectList class.
 * @constructor
 * OData page of project resources
 *
 * @property {string} [nextLink] URL to load the next page of projects
 */
export interface ProjectList extends Array<Project> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the QuotaList class.
 * @constructor
 * OData page of quota objects
 *
 * @property {string} [nextLink] URL to load the next page of quotas, or null
 * or missing if this is the last page
 */
export interface QuotaList extends Array<Quota> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the ServiceOperationList class.
 * @constructor
 * OData page of action (operation) objects
 *
 * @property {string} [nextLink] URL to load the next page of actions
 */
export interface ServiceOperationList extends Array<ServiceOperation> {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the FileList class.
 * @constructor
 * OData page of files
 *
 * @property {string} [nextLink] URL to load the next page of files
 */
export interface FileList extends Array<ProjectFile> {
  nextLink?: string;
}
