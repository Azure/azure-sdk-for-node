/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the ErrorModel class.
 * @constructor
 * Data Lake error information
 * @member {string} code Gets or sets the HTTP status code or error code
 * associated with this error
 * 
 * @member {string} message Gets or sets the error message to display.
 * 
 * @member {string} [target] Gets or sets the target of the error.
 * 
 * @member {array} [details] Gets or sets the list of error details
 * 
 * @member {object} [innerError] Gets or sets the inner exceptions or errors,
 * if any
 * 
 * @member {string} [innerError.trace] Gets or sets the stack trace for the
 * error
 * 
 * @member {string} [innerError.context] Gets or sets the context for the
 * error message
 * 
 */
function ErrorModel(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.code !== undefined) {
      this.code = parameters.code;
    }
    if (parameters.message !== undefined) {
      this.message = parameters.message;
    }
    if (parameters.target !== undefined) {
      this.target = parameters.target;
    }
    if (parameters.details) {
      var tempParametersdetails = [];
      parameters.details.forEach(function(element) {
        if (element) {
          element = new models['ErrorDetails'](element);
        }
        tempParametersdetails.push(element);
      });
      this.details = tempParametersdetails;
    }
    if (parameters.innerError) {
      this.innerError = new models['InnerError'](parameters.innerError);
    }
  }    
}


/**
 * Validate the payload against the ErrorModel schema
 *
 * @param {JSON} payload
 *
 */
ErrorModel.prototype.serialize = function () {
  var payload = {};
  if (this['code'] === null || this['code'] === undefined || typeof this['code'].valueOf() !== 'string') {
    throw new Error('this[\'code\'] cannot be null or undefined and it must be of type string.');
  }
  payload['code'] = this['code'];

  if (this['message'] === null || this['message'] === undefined || typeof this['message'].valueOf() !== 'string') {
    throw new Error('this[\'message\'] cannot be null or undefined and it must be of type string.');
  }
  payload['message'] = this['message'];

  if (this['target'] !== null && this['target'] !== undefined) {
    if (typeof this['target'].valueOf() !== 'string') {
      throw new Error('this[\'target\'] must be of type string.');
    }
    payload['target'] = this['target'];
  }

  if (util.isArray(this['details'])) {
    payload['details'] = [];
    for (var i = 0; i < this['details'].length; i++) {
      if (this['details'][i]) {
        if (payload['details'] === null || payload['details'] === undefined) {
          payload['details'] = {};
        }
        payload['details'][i] = this['details'][i].serialize();
      }
    }
  }

  if (this['innerError']) {
    payload['innerError'] = this['innerError'].serialize();
  }

  return payload;
};

/**
 * Deserialize the instance to ErrorModel schema
 *
 * @param {JSON} instance
 *
 */
ErrorModel.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['code'] !== undefined) {
      this['code'] = instance['code'];
    }

    if (instance['message'] !== undefined) {
      this['message'] = instance['message'];
    }

    if (instance['target'] !== undefined) {
      this['target'] = instance['target'];
    }

    if (instance['details']) {
      var tempInstancedetails = [];
      instance['details'].forEach(function(element1) {
        if (element1) {
          element1 = new models['ErrorDetails']().deserialize(element1);
        }
        tempInstancedetails.push(element1);
      });
      this['details'] = tempInstancedetails;
    }

    if (instance['innerError']) {
      this['innerError'] = new models['InnerError']().deserialize(instance['innerError']);
    }
  }

  return this;
};

module.exports = ErrorModel;
