/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

/**
 * @class
 * Initializes a new instance of the FirewallRule class.
 * @constructor
 * Data Lake firewall rule information
 * @member {string} [name] Gets or sets the Firewall Rule's name.
 * 
 * @member {string} [type] Gets or sets the namespace and type of the Firewall
 * Rule.
 * 
 * @member {string} [id] Gets or sets the Firewall Rule's subscription ID.
 * 
 * @member {string} [location] Gets or sets the Firewall Rule's regional
 * location.
 * 
 * @member {object} [properties] Gets or sets the properties defined by Data
 * Lake all properties are specific to each resource provider.
 * 
 * @member {string} [properties.startIpAddress] Gets or sets the start IP
 * address for the firewall rule.
 * 
 * @member {string} [properties.endIpAddress] Gets or sets the end IP address
 * for the firewall rule.
 * 
 */
function FirewallRule(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.type !== undefined) {
      this.type = parameters.type;
    }
    if (parameters.id !== undefined) {
      this.id = parameters.id;
    }
    if (parameters.location !== undefined) {
      this.location = parameters.location;
    }
    if (parameters.properties) {
      this.properties = new models['FirewallRuleProperties'](parameters.properties);
    }
  }    
}


/**
 * Validate the payload against the FirewallRule schema
 *
 * @param {JSON} payload
 *
 */
FirewallRule.prototype.serialize = function () {
  var payload = {};
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['name'] = this['name'];
  }

  if (this['type'] !== null && this['type'] !== undefined) {
    if (typeof this['type'].valueOf() !== 'string') {
      throw new Error('this[\'type\'] must be of type string.');
    }
    payload['type'] = this['type'];
  }

  if (this['id'] !== null && this['id'] !== undefined) {
    if (typeof this['id'].valueOf() !== 'string') {
      throw new Error('this[\'id\'] must be of type string.');
    }
    payload['id'] = this['id'];
  }

  if (this['location'] !== null && this['location'] !== undefined) {
    if (typeof this['location'].valueOf() !== 'string') {
      throw new Error('this[\'location\'] must be of type string.');
    }
    payload['location'] = this['location'];
  }

  if (this['properties']) {
    payload['properties'] = this['properties'].serialize();
  }

  return payload;
};

/**
 * Deserialize the instance to FirewallRule schema
 *
 * @param {JSON} instance
 *
 */
FirewallRule.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['name'] !== undefined) {
      this['name'] = instance['name'];
    }

    if (instance['type'] !== undefined) {
      this['type'] = instance['type'];
    }

    if (instance['id'] !== undefined) {
      this['id'] = instance['id'];
    }

    if (instance['location'] !== undefined) {
      this['location'] = instance['location'];
    }

    if (instance['properties']) {
      this['properties'] = new models['FirewallRuleProperties']().deserialize(instance['properties']);
    }
  }

  return this;
};

module.exports = FirewallRule;
