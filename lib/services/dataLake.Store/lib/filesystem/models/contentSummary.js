/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

/**
 * @class
 * Initializes a new instance of the ContentSummary class.
 * @constructor
 * Data Lake content summary information
 * @member {number} [directoryCount] Gets or sets the number of directories.
 * 
 * @member {number} [fileCount] Gets or sets the number of files.
 * 
 * @member {number} [length] Gets or sets the number of bytes used by the
 * contet.
 * 
 * @member {number} [quota] Gets or sets the namespace quota of this directory.
 * 
 * @member {number} [spaceConsumed] Gets or sets the disk space consumed by
 * the content.
 * 
 * @member {number} [spaceQuota] Gets or sets the disk space quota.
 * 
 */
function ContentSummary(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.directoryCount !== undefined) {
      this.directoryCount = parameters.directoryCount;
    }
    if (parameters.fileCount !== undefined) {
      this.fileCount = parameters.fileCount;
    }
    if (parameters.length !== undefined) {
      this.length = parameters.length;
    }
    if (parameters.quota !== undefined) {
      this.quota = parameters.quota;
    }
    if (parameters.spaceConsumed !== undefined) {
      this.spaceConsumed = parameters.spaceConsumed;
    }
    if (parameters.spaceQuota !== undefined) {
      this.spaceQuota = parameters.spaceQuota;
    }
  }    
}


/**
 * Validate the payload against the ContentSummary schema
 *
 * @param {JSON} payload
 *
 */
ContentSummary.prototype.serialize = function () {
  var payload = {};
  if (this['directoryCount'] !== null && this['directoryCount'] !== undefined) {
    if (typeof this['directoryCount'] !== 'number') {
      throw new Error('this[\'directoryCount\'] must be of type number.');
    }
    payload['directoryCount'] = this['directoryCount'];
  }

  if (this['fileCount'] !== null && this['fileCount'] !== undefined) {
    if (typeof this['fileCount'] !== 'number') {
      throw new Error('this[\'fileCount\'] must be of type number.');
    }
    payload['fileCount'] = this['fileCount'];
  }

  if (this['length'] !== null && this['length'] !== undefined) {
    if (typeof this['length'] !== 'number') {
      throw new Error('this[\'length\'] must be of type number.');
    }
    payload['length'] = this['length'];
  }

  if (this['quota'] !== null && this['quota'] !== undefined) {
    if (typeof this['quota'] !== 'number') {
      throw new Error('this[\'quota\'] must be of type number.');
    }
    payload['quota'] = this['quota'];
  }

  if (this['spaceConsumed'] !== null && this['spaceConsumed'] !== undefined) {
    if (typeof this['spaceConsumed'] !== 'number') {
      throw new Error('this[\'spaceConsumed\'] must be of type number.');
    }
    payload['spaceConsumed'] = this['spaceConsumed'];
  }

  if (this['spaceQuota'] !== null && this['spaceQuota'] !== undefined) {
    if (typeof this['spaceQuota'] !== 'number') {
      throw new Error('this[\'spaceQuota\'] must be of type number.');
    }
    payload['spaceQuota'] = this['spaceQuota'];
  }

  return payload;
};

/**
 * Deserialize the instance to ContentSummary schema
 *
 * @param {JSON} instance
 *
 */
ContentSummary.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['directoryCount'] !== undefined) {
      this['directoryCount'] = instance['directoryCount'];
    }

    if (instance['fileCount'] !== undefined) {
      this['fileCount'] = instance['fileCount'];
    }

    if (instance['length'] !== undefined) {
      this['length'] = instance['length'];
    }

    if (instance['quota'] !== undefined) {
      this['quota'] = instance['quota'];
    }

    if (instance['spaceConsumed'] !== undefined) {
      this['spaceConsumed'] = instance['spaceConsumed'];
    }

    if (instance['spaceQuota'] !== undefined) {
      this['spaceQuota'] = instance['spaceQuota'];
    }
  }

  return this;
};

module.exports = ContentSummary;
