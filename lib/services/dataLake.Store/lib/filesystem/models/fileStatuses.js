/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the FileStatuses class.
 * @constructor
 * Data Lake file status list information
 * @member {array} [fileStatus] Gets or sets the object containing the list of
 * properties of the files.
 * 
 */
function FileStatuses(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.fileStatus) {
      var tempParametersfileStatus = [];
      parameters.fileStatus.forEach(function(element) {
        if (element) {
          element = new models['FileStatusProperties'](element);
        }
        tempParametersfileStatus.push(element);
      });
      this.fileStatus = tempParametersfileStatus;
    }
  }    
}


/**
 * Validate the payload against the FileStatuses schema
 *
 * @param {JSON} payload
 *
 */
FileStatuses.prototype.serialize = function () {
  var payload = {};
  if (util.isArray(this['fileStatus'])) {
    payload['FileStatus'] = [];
    for (var i = 0; i < this['fileStatus'].length; i++) {
      if (this['fileStatus'][i]) {
        if (payload['FileStatus'] === null || payload['FileStatus'] === undefined) {
          payload['FileStatus'] = {};
        }
        payload['FileStatus'][i] = this['fileStatus'][i].serialize();
      }
    }
  }

  return payload;
};

/**
 * Deserialize the instance to FileStatuses schema
 *
 * @param {JSON} instance
 *
 */
FileStatuses.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['FileStatus']) {
      var tempInstanceFileStatus = [];
      instance['FileStatus'].forEach(function(element1) {
        if (element1) {
          element1 = new models['FileStatusProperties']().deserialize(element1);
        }
        tempInstanceFileStatus.push(element1);
      });
      this['fileStatus'] = tempInstanceFileStatus;
    }
  }

  return this;
};

module.exports = FileStatuses;
