/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

var util = require('util');

/**
 * @class
 * Initializes a new instance of the USqlCredentialList class.
 * @constructor
 * A Data Lake Analytics catalog U-SQL credential item list.
 * @member {array} [value] Gets or sets the list of credentials in the database
 * 
 * @member {string} [itemType] Gets or sets the job type of the current job
 * (i.e. Hive or USql). Possible values for this property include:
 * 'usqlDatabase', 'usqlSchema', 'usqlAssembly', 'usqlTable', 'uSqlView',
 * 'uSqlProcedure', 'uSqlCredential', 'usqlTableValuedFunction',
 * 'usqlTableStatistics', 'usqlSecret', 'usqlExternalDataSource', 'usqlType'.
 * 
 * @member {number} [count] Gets or sets the count of items in the list.
 * 
 * @member {string} [nextLink] Gets or sets the link to the next page of
 * results.
 * 
 */
function USqlCredentialList() {
}

util.inherits(USqlCredentialList, Array);

/**
 * Deserialize the instance to USqlCredentialList schema
 *
 * @param {JSON} instance
 *
 */
USqlCredentialList.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['value']) {
      var deserializedInstancevalue = [];
        instance['value'].forEach(function(element) {
        if (element) {
          element = new models['USqlCredential']().deserialize(element);
        }
        deserializedInstancevalue.push(element);
      });
      Array.prototype.push.apply(this, deserializedInstancevalue);
    }
    if (instance['nextLink'] !== undefined) {
      this['nextLink'] = instance.nextLink;
    }
  }

  return this;
};

module.exports = USqlCredentialList;
