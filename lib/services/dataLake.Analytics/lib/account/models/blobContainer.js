/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.14.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

var models = require('./index');

/**
 * @class
 * Initializes a new instance of the BlobContainer class.
 * @constructor
 * Azure Storage blob container information.
 * @member {string} [name] Gets or sets the name of the blob container.
 * 
 * @member {string} [id] Gets or sets the unique identifier of the blob
 * container.
 * 
 * @member {string} [type] Gets or sets the type of the blob container.
 * 
 * @member {object} [properties] Gets or sets the properties of the blob
 * container.
 * 
 * @member {date} [properties.lastModifiedTime] Gets or sets the last modified
 * time of the blob container.
 * 
 */
function BlobContainer(parameters) {
  if (parameters !== null && parameters !== undefined) {
    if (parameters.name !== undefined) {
      this.name = parameters.name;
    }
    if (parameters.id !== undefined) {
      this.id = parameters.id;
    }
    if (parameters.type !== undefined) {
      this.type = parameters.type;
    }
    if (parameters.properties) {
      this.properties = new models['BlobContainerProperties'](parameters.properties);
    }
  }    
}


/**
 * Validate the payload against the BlobContainer schema
 *
 * @param {JSON} payload
 *
 */
BlobContainer.prototype.serialize = function () {
  var payload = {};
  if (this['name'] !== null && this['name'] !== undefined) {
    if (typeof this['name'].valueOf() !== 'string') {
      throw new Error('this[\'name\'] must be of type string.');
    }
    payload['name'] = this['name'];
  }

  if (this['id'] !== null && this['id'] !== undefined) {
    if (typeof this['id'].valueOf() !== 'string') {
      throw new Error('this[\'id\'] must be of type string.');
    }
    payload['id'] = this['id'];
  }

  if (this['type'] !== null && this['type'] !== undefined) {
    if (typeof this['type'].valueOf() !== 'string') {
      throw new Error('this[\'type\'] must be of type string.');
    }
    payload['type'] = this['type'];
  }

  if (this['properties']) {
    payload['properties'] = this['properties'].serialize();
  }

  return payload;
};

/**
 * Deserialize the instance to BlobContainer schema
 *
 * @param {JSON} instance
 *
 */
BlobContainer.prototype.deserialize = function (instance) {
  if (instance) {
    if (instance['name'] !== undefined) {
      this['name'] = instance['name'];
    }

    if (instance['id'] !== undefined) {
      this['id'] = instance['id'];
    }

    if (instance['type'] !== undefined) {
      this['type'] = instance['type'];
    }

    if (instance['properties']) {
      this['properties'] = new models['BlobContainerProperties']().deserialize(instance['properties']);
    }
  }

  return this;
};

module.exports = BlobContainer;
