/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

'use strict';

const msRest = require('ms-rest');
const msRestAzure = require('ms-rest-azure');
const WebResource = msRest.WebResource;


/**
 * Creates an Azure subscription
 *
 * @param {string} billingAccountName The name of the commerce root billing
 * account.
 *
 * @param {string} invoiceSectionName The name of the invoice section.
 *
 * @param {object} body The subscription creation parameters.
 *
 * @param {string} [body.displayName] The display name of the subscription.
 *
 * @param {string} [body.billingProfileId] The ARM id of the billing profile.
 *
 * @param {string} [body.skuId] The commerce id of the sku.
 *
 * @param {string} [body.costCenter] optional customer cost center
 *
 * @param {object} [body.owner] rbac owner of the subscription
 *
 * @param {string} body.owner.objectId Object id of the Principal
 *
 * @param {object} [body.additionalParameters] Additional, untyped parameters
 * to support custom subscription creation scenarios.
 *
 * @param {object} [options] Optional Parameters.
 *
 * @param {object} [options.customHeaders] Headers that will be added to the
 * request
 *
 * @param {function} callback - The callback.
 *
 * @returns {function} callback(err, result, request, response)
 *
 *                      {Error}  err        - The Error object if an error occurred, null otherwise.
 *
 *                      {object} [result]   - The deserialized result object if an error did not occur.
 *                      See {@link SubscriptionCreationResult} for more
 *                      information.
 *
 *                      {object} [request]  - The HTTP Request object if an error did not occur.
 *
 *                      {stream} [response] - The HTTP Response stream if an error did not occur.
 */
function _createSubscription(billingAccountName, invoiceSectionName, body, options, callback) {
   /* jshint validthis: true */
  let client = this.client;
  if(!callback && typeof options === 'function') {
    callback = options;
    options = null;
  }

  if (!callback) {
    throw new Error('callback cannot be null.');
  }

  // Send request
  this.beginCreateSubscription(billingAccountName, invoiceSectionName, body, options, (err, parsedResult, httpRequest, response) => {
    if (err) return callback(err);

    let initialResult = new msRest.HttpOperationResponse();
    initialResult.request = httpRequest;
    initialResult.response = response;
    initialResult.body = response.body;
    client.getLongRunningOperationResult(initialResult, options, (err, pollingResult) => {
      if (err) return callback(err);

      // Create Result
      let result = null;

      httpRequest = pollingResult.request;
      response = pollingResult.response;
      let responseBody = pollingResult.body;
      if (responseBody === '') responseBody = null;

      // Deserialize Response
      let parsedResponse = null;
      try {
        parsedResponse = JSON.parse(responseBody);
        result = JSON.parse(responseBody);
        if (parsedResponse !== null && parsedResponse !== undefined) {
          let resultMapper = new client.models['SubscriptionCreationResult']().mapper();
          result = client.deserialize(resultMapper, parsedResponse, 'result');
        }
      } catch (error) {
        let deserializationError = new Error(`Error ${error} occurred in deserializing the responseBody - ${responseBody}`);
        deserializationError.request = msRest.stripRequest(httpRequest);
        deserializationError.response = msRest.stripResponse(response);
        return callback(deserializationError);
      }

      return callback(null, result, httpRequest, response);
    });
  });
}

/**
 * Creates an Azure subscription
 *
 * @param {string} billingAccountName The name of the commerce root billing
 * account.
 *
 * @param {string} invoiceSectionName The name of the invoice section.
 *
 * @param {object} body The subscription creation parameters.
 *
 * @param {string} [body.displayName] The display name of the subscription.
 *
 * @param {string} [body.billingProfileId] The ARM id of the billing profile.
 *
 * @param {string} [body.skuId] The commerce id of the sku.
 *
 * @param {string} [body.costCenter] optional customer cost center
 *
 * @param {object} [body.owner] rbac owner of the subscription
 *
 * @param {string} body.owner.objectId Object id of the Principal
 *
 * @param {object} [body.additionalParameters] Additional, untyped parameters
 * to support custom subscription creation scenarios.
 *
 * @param {object} [options] Optional Parameters.
 *
 * @param {object} [options.customHeaders] Headers that will be added to the
 * request
 *
 * @param {function} callback - The callback.
 *
 * @returns {function} callback(err, result, request, response)
 *
 *                      {Error}  err        - The Error object if an error occurred, null otherwise.
 *
 *                      {object} [result]   - The deserialized result object if an error did not occur.
 *                      See {@link SubscriptionCreationResult} for more
 *                      information.
 *
 *                      {object} [request]  - The HTTP Request object if an error did not occur.
 *
 *                      {stream} [response] - The HTTP Response stream if an error did not occur.
 */
function _beginCreateSubscription(billingAccountName, invoiceSectionName, body, options, callback) {
   /* jshint validthis: true */
  let client = this.client;
  if(!callback && typeof options === 'function') {
    callback = options;
    options = null;
  }
  if (!callback) {
    throw new Error('callback cannot be null.');
  }
  let apiVersion = '2018-11-01-preview';
  // Validate
  try {
    if (billingAccountName === null || billingAccountName === undefined || typeof billingAccountName.valueOf() !== 'string') {
      throw new Error('billingAccountName cannot be null or undefined and it must be of type string.');
    }
    if (invoiceSectionName === null || invoiceSectionName === undefined || typeof invoiceSectionName.valueOf() !== 'string') {
      throw new Error('invoiceSectionName cannot be null or undefined and it must be of type string.');
    }
    if (body === null || body === undefined) {
      throw new Error('body cannot be null or undefined.');
    }
    if (this.client.acceptLanguage !== null && this.client.acceptLanguage !== undefined && typeof this.client.acceptLanguage.valueOf() !== 'string') {
      throw new Error('this.client.acceptLanguage must be of type string.');
    }
  } catch (error) {
    return callback(error);
  }

  // Construct URL
  let baseUrl = this.client.baseUri;
  let requestUrl = baseUrl + (baseUrl.endsWith('/') ? '' : '/') + 'providers/Microsoft.Billing/billingAccounts/{billingAccountName}/invoiceSections/{invoiceSectionName}/providers/Microsoft.Subscription/createSubscription';
  requestUrl = requestUrl.replace('{billingAccountName}', encodeURIComponent(billingAccountName));
  requestUrl = requestUrl.replace('{invoiceSectionName}', encodeURIComponent(invoiceSectionName));
  let queryParameters = [];
  queryParameters.push('api-version=' + encodeURIComponent(apiVersion));
  if (queryParameters.length > 0) {
    requestUrl += '?' + queryParameters.join('&');
  }

  // Create HTTP transport objects
  let httpRequest = new WebResource();
  httpRequest.method = 'POST';
  httpRequest.url = requestUrl;
  httpRequest.headers = {};
  // Set Headers
  httpRequest.headers['Content-Type'] = 'application/json; charset=utf-8';
  if (this.client.generateClientRequestId) {
      httpRequest.headers['x-ms-client-request-id'] = msRestAzure.generateUuid();
  }
  if (this.client.acceptLanguage !== undefined && this.client.acceptLanguage !== null) {
    httpRequest.headers['accept-language'] = this.client.acceptLanguage;
  }
  if(options) {
    for(let headerName in options['customHeaders']) {
      if (options['customHeaders'].hasOwnProperty(headerName)) {
        httpRequest.headers[headerName] = options['customHeaders'][headerName];
      }
    }
  }
  // Serialize Request
  let requestContent = null;
  let requestModel = null;
  try {
    if (body !== null && body !== undefined) {
      let requestModelMapper = new client.models['SubscriptionCreationParameters']().mapper();
      requestModel = client.serialize(requestModelMapper, body, 'body');
      requestContent = JSON.stringify(requestModel);
    }
  } catch (error) {
    let serializationError = new Error(`Error "${error.message}" occurred in serializing the ` +
        `payload - ${JSON.stringify(body, null, 2)}.`);
    return callback(serializationError);
  }
  httpRequest.body = requestContent;
  // Send Request
  return client.pipeline(httpRequest, (err, response, responseBody) => {
    if (err) {
      return callback(err);
    }
    let statusCode = response.statusCode;
    if (statusCode !== 200 && statusCode !== 202) {
      let error = new Error(responseBody);
      error.statusCode = response.statusCode;
      error.request = msRest.stripRequest(httpRequest);
      error.response = msRest.stripResponse(response);
      if (responseBody === '') responseBody = null;
      let parsedErrorResponse;
      try {
        parsedErrorResponse = JSON.parse(responseBody);
        if (parsedErrorResponse) {
          let internalError = null;
          if (parsedErrorResponse.error) internalError = parsedErrorResponse.error;
          error.code = internalError ? internalError.code : parsedErrorResponse.code;
          error.message = internalError ? internalError.message : parsedErrorResponse.message;
        }
        if (parsedErrorResponse !== null && parsedErrorResponse !== undefined) {
          let resultMapper = new client.models['ErrorResponse']().mapper();
          error.body = client.deserialize(resultMapper, parsedErrorResponse, 'error.body');
        }
      } catch (defaultError) {
        error.message = `Error "${defaultError.message}" occurred in deserializing the responseBody ` +
                         `- "${responseBody}" for the default response.`;
        return callback(error);
      }
      return callback(error);
    }
    // Create Result
    let result = null;
    if (responseBody === '') responseBody = null;
    // Deserialize Response
    if (statusCode === 200) {
      let parsedResponse = null;
      try {
        parsedResponse = JSON.parse(responseBody);
        result = JSON.parse(responseBody);
        if (parsedResponse !== null && parsedResponse !== undefined) {
          let resultMapper = new client.models['SubscriptionCreationResult']().mapper();
          result = client.deserialize(resultMapper, parsedResponse, 'result');
        }
      } catch (error) {
        let deserializationError = new Error(`Error ${error} occurred in deserializing the responseBody - ${responseBody}`);
        deserializationError.request = msRest.stripRequest(httpRequest);
        deserializationError.response = msRest.stripResponse(response);
        return callback(deserializationError);
      }
    }

    return callback(null, result, httpRequest, response);
  });
}

/** Class representing a SubscriptionFactory. */
class SubscriptionFactory {
  /**
   * Create a SubscriptionFactory.
   * @param {SubscriptionClient} client Reference to the service client.
   */
  constructor(client) {
    this.client = client;
    this._createSubscription = _createSubscription;
    this._beginCreateSubscription = _beginCreateSubscription;
  }

  /**
   * Creates an Azure subscription
   *
   * @param {string} billingAccountName The name of the commerce root billing
   * account.
   *
   * @param {string} invoiceSectionName The name of the invoice section.
   *
   * @param {object} body The subscription creation parameters.
   *
   * @param {string} [body.displayName] The display name of the subscription.
   *
   * @param {string} [body.billingProfileId] The ARM id of the billing profile.
   *
   * @param {string} [body.skuId] The commerce id of the sku.
   *
   * @param {string} [body.costCenter] optional customer cost center
   *
   * @param {object} [body.owner] rbac owner of the subscription
   *
   * @param {string} body.owner.objectId Object id of the Principal
   *
   * @param {object} [body.additionalParameters] Additional, untyped parameters
   * to support custom subscription creation scenarios.
   *
   * @param {object} [options] Optional Parameters.
   *
   * @param {object} [options.customHeaders] Headers that will be added to the
   * request
   *
   * @returns {Promise} A promise is returned
   *
   * @resolve {HttpOperationResponse<SubscriptionCreationResult>} - The deserialized result object.
   *
   * @reject {Error} - The error object.
   */
  createSubscriptionWithHttpOperationResponse(billingAccountName, invoiceSectionName, body, options) {
    let client = this.client;
    let self = this;
    return new Promise((resolve, reject) => {
      self._createSubscription(billingAccountName, invoiceSectionName, body, options, (err, result, request, response) => {
        let httpOperationResponse = new msRest.HttpOperationResponse(request, response);
        httpOperationResponse.body = result;
        if (err) { reject(err); }
        else { resolve(httpOperationResponse); }
        return;
      });
    });
  }

  /**
   * Creates an Azure subscription
   *
   * @param {string} billingAccountName The name of the commerce root billing
   * account.
   *
   * @param {string} invoiceSectionName The name of the invoice section.
   *
   * @param {object} body The subscription creation parameters.
   *
   * @param {string} [body.displayName] The display name of the subscription.
   *
   * @param {string} [body.billingProfileId] The ARM id of the billing profile.
   *
   * @param {string} [body.skuId] The commerce id of the sku.
   *
   * @param {string} [body.costCenter] optional customer cost center
   *
   * @param {object} [body.owner] rbac owner of the subscription
   *
   * @param {string} body.owner.objectId Object id of the Principal
   *
   * @param {object} [body.additionalParameters] Additional, untyped parameters
   * to support custom subscription creation scenarios.
   *
   * @param {object} [options] Optional Parameters.
   *
   * @param {object} [options.customHeaders] Headers that will be added to the
   * request
   *
   * @param {function} [optionalCallback] - The optional callback.
   *
   * @returns {function|Promise} If a callback was passed as the last parameter
   * then it returns the callback else returns a Promise.
   *
   * {Promise} A promise is returned
   *
   *                      @resolve {SubscriptionCreationResult} - The deserialized result object.
   *
   *                      @reject {Error} - The error object.
   *
   * {function} optionalCallback(err, result, request, response)
   *
   *                      {Error}  err        - The Error object if an error occurred, null otherwise.
   *
   *                      {object} [result]   - The deserialized result object if an error did not occur.
   *                      See {@link SubscriptionCreationResult} for more
   *                      information.
   *
   *                      {object} [request]  - The HTTP Request object if an error did not occur.
   *
   *                      {stream} [response] - The HTTP Response stream if an error did not occur.
   */
  createSubscription(billingAccountName, invoiceSectionName, body, options, optionalCallback) {
    let client = this.client;
    let self = this;
    if (!optionalCallback && typeof options === 'function') {
      optionalCallback = options;
      options = null;
    }
    if (!optionalCallback) {
      return new Promise((resolve, reject) => {
        self._createSubscription(billingAccountName, invoiceSectionName, body, options, (err, result, request, response) => {
          if (err) { reject(err); }
          else { resolve(result); }
          return;
        });
      });
    } else {
      return self._createSubscription(billingAccountName, invoiceSectionName, body, options, optionalCallback);
    }
  }

  /**
   * Creates an Azure subscription
   *
   * @param {string} billingAccountName The name of the commerce root billing
   * account.
   *
   * @param {string} invoiceSectionName The name of the invoice section.
   *
   * @param {object} body The subscription creation parameters.
   *
   * @param {string} [body.displayName] The display name of the subscription.
   *
   * @param {string} [body.billingProfileId] The ARM id of the billing profile.
   *
   * @param {string} [body.skuId] The commerce id of the sku.
   *
   * @param {string} [body.costCenter] optional customer cost center
   *
   * @param {object} [body.owner] rbac owner of the subscription
   *
   * @param {string} body.owner.objectId Object id of the Principal
   *
   * @param {object} [body.additionalParameters] Additional, untyped parameters
   * to support custom subscription creation scenarios.
   *
   * @param {object} [options] Optional Parameters.
   *
   * @param {object} [options.customHeaders] Headers that will be added to the
   * request
   *
   * @returns {Promise} A promise is returned
   *
   * @resolve {HttpOperationResponse<SubscriptionCreationResult>} - The deserialized result object.
   *
   * @reject {Error} - The error object.
   */
  beginCreateSubscriptionWithHttpOperationResponse(billingAccountName, invoiceSectionName, body, options) {
    let client = this.client;
    let self = this;
    return new Promise((resolve, reject) => {
      self._beginCreateSubscription(billingAccountName, invoiceSectionName, body, options, (err, result, request, response) => {
        let httpOperationResponse = new msRest.HttpOperationResponse(request, response);
        httpOperationResponse.body = result;
        if (err) { reject(err); }
        else { resolve(httpOperationResponse); }
        return;
      });
    });
  }

  /**
   * Creates an Azure subscription
   *
   * @param {string} billingAccountName The name of the commerce root billing
   * account.
   *
   * @param {string} invoiceSectionName The name of the invoice section.
   *
   * @param {object} body The subscription creation parameters.
   *
   * @param {string} [body.displayName] The display name of the subscription.
   *
   * @param {string} [body.billingProfileId] The ARM id of the billing profile.
   *
   * @param {string} [body.skuId] The commerce id of the sku.
   *
   * @param {string} [body.costCenter] optional customer cost center
   *
   * @param {object} [body.owner] rbac owner of the subscription
   *
   * @param {string} body.owner.objectId Object id of the Principal
   *
   * @param {object} [body.additionalParameters] Additional, untyped parameters
   * to support custom subscription creation scenarios.
   *
   * @param {object} [options] Optional Parameters.
   *
   * @param {object} [options.customHeaders] Headers that will be added to the
   * request
   *
   * @param {function} [optionalCallback] - The optional callback.
   *
   * @returns {function|Promise} If a callback was passed as the last parameter
   * then it returns the callback else returns a Promise.
   *
   * {Promise} A promise is returned
   *
   *                      @resolve {SubscriptionCreationResult} - The deserialized result object.
   *
   *                      @reject {Error} - The error object.
   *
   * {function} optionalCallback(err, result, request, response)
   *
   *                      {Error}  err        - The Error object if an error occurred, null otherwise.
   *
   *                      {object} [result]   - The deserialized result object if an error did not occur.
   *                      See {@link SubscriptionCreationResult} for more
   *                      information.
   *
   *                      {object} [request]  - The HTTP Request object if an error did not occur.
   *
   *                      {stream} [response] - The HTTP Response stream if an error did not occur.
   */
  beginCreateSubscription(billingAccountName, invoiceSectionName, body, options, optionalCallback) {
    let client = this.client;
    let self = this;
    if (!optionalCallback && typeof options === 'function') {
      optionalCallback = options;
      options = null;
    }
    if (!optionalCallback) {
      return new Promise((resolve, reject) => {
        self._beginCreateSubscription(billingAccountName, invoiceSectionName, body, options, (err, result, request, response) => {
          if (err) { reject(err); }
          else { resolve(result); }
          return;
        });
      });
    } else {
      return self._beginCreateSubscription(billingAccountName, invoiceSectionName, body, options, optionalCallback);
    }
  }

}

module.exports = SubscriptionFactory;
