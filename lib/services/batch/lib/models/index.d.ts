/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 * 
 * Code generated by Microsoft (R) AutoRest Code Generator 0.16.0.0
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
// TODO: Include PageTemplateModels here too?? Probably
 */


/**
 * @class
 * Initializes a new instance of the PoolUsageMetrics class.
 * @constructor
 * Usage metrics for a pool across an aggregation interval.
 * @member {string} poolId Gets or sets the id of the pool whose metrics are
 * being aggregated.
 * 
 * @member {date} startTime Gets or sets the start time of the aggregation
 * interval.
 * 
 * @member {date} endTime Gets or sets the end time of the aggregation
 * interval.
 * 
 * @member {string} vmSize Gets or sets the size of virtual machines in the
 * pool.  All VMs in a pool are the same size.
 * 
 * @member {number} totalCoreHours Gets or sets the total core hours used in
 * the pool during this aggregation interval.
 * 
 * @member {number} dataIngressGiB Gets or sets the cross data center network
 * ingress in GiB to the pool during this interval.
 * 
 * @member {number} dataEgressGiB Gets or sets the cross data center network
 * egress in GiB from the pool during this interval.
 * 
 */
export interface PoolUsageMetrics {
    poolId: string;
    startTime: Date;
    endTime: Date;
    vmSize: string;
    totalCoreHours: number;
    dataIngressGiB: number;
    dataEgressGiB: number;
}

/**
 * @class
 * Initializes a new instance of the UsageStatistics class.
 * @constructor
 * Statistics related to pool usage information.
 * @member {date} startTime Gets or sets the start time of the time range
 * covered by the statistics.
 * 
 * @member {date} lastUpdateTime Gets or sets the time at which the statistics
 * were last updated. All statistics are limited to the range between
 * startTime and lastUpdateTime.
 * 
 * @member {moment.duration} dedicatedCoreTime Gets or sets the aggregated
 * wall-clock time of the dedicated compute node cores being part of the pool.
 * 
 */
export interface UsageStatistics {
    startTime: Date;
    lastUpdateTime: Date;
    dedicatedCoreTime: moment.Duration;
}

/**
 * @class
 * Initializes a new instance of the ResourceStatistics class.
 * @constructor
 * Statistics related to resource consumption by compute nodes in a pool.
 * @member {date} startTime Gets or sets the start time of the time range
 * covered by the statistics.
 * 
 * @member {date} lastUpdateTime Gets or sets the time at which the statistics
 * were last updated. All statistics are limited to the range between
 * startTime and lastUpdateTime.
 * 
 * @member {number} avgCPUPercentage Gets or sets the average CPU usage across
 * all nodes in the pool (percentage per node).
 * 
 * @member {number} avgMemoryGiB Gets or sets the average memory usage in GiB
 * across all nodes in the pool.
 * 
 * @member {number} peakMemoryGiB Gets or sets the peak memory usage in GiB
 * across all nodes in the pool.
 * 
 * @member {number} avgDiskGiB Gets or sets the average used disk space in GiB
 * across all nodes in the pool.
 * 
 * @member {number} peakDiskGiB Gets or sets the peak used disk space in GiB
 * across all nodes in the pool.
 * 
 * @member {number} diskReadIOps Gets or sets the total number of disk read
 * operations across all nodes in the pool.
 * 
 * @member {number} diskWriteIOps Gets or sets the total number of disk write
 * operations across all nodes in the pool.
 * 
 * @member {number} diskReadGiB Gets or sets the total amount of data in GiB
 * of disk reads across all nodes in the pool.
 * 
 * @member {number} diskWriteGiB Gets or sets the total amount of data in GiB
 * of disk writes across all nodes in the pool.
 * 
 * @member {number} networkReadGiB Gets or sets the total amount of data in
 * GiB of network reads across all nodes in the pool.
 * 
 * @member {number} networkWriteGiB Gets or sets the total amount of data in
 * GiB of network writes across all nodes in the pool.
 * 
 */
export interface ResourceStatistics {
    startTime: Date;
    lastUpdateTime: Date;
    avgCPUPercentage: number;
    avgMemoryGiB: number;
    peakMemoryGiB: number;
    avgDiskGiB: number;
    peakDiskGiB: number;
    diskReadIOps: number;
    diskWriteIOps: number;
    diskReadGiB: number;
    diskWriteGiB: number;
    networkReadGiB: number;
    networkWriteGiB: number;
}

/**
 * @class
 * Initializes a new instance of the PoolStatistics class.
 * @constructor
 * Contains utilization and resource usage statistics for the lifetime of a
 * pool.
 * @member {string} url Gets or sets the URL for the statistics.
 * 
 * @member {date} startTime Gets or sets the start time of the time range
 * covered by the statistics.
 * 
 * @member {date} lastUpdateTime Gets or sets the time at which the statistics
 * were last updated. All statistics are limited to the range between
 * startTime and lastUpdateTime.
 * 
 * @member {object} [usageStats] Gets or sets statistics related to pool usage
 * information, such as the amount of core-time used.
 * 
 * @member {date} [usageStats.startTime] Gets or sets the start time of the
 * time range covered by the statistics.
 * 
 * @member {date} [usageStats.lastUpdateTime] Gets or sets the time at which
 * the statistics were last updated. All statistics are limited to the range
 * between startTime and lastUpdateTime.
 * 
 * @member {moment.duration} [usageStats.dedicatedCoreTime] Gets or sets the
 * aggregated wall-clock time of the dedicated compute node cores being part
 * of the pool.
 * 
 * @member {object} [resourceStats] Gets or sets statistics related to
 * resource consumption by compute nodes in the pool.
 * 
 * @member {date} [resourceStats.startTime] Gets or sets the start time of the
 * time range covered by the statistics.
 * 
 * @member {date} [resourceStats.lastUpdateTime] Gets or sets the time at
 * which the statistics were last updated. All statistics are limited to the
 * range between startTime and lastUpdateTime.
 * 
 * @member {number} [resourceStats.avgCPUPercentage] Gets or sets the average
 * CPU usage across all nodes in the pool (percentage per node).
 * 
 * @member {number} [resourceStats.avgMemoryGiB] Gets or sets the average
 * memory usage in GiB across all nodes in the pool.
 * 
 * @member {number} [resourceStats.peakMemoryGiB] Gets or sets the peak memory
 * usage in GiB across all nodes in the pool.
 * 
 * @member {number} [resourceStats.avgDiskGiB] Gets or sets the average used
 * disk space in GiB across all nodes in the pool.
 * 
 * @member {number} [resourceStats.peakDiskGiB] Gets or sets the peak used
 * disk space in GiB across all nodes in the pool.
 * 
 * @member {number} [resourceStats.diskReadIOps] Gets or sets the total number
 * of disk read operations across all nodes in the pool.
 * 
 * @member {number} [resourceStats.diskWriteIOps] Gets or sets the total
 * number of disk write operations across all nodes in the pool.
 * 
 * @member {number} [resourceStats.diskReadGiB] Gets or sets the total amount
 * of data in GiB of disk reads across all nodes in the pool.
 * 
 * @member {number} [resourceStats.diskWriteGiB] Gets or sets the total amount
 * of data in GiB of disk writes across all nodes in the pool.
 * 
 * @member {number} [resourceStats.networkReadGiB] Gets or sets the total
 * amount of data in GiB of network reads across all nodes in the pool.
 * 
 * @member {number} [resourceStats.networkWriteGiB] Gets or sets the total
 * amount of data in GiB of network writes across all nodes in the pool.
 * 
 */
export interface PoolStatistics {
    url: string;
    startTime: Date;
    lastUpdateTime: Date;
    usageStats?: UsageStatistics;
    resourceStats?: ResourceStatistics;
}

/**
 * @class
 * Initializes a new instance of the JobStatistics class.
 * @constructor
 * Resource usage statistics for a job.
 * @member {string} url Gets or sets the URL for the statistics.
 * 
 * @member {date} startTime Gets or sets the start time of the time range
 * covered by the statistics.
 * 
 * @member {date} lastUpdateTime Gets or sets the time at which the statistics
 * were last updated. All statistics are limited to the range between
 * StartTime and LastUpdateTime.
 * 
 * @member {moment.duration} userCPUTime Gets or sets the total user mode CPU
 * time (summed across all cores and all compute nodes) consumed by all the
 * tasks in the job.
 * 
 * @member {moment.duration} kernelCPUTime Gets or sets the total kernel mode
 * CPU time (summed across all cores and all compute nodes) consumed by all
 * the tasks in the job.
 * 
 * @member {moment.duration} wallClockTime Gets or sets the total wall clock
 * time of all the tasks in the job.
 * 
 * @member {number} readIOps Gets or sets the total number of I/O read
 * operations performed by all the tasks in the job.
 * 
 * @member {number} writeIOps Gets or sets the total number of I/O write
 * operations performed by all the tasks in the job.
 * 
 * @member {number} readIOGiB Gets or sets the total amount of data in GiB of
 * I/O read by all the tasks in the job.
 * 
 * @member {number} writeIOGiB Gets or sets the total amount of data in GiB of
 * I/O written by all the tasks in the job.
 * 
 * @member {number} numSucceededTasks Gets or sets the total number of tasks
 * successfully completed in the job during the given time range.
 * 
 * @member {number} numFailedTasks Gets or sets the total number of tasks in
 * the job that failed during the given time range.
 * 
 * @member {number} numTaskRetries Gets or sets the total number of retries on
 * all the tasks in the job during the given time range.
 * 
 * @member {moment.duration} waitTime Gets or sets the total wait time of all
 * the tasks in the job.  The wait time for a task is defined as the elapsed
 * time between the creation of the task creation and the start of task
 * execution.  This value is reported only in the account lifetime
 * statistics; it is not included in individual job statistics.
 * 
 */
export interface JobStatistics {
    url: string;
    startTime: Date;
    lastUpdateTime: Date;
    userCPUTime: moment.Duration;
    kernelCPUTime: moment.Duration;
    wallClockTime: moment.Duration;
    readIOps: number;
    writeIOps: number;
    readIOGiB: number;
    writeIOGiB: number;
    numSucceededTasks: number;
    numFailedTasks: number;
    numTaskRetries: number;
    waitTime: moment.Duration;
}

/**
 * @class
 * Initializes a new instance of the NameValuePair class.
 * @constructor
 * Represents a name-value pair.
 * @member {string} [name] Gets or sets the name in the name-value pair.
 * 
 * @member {string} [value] Gets or sets the value in the name-value pair.
 * 
 */
export interface NameValuePair {
    name?: string;
    value?: string;
}

/**
 * @class
 * Initializes a new instance of the DeleteCertificateError class.
 * @constructor
 * An error encountered by the Batch service when deleting a certificate.
 * @member {string} [code] Gets or sets an identifier for the certificate
 * deletion error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [message] Gets or sets a message describing the
 * certificate deletion error, intended to be suitable for display in a user
 * interface.
 * 
 * @member {array} [values] Gets or sets a list of additional error details
 * related to the certificate deletion error.
 * 
 */
export interface DeleteCertificateError {
    code?: string;
    message?: string;
    values?: NameValuePair[];
}

/**
 * @class
 * Initializes a new instance of the Certificate class.
 * @constructor
 * A certificate that can be installed on compute nodes and can be used to
 * authenticate operations on the machine.
 * @member {string} [thumbprint] Get or sets the X.509 thumbprint of the
 * certificate. This is a sequence of up to 40 hex digits (it may include
 * spaces but these are removed).
 * 
 * @member {string} [thumbprintAlgorithm] Gets or sets the algorithm used to
 * derive the thumbprint. This must be sha1.
 * 
 * @member {string} [url] Gets or sets the URL of the certificate.
 * 
 * @member {string} [state] Gets or sets the current state of the certificate.
 * Possible values include: 'active', 'deleting', 'deletefailed'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the
 * certificate entered its current state.
 * 
 * @member {string} [previousState] Gets or sets the previous state of the
 * certificate. This property is not set if the certificate is in its initial
 * Active state. Possible values include: 'active', 'deleting', 'deletefailed'
 * 
 * @member {date} [previousStateTransitionTime] Gets or sets the time at which
 * the certificate entered its previous state.  This property is not set if
 * the certificate is in its initial Active state.
 * 
 * @member {string} [publicData] Gets or sets the public part of the
 * certificate as a base-64 encoded .cer file.
 * 
 * @member {object} [deleteCertificateError] Gets or sets the error that
 * occurred on the last attempt to delete this certificate.  This property is
 * set only if the certificate is in the deletefailed state.
 * 
 * @member {string} [deleteCertificateError.code] Gets or sets an identifier
 * for the certificate deletion error.  Codes are invariant and are intended
 * to be consumed programmatically.
 * 
 * @member {string} [deleteCertificateError.message] Gets or sets a message
 * describing the certificate deletion error, intended to be suitable for
 * display in a user interface.
 * 
 * @member {array} [deleteCertificateError.values] Gets or sets a list of
 * additional error details related to the certificate deletion error.
 * 
 */
export interface Certificate {
    thumbprint?: string;
    thumbprintAlgorithm?: string;
    url?: string;
    state?: string;
    stateTransitionTime?: Date;
    previousState?: string;
    previousStateTransitionTime?: Date;
    publicData?: string;
    deleteCertificateError?: DeleteCertificateError;
}

/**
 * @class
 * Initializes a new instance of the ApplicationPackageReference class.
 * @constructor
 * A reference to an application package to be installed on compute nodes in a
 * pool.
 * @member {string} applicationId Gets or sets the application package id.
 * 
 * @member {string} [version] Gets or sets the application package version. If
 * not specified, the default is used.
 * 
 */
export interface ApplicationPackageReference {
    applicationId: string;
    version?: string;
}

/**
 * @class
 * Initializes a new instance of the ApplicationSummary class.
 * @constructor
 * Contains information about an application in an Azure Batch account.
 * @member {string} id Gets or sets a string that uniquely identifies the
 * application within the account.
 * 
 * @member {string} displayName Gets or sets the display name for the
 * application.
 * 
 * @member {array} versions
 * 
 */
export interface ApplicationSummary {
    id: string;
    displayName: string;
    versions: string[];
}

/**
 * @class
 * Initializes a new instance of the CertificateAddParameter class.
 * @constructor
 * A certificate that can be installed on compute nodes and can be used to
 * authenticate operations on the machine.
 * @member {string} thumbprint Get or sets the X.509 thumbprint of the
 * certificate. This is a sequence of up to 40 hex digits (it may include
 * spaces but these are removed).
 * 
 * @member {string} thumbprintAlgorithm Gets or sets the algorithm used to
 * derive the thumbprint. This must be sha1.
 * 
 * @member {string} data Gets or sets the base64-encoded contents of the .pfx
 * file containing the certificate. The maximum size is 10KB. This property
 * is not populated by the Get Certificate operation.
 * 
 * @member {string} [certificateFormat] Gets or sets the format of the
 * certificate data. This must be pfx. Possible values include: 'pfx', 'cer',
 * 'unmapped'
 * 
 * @member {string} [password] Gets or sets the password to access the
 * certificate's private key. This property is not populated by the Get
 * Certificate operation.
 * 
 */
export interface CertificateAddParameter {
    thumbprint: string;
    thumbprintAlgorithm: string;
    data: string;
    certificateFormat?: string;
    password?: string;
}

/**
 * @class
 * Initializes a new instance of the FileProperties class.
 * @constructor
 * The properties of a file on a compute node.
 * @member {date} [creationTime] Gets or sets the file creation time.
 * 
 * @member {date} lastModified Gets or sets the time at which the file was
 * last modified.
 * 
 * @member {number} contentLength Gets or sets the length of the file.
 * 
 * @member {string} [contentType] Gets or sets the content type of the file.
 * 
 */
export interface FileProperties {
    creationTime?: Date;
    lastModified: Date;
    contentLength: number;
    contentType?: string;
}

/**
 * @class
 * Initializes a new instance of the NodeFile class.
 * @constructor
 * Information about a file or directory on a compute node.
 * @member {string} [name] Gets or sets the file path.
 * 
 * @member {string} [url] Gets or sets the URL of the file.
 * 
 * @member {boolean} [isDirectory] Gets or sets whether the object represents
 * a directory.
 * 
 * @member {object} [properties] Gets or sets the file properties.
 * 
 * @member {date} [properties.creationTime] Gets or sets the file creation
 * time.
 * 
 * @member {date} [properties.lastModified] Gets or sets the time at which the
 * file was last modified.
 * 
 * @member {number} [properties.contentLength] Gets or sets the length of the
 * file.
 * 
 * @member {string} [properties.contentType] Gets or sets the content type of
 * the file.
 * 
 */
export interface NodeFile {
    name?: string;
    url?: string;
    isDirectory?: boolean;
    properties?: FileProperties;
}

/**
 * @class
 * Initializes a new instance of the Schedule class.
 * @constructor
 * The schedule according to which jobs will be created
 * @member {date} [doNotRunUntil] Gets or sets the earliest time at which any
 * job may be created under this job schedule. If you do not specify a
 * doNotRunUntil time, the schedule becomes ready to create jobs immediately.
 * 
 * @member {date} [doNotRunAfter] Gets or sets a time after which no job will
 * be created under this job schedule.  The schedule will move to the
 * completed state as soon as this deadline is past and there is no active
 * job under this job schedule.
 * 
 * @member {moment.duration} [startWindow] Gets or sets the time interval,
 * starting from the time at which the schedule indicates a job should be
 * created, within which a job must be created.  If a job is not created
 * within the startWindow interval, then the 'opportunity' is lost; no job
 * will be created until the next recurrence of the schedule.
 * 
 * @member {moment.duration} [recurrenceInterval] Gets or sets the time
 * interval between the start times of two successive jobs under the job
 * schedule. A job schedule can have at most one active job under it at any
 * given time.
 * 
 */
export interface Schedule {
    doNotRunUntil?: Date;
    doNotRunAfter?: Date;
    startWindow?: moment.Duration;
    recurrenceInterval?: moment.Duration;
}

/**
 * @class
 * Initializes a new instance of the JobConstraints class.
 * @constructor
 * Specifies the execution constraints for jobs created on a schedule.
 * @member {moment.duration} [maxWallClockTime] Gets or sets the maximum
 * elapsed time that the job may run, measured from the time the job starts.
 * If the job does not complete within the time limit, the Batch service
 * terminates it and any tasks that are still running.
 * 
 * @member {number} [maxTaskRetryCount] Gets or sets the maximum number of
 * times each task may be retried. The Batch service retries a task if its
 * exit code is nonzero.
 * 
 */
export interface JobConstraints {
    maxWallClockTime?: moment.Duration;
    maxTaskRetryCount?: number;
}

/**
 * @class
 * Initializes a new instance of the ResourceFile class.
 * @constructor
 * A file to be downloaded from Azure blob storage to a compute node.
 * @member {string} [blobSource] Gets or sets the URL of a blob in Azure
 * storage. The Batch service downloads the blob to the specified file path.
 * The URL must be readable using anonymous access.
 * 
 * @member {string} [filePath] Gets or sets the location on the compute node
 * to which the file should be downloaded.
 * 
 */
export interface ResourceFile {
    blobSource?: string;
    filePath?: string;
}

/**
 * @class
 * Initializes a new instance of the EnvironmentSetting class.
 * @constructor
 * An environment variable to be set on a task process.
 * @member {string} [name] Gets or sets the name of the environment variable.
 * 
 * @member {string} [value] Gets or sets the value of the environment variable.
 * 
 */
export interface EnvironmentSetting {
    name?: string;
    value?: string;
}

/**
 * @class
 * Initializes a new instance of the TaskConstraints class.
 * @constructor
 * Constraints to apply to the Job Manager task.
 * @member {moment.duration} [maxWallClockTime] Gets or sets the maximum
 * elapsed time that the task may run, measured from the time the task
 * starts. If the task does not complete within the time limit, the Batch
 * service terminates it.
 * 
 * @member {moment.duration} [retentionTime] Gets or sets the minimum time to
 * retain the working directory for the task on the compute node where it
 * ran. After this time, the Batch service may delete the working directory
 * and all its contents. The default is infinite.
 * 
 * @member {number} [maxTaskRetryCount] Gets or sets the maximum number of
 * times the task may be retried. The Batch service retries a task if its
 * exit code is nonzero.
 * 
 */
export interface TaskConstraints {
    maxWallClockTime?: moment.Duration;
    retentionTime?: moment.Duration;
    maxTaskRetryCount?: number;
}

/**
 * @class
 * Initializes a new instance of the JobManagerTask class.
 * @constructor
 * Specifies details of a Job Manager task.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * Job Manager task. A GUID is recommended.
 * 
 * @member {string} [displayName] Gets or sets the display name of the Job
 * Manager task.
 * 
 * @member {string} [commandLine] Gets or sets the command line of the Job
 * Manager task.
 * 
 * @member {array} [resourceFiles] Gets or sets a list of files that Batch
 * will download to the compute node before running the command line.
 * 
 * @member {array} [environmentSettings] Gets or sets a list of environment
 * variable settings for the Job Manager task.
 * 
 * @member {object} [constraints] Gets or sets constraints that apply to the
 * Job Manager task.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the task may run, measured from the time the
 * task starts. If the task does not complete within the time limit, the
 * Batch service terminates it.
 * 
 * @member {moment.duration} [constraints.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the task on the compute
 * node where it ran. After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {boolean} [killJobOnCompletion] Gets or sets whether completion of
 * the Job Manager task signifies completion of the entire job.
 * 
 * @member {boolean} [runElevated] Gets or sets whether to run the Job Manager
 * task in elevated mode. The default value is false.
 * 
 * @member {boolean} [runExclusive] Gets or sets whether the Job Manager task
 * requires exclusive use of the compute node where it runs.
 * 
 */
export interface JobManagerTask {
    id?: string;
    displayName?: string;
    commandLine?: string;
    resourceFiles?: ResourceFile[];
    environmentSettings?: EnvironmentSetting[];
    constraints?: TaskConstraints;
    killJobOnCompletion?: boolean;
    runElevated?: boolean;
    runExclusive?: boolean;
}

/**
 * @class
 * Initializes a new instance of the JobPreparationTask class.
 * @constructor
 * A Job Preparation task to run before any tasks of the job on any given
 * compute node.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * job preparation task within the job. The id can contain any combination of
 * alphanumeric characters including hyphens and underscores and cannot
 * contain more than 64 characters.
 * 
 * @member {string} [commandLine] Gets or sets the command line of the Job
 * Preparation task.
 * 
 * @member {array} [resourceFiles] Gets or sets a list of files that Batch
 * will download to the compute node before running the command line.
 * 
 * @member {array} [environmentSettings] Gets or sets a list of environment
 * variable settings for the Job Preparation task.
 * 
 * @member {object} [constraints] Gets or sets constraints that apply to the
 * Job Preparation task..
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the task may run, measured from the time the
 * task starts. If the task does not complete within the time limit, the
 * Batch service terminates it.
 * 
 * @member {moment.duration} [constraints.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the task on the compute
 * node where it ran. After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {boolean} [waitForSuccess] Gets or sets whether the Batch Service
 * should wait for the Job Preparation task to complete successfully before
 * scheduling any other tasks of the job on the compute node.
 * 
 * @member {boolean} [runElevated] Gets or sets whether to run the Job
 * Preparation task in elevated mode. The default value is false.
 * 
 * @member {boolean} [rerunOnNodeRebootAfterSuccess] Gets or sets whether the
 * Batch service should rerun the Job Preparation task after a compute node
 * reboots. The default value is true.
 * 
 */
export interface JobPreparationTask {
    id?: string;
    commandLine?: string;
    resourceFiles?: ResourceFile[];
    environmentSettings?: EnvironmentSetting[];
    constraints?: TaskConstraints;
    waitForSuccess?: boolean;
    runElevated?: boolean;
    rerunOnNodeRebootAfterSuccess?: boolean;
}

/**
 * @class
 * Initializes a new instance of the JobReleaseTask class.
 * @constructor
 * A Job Release task to run on job completion on any compute node where the
 * job has run.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * Job Release task within the job. The id can contain any combination of
 * alphanumeric characters including hyphens and underscores and cannot
 * contain more than 64 characters.
 * 
 * @member {string} [commandLine] Gets or sets the command line of the Job
 * Release task.
 * 
 * @member {array} [resourceFiles] Gets or sets a list of files that Batch
 * will download to the compute node before running the command line.
 * 
 * @member {array} [environmentSettings] Gets or sets a list of environment
 * variable settings for the Job Release task.
 * 
 * @member {moment.duration} [maxWallClockTime] Gets or sets the maximum
 * elapsed time that the Job Release task may run on a given compute node,
 * measured from the time the task starts. If the task does not complete
 * within the time limit, the Batch service terminates it. The default value
 * is 15 minutes.
 * 
 * @member {moment.duration} [retentionTime] Gets or sets the minimum time to
 * retain the working directory for the Job Release task on the compute node.
 * After this time, the Batch service may delete the working directory and
 * all its contents. The default is infinite.
 * 
 * @member {boolean} [runElevated] Gets or sets whether to run the Job Release
 * task in elevated mode. The default value is false.
 * 
 */
export interface JobReleaseTask {
    id?: string;
    commandLine?: string;
    resourceFiles?: ResourceFile[];
    environmentSettings?: EnvironmentSetting[];
    maxWallClockTime?: moment.Duration;
    retentionTime?: moment.Duration;
    runElevated?: boolean;
}

/**
 * @class
 * Initializes a new instance of the TaskSchedulingPolicy class.
 * @constructor
 * Specifies how tasks should be distributed across compute nodes.
 * @member {string} nodeFillType Gets or sets how tasks should be distributed
 * across compute nodes. Possible values include: 'spread', 'pack', 'unmapped'
 * 
 */
export interface TaskSchedulingPolicy {
    nodeFillType: string;
}

/**
 * @class
 * Initializes a new instance of the StartTask class.
 * @constructor
 * A task defined on a pool and run by compute nodes when they join the pool.
 * @member {string} [commandLine] Gets or sets the command line of the start
 * task.
 * 
 * @member {array} [resourceFiles] Gets or sets a list of files that Batch
 * will download to the compute node before running the command line.
 * 
 * @member {array} [environmentSettings] Gets or sets a list of environment
 * variable settings for the start task.
 * 
 * @member {boolean} [runElevated] Gets or sets whether to run the start task
 * in elevated mode. The default value is false.
 * 
 * @member {number} [maxTaskRetryCount] Gets or sets the maximum number of
 * times the task may be retried.
 * 
 * @member {boolean} [waitForSuccess] Gets or sets whether the Batch Service
 * should wait for the start task to complete successfully (that is, to exit
 * with exit code 0) before scheduling any tasks on the compute node.
 * 
 */
export interface StartTask {
    commandLine?: string;
    resourceFiles?: ResourceFile[];
    environmentSettings?: EnvironmentSetting[];
    runElevated?: boolean;
    maxTaskRetryCount?: number;
    waitForSuccess?: boolean;
}

/**
 * @class
 * Initializes a new instance of the CertificateReference class.
 * @constructor
 * A reference to a certificate to be installed on compute nodes in a pool.
 * @member {string} thumbprint Gets or sets the thumbprint of the certificate.
 * 
 * @member {string} thumbprintAlgorithm Gets or sets the algorithm with which
 * the thumbprint is associated.  This must be sha1.
 * 
 * @member {string} [storeLocation] Gets or sets the location of the
 * certificate store on the compute node into which to install the
 * certificate. The default value is CurrentUser. Possible values include:
 * 'currentuser', 'localmachine', 'unmapped'
 * 
 * @member {string} [storeName] Gets or sets the name of the certificate store
 * on the compute node into which to install the certificate. The default
 * value is My.
 * 
 * @member {string} [visibility] Gets or sets which user accounts on the
 * compute node should have access to the private data of the certificate.
 * This may be any subset of the values 'starttask', 'task' and 'rdp',
 * separated by commas. The default is all accounts, corresponding to the
 * string 'starttask,task,rdp'.
 * 
 */
export interface CertificateReference {
    thumbprint: string;
    thumbprintAlgorithm: string;
    storeLocation?: string;
    storeName?: string;
    visibility?: string;
}

/**
 * @class
 * Initializes a new instance of the MetadataItem class.
 * @constructor
 * A metadata item associated with an Azure Batch resource. The Batch service
 * does not assign any meaning to metadata; it is solely for the use of user
 * code.
 * @member {string} [name] Gets or sets the name of the metadata item.
 * 
 * @member {string} [value] Gets or sets the value of the metadata item.
 * 
 */
export interface MetadataItem {
    name?: string;
    value?: string;
}

/**
 * @class
 * Initializes a new instance of the PoolSpecification class.
 * @constructor
 * Specification for creating a new pool.
 * @member {string} [displayName] Gets or sets the display name for the pool.
 * 
 * @member {string} [vmSize] Gets or sets the size of the virtual machines in
 * the pool. All VMs in a pool are the same size.
 * 
 * @member {string} [osFamily] Gets or sets the Azure Guest OS family to be
 * installed on the virtual machines in the pool.
 * 
 * @member {string} [targetOSVersion] Gets or sets the Azure Guest OS version
 * to be installed on the virtual machines in the pool.
 * 
 * @member {number} [maxTasksPerNode] Gets or sets the maximum number of tasks
 * that can run concurrently on a single compute node in the pool.
 * 
 * @member {object} [taskSchedulingPolicy] Gets or sets how tasks are
 * distributed among compute nodes in the pool.
 * 
 * @member {string} [taskSchedulingPolicy.nodeFillType] Gets or sets how tasks
 * should be distributed across compute nodes. Possible values include:
 * 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration} [resizeTimeout] Gets or sets the timeout for
 * allocation of compute nodes to the pool.
 * 
 * @member {number} [targetDedicated] Gets or sets the desired number of
 * compute nodes in the pool.
 * 
 * @member {boolean} [enableAutoScale] Gets or sets whether the pool size
 * should automatically adjust over time.
 * 
 * @member {string} [autoScaleFormula] Gets or sets the formula for the
 * desired number of compute nodes in the pool.
 * 
 * @member {moment.duration} [autoScaleEvaluationInterval] Gets or sets a time
 * interval for the desired AutoScale evaluation period in the pool.
 * 
 * @member {boolean} [enableInterNodeCommunication] Gets or sets whether the
 * pool permits direct communication between nodes.
 * 
 * @member {object} [startTask] Gets or sets a task to run on each compute
 * node as it joins the pool. The task runs when the node is added to the
 * pool or when the node is restarted.
 * 
 * @member {string} [startTask.commandLine] Gets or sets the command line of
 * the start task.
 * 
 * @member {array} [startTask.resourceFiles] Gets or sets a list of files that
 * Batch will download to the compute node before running the command line.
 * 
 * @member {array} [startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [startTask.runElevated] Gets or sets whether to run the
 * start task in elevated mode. The default value is false.
 * 
 * @member {number} [startTask.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried.
 * 
 * @member {boolean} [startTask.waitForSuccess] Gets or sets whether the Batch
 * Service should wait for the start task to complete successfully (that is,
 * to exit with exit code 0) before scheduling any tasks on the compute node.
 * 
 * @member {array} [certificateReferences] Gets or sets a list of certificates
 * to be installed on each compute node in the pool.
 * 
 * @member {array} [applicationPackageReferences] Gets or sets the list of
 * application packages to be installed on each compute node in the pool.
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the pool as metadata.
 * 
 */
export interface PoolSpecification {
    displayName?: string;
    vmSize?: string;
    osFamily?: string;
    targetOSVersion?: string;
    maxTasksPerNode?: number;
    taskSchedulingPolicy?: TaskSchedulingPolicy;
    resizeTimeout?: moment.Duration;
    targetDedicated?: number;
    enableAutoScale?: boolean;
    autoScaleFormula?: string;
    autoScaleEvaluationInterval?: moment.Duration;
    enableInterNodeCommunication?: boolean;
    startTask?: StartTask;
    certificateReferences?: CertificateReference[];
    applicationPackageReferences?: ApplicationPackageReference[];
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the AutoPoolSpecification class.
 * @constructor
 * Specifies characteristics for a temporary 'auto pool'. The Batch service
 * will create this auto pool, run all the tasks for the job on it, and will
 * delete the pool once the job has completed.
 * @member {string} [autoPoolIdPrefix] Gets or sets a prefix to be added to
 * the unique identifier when a pool is automatically created. The prefix can
 * be up to 20 characters long.
 * 
 * @member {string} poolLifetimeOption Gets or sets the minimum lifetime of
 * created auto pools, and how multiple jobs on a schedule are assigned to
 * pools. Possible values include: 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean} [keepAlive] Gets or sets whether to keep an auto pool
 * alive after its lifetime expires.
 * 
 * @member {object} [pool] Gets or sets the pool specification for the auto
 * pool.
 * 
 * @member {string} [pool.displayName] Gets or sets the display name for the
 * pool.
 * 
 * @member {string} [pool.vmSize] Gets or sets the size of the virtual
 * machines in the pool. All VMs in a pool are the same size.
 * 
 * @member {string} [pool.osFamily] Gets or sets the Azure Guest OS family to
 * be installed on the virtual machines in the pool.
 * 
 * @member {string} [pool.targetOSVersion] Gets or sets the Azure Guest OS
 * version to be installed on the virtual machines in the pool.
 * 
 * @member {number} [pool.maxTasksPerNode] Gets or sets the maximum number of
 * tasks that can run concurrently on a single compute node in the pool.
 * 
 * @member {object} [pool.taskSchedulingPolicy] Gets or sets how tasks are
 * distributed among compute nodes in the pool.
 * 
 * @member {string} [pool.taskSchedulingPolicy.nodeFillType] Gets or sets how
 * tasks should be distributed across compute nodes. Possible values include:
 * 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration} [pool.resizeTimeout] Gets or sets the timeout for
 * allocation of compute nodes to the pool.
 * 
 * @member {number} [pool.targetDedicated] Gets or sets the desired number of
 * compute nodes in the pool.
 * 
 * @member {boolean} [pool.enableAutoScale] Gets or sets whether the pool size
 * should automatically adjust over time.
 * 
 * @member {string} [pool.autoScaleFormula] Gets or sets the formula for the
 * desired number of compute nodes in the pool.
 * 
 * @member {moment.duration} [pool.autoScaleEvaluationInterval] Gets or sets a
 * time interval for the desired AutoScale evaluation period in the pool.
 * 
 * @member {boolean} [pool.enableInterNodeCommunication] Gets or sets whether
 * the pool permits direct communication between nodes.
 * 
 * @member {object} [pool.startTask] Gets or sets a task to run on each
 * compute node as it joins the pool. The task runs when the node is added to
 * the pool or when the node is restarted.
 * 
 * @member {string} [pool.startTask.commandLine] Gets or sets the command line
 * of the start task.
 * 
 * @member {array} [pool.startTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [pool.startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [pool.startTask.runElevated] Gets or sets whether to run
 * the start task in elevated mode. The default value is false.
 * 
 * @member {number} [pool.startTask.maxTaskRetryCount] Gets or sets the
 * maximum number of times the task may be retried.
 * 
 * @member {boolean} [pool.startTask.waitForSuccess] Gets or sets whether the
 * Batch Service should wait for the start task to complete successfully
 * (that is, to exit with exit code 0) before scheduling any tasks on the
 * compute node.
 * 
 * @member {array} [pool.certificateReferences] Gets or sets a list of
 * certificates to be installed on each compute node in the pool.
 * 
 * @member {array} [pool.applicationPackageReferences] Gets or sets the list
 * of application packages to be installed on each compute node in the pool.
 * 
 * @member {array} [pool.metadata] Gets or sets a list of name-value pairs
 * associated with the pool as metadata.
 * 
 */
export interface AutoPoolSpecification {
    autoPoolIdPrefix?: string;
    poolLifetimeOption: string;
    keepAlive?: boolean;
    pool?: PoolSpecification;
}

/**
 * @class
 * Initializes a new instance of the PoolInformation class.
 * @constructor
 * Specifies how a job should be assigned to a pool.
 * @member {string} [poolId] Gets or sets the id of an existing pool. All the
 * tasks of the job will run on the specified pool. You must specify either
 * PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [autoPoolSpecification] Gets or sets characteristics for a
 * temporary 'auto pool.' The Batch service will create this auto pool and
 * run all the tasks of the job on it, and will delete the pool once the job
 * has completed. You must specify either PoolId or AutoPoolSpecification,
 * but not both.
 * 
 * @member {string} [autoPoolSpecification.autoPoolIdPrefix] Gets or sets a
 * prefix to be added to the unique identifier when a pool is automatically
 * created. The prefix can be up to 20 characters long.
 * 
 * @member {string} [autoPoolSpecification.poolLifetimeOption] Gets or sets
 * the minimum lifetime of created auto pools, and how multiple jobs on a
 * schedule are assigned to pools. Possible values include: 'jobschedule',
 * 'job', 'unmapped'
 * 
 * @member {boolean} [autoPoolSpecification.keepAlive] Gets or sets whether to
 * keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [autoPoolSpecification.pool] Gets or sets the pool
 * specification for the auto pool.
 * 
 * @member {string} [autoPoolSpecification.pool.displayName] Gets or sets the
 * display name for the pool.
 * 
 * @member {string} [autoPoolSpecification.pool.vmSize] Gets or sets the size
 * of the virtual machines in the pool. All VMs in a pool are the same size.
 * 
 * @member {string} [autoPoolSpecification.pool.osFamily] Gets or sets the
 * Azure Guest OS family to be installed on the virtual machines in the pool.
 * 
 * @member {string} [autoPoolSpecification.pool.targetOSVersion] Gets or sets
 * the Azure Guest OS version to be installed on the virtual machines in the
 * pool.
 * 
 * @member {number} [autoPoolSpecification.pool.maxTasksPerNode] Gets or sets
 * the maximum number of tasks that can run concurrently on a single compute
 * node in the pool.
 * 
 * @member {object} [autoPoolSpecification.pool.taskSchedulingPolicy] Gets or
 * sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType] Gets or
 * sets how tasks should be distributed across compute nodes. Possible values
 * include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration} [autoPoolSpecification.pool.resizeTimeout] Gets
 * or sets the timeout for allocation of compute nodes to the pool.
 * 
 * @member {number} [autoPoolSpecification.pool.targetDedicated] Gets or sets
 * the desired number of compute nodes in the pool.
 * 
 * @member {boolean} [autoPoolSpecification.pool.enableAutoScale] Gets or sets
 * whether the pool size should automatically adjust over time.
 * 
 * @member {string} [autoPoolSpecification.pool.autoScaleFormula] Gets or sets
 * the formula for the desired number of compute nodes in the pool.
 * 
 * @member {moment.duration}
 * [autoPoolSpecification.pool.autoScaleEvaluationInterval] Gets or sets a
 * time interval for the desired AutoScale evaluation period in the pool.
 * 
 * @member {boolean} [autoPoolSpecification.pool.enableInterNodeCommunication]
 * Gets or sets whether the pool permits direct communication between nodes.
 * 
 * @member {object} [autoPoolSpecification.pool.startTask] Gets or sets a task
 * to run on each compute node as it joins the pool. The task runs when the
 * node is added to the pool or when the node is restarted.
 * 
 * @member {string} [autoPoolSpecification.pool.startTask.commandLine] Gets or
 * sets the command line of the start task.
 * 
 * @member {array} [autoPoolSpecification.pool.startTask.resourceFiles] Gets
 * or sets a list of files that Batch will download to the compute node
 * before running the command line.
 * 
 * @member {array} [autoPoolSpecification.pool.startTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean} [autoPoolSpecification.pool.startTask.runElevated] Gets
 * or sets whether to run the start task in elevated mode. The default value
 * is false.
 * 
 * @member {number} [autoPoolSpecification.pool.startTask.maxTaskRetryCount]
 * Gets or sets the maximum number of times the task may be retried.
 * 
 * @member {boolean} [autoPoolSpecification.pool.startTask.waitForSuccess]
 * Gets or sets whether the Batch Service should wait for the start task to
 * complete successfully (that is, to exit with exit code 0) before
 * scheduling any tasks on the compute node.
 * 
 * @member {array} [autoPoolSpecification.pool.certificateReferences] Gets or
 * sets a list of certificates to be installed on each compute node in the
 * pool.
 * 
 * @member {array} [autoPoolSpecification.pool.applicationPackageReferences]
 * Gets or sets the list of application packages to be installed on each
 * compute node in the pool.
 * 
 * @member {array} [autoPoolSpecification.pool.metadata] Gets or sets a list
 * of name-value pairs associated with the pool as metadata.
 * 
 */
export interface PoolInformation {
    poolId?: string;
    autoPoolSpecification?: AutoPoolSpecification;
}

/**
 * @class
 * Initializes a new instance of the JobSpecification class.
 * @constructor
 * Specifies details of the jobs to be created on a schedule.
 * @member {number} [priority] Gets or sets the priority of jobs created under
 * this schedule. Priority values can range from -1000 to 1000, with -1000
 * being the lowest priority and 1000 being the highest priority. The default
 * value is 0.
 * 
 * @member {string} [displayName] Gets or sets the display name for jobs
 * created under this schedule. It need not be unique and can contain any
 * Unicode characters up to a maximum length of 1024.
 * 
 * @member {boolean} [usesTaskDependencies] Gets or sets the flag that
 * determines if this job will use tasks with dependencies.
 * 
 * @member {object} [constraints] Gets or sets the execution constraints for
 * jobs created under this schedule.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the job may run, measured from the time the job
 * starts. If the job does not complete within the time limit, the Batch
 * service terminates it and any tasks that are still running.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times each task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {object} [jobManagerTask] Gets or sets the details of a Job Manager
 * task to be launched when a job is started under this schedule.
 * 
 * @member {string} [jobManagerTask.id] Gets or sets a string that uniquely
 * identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobManagerTask.displayName] Gets or sets the display name
 * of the Job Manager task.
 * 
 * @member {string} [jobManagerTask.commandLine] Gets or sets the command line
 * of the Job Manager task.
 * 
 * @member {array} [jobManagerTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [jobManagerTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobManagerTask.constraints] Gets or sets constraints that
 * apply to the Job Manager task.
 * 
 * @member {moment.duration} [jobManagerTask.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the task may run, measured from
 * the time the task starts. If the task does not complete within the time
 * limit, the Batch service terminates it.
 * 
 * @member {moment.duration} [jobManagerTask.constraints.retentionTime] Gets
 * or sets the minimum time to retain the working directory for the task on
 * the compute node where it ran. After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {number} [jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobManagerTask.killJobOnCompletion] Gets or sets whether
 * completion of the Job Manager task signifies completion of the entire job.
 * 
 * @member {boolean} [jobManagerTask.runElevated] Gets or sets whether to run
 * the Job Manager task in elevated mode. The default value is false.
 * 
 * @member {boolean} [jobManagerTask.runExclusive] Gets or sets whether the
 * Job Manager task requires exclusive use of the compute node where it runs.
 * 
 * @member {object} [jobPreparationTask] Gets or sets the Job Preparation task
 * for jobs created under this schedule.
 * 
 * @member {string} [jobPreparationTask.id] Gets or sets a string that
 * uniquely identifies the job preparation task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobPreparationTask.commandLine] Gets or sets the command
 * line of the Job Preparation task.
 * 
 * @member {array} [jobPreparationTask.resourceFiles] Gets or sets a list of
 * files that Batch will download to the compute node before running the
 * command line.
 * 
 * @member {array} [jobPreparationTask.environmentSettings] Gets or sets a
 * list of environment variable settings for the Job Preparation task.
 * 
 * @member {object} [jobPreparationTask.constraints] Gets or sets constraints
 * that apply to the Job Preparation task..
 * 
 * @member {moment.duration} [jobPreparationTask.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the task may run, measured from
 * the time the task starts. If the task does not complete within the time
 * limit, the Batch service terminates it.
 * 
 * @member {moment.duration} [jobPreparationTask.constraints.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the task
 * on the compute node where it ran. After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {number} [jobPreparationTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobPreparationTask.waitForSuccess] Gets or sets whether
 * the Batch Service should wait for the Job Preparation task to complete
 * successfully before scheduling any other tasks of the job on the compute
 * node.
 * 
 * @member {boolean} [jobPreparationTask.runElevated] Gets or sets whether to
 * run the Job Preparation task in elevated mode. The default value is false.
 * 
 * @member {boolean} [jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobReleaseTask] Gets or sets the Job Release task for
 * jobs created under this schedule.
 * 
 * @member {string} [jobReleaseTask.id] Gets or sets a string that uniquely
 * identifies the Job Release task within the job. The id can contain any
 * combination of alphanumeric characters including hyphens and underscores
 * and cannot contain more than 64 characters.
 * 
 * @member {string} [jobReleaseTask.commandLine] Gets or sets the command line
 * of the Job Release task.
 * 
 * @member {array} [jobReleaseTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [jobReleaseTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the Job Release task.
 * 
 * @member {moment.duration} [jobReleaseTask.maxWallClockTime] Gets or sets
 * the maximum elapsed time that the Job Release task may run on a given
 * compute node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobReleaseTask.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the Job Release task on
 * the compute node.  After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobReleaseTask.runElevated] Gets or sets whether to run
 * the Job Release task in elevated mode. The default value is false.
 * 
 * @member {array} [commonEnvironmentSettings] Gets or sets a list of common
 * environment variable settings.  These environment variables are set for
 * all tasks in jobs created under this schedule (including the Job Manager,
 * Job Preparation and Job Release tasks).
 * 
 * @member {object} [poolInfo] Gets or sets the pool on which the Batch
 * service runs the tasks of jobs created under this schedule.
 * 
 * @member {string} [poolInfo.poolId] Gets or sets the id of an existing pool.
 * All the tasks of the job will run on the specified pool. You must specify
 * either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [poolInfo.autoPoolSpecification] Gets or sets
 * characteristics for a temporary 'auto pool.' The Batch service will create
 * this auto pool and run all the tasks of the job on it, and will delete the
 * pool once the job has completed. You must specify either PoolId or
 * AutoPoolSpecification, but not both.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool] Gets or sets the
 * pool specification for the auto pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.targetOSVersion] Gets
 * or sets the Azure Guest OS version to be installed on the virtual machines
 * in the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.maxTasksPerNode] Gets
 * or sets the maximum number of tasks that can run concurrently on a single
 * compute node in the pool.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets or sets the
 * timeout for allocation of compute nodes to the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.targetDedicated] Gets
 * or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval] Gets or
 * sets a time interval for the desired AutoScale evaluation period in the
 * pool.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication] Gets or
 * sets whether the pool permits direct communication between nodes.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.startTask.commandLine] Gets or sets
 * the command line of the start task.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.resourceFiles] Gets or sets
 * a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.runElevated] Gets or sets
 * whether to run the start task in elevated mode. The default value is false.
 * 
 * @member {number}
 * [poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess] Gets or
 * sets whether the Batch Service should wait for the start task to complete
 * successfully (that is, to exit with exit code 0) before scheduling any
 * tasks on the compute node.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.applicationPackageReferences] Gets or
 * sets the list of application packages to be installed on each compute node
 * in the pool.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.metadata] Gets or sets
 * a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with each job created under this schedule as metadata.
 * 
 */
export interface JobSpecification {
    priority?: number;
    displayName?: string;
    usesTaskDependencies?: boolean;
    constraints?: JobConstraints;
    jobManagerTask?: JobManagerTask;
    jobPreparationTask?: JobPreparationTask;
    jobReleaseTask?: JobReleaseTask;
    commonEnvironmentSettings?: EnvironmentSetting[];
    poolInfo?: PoolInformation;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the RecentJob class.
 * @constructor
 * Information about the most recent job to run under the job schedule.
 * @member {string} [id] Gets or sets the id of the job.
 * 
 * @member {string} [url] Gets or sets the URL of the job.
 * 
 */
export interface RecentJob {
    id?: string;
    url?: string;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleExecutionInformation class.
 * @constructor
 * Specifies how tasks should be run in a job associated with a job schedule.
 * @member {date} [nextRunTime] Gets or sets the next time at which a job will
 * be created under this schedule.
 * 
 * @member {object} [recentJob] Gets or sets information about the most recent
 * job under the job schedule.
 * 
 * @member {string} [recentJob.id] Gets or sets the id of the job.
 * 
 * @member {string} [recentJob.url] Gets or sets the URL of the job.
 * 
 * @member {date} [endTime] Gets or sets the time at which the schedule ended.
 * This property is set only if the job schedule is in the completed state.
 * 
 */
export interface JobScheduleExecutionInformation {
    nextRunTime?: Date;
    recentJob?: RecentJob;
    endTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleStatistics class.
 * @constructor
 * The lifetime resource usage statistics for a job schedule.
 * @member {string} url Gets or sets the URL for the statistics.
 * 
 * @member {date} startTime Gets or sets the start time of the time range
 * covered by the statistics.
 * 
 * @member {date} lastUpdateTime Gets or sets the time at which the statistics
 * were last updated. All statistics are limited to the range between
 * startTime and lastUpdateTime.
 * 
 * @member {moment.duration} userCPUTime Gets or sets the total user mode CPU
 * time (summed across all cores and all compute nodes) consumed by all the
 * tasks in all the jobs created under the schedule.
 * 
 * @member {moment.duration} kernelCPUTime Gets or sets the total kernel mode
 * CPU time (summed across all cores and all compute nodes) consumed by all
 * the tasks in all the jobs created under the schedule.
 * 
 * @member {moment.duration} wallClockTime Gets or sets the total wall clock
 * time of all the tasks in all the jobs created under the schedule.
 * 
 * @member {number} readIOps Gets or sets the total number of I/O read
 * operations performed by all the tasks in all the jobs created under the
 * schedule.
 * 
 * @member {number} writeIOps Gets or sets the total number of I/O write
 * operations performed by all the tasks in all the jobs created under the
 * schedule.
 * 
 * @member {number} readIOGiB Gets or sets the total amount of data in GiB of
 * I/O read by all the tasks in all the jobs created under the schedule.
 * 
 * @member {number} writeIOGiB Gets or sets the total amount of data in GiB of
 * I/O written by all the tasks in all the jobs created under the schedule.
 * 
 * @member {number} numSucceededTasks Gets or sets the total number of tasks
 * successfully completed during the given time range in jobs created under
 * the schedule.  A task completes successfully if it returns exit code 0.
 * 
 * @member {number} numFailedTasks Gets or sets the total number of tasks that
 * failed during the given time range in jobs created under the schedule. A
 * task fails if it exhausts its maximum retry count without returning exit
 * code 0.
 * 
 * @member {number} numTaskRetries Gets or sets the total number of retries
 * during the given time range on all the tasks in jobs created under the
 * schedule.
 * 
 * @member {moment.duration} waitTime Gets or sets the total wait time of all
 * the tasks in jobs created under the schedule.
 * 
 */
export interface JobScheduleStatistics {
    url: string;
    startTime: Date;
    lastUpdateTime: Date;
    userCPUTime: moment.Duration;
    kernelCPUTime: moment.Duration;
    wallClockTime: moment.Duration;
    readIOps: number;
    writeIOps: number;
    readIOGiB: number;
    writeIOGiB: number;
    numSucceededTasks: number;
    numFailedTasks: number;
    numTaskRetries: number;
    waitTime: moment.Duration;
}

/**
 * @class
 * Initializes a new instance of the CloudJobSchedule class.
 * @constructor
 * A job schedule that allows recurring jobs by specifying when to run jobs
 * and a specification used to create each job.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * schedule within the account. A GUID is recommended.
 * 
 * @member {string} [displayName] Gets or sets the display name for the
 * schedule.
 * 
 * @member {string} [url] Gets or sets the URL of the job schedule.
 * 
 * @member {string} [eTag] Gets or sets the ETag of the job schedule.
 * 
 * @member {date} [lastModified] Gets or sets the last modified time of the
 * job schedule.
 * 
 * @member {date} [creationTime] Gets or sets the creation time of the job
 * schedule.
 * 
 * @member {string} [state] Gets or sets the current state of the job
 * schedule. Possible values include: 'active', 'completed', 'disabled',
 * 'terminating', 'deleting'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the job
 * schedule entered the current state.
 * 
 * @member {string} [previousState] Gets or sets the previous state of the job
 * schedule. Possible values include: 'active', 'completed', 'disabled',
 * 'terminating', 'deleting'
 * 
 * @member {date} [previousStateTransitionTime] Gets or sets the time at which
 * the job schedule entered its previous state.
 * 
 * @member {object} [schedule] Gets or sets the schedule according to which
 * jobs will be created.
 * 
 * @member {date} [schedule.doNotRunUntil] Gets or sets the earliest time at
 * which any job may be created under this job schedule. If you do not
 * specify a doNotRunUntil time, the schedule becomes ready to create jobs
 * immediately.
 * 
 * @member {date} [schedule.doNotRunAfter] Gets or sets a time after which no
 * job will be created under this job schedule.  The schedule will move to
 * the completed state as soon as this deadline is past and there is no
 * active job under this job schedule.
 * 
 * @member {moment.duration} [schedule.startWindow] Gets or sets the time
 * interval, starting from the time at which the schedule indicates a job
 * should be created, within which a job must be created.  If a job is not
 * created within the startWindow interval, then the 'opportunity' is lost;
 * no job will be created until the next recurrence of the schedule.
 * 
 * @member {moment.duration} [schedule.recurrenceInterval] Gets or sets the
 * time interval between the start times of two successive jobs under the job
 * schedule. A job schedule can have at most one active job under it at any
 * given time.
 * 
 * @member {object} [jobSpecification] Gets or sets the details of the jobs to
 * be created on this schedule.
 * 
 * @member {number} [jobSpecification.priority] Gets or sets the priority of
 * jobs created under this schedule. Priority values can range from -1000 to
 * 1000, with -1000 being the lowest priority and 1000 being the highest
 * priority. The default value is 0.
 * 
 * @member {string} [jobSpecification.displayName] Gets or sets the display
 * name for jobs created under this schedule. It need not be unique and can
 * contain any Unicode characters up to a maximum length of 1024.
 * 
 * @member {boolean} [jobSpecification.usesTaskDependencies] Gets or sets the
 * flag that determines if this job will use tasks with dependencies.
 * 
 * @member {object} [jobSpecification.constraints] Gets or sets the execution
 * constraints for jobs created under this schedule.
 * 
 * @member {moment.duration} [jobSpecification.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the job may run, measured from
 * the time the job starts. If the job does not complete within the time
 * limit, the Batch service terminates it and any tasks that are still
 * running.
 * 
 * @member {number} [jobSpecification.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times each task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {object} [jobSpecification.jobManagerTask] Gets or sets the details
 * of a Job Manager task to be launched when a job is started under this
 * schedule.
 * 
 * @member {string} [jobSpecification.jobManagerTask.id] Gets or sets a string
 * that uniquely identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobSpecification.jobManagerTask.displayName] Gets or sets
 * the display name of the Job Manager task.
 * 
 * @member {string} [jobSpecification.jobManagerTask.commandLine] Gets or sets
 * the command line of the Job Manager task.
 * 
 * @member {array} [jobSpecification.jobManagerTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobManagerTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobSpecification.jobManagerTask.constraints] Gets or sets
 * constraints that apply to the Job Manager task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.retentionTime] Gets or sets
 * the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.killJobOnCompletion]
 * Gets or sets whether completion of the Job Manager task signifies
 * completion of the entire job.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runElevated] Gets or
 * sets whether to run the Job Manager task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runExclusive] Gets or
 * sets whether the Job Manager task requires exclusive use of the compute
 * node where it runs.
 * 
 * @member {object} [jobSpecification.jobPreparationTask] Gets or sets the Job
 * Preparation task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.id] Gets or sets a
 * string that uniquely identifies the job preparation task within the job.
 * The id can contain any combination of alphanumeric characters including
 * hyphens and underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.commandLine] Gets or
 * sets the command line of the Job Preparation task.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the Job
 * Preparation task.
 * 
 * @member {object} [jobSpecification.jobPreparationTask.constraints] Gets or
 * sets constraints that apply to the Job Preparation task..
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.retentionTime] Gets or
 * sets the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobPreparationTask.constraints.maxTaskRetryCount] Gets
 * or sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.waitForSuccess] Gets
 * or sets whether the Batch Service should wait for the Job Preparation task
 * to complete successfully before scheduling any other tasks of the job on
 * the compute node.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.runElevated] Gets or
 * sets whether to run the Job Preparation task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean}
 * [jobSpecification.jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobSpecification.jobReleaseTask] Gets or sets the Job
 * Release task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.id] Gets or sets a string
 * that uniquely identifies the Job Release task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.commandLine] Gets or sets
 * the command line of the Job Release task.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Release task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobReleaseTask.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the Job Release task may run on a given compute
 * node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobSpecification.jobReleaseTask.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the Job
 * Release task on the compute node.  After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobSpecification.jobReleaseTask.runElevated] Gets or
 * sets whether to run the Job Release task in elevated mode. The default
 * value is false.
 * 
 * @member {array} [jobSpecification.commonEnvironmentSettings] Gets or sets a
 * list of common environment variable settings.  These environment variables
 * are set for all tasks in jobs created under this schedule (including the
 * Job Manager, Job Preparation and Job Release tasks).
 * 
 * @member {object} [jobSpecification.poolInfo] Gets or sets the pool on which
 * the Batch service runs the tasks of jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.poolInfo.poolId] Gets or sets the id of
 * an existing pool. All the tasks of the job will run on the specified pool.
 * You must specify either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification] Gets or
 * sets characteristics for a temporary 'auto pool.' The Batch service will
 * create this auto pool and run all the tasks of the job on it, and will
 * delete the pool once the job has completed. You must specify either PoolId
 * or AutoPoolSpecification, but not both.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification.pool]
 * Gets or sets the pool specification for the auto pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetOSVersion]
 * Gets or sets the Azure Guest OS version to be installed on the virtual
 * machines in the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.maxTasksPerNode]
 * Gets or sets the maximum number of tasks that can run concurrently on a
 * single compute node in the pool.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets
 * or sets the timeout for allocation of compute nodes to the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetDedicated]
 * Gets or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval]
 * Gets or sets a time interval for the desired AutoScale evaluation period
 * in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication]
 * Gets or sets whether the pool permits direct communication between nodes.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.commandLine]
 * Gets or sets the command line of the start task.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.resourceFiles]
 * Gets or sets a list of files that Batch will download to the compute node
 * before running the command line.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.runElevated]
 * Gets or sets whether to run the start task in elevated mode. The default
 * value is false.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount]
 * Gets or sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess]
 * Gets or sets whether the Batch Service should wait for the start task to
 * complete successfully (that is, to exit with exit code 0) before
 * scheduling any tasks on the compute node.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.applicationPackageReferences]
 * Gets or sets the list of application packages to be installed on each
 * compute node in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.metadata] Gets or
 * sets a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [jobSpecification.metadata] Gets or sets a list of
 * name-value pairs associated with each job created under this schedule as
 * metadata.
 * 
 * @member {object} [executionInfo] Gets or sets information about jobs that
 * have been and will be run under this schedule.
 * 
 * @member {date} [executionInfo.nextRunTime] Gets or sets the next time at
 * which a job will be created under this schedule.
 * 
 * @member {object} [executionInfo.recentJob] Gets or sets information about
 * the most recent job under the job schedule.
 * 
 * @member {string} [executionInfo.recentJob.id] Gets or sets the id of the
 * job.
 * 
 * @member {string} [executionInfo.recentJob.url] Gets or sets the URL of the
 * job.
 * 
 * @member {date} [executionInfo.endTime] Gets or sets the time at which the
 * schedule ended. This property is set only if the job schedule is in the
 * completed state.
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the schedule as metadata.
 * 
 * @member {object} [stats] Gets or sets the lifetime resource usage
 * statistics for the job schedule.
 * 
 * @member {string} [stats.url] Gets or sets the URL for the statistics.
 * 
 * @member {date} [stats.startTime] Gets or sets the start time of the time
 * range covered by the statistics.
 * 
 * @member {date} [stats.lastUpdateTime] Gets or sets the time at which the
 * statistics were last updated. All statistics are limited to the range
 * between startTime and lastUpdateTime.
 * 
 * @member {moment.duration} [stats.userCPUTime] Gets or sets the total user
 * mode CPU time (summed across all cores and all compute nodes) consumed by
 * all the tasks in all the jobs created under the schedule.
 * 
 * @member {moment.duration} [stats.kernelCPUTime] Gets or sets the total
 * kernel mode CPU time (summed across all cores and all compute nodes)
 * consumed by all the tasks in all the jobs created under the schedule.
 * 
 * @member {moment.duration} [stats.wallClockTime] Gets or sets the total wall
 * clock time of all the tasks in all the jobs created under the schedule.
 * 
 * @member {number} [stats.readIOps] Gets or sets the total number of I/O read
 * operations performed by all the tasks in all the jobs created under the
 * schedule.
 * 
 * @member {number} [stats.writeIOps] Gets or sets the total number of I/O
 * write operations performed by all the tasks in all the jobs created under
 * the schedule.
 * 
 * @member {number} [stats.readIOGiB] Gets or sets the total amount of data in
 * GiB of I/O read by all the tasks in all the jobs created under the
 * schedule.
 * 
 * @member {number} [stats.writeIOGiB] Gets or sets the total amount of data
 * in GiB of I/O written by all the tasks in all the jobs created under the
 * schedule.
 * 
 * @member {number} [stats.numSucceededTasks] Gets or sets the total number of
 * tasks successfully completed during the given time range in jobs created
 * under the schedule.  A task completes successfully if it returns exit code
 * 0.
 * 
 * @member {number} [stats.numFailedTasks] Gets or sets the total number of
 * tasks that failed during the given time range in jobs created under the
 * schedule. A task fails if it exhausts its maximum retry count without
 * returning exit code 0.
 * 
 * @member {number} [stats.numTaskRetries] Gets or sets the total number of
 * retries during the given time range on all the tasks in jobs created under
 * the schedule.
 * 
 * @member {moment.duration} [stats.waitTime] Gets or sets the total wait time
 * of all the tasks in jobs created under the schedule.
 * 
 */
export interface CloudJobSchedule {
    id?: string;
    displayName?: string;
    url?: string;
    eTag?: string;
    lastModified?: Date;
    creationTime?: Date;
    state?: string;
    stateTransitionTime?: Date;
    previousState?: string;
    previousStateTransitionTime?: Date;
    schedule?: Schedule;
    jobSpecification?: JobSpecification;
    executionInfo?: JobScheduleExecutionInformation;
    metadata?: MetadataItem[];
    stats?: JobScheduleStatistics;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleAddParameter class.
 * @constructor
 * A job schedule that allows recurring jobs by specifying when to run jobs
 * and a specification used to create each job.
 * @member {string} id Gets or sets a string that uniquely identifies the
 * schedule within the account. A GUID is recommended.
 * 
 * @member {string} [displayName] Gets or sets the display name for the
 * schedule.
 * 
 * @member {object} schedule Gets or sets the schedule according to which jobs
 * will be created.
 * 
 * @member {date} [schedule.doNotRunUntil] Gets or sets the earliest time at
 * which any job may be created under this job schedule. If you do not
 * specify a doNotRunUntil time, the schedule becomes ready to create jobs
 * immediately.
 * 
 * @member {date} [schedule.doNotRunAfter] Gets or sets a time after which no
 * job will be created under this job schedule.  The schedule will move to
 * the completed state as soon as this deadline is past and there is no
 * active job under this job schedule.
 * 
 * @member {moment.duration} [schedule.startWindow] Gets or sets the time
 * interval, starting from the time at which the schedule indicates a job
 * should be created, within which a job must be created.  If a job is not
 * created within the startWindow interval, then the 'opportunity' is lost;
 * no job will be created until the next recurrence of the schedule.
 * 
 * @member {moment.duration} [schedule.recurrenceInterval] Gets or sets the
 * time interval between the start times of two successive jobs under the job
 * schedule. A job schedule can have at most one active job under it at any
 * given time.
 * 
 * @member {object} jobSpecification Gets or sets the details of the jobs to
 * be created on this schedule.
 * 
 * @member {number} [jobSpecification.priority] Gets or sets the priority of
 * jobs created under this schedule. Priority values can range from -1000 to
 * 1000, with -1000 being the lowest priority and 1000 being the highest
 * priority. The default value is 0.
 * 
 * @member {string} [jobSpecification.displayName] Gets or sets the display
 * name for jobs created under this schedule. It need not be unique and can
 * contain any Unicode characters up to a maximum length of 1024.
 * 
 * @member {boolean} [jobSpecification.usesTaskDependencies] Gets or sets the
 * flag that determines if this job will use tasks with dependencies.
 * 
 * @member {object} [jobSpecification.constraints] Gets or sets the execution
 * constraints for jobs created under this schedule.
 * 
 * @member {moment.duration} [jobSpecification.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the job may run, measured from
 * the time the job starts. If the job does not complete within the time
 * limit, the Batch service terminates it and any tasks that are still
 * running.
 * 
 * @member {number} [jobSpecification.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times each task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {object} [jobSpecification.jobManagerTask] Gets or sets the details
 * of a Job Manager task to be launched when a job is started under this
 * schedule.
 * 
 * @member {string} [jobSpecification.jobManagerTask.id] Gets or sets a string
 * that uniquely identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobSpecification.jobManagerTask.displayName] Gets or sets
 * the display name of the Job Manager task.
 * 
 * @member {string} [jobSpecification.jobManagerTask.commandLine] Gets or sets
 * the command line of the Job Manager task.
 * 
 * @member {array} [jobSpecification.jobManagerTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobManagerTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobSpecification.jobManagerTask.constraints] Gets or sets
 * constraints that apply to the Job Manager task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.retentionTime] Gets or sets
 * the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.killJobOnCompletion]
 * Gets or sets whether completion of the Job Manager task signifies
 * completion of the entire job.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runElevated] Gets or
 * sets whether to run the Job Manager task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runExclusive] Gets or
 * sets whether the Job Manager task requires exclusive use of the compute
 * node where it runs.
 * 
 * @member {object} [jobSpecification.jobPreparationTask] Gets or sets the Job
 * Preparation task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.id] Gets or sets a
 * string that uniquely identifies the job preparation task within the job.
 * The id can contain any combination of alphanumeric characters including
 * hyphens and underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.commandLine] Gets or
 * sets the command line of the Job Preparation task.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the Job
 * Preparation task.
 * 
 * @member {object} [jobSpecification.jobPreparationTask.constraints] Gets or
 * sets constraints that apply to the Job Preparation task..
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.retentionTime] Gets or
 * sets the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobPreparationTask.constraints.maxTaskRetryCount] Gets
 * or sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.waitForSuccess] Gets
 * or sets whether the Batch Service should wait for the Job Preparation task
 * to complete successfully before scheduling any other tasks of the job on
 * the compute node.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.runElevated] Gets or
 * sets whether to run the Job Preparation task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean}
 * [jobSpecification.jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobSpecification.jobReleaseTask] Gets or sets the Job
 * Release task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.id] Gets or sets a string
 * that uniquely identifies the Job Release task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.commandLine] Gets or sets
 * the command line of the Job Release task.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Release task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobReleaseTask.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the Job Release task may run on a given compute
 * node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobSpecification.jobReleaseTask.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the Job
 * Release task on the compute node.  After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobSpecification.jobReleaseTask.runElevated] Gets or
 * sets whether to run the Job Release task in elevated mode. The default
 * value is false.
 * 
 * @member {array} [jobSpecification.commonEnvironmentSettings] Gets or sets a
 * list of common environment variable settings.  These environment variables
 * are set for all tasks in jobs created under this schedule (including the
 * Job Manager, Job Preparation and Job Release tasks).
 * 
 * @member {object} [jobSpecification.poolInfo] Gets or sets the pool on which
 * the Batch service runs the tasks of jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.poolInfo.poolId] Gets or sets the id of
 * an existing pool. All the tasks of the job will run on the specified pool.
 * You must specify either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification] Gets or
 * sets characteristics for a temporary 'auto pool.' The Batch service will
 * create this auto pool and run all the tasks of the job on it, and will
 * delete the pool once the job has completed. You must specify either PoolId
 * or AutoPoolSpecification, but not both.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification.pool]
 * Gets or sets the pool specification for the auto pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetOSVersion]
 * Gets or sets the Azure Guest OS version to be installed on the virtual
 * machines in the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.maxTasksPerNode]
 * Gets or sets the maximum number of tasks that can run concurrently on a
 * single compute node in the pool.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets
 * or sets the timeout for allocation of compute nodes to the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetDedicated]
 * Gets or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval]
 * Gets or sets a time interval for the desired AutoScale evaluation period
 * in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication]
 * Gets or sets whether the pool permits direct communication between nodes.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.commandLine]
 * Gets or sets the command line of the start task.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.resourceFiles]
 * Gets or sets a list of files that Batch will download to the compute node
 * before running the command line.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.runElevated]
 * Gets or sets whether to run the start task in elevated mode. The default
 * value is false.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount]
 * Gets or sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess]
 * Gets or sets whether the Batch Service should wait for the start task to
 * complete successfully (that is, to exit with exit code 0) before
 * scheduling any tasks on the compute node.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.applicationPackageReferences]
 * Gets or sets the list of application packages to be installed on each
 * compute node in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.metadata] Gets or
 * sets a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [jobSpecification.metadata] Gets or sets a list of
 * name-value pairs associated with each job created under this schedule as
 * metadata.
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the schedule as metadata.
 * 
 */
export interface JobScheduleAddParameter {
    id: string;
    displayName?: string;
    schedule: Schedule;
    jobSpecification: JobSpecification;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the JobSchedulingError class.
 * @constructor
 * An error encountered by the Batch service when scheduling a job.
 * @member {string} category Gets or sets the category of the job scheduling
 * error. Possible values include: 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [code] Gets or sets an identifier for the job scheduling
 * error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [message] Gets or sets a message describing the job
 * scheduling error, intended to be suitable for display in a user interface.
 * 
 * @member {array} [details] Gets or sets a list of additional error details
 * related to the scheduling error.
 * 
 */
export interface JobSchedulingError {
    category: string;
    code?: string;
    message?: string;
    details?: NameValuePair[];
}

/**
 * @class
 * Initializes a new instance of the JobExecutionInformation class.
 * @constructor
 * Contains information about the execution of a job in the Azure Batch
 * service.
 * @member {date} startTime Gets or sets the start time of the job.
 * 
 * @member {date} [endTime] Gets or sets the completion time of the job. This
 * property is set only if the job is in the completed state.
 * 
 * @member {string} [poolId] Gets or sets the id of the pool to which this job
 * is assigned.
 * 
 * @member {object} [schedulingError] Gets or sets details of any error
 * encountered by the service in starting the job.
 * 
 * @member {string} [schedulingError.category] Gets or sets the category of
 * the job scheduling error. Possible values include: 'usererror',
 * 'servererror', 'unmapped'
 * 
 * @member {string} [schedulingError.code] Gets or sets an identifier for the
 * job scheduling error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [schedulingError.message] Gets or sets a message
 * describing the job scheduling error, intended to be suitable for display
 * in a user interface.
 * 
 * @member {array} [schedulingError.details] Gets or sets a list of additional
 * error details related to the scheduling error.
 * 
 * @member {string} [terminateReason] Gets or sets a string describing the
 * reason the job ended.
 * 
 */
export interface JobExecutionInformation {
    startTime: Date;
    endTime?: Date;
    poolId?: string;
    schedulingError?: JobSchedulingError;
    terminateReason?: string;
}

/**
 * @class
 * Initializes a new instance of the CloudJob class.
 * @constructor
 * An Azure Batch job.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * job within the account. The id can contain any combination of alphanumeric
 * characters including hyphens and underscores, and cannot contain more than
 * 64 characters. It is common to use a GUID for the id.
 * 
 * @member {string} [displayName] Gets or sets the display name for the job.
 * 
 * @member {boolean} [usesTaskDependencies] Gets or sets the flag that
 * determines if this job will use tasks with dependencies.
 * 
 * @member {string} [url] Gets or sets the URL of the job.
 * 
 * @member {string} [eTag] Gets or sets the ETag of the job.
 * 
 * @member {date} [lastModified] Gets or sets the last modified time of the
 * job.
 * 
 * @member {date} [creationTime] Gets or sets the creation time of the job.
 * 
 * @member {string} [state] Gets or sets the current state of the job.
 * Possible values include: 'active', 'disabling', 'disabled', 'enabling',
 * 'terminating', 'completed', 'deleting'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the job
 * entered its current state.
 * 
 * @member {string} [previousState] Gets or sets the previous state of the
 * job. This property is not set if the job is in its initial Active state.
 * Possible values include: 'active', 'disabling', 'disabled', 'enabling',
 * 'terminating', 'completed', 'deleting'
 * 
 * @member {date} [previousStateTransitionTime] Gets or sets the time at which
 * the job entered its previous state. This property is not set if the job is
 * in its initial Active state.
 * 
 * @member {number} [priority] Gets or sets the priority of the job. Priority
 * values can range from -1000 to 1000, with -1000 being the lowest priority
 * and 1000 being the highest priority. The default value is 0.
 * 
 * @member {object} [constraints] Gets or sets the execution constraints for
 * the job.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the job may run, measured from the time the job
 * starts. If the job does not complete within the time limit, the Batch
 * service terminates it and any tasks that are still running.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times each task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {object} [jobManagerTask] Gets or sets details of a Job Manager
 * task to be launched when the job is started.
 * 
 * @member {string} [jobManagerTask.id] Gets or sets a string that uniquely
 * identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobManagerTask.displayName] Gets or sets the display name
 * of the Job Manager task.
 * 
 * @member {string} [jobManagerTask.commandLine] Gets or sets the command line
 * of the Job Manager task.
 * 
 * @member {array} [jobManagerTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [jobManagerTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobManagerTask.constraints] Gets or sets constraints that
 * apply to the Job Manager task.
 * 
 * @member {moment.duration} [jobManagerTask.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the task may run, measured from
 * the time the task starts. If the task does not complete within the time
 * limit, the Batch service terminates it.
 * 
 * @member {moment.duration} [jobManagerTask.constraints.retentionTime] Gets
 * or sets the minimum time to retain the working directory for the task on
 * the compute node where it ran. After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {number} [jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobManagerTask.killJobOnCompletion] Gets or sets whether
 * completion of the Job Manager task signifies completion of the entire job.
 * 
 * @member {boolean} [jobManagerTask.runElevated] Gets or sets whether to run
 * the Job Manager task in elevated mode. The default value is false.
 * 
 * @member {boolean} [jobManagerTask.runExclusive] Gets or sets whether the
 * Job Manager task requires exclusive use of the compute node where it runs.
 * 
 * @member {object} [jobPreparationTask] Gets or sets the Job Preparation task.
 * 
 * @member {string} [jobPreparationTask.id] Gets or sets a string that
 * uniquely identifies the job preparation task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobPreparationTask.commandLine] Gets or sets the command
 * line of the Job Preparation task.
 * 
 * @member {array} [jobPreparationTask.resourceFiles] Gets or sets a list of
 * files that Batch will download to the compute node before running the
 * command line.
 * 
 * @member {array} [jobPreparationTask.environmentSettings] Gets or sets a
 * list of environment variable settings for the Job Preparation task.
 * 
 * @member {object} [jobPreparationTask.constraints] Gets or sets constraints
 * that apply to the Job Preparation task..
 * 
 * @member {moment.duration} [jobPreparationTask.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the task may run, measured from
 * the time the task starts. If the task does not complete within the time
 * limit, the Batch service terminates it.
 * 
 * @member {moment.duration} [jobPreparationTask.constraints.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the task
 * on the compute node where it ran. After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {number} [jobPreparationTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobPreparationTask.waitForSuccess] Gets or sets whether
 * the Batch Service should wait for the Job Preparation task to complete
 * successfully before scheduling any other tasks of the job on the compute
 * node.
 * 
 * @member {boolean} [jobPreparationTask.runElevated] Gets or sets whether to
 * run the Job Preparation task in elevated mode. The default value is false.
 * 
 * @member {boolean} [jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobReleaseTask] Gets or sets the Job Release task.
 * 
 * @member {string} [jobReleaseTask.id] Gets or sets a string that uniquely
 * identifies the Job Release task within the job. The id can contain any
 * combination of alphanumeric characters including hyphens and underscores
 * and cannot contain more than 64 characters.
 * 
 * @member {string} [jobReleaseTask.commandLine] Gets or sets the command line
 * of the Job Release task.
 * 
 * @member {array} [jobReleaseTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [jobReleaseTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the Job Release task.
 * 
 * @member {moment.duration} [jobReleaseTask.maxWallClockTime] Gets or sets
 * the maximum elapsed time that the Job Release task may run on a given
 * compute node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobReleaseTask.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the Job Release task on
 * the compute node.  After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobReleaseTask.runElevated] Gets or sets whether to run
 * the Job Release task in elevated mode. The default value is false.
 * 
 * @member {array} [commonEnvironmentSettings] Gets or sets the list of common
 * environment variable settings.  These environment variables are set for
 * all tasks in the job (including the Job Manager, Job Preparation and Job
 * Release tasks).
 * 
 * @member {object} [poolInfo] Gets or sets the pool on which the Batch
 * service runs the jobâ€™s tasks.
 * 
 * @member {string} [poolInfo.poolId] Gets or sets the id of an existing pool.
 * All the tasks of the job will run on the specified pool. You must specify
 * either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [poolInfo.autoPoolSpecification] Gets or sets
 * characteristics for a temporary 'auto pool.' The Batch service will create
 * this auto pool and run all the tasks of the job on it, and will delete the
 * pool once the job has completed. You must specify either PoolId or
 * AutoPoolSpecification, but not both.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool] Gets or sets the
 * pool specification for the auto pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.targetOSVersion] Gets
 * or sets the Azure Guest OS version to be installed on the virtual machines
 * in the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.maxTasksPerNode] Gets
 * or sets the maximum number of tasks that can run concurrently on a single
 * compute node in the pool.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets or sets the
 * timeout for allocation of compute nodes to the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.targetDedicated] Gets
 * or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval] Gets or
 * sets a time interval for the desired AutoScale evaluation period in the
 * pool.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication] Gets or
 * sets whether the pool permits direct communication between nodes.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.startTask.commandLine] Gets or sets
 * the command line of the start task.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.resourceFiles] Gets or sets
 * a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.runElevated] Gets or sets
 * whether to run the start task in elevated mode. The default value is false.
 * 
 * @member {number}
 * [poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess] Gets or
 * sets whether the Batch Service should wait for the start task to complete
 * successfully (that is, to exit with exit code 0) before scheduling any
 * tasks on the compute node.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.applicationPackageReferences] Gets or
 * sets the list of application packages to be installed on each compute node
 * in the pool.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.metadata] Gets or sets
 * a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the job as metadata.
 * 
 * @member {object} [executionInfo] Gets or sets the execution information for
 * the job.
 * 
 * @member {date} [executionInfo.startTime] Gets or sets the start time of the
 * job.
 * 
 * @member {date} [executionInfo.endTime] Gets or sets the completion time of
 * the job. This property is set only if the job is in the completed state.
 * 
 * @member {string} [executionInfo.poolId] Gets or sets the id of the pool to
 * which this job is assigned.
 * 
 * @member {object} [executionInfo.schedulingError] Gets or sets details of
 * any error encountered by the service in starting the job.
 * 
 * @member {string} [executionInfo.schedulingError.category] Gets or sets the
 * category of the job scheduling error. Possible values include:
 * 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [executionInfo.schedulingError.code] Gets or sets an
 * identifier for the job scheduling error.  Codes are invariant and are
 * intended to be consumed programmatically.
 * 
 * @member {string} [executionInfo.schedulingError.message] Gets or sets a
 * message describing the job scheduling error, intended to be suitable for
 * display in a user interface.
 * 
 * @member {array} [executionInfo.schedulingError.details] Gets or sets a list
 * of additional error details related to the scheduling error.
 * 
 * @member {string} [executionInfo.terminateReason] Gets or sets a string
 * describing the reason the job ended.
 * 
 * @member {object} [stats] Gets or sets resource usage statistics for the
 * entire lifetime of the job.
 * 
 * @member {string} [stats.url] Gets or sets the URL for the statistics.
 * 
 * @member {date} [stats.startTime] Gets or sets the start time of the time
 * range covered by the statistics.
 * 
 * @member {date} [stats.lastUpdateTime] Gets or sets the time at which the
 * statistics were last updated. All statistics are limited to the range
 * between StartTime and LastUpdateTime.
 * 
 * @member {moment.duration} [stats.userCPUTime] Gets or sets the total user
 * mode CPU time (summed across all cores and all compute nodes) consumed by
 * all the tasks in the job.
 * 
 * @member {moment.duration} [stats.kernelCPUTime] Gets or sets the total
 * kernel mode CPU time (summed across all cores and all compute nodes)
 * consumed by all the tasks in the job.
 * 
 * @member {moment.duration} [stats.wallClockTime] Gets or sets the total wall
 * clock time of all the tasks in the job.
 * 
 * @member {number} [stats.readIOps] Gets or sets the total number of I/O read
 * operations performed by all the tasks in the job.
 * 
 * @member {number} [stats.writeIOps] Gets or sets the total number of I/O
 * write operations performed by all the tasks in the job.
 * 
 * @member {number} [stats.readIOGiB] Gets or sets the total amount of data in
 * GiB of I/O read by all the tasks in the job.
 * 
 * @member {number} [stats.writeIOGiB] Gets or sets the total amount of data
 * in GiB of I/O written by all the tasks in the job.
 * 
 * @member {number} [stats.numSucceededTasks] Gets or sets the total number of
 * tasks successfully completed in the job during the given time range.
 * 
 * @member {number} [stats.numFailedTasks] Gets or sets the total number of
 * tasks in the job that failed during the given time range.
 * 
 * @member {number} [stats.numTaskRetries] Gets or sets the total number of
 * retries on all the tasks in the job during the given time range.
 * 
 * @member {moment.duration} [stats.waitTime] Gets or sets the total wait time
 * of all the tasks in the job.  The wait time for a task is defined as the
 * elapsed time between the creation of the task creation and the start of
 * task execution.  This value is reported only in the account lifetime
 * statistics; it is not included in individual job statistics.
 * 
 */
export interface CloudJob {
    id?: string;
    displayName?: string;
    usesTaskDependencies?: boolean;
    url?: string;
    eTag?: string;
    lastModified?: Date;
    creationTime?: Date;
    state?: string;
    stateTransitionTime?: Date;
    previousState?: string;
    previousStateTransitionTime?: Date;
    priority?: number;
    constraints?: JobConstraints;
    jobManagerTask?: JobManagerTask;
    jobPreparationTask?: JobPreparationTask;
    jobReleaseTask?: JobReleaseTask;
    commonEnvironmentSettings?: EnvironmentSetting[];
    poolInfo?: PoolInformation;
    metadata?: MetadataItem[];
    executionInfo?: JobExecutionInformation;
    stats?: JobStatistics;
}

/**
 * @class
 * Initializes a new instance of the JobAddParameter class.
 * @constructor
 * An Azure Batch job to add.
 * @member {string} id Gets or sets a string that uniquely identifies the job
 * within the account. The id can contain any combination of alphanumeric
 * characters including hyphens and underscores, and cannot contain more than
 * 64 characters. It is common to use a GUID for the id.
 * 
 * @member {string} [displayName] Gets or sets the display name for the job.
 * 
 * @member {number} [priority] Gets or sets the priority of the job. Priority
 * values can range from -1000 to 1000, with -1000 being the lowest priority
 * and 1000 being the highest priority. The default value is 0.
 * 
 * @member {object} [constraints] Gets or sets the execution constraints for
 * the job.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the job may run, measured from the time the job
 * starts. If the job does not complete within the time limit, the Batch
 * service terminates it and any tasks that are still running.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times each task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {object} [jobManagerTask] Gets or sets details of a Job Manager
 * task to be launched when the job is started.
 * 
 * @member {string} [jobManagerTask.id] Gets or sets a string that uniquely
 * identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobManagerTask.displayName] Gets or sets the display name
 * of the Job Manager task.
 * 
 * @member {string} [jobManagerTask.commandLine] Gets or sets the command line
 * of the Job Manager task.
 * 
 * @member {array} [jobManagerTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [jobManagerTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobManagerTask.constraints] Gets or sets constraints that
 * apply to the Job Manager task.
 * 
 * @member {moment.duration} [jobManagerTask.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the task may run, measured from
 * the time the task starts. If the task does not complete within the time
 * limit, the Batch service terminates it.
 * 
 * @member {moment.duration} [jobManagerTask.constraints.retentionTime] Gets
 * or sets the minimum time to retain the working directory for the task on
 * the compute node where it ran. After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {number} [jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobManagerTask.killJobOnCompletion] Gets or sets whether
 * completion of the Job Manager task signifies completion of the entire job.
 * 
 * @member {boolean} [jobManagerTask.runElevated] Gets or sets whether to run
 * the Job Manager task in elevated mode. The default value is false.
 * 
 * @member {boolean} [jobManagerTask.runExclusive] Gets or sets whether the
 * Job Manager task requires exclusive use of the compute node where it runs.
 * 
 * @member {object} [jobPreparationTask] Gets or sets the Job Preparation task.
 * 
 * @member {string} [jobPreparationTask.id] Gets or sets a string that
 * uniquely identifies the job preparation task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobPreparationTask.commandLine] Gets or sets the command
 * line of the Job Preparation task.
 * 
 * @member {array} [jobPreparationTask.resourceFiles] Gets or sets a list of
 * files that Batch will download to the compute node before running the
 * command line.
 * 
 * @member {array} [jobPreparationTask.environmentSettings] Gets or sets a
 * list of environment variable settings for the Job Preparation task.
 * 
 * @member {object} [jobPreparationTask.constraints] Gets or sets constraints
 * that apply to the Job Preparation task..
 * 
 * @member {moment.duration} [jobPreparationTask.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the task may run, measured from
 * the time the task starts. If the task does not complete within the time
 * limit, the Batch service terminates it.
 * 
 * @member {moment.duration} [jobPreparationTask.constraints.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the task
 * on the compute node where it ran. After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {number} [jobPreparationTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobPreparationTask.waitForSuccess] Gets or sets whether
 * the Batch Service should wait for the Job Preparation task to complete
 * successfully before scheduling any other tasks of the job on the compute
 * node.
 * 
 * @member {boolean} [jobPreparationTask.runElevated] Gets or sets whether to
 * run the Job Preparation task in elevated mode. The default value is false.
 * 
 * @member {boolean} [jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobReleaseTask] Gets or sets the Job Release task.
 * 
 * @member {string} [jobReleaseTask.id] Gets or sets a string that uniquely
 * identifies the Job Release task within the job. The id can contain any
 * combination of alphanumeric characters including hyphens and underscores
 * and cannot contain more than 64 characters.
 * 
 * @member {string} [jobReleaseTask.commandLine] Gets or sets the command line
 * of the Job Release task.
 * 
 * @member {array} [jobReleaseTask.resourceFiles] Gets or sets a list of files
 * that Batch will download to the compute node before running the command
 * line.
 * 
 * @member {array} [jobReleaseTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the Job Release task.
 * 
 * @member {moment.duration} [jobReleaseTask.maxWallClockTime] Gets or sets
 * the maximum elapsed time that the Job Release task may run on a given
 * compute node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobReleaseTask.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the Job Release task on
 * the compute node.  After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobReleaseTask.runElevated] Gets or sets whether to run
 * the Job Release task in elevated mode. The default value is false.
 * 
 * @member {array} [commonEnvironmentSettings] Gets or sets the list of common
 * environment variable settings.  These environment variables are set for
 * all tasks in the job (including the Job Manager, Job Preparation and Job
 * Release tasks).
 * 
 * @member {object} poolInfo Gets or sets the pool on which the Batch service
 * runs the jobâ€™s tasks.
 * 
 * @member {string} [poolInfo.poolId] Gets or sets the id of an existing pool.
 * All the tasks of the job will run on the specified pool. You must specify
 * either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [poolInfo.autoPoolSpecification] Gets or sets
 * characteristics for a temporary 'auto pool.' The Batch service will create
 * this auto pool and run all the tasks of the job on it, and will delete the
 * pool once the job has completed. You must specify either PoolId or
 * AutoPoolSpecification, but not both.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool] Gets or sets the
 * pool specification for the auto pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.targetOSVersion] Gets
 * or sets the Azure Guest OS version to be installed on the virtual machines
 * in the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.maxTasksPerNode] Gets
 * or sets the maximum number of tasks that can run concurrently on a single
 * compute node in the pool.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets or sets the
 * timeout for allocation of compute nodes to the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.targetDedicated] Gets
 * or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval] Gets or
 * sets a time interval for the desired AutoScale evaluation period in the
 * pool.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication] Gets or
 * sets whether the pool permits direct communication between nodes.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.startTask.commandLine] Gets or sets
 * the command line of the start task.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.resourceFiles] Gets or sets
 * a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.runElevated] Gets or sets
 * whether to run the start task in elevated mode. The default value is false.
 * 
 * @member {number}
 * [poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess] Gets or
 * sets whether the Batch Service should wait for the start task to complete
 * successfully (that is, to exit with exit code 0) before scheduling any
 * tasks on the compute node.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.applicationPackageReferences] Gets or
 * sets the list of application packages to be installed on each compute node
 * in the pool.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.metadata] Gets or sets
 * a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the job as metadata.
 * 
 * @member {boolean} [usesTaskDependencies] Gets or sets the flag that
 * determines if this job will use tasks with dependencies.
 * 
 */
export interface JobAddParameter {
    id: string;
    displayName?: string;
    priority?: number;
    constraints?: JobConstraints;
    jobManagerTask?: JobManagerTask;
    jobPreparationTask?: JobPreparationTask;
    jobReleaseTask?: JobReleaseTask;
    commonEnvironmentSettings?: EnvironmentSetting[];
    poolInfo: PoolInformation;
    metadata?: MetadataItem[];
    usesTaskDependencies?: boolean;
}

/**
 * @class
 * Initializes a new instance of the TaskSchedulingError class.
 * @constructor
 * Information about an error when scheduling a task.
 * @member {string} category Gets or sets the category of the task scheduling
 * error. Possible values include: 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [code] Gets or sets an identifier for the task scheduling
 * error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [message] Gets or sets a message describing the task
 * scheduling error, intended to be suitable for display in a user interface.
 * 
 * @member {array} [details] Gets or sets the list of additional error details
 * related to the scheduling error.
 * 
 */
export interface TaskSchedulingError {
    category: string;
    code?: string;
    message?: string;
    details?: NameValuePair[];
}

/**
 * @class
 * Initializes a new instance of the JobPreparationTaskExecutionInformation class.
 * @constructor
 * Contains information about the execution of a Job Preparation task on a
 * compute node.
 * @member {date} startTime Gets or sets the time at which the Job Preparation
 * task started running.
 * 
 * @member {date} [endTime] Gets or sets the time at which the Job Preparation
 * task completed. This property is set only if the task is in the Completed
 * state.
 * 
 * @member {string} state Gets or sets the current running state of the Job
 * Preparation task on the compute node. Possible values include: 'running',
 * 'completed'
 * 
 * @member {string} [taskRootDirectory] Gets or sets the root directory of the
 * Job Preparation task on the compute node.
 * 
 * @member {string} [taskRootDirectoryUrl] Gets or sets the URL to the root
 * directory of the Job Preparation task on the compute node.
 * 
 * @member {number} [exitCode] Gets or sets the exit code of the Job
 * Preparation task. This property is set only if the task is in the
 * Completed state.
 * 
 * @member {object} [schedulingError] Gets or sets any error starting the Job
 * Preparation task.
 * 
 * @member {string} [schedulingError.category] Gets or sets the category of
 * the task scheduling error. Possible values include: 'usererror',
 * 'servererror', 'unmapped'
 * 
 * @member {string} [schedulingError.code] Gets or sets an identifier for the
 * task scheduling error.  Codes are invariant and are intended to be
 * consumed programmatically.
 * 
 * @member {string} [schedulingError.message] Gets or sets a message
 * describing the task scheduling error, intended to be suitable for display
 * in a user interface.
 * 
 * @member {array} [schedulingError.details] Gets or sets the list of
 * additional error details related to the scheduling error.
 * 
 * @member {number} retryCount Gets or sets the number of times the Job
 * Preparation task has been retried by the Batch service.
 * 
 * @member {date} [lastRetryTime] Gets or sets the most recent time at which a
 * retry of the Job Preparation task started running. This property is set
 * only if the task was retried (i.e. retryCount is nonzero).
 * 
 */
export interface JobPreparationTaskExecutionInformation {
    startTime: Date;
    endTime?: Date;
    state: string;
    taskRootDirectory?: string;
    taskRootDirectoryUrl?: string;
    exitCode?: number;
    schedulingError?: TaskSchedulingError;
    retryCount: number;
    lastRetryTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobReleaseTaskExecutionInformation class.
 * @constructor
 * Contains information about the execution of a Job Release task on a compute
 * node.
 * @member {date} startTime Gets or sets the time at which the Job Release
 * task started running.
 * 
 * @member {date} [endTime] Gets or sets the time at which the Job Release
 * task completed. This property is set only if the task is in the Completed
 * state.
 * 
 * @member {string} state Gets or sets the current running state of the Job
 * Release task on the compute node. Possible values include: 'running',
 * 'completed'
 * 
 * @member {string} [taskRootDirectory] Gets or sets the root directory of the
 * Job Release task on the compute node.
 * 
 * @member {string} [taskRootDirectoryUrl] Gets or sets the URL to the root
 * directory of the Job Release task on the compute node.
 * 
 * @member {number} [exitCode] Gets or sets the exit code of the Job Release
 * task. This property is set only if the task is in the Completed state.
 * 
 * @member {object} [schedulingError] Gets or sets any error starting the Job
 * Release task.
 * 
 * @member {string} [schedulingError.category] Gets or sets the category of
 * the task scheduling error. Possible values include: 'usererror',
 * 'servererror', 'unmapped'
 * 
 * @member {string} [schedulingError.code] Gets or sets an identifier for the
 * task scheduling error.  Codes are invariant and are intended to be
 * consumed programmatically.
 * 
 * @member {string} [schedulingError.message] Gets or sets a message
 * describing the task scheduling error, intended to be suitable for display
 * in a user interface.
 * 
 * @member {array} [schedulingError.details] Gets or sets the list of
 * additional error details related to the scheduling error.
 * 
 */
export interface JobReleaseTaskExecutionInformation {
    startTime: Date;
    endTime?: Date;
    state: string;
    taskRootDirectory?: string;
    taskRootDirectoryUrl?: string;
    exitCode?: number;
    schedulingError?: TaskSchedulingError;
}

/**
 * @class
 * Initializes a new instance of the JobPreparationAndReleaseTaskExecutionInformation class.
 * @constructor
 * The status of the Job Preparation and Job Release tasks on a particular
 * compute node.
 * @member {string} [poolId] Gets or sets the id of the pool containing the
 * compute node to which this entry refers.
 * 
 * @member {string} [nodeId] Gets or sets the id of the compute node to which
 * this entry refers.
 * 
 * @member {string} [nodeUrl] Gets or sets the URL of the compute node to
 * which this entry refers.
 * 
 * @member {object} [jobPreparationTaskExecutionInfo] Gets or sets information
 * about the execution status of the Job Preparation task on this compute
 * node.
 * 
 * @member {date} [jobPreparationTaskExecutionInfo.startTime] Gets or sets the
 * time at which the Job Preparation task started running.
 * 
 * @member {date} [jobPreparationTaskExecutionInfo.endTime] Gets or sets the
 * time at which the Job Preparation task completed. This property is set
 * only if the task is in the Completed state.
 * 
 * @member {string} [jobPreparationTaskExecutionInfo.state] Gets or sets the
 * current running state of the Job Preparation task on the compute node.
 * Possible values include: 'running', 'completed'
 * 
 * @member {string} [jobPreparationTaskExecutionInfo.taskRootDirectory] Gets
 * or sets the root directory of the Job Preparation task on the compute node.
 * 
 * @member {string} [jobPreparationTaskExecutionInfo.taskRootDirectoryUrl]
 * Gets or sets the URL to the root directory of the Job Preparation task on
 * the compute node.
 * 
 * @member {number} [jobPreparationTaskExecutionInfo.exitCode] Gets or sets
 * the exit code of the Job Preparation task. This property is set only if
 * the task is in the Completed state.
 * 
 * @member {object} [jobPreparationTaskExecutionInfo.schedulingError] Gets or
 * sets any error starting the Job Preparation task.
 * 
 * @member {string} [jobPreparationTaskExecutionInfo.schedulingError.category]
 * Gets or sets the category of the task scheduling error. Possible values
 * include: 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [jobPreparationTaskExecutionInfo.schedulingError.code]
 * Gets or sets an identifier for the task scheduling error.  Codes are
 * invariant and are intended to be consumed programmatically.
 * 
 * @member {string} [jobPreparationTaskExecutionInfo.schedulingError.message]
 * Gets or sets a message describing the task scheduling error, intended to
 * be suitable for display in a user interface.
 * 
 * @member {array} [jobPreparationTaskExecutionInfo.schedulingError.details]
 * Gets or sets the list of additional error details related to the
 * scheduling error.
 * 
 * @member {number} [jobPreparationTaskExecutionInfo.retryCount] Gets or sets
 * the number of times the Job Preparation task has been retried by the Batch
 * service.
 * 
 * @member {date} [jobPreparationTaskExecutionInfo.lastRetryTime] Gets or sets
 * the most recent time at which a retry of the Job Preparation task started
 * running. This property is set only if the task was retried (i.e.
 * retryCount is nonzero).
 * 
 * @member {object} [jobReleaseTaskExecutionInfo] Gets or sets information
 * about the execution status of the Job Release task on this compute node.
 * This property is set only if the Job Release task has run on the node.
 * 
 * @member {date} [jobReleaseTaskExecutionInfo.startTime] Gets or sets the
 * time at which the Job Release task started running.
 * 
 * @member {date} [jobReleaseTaskExecutionInfo.endTime] Gets or sets the time
 * at which the Job Release task completed. This property is set only if the
 * task is in the Completed state.
 * 
 * @member {string} [jobReleaseTaskExecutionInfo.state] Gets or sets the
 * current running state of the Job Release task on the compute node.
 * Possible values include: 'running', 'completed'
 * 
 * @member {string} [jobReleaseTaskExecutionInfo.taskRootDirectory] Gets or
 * sets the root directory of the Job Release task on the compute node.
 * 
 * @member {string} [jobReleaseTaskExecutionInfo.taskRootDirectoryUrl] Gets or
 * sets the URL to the root directory of the Job Release task on the compute
 * node.
 * 
 * @member {number} [jobReleaseTaskExecutionInfo.exitCode] Gets or sets the
 * exit code of the Job Release task. This property is set only if the task
 * is in the Completed state.
 * 
 * @member {object} [jobReleaseTaskExecutionInfo.schedulingError] Gets or sets
 * any error starting the Job Release task.
 * 
 * @member {string} [jobReleaseTaskExecutionInfo.schedulingError.category]
 * Gets or sets the category of the task scheduling error. Possible values
 * include: 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [jobReleaseTaskExecutionInfo.schedulingError.code] Gets or
 * sets an identifier for the task scheduling error.  Codes are invariant and
 * are intended to be consumed programmatically.
 * 
 * @member {string} [jobReleaseTaskExecutionInfo.schedulingError.message] Gets
 * or sets a message describing the task scheduling error, intended to be
 * suitable for display in a user interface.
 * 
 * @member {array} [jobReleaseTaskExecutionInfo.schedulingError.details] Gets
 * or sets the list of additional error details related to the scheduling
 * error.
 * 
 */
export interface JobPreparationAndReleaseTaskExecutionInformation {
    poolId?: string;
    nodeId?: string;
    nodeUrl?: string;
    jobPreparationTaskExecutionInfo?: JobPreparationTaskExecutionInformation;
    jobReleaseTaskExecutionInfo?: JobReleaseTaskExecutionInformation;
}

/**
 * @class
 * Initializes a new instance of the AutoScaleRunError class.
 * @constructor
 * An error that occurred when executing or evaluating a pool autoscale
 * formula.
 * @member {string} [code] Gets or sets an identifier for the autoscale error.
 * Codes are invariant and are intended to be consumed programmatically.
 * 
 * @member {string} [message] Gets or sets a message describing the autoscale
 * error, intended to be suitable for display in a user interface.
 * 
 * @member {array} [values] Gets or sets a list of additional error details
 * related to the autoscale error.
 * 
 */
export interface AutoScaleRunError {
    code?: string;
    message?: string;
    values?: NameValuePair[];
}

/**
 * @class
 * Initializes a new instance of the AutoScaleRun class.
 * @constructor
 * The results and errors from an execution of a pool autoscale formula.
 * @member {date} timestamp Gets or sets the time at which the autoscale
 * formula was last evaluated.
 * 
 * @member {string} [results] Gets or sets the final values of all variables
 * used in the evaluation of the autoscale formula.  Each variable value is
 * returned in the form $variable=value, and variables are separated by
 * semicolons.
 * 
 * @member {object} [error] Gets or sets details of the error encountered
 * evaluating the autoscale formula on the pool, if the evaluation was
 * unsuccessful.
 * 
 * @member {string} [error.code] Gets or sets an identifier for the autoscale
 * error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [error.message] Gets or sets a message describing the
 * autoscale error, intended to be suitable for display in a user interface.
 * 
 * @member {array} [error.values] Gets or sets a list of additional error
 * details related to the autoscale error.
 * 
 */
export interface AutoScaleRun {
    timestamp: Date;
    results?: string;
    error?: AutoScaleRunError;
}

/**
 * @class
 * Initializes a new instance of the ResizeError class.
 * @constructor
 * An error that occurred when resizing a pool.
 * @member {string} [code] Gets or sets an identifier for the pool resize
 * error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [message] Gets or sets a message describing the pool
 * resize error, intended to be suitable for display in a user interface.
 * 
 * @member {array} [values] Gets or sets a list of additional error details
 * related to the pool resize error.
 * 
 */
export interface ResizeError {
    code?: string;
    message?: string;
    values?: NameValuePair[];
}

/**
 * @class
 * Initializes a new instance of the CloudPool class.
 * @constructor
 * A pool in the Azure Batch service.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * pool within the account. The id can contain any combination of
 * alphanumeric characters including hyphens and underscores, and cannot
 * contain more than 64 characters.
 * 
 * @member {string} [displayName] Gets or sets the display name for the pool.
 * 
 * @member {string} [url] Gets or sets the URL of the pool.
 * 
 * @member {string} [eTag] Gets or sets the ETag of the pool.
 * 
 * @member {date} [lastModified] Gets or sets the last modified time of the
 * pool.
 * 
 * @member {date} [creationTime] Gets or sets the creation time of the pool.
 * 
 * @member {string} [state] Gets or sets the current state of the pool.
 * Possible values include: 'active', 'deleting', 'upgrading'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the
 * pool entered its current state.
 * 
 * @member {string} [allocationState] Gets or sets whether the pool is
 * resizing. Possible values include: 'steady', 'resizing', 'stopping'
 * 
 * @member {date} [allocationStateTransitionTime] Gets or sets the time at
 * which the pool entered its current allocation state.
 * 
 * @member {string} [vmSize] Gets or sets the size of virtual machines in the
 * pool.  All VMs in a pool are the same size.
 * 
 * @member {string} [osFamily] Gets or sets the Azure Guest OS family to be
 * installed on the virtual machines in the pool.
 * 
 * @member {string} [targetOSVersion] Gets or sets the Azure Guest OS version
 * to be installed on the virtual machines in the pool. The default value is
 * * which specifies the latest operating system version for the specified
 * family.
 * 
 * @member {string} [currentOSVersion] Gets or sets the Azure Guest OS Version
 * currently installed on the virtual machines in the pool. This may differ
 * from TargetOSVersion if the pool state is Upgrading.
 * 
 * @member {moment.duration} [resizeTimeout] Gets or sets the timeout for
 * allocation of compute nodes to the pool. In a Get Pool operation, this is
 * the timeout for the most recent resize operation. The default value is 10
 * minutes.
 * 
 * @member {object} [resizeError] Gets or sets details of any error
 * encountered while performing the last resize on the pool. This property is
 * set only if an error occurred during the last pool resize, and only when
 * the pool AllocationState is Steady.
 * 
 * @member {string} [resizeError.code] Gets or sets an identifier for the pool
 * resize error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [resizeError.message] Gets or sets a message describing
 * the pool resize error, intended to be suitable for display in a user
 * interface.
 * 
 * @member {array} [resizeError.values] Gets or sets a list of additional
 * error details related to the pool resize error.
 * 
 * @member {number} [currentDedicated] Gets or sets the number of compute
 * nodes currently in the pool.
 * 
 * @member {number} [targetDedicated] Gets or sets the desired number of
 * compute nodes in the pool. This property must have the default value if
 * EnableAutoScale is true. It is required if EnableAutoScale is false.
 * 
 * @member {boolean} [enableAutoScale] Gets or sets whether the pool size
 * should automatically adjust over time. If true, the AutoScaleFormula
 * property must be set. If false, the TargetDedicated property must be set.
 * 
 * @member {string} [autoScaleFormula] Gets or sets a formula for the desired
 * number of compute nodes in the pool.
 * 
 * @member {moment.duration} [autoScaleEvaluationInterval] Gets or sets a time
 * interval for the desired AutoScale evaluation period in the pool.
 * 
 * @member {object} [autoScaleRun] Gets or sets the results and errors from
 * the last execution of the autoscale formula.
 * 
 * @member {date} [autoScaleRun.timestamp] Gets or sets the time at which the
 * autoscale formula was last evaluated.
 * 
 * @member {string} [autoScaleRun.results] Gets or sets the final values of
 * all variables used in the evaluation of the autoscale formula.  Each
 * variable value is returned in the form $variable=value, and variables are
 * separated by semicolons.
 * 
 * @member {object} [autoScaleRun.error] Gets or sets details of the error
 * encountered evaluating the autoscale formula on the pool, if the
 * evaluation was unsuccessful.
 * 
 * @member {string} [autoScaleRun.error.code] Gets or sets an identifier for
 * the autoscale error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [autoScaleRun.error.message] Gets or sets a message
 * describing the autoscale error, intended to be suitable for display in a
 * user interface.
 * 
 * @member {array} [autoScaleRun.error.values] Gets or sets a list of
 * additional error details related to the autoscale error.
 * 
 * @member {boolean} [enableInterNodeCommunication] Gets or sets whether the
 * pool permits direct communication between nodes.
 * 
 * @member {object} [startTask] Gets or sets a task specified to run on each
 * compute node as it joins the pool.
 * 
 * @member {string} [startTask.commandLine] Gets or sets the command line of
 * the start task.
 * 
 * @member {array} [startTask.resourceFiles] Gets or sets a list of files that
 * Batch will download to the compute node before running the command line.
 * 
 * @member {array} [startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [startTask.runElevated] Gets or sets whether to run the
 * start task in elevated mode. The default value is false.
 * 
 * @member {number} [startTask.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried.
 * 
 * @member {boolean} [startTask.waitForSuccess] Gets or sets whether the Batch
 * Service should wait for the start task to complete successfully (that is,
 * to exit with exit code 0) before scheduling any tasks on the compute node.
 * 
 * @member {array} [certificateReferences] Gets or sets the list of
 * certificates to be installed on each compute node in the pool.
 * 
 * @member {array} [applicationPackageReferences] Gets or sets the list of
 * application packages to be installed on each compute node in the pool.
 * 
 * @member {number} [maxTasksPerNode] Gets or sets the maximum number of tasks
 * that can run concurrently on a single compute node in the pool.
 * 
 * @member {object} [taskSchedulingPolicy] Gets or sets how the Batch service
 * distributes tasks between compute nodes in the pool.
 * 
 * @member {string} [taskSchedulingPolicy.nodeFillType] Gets or sets how tasks
 * should be distributed across compute nodes. Possible values include:
 * 'spread', 'pack', 'unmapped'
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the pool as metadata.
 * 
 * @member {object} [stats] Gets or sets utilization and resource usage
 * statistics for the entire lifetime of the pool.
 * 
 * @member {string} [stats.url] Gets or sets the URL for the statistics.
 * 
 * @member {date} [stats.startTime] Gets or sets the start time of the time
 * range covered by the statistics.
 * 
 * @member {date} [stats.lastUpdateTime] Gets or sets the time at which the
 * statistics were last updated. All statistics are limited to the range
 * between startTime and lastUpdateTime.
 * 
 * @member {object} [stats.usageStats] Gets or sets statistics related to pool
 * usage information, such as the amount of core-time used.
 * 
 * @member {date} [stats.usageStats.startTime] Gets or sets the start time of
 * the time range covered by the statistics.
 * 
 * @member {date} [stats.usageStats.lastUpdateTime] Gets or sets the time at
 * which the statistics were last updated. All statistics are limited to the
 * range between startTime and lastUpdateTime.
 * 
 * @member {moment.duration} [stats.usageStats.dedicatedCoreTime] Gets or sets
 * the aggregated wall-clock time of the dedicated compute node cores being
 * part of the pool.
 * 
 * @member {object} [stats.resourceStats] Gets or sets statistics related to
 * resource consumption by compute nodes in the pool.
 * 
 * @member {date} [stats.resourceStats.startTime] Gets or sets the start time
 * of the time range covered by the statistics.
 * 
 * @member {date} [stats.resourceStats.lastUpdateTime] Gets or sets the time
 * at which the statistics were last updated. All statistics are limited to
 * the range between startTime and lastUpdateTime.
 * 
 * @member {number} [stats.resourceStats.avgCPUPercentage] Gets or sets the
 * average CPU usage across all nodes in the pool (percentage per node).
 * 
 * @member {number} [stats.resourceStats.avgMemoryGiB] Gets or sets the
 * average memory usage in GiB across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.peakMemoryGiB] Gets or sets the peak
 * memory usage in GiB across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.avgDiskGiB] Gets or sets the average
 * used disk space in GiB across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.peakDiskGiB] Gets or sets the peak
 * used disk space in GiB across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.diskReadIOps] Gets or sets the total
 * number of disk read operations across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.diskWriteIOps] Gets or sets the total
 * number of disk write operations across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.diskReadGiB] Gets or sets the total
 * amount of data in GiB of disk reads across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.diskWriteGiB] Gets or sets the total
 * amount of data in GiB of disk writes across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.networkReadGiB] Gets or sets the
 * total amount of data in GiB of network reads across all nodes in the pool.
 * 
 * @member {number} [stats.resourceStats.networkWriteGiB] Gets or sets the
 * total amount of data in GiB of network writes across all nodes in the pool.
 * 
 */
export interface CloudPool {
    id?: string;
    displayName?: string;
    url?: string;
    eTag?: string;
    lastModified?: Date;
    creationTime?: Date;
    state?: string;
    stateTransitionTime?: Date;
    allocationState?: string;
    allocationStateTransitionTime?: Date;
    vmSize?: string;
    osFamily?: string;
    targetOSVersion?: string;
    currentOSVersion?: string;
    resizeTimeout?: moment.Duration;
    resizeError?: ResizeError;
    currentDedicated?: number;
    targetDedicated?: number;
    enableAutoScale?: boolean;
    autoScaleFormula?: string;
    autoScaleEvaluationInterval?: moment.Duration;
    autoScaleRun?: AutoScaleRun;
    enableInterNodeCommunication?: boolean;
    startTask?: StartTask;
    certificateReferences?: CertificateReference[];
    applicationPackageReferences?: ApplicationPackageReference[];
    maxTasksPerNode?: number;
    taskSchedulingPolicy?: TaskSchedulingPolicy;
    metadata?: MetadataItem[];
    stats?: PoolStatistics;
}

/**
 * @class
 * Initializes a new instance of the PoolAddParameter class.
 * @constructor
 * A pool in the Azure Batch service to add.
 * @member {string} id Gets or sets a string that uniquely identifies the pool
 * within the account. The id can contain any combination of alphanumeric
 * characters including hyphens and underscores, and cannot contain more than
 * 64 characters.
 * 
 * @member {string} [displayName] Gets or sets the display name for the pool.
 * 
 * @member {string} vmSize Gets or sets the size of virtual machines in the
 * pool.  All VMs in a pool are the same size.
 * 
 * @member {string} osFamily Gets or sets the Azure Guest OS family to be
 * installed on the virtual machines in the pool.
 * 
 * @member {string} [targetOSVersion] Gets or sets the Azure Guest OS version
 * to be installed on the virtual machines in the pool. The default value is
 * * which specifies the latest operating system version for the specified
 * family.
 * 
 * @member {moment.duration} [resizeTimeout] Gets or sets the timeout for
 * allocation of compute nodes to the pool. In a Get Pool operation, this is
 * the timeout for the most recent resize operation. The default value is 10
 * minutes.
 * 
 * @member {number} [targetDedicated] Gets or sets the desired number of
 * compute nodes in the pool. This property must have the default value if
 * EnableAutoScale is true. It is required if EnableAutoScale is false.
 * 
 * @member {boolean} [enableAutoScale] Gets or sets whether the pool size
 * should automatically adjust over time. If true, the AutoScaleFormula
 * property must be set. If false, the TargetDedicated property must be set.
 * 
 * @member {string} [autoScaleFormula] Gets or sets a formula for the desired
 * number of compute nodes in the pool.
 * 
 * @member {moment.duration} [autoScaleEvaluationInterval] Gets or sets a time
 * interval for the desired autoscale evaluation period in the pool.
 * 
 * @member {boolean} [enableInterNodeCommunication] Gets or sets whether the
 * pool permits direct communication between nodes.
 * 
 * @member {object} [startTask] Gets or sets a task specified to run on each
 * compute node as it joins the pool.
 * 
 * @member {string} [startTask.commandLine] Gets or sets the command line of
 * the start task.
 * 
 * @member {array} [startTask.resourceFiles] Gets or sets a list of files that
 * Batch will download to the compute node before running the command line.
 * 
 * @member {array} [startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [startTask.runElevated] Gets or sets whether to run the
 * start task in elevated mode. The default value is false.
 * 
 * @member {number} [startTask.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried.
 * 
 * @member {boolean} [startTask.waitForSuccess] Gets or sets whether the Batch
 * Service should wait for the start task to complete successfully (that is,
 * to exit with exit code 0) before scheduling any tasks on the compute node.
 * 
 * @member {array} [certificateReferences] Gets or sets the list of
 * certificates to be installed on each compute node in the pool.
 * 
 * @member {array} [applicationPackageReferences] Gets or sets the list of
 * application packages to be installed on each compute node in the pool.
 * 
 * @member {number} [maxTasksPerNode] Gets or sets the maximum number of tasks
 * that can run concurrently on a single compute node in the pool.
 * 
 * @member {object} [taskSchedulingPolicy] Gets or sets how the Batch service
 * distributes tasks between compute nodes in the pool.
 * 
 * @member {string} [taskSchedulingPolicy.nodeFillType] Gets or sets how tasks
 * should be distributed across compute nodes. Possible values include:
 * 'spread', 'pack', 'unmapped'
 * 
 * @member {array} [metadata] Gets or sets a list of name-value pairs
 * associated with the pool as metadata.
 * 
 */
export interface PoolAddParameter {
    id: string;
    displayName?: string;
    vmSize: string;
    osFamily: string;
    targetOSVersion?: string;
    resizeTimeout?: moment.Duration;
    targetDedicated?: number;
    enableAutoScale?: boolean;
    autoScaleFormula?: string;
    autoScaleEvaluationInterval?: moment.Duration;
    enableInterNodeCommunication?: boolean;
    startTask?: StartTask;
    certificateReferences?: CertificateReference[];
    applicationPackageReferences?: ApplicationPackageReference[];
    maxTasksPerNode?: number;
    taskSchedulingPolicy?: TaskSchedulingPolicy;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the AffinityInformation class.
 * @constructor
 * A locality hint that can be used by the Batch service to select a compute
 * node on which to start a task.
 * @member {string} [affinityId] Gets or sets an opaque string representing
 * the location of a compute node or a task that has run previously.  You can
 * pass the AffinityId of a compute node or task to indicate that this task
 * needs to be placed close to the node or task.
 * 
 */
export interface AffinityInformation {
    affinityId?: string;
}

/**
 * @class
 * Initializes a new instance of the TaskExecutionInformation class.
 * @constructor
 * Information about the execution of a task.
 * @member {date} [startTime] Gets or sets the time at which the task started
 * running. If the task has been restarted or retried, this is the most
 * recent time at which the task started running.
 * 
 * @member {date} [endTime] Gets or sets the time at which the task completed.
 * This property is set only if the task is in the Completed state.
 * 
 * @member {number} [exitCode] Gets or sets the exit code of the task. This
 * property is set only if the task is in completed state.
 * 
 * @member {object} [schedulingError] Gets or sets details of any error
 * encountered scheduling the task.
 * 
 * @member {string} [schedulingError.category] Gets or sets the category of
 * the task scheduling error. Possible values include: 'usererror',
 * 'servererror', 'unmapped'
 * 
 * @member {string} [schedulingError.code] Gets or sets an identifier for the
 * task scheduling error.  Codes are invariant and are intended to be
 * consumed programmatically.
 * 
 * @member {string} [schedulingError.message] Gets or sets a message
 * describing the task scheduling error, intended to be suitable for display
 * in a user interface.
 * 
 * @member {array} [schedulingError.details] Gets or sets the list of
 * additional error details related to the scheduling error.
 * 
 * @member {number} retryCount Gets or sets the number of times the task has
 * been retried by the Batch service.
 * 
 * @member {date} [lastRetryTime] Gets or sets the most recent time at which a
 * retry of the task started running.
 * 
 * @member {number} requeueCount Gets or sets the number of times the task has
 * been requeued by the Batch service as the result of a user request.
 * 
 * @member {date} [lastRequeueTime] Gets or sets the most recent time at which
 * the task has been requeued by the Batch service as the result of a user
 * request.
 * 
 */
export interface TaskExecutionInformation {
    startTime?: Date;
    endTime?: Date;
    exitCode?: number;
    schedulingError?: TaskSchedulingError;
    retryCount: number;
    lastRetryTime?: Date;
    requeueCount: number;
    lastRequeueTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeInformation class.
 * @constructor
 * Information about the compute node on which a task ran.
 * @member {string} [affinityId] Gets or sets an identifier for the compute
 * node on which the task ran, which can be passed when adding a task to
 * request that the task be scheduled close to this compute node.
 * 
 * @member {string} [nodeUrl] Gets or sets the URL of the node on which the
 * task ran.
 * 
 * @member {string} [poolId] Gets or sets the id of the pool on which the task
 * ran.
 * 
 * @member {string} [nodeId] Gets or sets the id of the node on which the task
 * ran.
 * 
 * @member {string} [taskRootDirectory] Gets or sets the root directory of the
 * task on the compute node.
 * 
 * @member {string} [taskRootDirectoryUrl] Gets or sets the URL to the root
 * directory of the task on the compute node.
 * 
 */
export interface ComputeNodeInformation {
    affinityId?: string;
    nodeUrl?: string;
    poolId?: string;
    nodeId?: string;
    taskRootDirectory?: string;
    taskRootDirectoryUrl?: string;
}

/**
 * @class
 * Initializes a new instance of the MultiInstanceSettings class.
 * @constructor
 * Information about the settings required for multi-instance task.
 * @member {number} numberOfInstances Gets or sets the number of compute node
 * instances used for multi-instance task.
 * 
 * @member {string} [coordinationCommandLine] Gets or sets the command to be
 * run on the compute node instances to setup coordination among the subtasks.
 * 
 * @member {array} [commonResourceFiles] Gets or sets a list of files that
 * Batch will download on all subtasks.
 * 
 */
export interface MultiInstanceSettings {
    numberOfInstances: number;
    coordinationCommandLine?: string;
    commonResourceFiles?: ResourceFile[];
}

/**
 * @class
 * Initializes a new instance of the TaskStatistics class.
 * @constructor
 * Resource usage statistics for a task.
 * @member {string} url Gets or sets the URL for the statistics.
 * 
 * @member {date} startTime Gets or sets the start time of the time range
 * covered by the statistics.
 * 
 * @member {date} lastUpdateTime Gets or sets the time at which the statistics
 * were last updated. All statistics are limited to the range between
 * startTime and lastUpdateTime.
 * 
 * @member {moment.duration} userCPUTime Gets or sets the total user mode CPU
 * time (summed across all cores and all compute nodes) consumed by the task.
 * 
 * @member {moment.duration} kernelCPUTime Gets or sets the total kernel mode
 * CPU time (summed across all cores and all compute nodes) consumed by the
 * task.
 * 
 * @member {moment.duration} wallClockTime Gets or sets the total wall clock
 * time of the task.
 * 
 * @member {number} readIOps Gets or sets the total number of I/O read
 * operations performed by the task.
 * 
 * @member {number} writeIOps Gets or sets the total number of I/O write
 * operations performed by the task.
 * 
 * @member {number} readIOGiB Gets or sets the total amount of data in GiB of
 * I/O read by the task.
 * 
 * @member {number} writeIOGiB Gets or sets the total amount of data in GiB of
 * I/O written by the task.
 * 
 * @member {moment.duration} waitTime Gets or sets the elapsed time between
 * the creation of the task and the start of task execution.
 * 
 */
export interface TaskStatistics {
    url: string;
    startTime: Date;
    lastUpdateTime: Date;
    userCPUTime: moment.Duration;
    kernelCPUTime: moment.Duration;
    wallClockTime: moment.Duration;
    readIOps: number;
    writeIOps: number;
    readIOGiB: number;
    writeIOGiB: number;
    waitTime: moment.Duration;
}

/**
 * @class
 * Initializes a new instance of the TaskDependencies class.
 * @constructor
 * Specifies any dependencies of a task.  Any task that is explicitly
 * specified or within a dependency range must complete before the dependant
 * task will be scheduled.
 * @member {array} [taskIds] Gets or sets the list of task ids that must
 * complete before this task can be scheduled.
 * 
 * @member {array} [taskIdRanges] Gets or sets the list of task ranges that
 * must complete before this task can be scheduled.
 * 
 */
export interface TaskDependencies {
    taskIds?: string[];
    taskIdRanges?: TaskIdRange[];
}

/**
 * @class
 * Initializes a new instance of the TaskIdRange class.
 * @constructor
 * @member {number} start Gets or sets the first task id in the range.
 * 
 * @member {number} end Gets or sets the last task id in the range.
 * 
 */
export interface TaskIdRange {
    start: number;
    end: number;
}

/**
 * @class
 * Initializes a new instance of the CloudTask class.
 * @constructor
 * An Azure Batch task.
 * @member {string} [id] Gets or sets a string that uniquely identifies the
 * task within the job. The id can contain any combination of alphanumeric
 * characters including hyphens and underscores, and cannot contain more than
 * 64 characters.
 * 
 * @member {string} [displayName] Gets or sets a display name for the task.
 * 
 * @member {string} [url] Gets or sets the URL of the task.
 * 
 * @member {string} [eTag] Gets or sets the ETag of the task.
 * 
 * @member {date} [lastModified] Gets or sets the last modified time of the
 * task.
 * 
 * @member {date} [creationTime] Gets or sets the creation time of the task.
 * 
 * @member {string} [state] Gets or sets the current state of the task.
 * Possible values include: 'active', 'preparing', 'running', 'completed'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the
 * task entered its current state.
 * 
 * @member {string} [previousState] Gets or sets the previous state of the
 * task. This property is not set if the task is in its initial Active state.
 * Possible values include: 'active', 'preparing', 'running', 'completed'
 * 
 * @member {date} [previousStateTransitionTime] Gets or sets the time at which
 * the task entered its previous state. This property is not set if the task
 * is in its initial Active state.
 * 
 * @member {string} [commandLine] Gets or sets the command line of the task.
 * For multi-instance tasks, the command line is executed on the primary
 * subtask after all the subtasks have finished executing the coordianation
 * command line.
 * 
 * @member {array} [resourceFiles] Gets or sets a list of files that Batch
 * will download to the compute node before running the command line. For
 * multi-instance tasks, the resource files will only be downloaded to the
 * compute node on which the primary subtask is executed.
 * 
 * @member {array} [environmentSettings] Gets or sets a list of environment
 * variable settings for the task.
 * 
 * @member {object} [affinityInfo] Gets or sets a locality hint that can be
 * used by the Batch service to select a compute node on which to start the
 * new task.
 * 
 * @member {string} [affinityInfo.affinityId] Gets or sets an opaque string
 * representing the location of a compute node or a task that has run
 * previously.  You can pass the AffinityId of a compute node or task to
 * indicate that this task needs to be placed close to the node or task.
 * 
 * @member {object} [constraints] Gets or sets the execution constraints that
 * apply to this task.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the task may run, measured from the time the
 * task starts. If the task does not complete within the time limit, the
 * Batch service terminates it.
 * 
 * @member {moment.duration} [constraints.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the task on the compute
 * node where it ran. After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {boolean} [runElevated] Gets or sets whether to run the task in
 * elevated mode.
 * 
 * @member {object} [executionInfo] Gets or sets information about the
 * execution of the task.
 * 
 * @member {date} [executionInfo.startTime] Gets or sets the time at which the
 * task started running. If the task has been restarted or retried, this is
 * the most recent time at which the task started running.
 * 
 * @member {date} [executionInfo.endTime] Gets or sets the time at which the
 * task completed. This property is set only if the task is in the Completed
 * state.
 * 
 * @member {number} [executionInfo.exitCode] Gets or sets the exit code of the
 * task. This property is set only if the task is in completed state.
 * 
 * @member {object} [executionInfo.schedulingError] Gets or sets details of
 * any error encountered scheduling the task.
 * 
 * @member {string} [executionInfo.schedulingError.category] Gets or sets the
 * category of the task scheduling error. Possible values include:
 * 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [executionInfo.schedulingError.code] Gets or sets an
 * identifier for the task scheduling error.  Codes are invariant and are
 * intended to be consumed programmatically.
 * 
 * @member {string} [executionInfo.schedulingError.message] Gets or sets a
 * message describing the task scheduling error, intended to be suitable for
 * display in a user interface.
 * 
 * @member {array} [executionInfo.schedulingError.details] Gets or sets the
 * list of additional error details related to the scheduling error.
 * 
 * @member {number} [executionInfo.retryCount] Gets or sets the number of
 * times the task has been retried by the Batch service.
 * 
 * @member {date} [executionInfo.lastRetryTime] Gets or sets the most recent
 * time at which a retry of the task started running.
 * 
 * @member {number} [executionInfo.requeueCount] Gets or sets the number of
 * times the task has been requeued by the Batch service as the result of a
 * user request.
 * 
 * @member {date} [executionInfo.lastRequeueTime] Gets or sets the most recent
 * time at which the task has been requeued by the Batch service as the
 * result of a user request.
 * 
 * @member {object} [nodeInfo] Gets or sets information about the compute node
 * on which the task ran.
 * 
 * @member {string} [nodeInfo.affinityId] Gets or sets an identifier for the
 * compute node on which the task ran, which can be passed when adding a task
 * to request that the task be scheduled close to this compute node.
 * 
 * @member {string} [nodeInfo.nodeUrl] Gets or sets the URL of the node on
 * which the task ran.
 * 
 * @member {string} [nodeInfo.poolId] Gets or sets the id of the pool on which
 * the task ran.
 * 
 * @member {string} [nodeInfo.nodeId] Gets or sets the id of the node on which
 * the task ran.
 * 
 * @member {string} [nodeInfo.taskRootDirectory] Gets or sets the root
 * directory of the task on the compute node.
 * 
 * @member {string} [nodeInfo.taskRootDirectoryUrl] Gets or sets the URL to
 * the root directory of the task on the compute node.
 * 
 * @member {object} [multiInstanceSettings] Gets or sets information about how
 * to run the multi-instance task.
 * 
 * @member {number} [multiInstanceSettings.numberOfInstances] Gets or sets the
 * number of compute node instances used for multi-instance task.
 * 
 * @member {string} [multiInstanceSettings.coordinationCommandLine] Gets or
 * sets the command to be run on the compute node instances to setup
 * coordination among the subtasks.
 * 
 * @member {array} [multiInstanceSettings.commonResourceFiles] Gets or sets a
 * list of files that Batch will download on all subtasks.
 * 
 * @member {object} [stats] Gets or sets resource usage statistics for the
 * task.
 * 
 * @member {string} [stats.url] Gets or sets the URL for the statistics.
 * 
 * @member {date} [stats.startTime] Gets or sets the start time of the time
 * range covered by the statistics.
 * 
 * @member {date} [stats.lastUpdateTime] Gets or sets the time at which the
 * statistics were last updated. All statistics are limited to the range
 * between startTime and lastUpdateTime.
 * 
 * @member {moment.duration} [stats.userCPUTime] Gets or sets the total user
 * mode CPU time (summed across all cores and all compute nodes) consumed by
 * the task.
 * 
 * @member {moment.duration} [stats.kernelCPUTime] Gets or sets the total
 * kernel mode CPU time (summed across all cores and all compute nodes)
 * consumed by the task.
 * 
 * @member {moment.duration} [stats.wallClockTime] Gets or sets the total wall
 * clock time of the task.
 * 
 * @member {number} [stats.readIOps] Gets or sets the total number of I/O read
 * operations performed by the task.
 * 
 * @member {number} [stats.writeIOps] Gets or sets the total number of I/O
 * write operations performed by the task.
 * 
 * @member {number} [stats.readIOGiB] Gets or sets the total amount of data in
 * GiB of I/O read by the task.
 * 
 * @member {number} [stats.writeIOGiB] Gets or sets the total amount of data
 * in GiB of I/O written by the task.
 * 
 * @member {moment.duration} [stats.waitTime] Gets or sets the elapsed time
 * between the creation of the task and the start of task execution.
 * 
 * @member {object} [dependsOn] Gets or sets any dependencies this task has.
 * 
 * @member {array} [dependsOn.taskIds] Gets or sets the list of task ids that
 * must complete before this task can be scheduled.
 * 
 * @member {array} [dependsOn.taskIdRanges] Gets or sets the list of task
 * ranges that must complete before this task can be scheduled.
 * 
 */
export interface CloudTask {
    id?: string;
    displayName?: string;
    url?: string;
    eTag?: string;
    lastModified?: Date;
    creationTime?: Date;
    state?: string;
    stateTransitionTime?: Date;
    previousState?: string;
    previousStateTransitionTime?: Date;
    commandLine?: string;
    resourceFiles?: ResourceFile[];
    environmentSettings?: EnvironmentSetting[];
    affinityInfo?: AffinityInformation;
    constraints?: TaskConstraints;
    runElevated?: boolean;
    executionInfo?: TaskExecutionInformation;
    nodeInfo?: ComputeNodeInformation;
    multiInstanceSettings?: MultiInstanceSettings;
    stats?: TaskStatistics;
    dependsOn?: TaskDependencies;
}

/**
 * @class
 * Initializes a new instance of the TaskAddParameter class.
 * @constructor
 * An Azure Batch task to add.
 * @member {string} id Gets or sets a string that uniquely identifies the task
 * within the job. The id can contain any combination of alphanumeric
 * characters including hyphens and underscores, and cannot contain more than
 * 64 characters.
 * 
 * @member {string} [displayName] Gets or sets a display name for the task.
 * 
 * @member {string} commandLine Gets or sets the command line of the task. For
 * multi-instance tasks, the command line is executed on the primary subtask
 * after all the subtasks have finished executing the coordianation command
 * line.
 * 
 * @member {array} [resourceFiles] Gets or sets a list of files that Batch
 * will download to the compute node before running the command line. For
 * multi-instance tasks, the resource files will only be downloaded to the
 * compute node on which the primary subtask is executed.
 * 
 * @member {array} [environmentSettings] Gets or sets a list of environment
 * variable settings for the task.
 * 
 * @member {object} [affinityInfo] Gets or sets a locality hint that can be
 * used by the Batch service to select a compute node on which to start the
 * new task.
 * 
 * @member {string} [affinityInfo.affinityId] Gets or sets an opaque string
 * representing the location of a compute node or a task that has run
 * previously.  You can pass the AffinityId of a compute node or task to
 * indicate that this task needs to be placed close to the node or task.
 * 
 * @member {object} [constraints] Gets or sets the execution constraints that
 * apply to this task.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the task may run, measured from the time the
 * task starts. If the task does not complete within the time limit, the
 * Batch service terminates it.
 * 
 * @member {moment.duration} [constraints.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the task on the compute
 * node where it ran. After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {boolean} [runElevated] Gets or sets whether to run the task in
 * elevated mode.
 * 
 * @member {object} [multiInstanceSettings] Gets or sets information about how
 * to run the multi-instance task.
 * 
 * @member {number} [multiInstanceSettings.numberOfInstances] Gets or sets the
 * number of compute node instances used for multi-instance task.
 * 
 * @member {string} [multiInstanceSettings.coordinationCommandLine] Gets or
 * sets the command to be run on the compute node instances to setup
 * coordination among the subtasks.
 * 
 * @member {array} [multiInstanceSettings.commonResourceFiles] Gets or sets a
 * list of files that Batch will download on all subtasks.
 * 
 * @member {object} [dependsOn] Gets or sets any dependencies this task has.
 * 
 * @member {array} [dependsOn.taskIds] Gets or sets the list of task ids that
 * must complete before this task can be scheduled.
 * 
 * @member {array} [dependsOn.taskIdRanges] Gets or sets the list of task
 * ranges that must complete before this task can be scheduled.
 * 
 */
export interface TaskAddParameter {
    id: string;
    displayName?: string;
    commandLine: string;
    resourceFiles?: ResourceFile[];
    environmentSettings?: EnvironmentSetting[];
    affinityInfo?: AffinityInformation;
    constraints?: TaskConstraints;
    runElevated?: boolean;
    multiInstanceSettings?: MultiInstanceSettings;
    dependsOn?: TaskDependencies;
}

/**
 * @class
 * Initializes a new instance of the SubtaskInformation class.
 * @constructor
 * Information about an Azure Batch subtask.
 * @member {number} [id] Gets or sets the id of the subtask.
 * 
 * @member {object} [nodeInfo] Gets or sets information about the compute node
 * on which the subtask ran.
 * 
 * @member {string} [nodeInfo.affinityId] Gets or sets an identifier for the
 * compute node on which the task ran, which can be passed when adding a task
 * to request that the task be scheduled close to this compute node.
 * 
 * @member {string} [nodeInfo.nodeUrl] Gets or sets the URL of the node on
 * which the task ran.
 * 
 * @member {string} [nodeInfo.poolId] Gets or sets the id of the pool on which
 * the task ran.
 * 
 * @member {string} [nodeInfo.nodeId] Gets or sets the id of the node on which
 * the task ran.
 * 
 * @member {string} [nodeInfo.taskRootDirectory] Gets or sets the root
 * directory of the task on the compute node.
 * 
 * @member {string} [nodeInfo.taskRootDirectoryUrl] Gets or sets the URL to
 * the root directory of the task on the compute node.
 * 
 * @member {date} [startTime] Gets or sets the time at which the subtask
 * started running. If the subtask has been restarted or retried, this is the
 * most recent time at which the subtask started running.
 * 
 * @member {date} [endTime] Gets or sets the time at which the subtask
 * completed. This property is set only if the subtask is in the Completed
 * state.
 * 
 * @member {number} [exitCode] Gets or sets the exit code of the subtask. This
 * property is set only if the subtask is in the Completed state.
 * 
 * @member {object} [schedulingError] Gets or sets details of any error
 * encountered scheduling the subtask.
 * 
 * @member {string} [schedulingError.category] Gets or sets the category of
 * the task scheduling error. Possible values include: 'usererror',
 * 'servererror', 'unmapped'
 * 
 * @member {string} [schedulingError.code] Gets or sets an identifier for the
 * task scheduling error.  Codes are invariant and are intended to be
 * consumed programmatically.
 * 
 * @member {string} [schedulingError.message] Gets or sets a message
 * describing the task scheduling error, intended to be suitable for display
 * in a user interface.
 * 
 * @member {array} [schedulingError.details] Gets or sets the list of
 * additional error details related to the scheduling error.
 * 
 * @member {string} [state] Gets or sets the current state of the subtask.
 * Possible values include: 'active', 'preparing', 'running', 'completed'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the
 * subtask entered its current state.
 * 
 * @member {string} [previousState] Gets or sets the previous state of the
 * subtask. This property is not set if the subtask is in its initial Active
 * state. Possible values include: 'active', 'preparing', 'running',
 * 'completed'
 * 
 * @member {date} [previousStateTransitionTime] Gets or sets the time at which
 * the subtask entered its previous state. This property is not set if the
 * subtask is in its initial Active state.
 * 
 */
export interface SubtaskInformation {
    id?: number;
    nodeInfo?: ComputeNodeInformation;
    startTime?: Date;
    endTime?: Date;
    exitCode?: number;
    schedulingError?: TaskSchedulingError;
    state?: string;
    stateTransitionTime?: Date;
    previousState?: string;
    previousStateTransitionTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the CloudTaskListSubtasksResult class.
 * @constructor
 * Response to a CloudTaskOperations.ListSubtasks request.
 * @member {array} [value] Gets or sets the list of information of subtasks.
 * 
 */
export interface CloudTaskListSubtasksResult {
    value?: SubtaskInformation[];
}

/**
 * @class
 * Initializes a new instance of the TaskInformation class.
 * @constructor
 * Information about a task running on a compute node.
 * @member {string} [taskUrl] Gets or sets the URL of the task.
 * 
 * @member {string} [jobId] Gets or sets the id of the job to which the task
 * belongs.
 * 
 * @member {string} [taskId] Gets or sets the id of the task.
 * 
 * @member {number} [subtaskId] Gets or sets the id of the subtask if the task
 * is a multi-instance task.
 * 
 * @member {string} taskState Gets or sets the current state of the task.
 * Possible values include: 'active', 'preparing', 'running', 'completed'
 * 
 * @member {object} [executionInfo] Gets or sets information about the
 * execution of the task.
 * 
 * @member {date} [executionInfo.startTime] Gets or sets the time at which the
 * task started running. If the task has been restarted or retried, this is
 * the most recent time at which the task started running.
 * 
 * @member {date} [executionInfo.endTime] Gets or sets the time at which the
 * task completed. This property is set only if the task is in the Completed
 * state.
 * 
 * @member {number} [executionInfo.exitCode] Gets or sets the exit code of the
 * task. This property is set only if the task is in completed state.
 * 
 * @member {object} [executionInfo.schedulingError] Gets or sets details of
 * any error encountered scheduling the task.
 * 
 * @member {string} [executionInfo.schedulingError.category] Gets or sets the
 * category of the task scheduling error. Possible values include:
 * 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [executionInfo.schedulingError.code] Gets or sets an
 * identifier for the task scheduling error.  Codes are invariant and are
 * intended to be consumed programmatically.
 * 
 * @member {string} [executionInfo.schedulingError.message] Gets or sets a
 * message describing the task scheduling error, intended to be suitable for
 * display in a user interface.
 * 
 * @member {array} [executionInfo.schedulingError.details] Gets or sets the
 * list of additional error details related to the scheduling error.
 * 
 * @member {number} [executionInfo.retryCount] Gets or sets the number of
 * times the task has been retried by the Batch service.
 * 
 * @member {date} [executionInfo.lastRetryTime] Gets or sets the most recent
 * time at which a retry of the task started running.
 * 
 * @member {number} [executionInfo.requeueCount] Gets or sets the number of
 * times the task has been requeued by the Batch service as the result of a
 * user request.
 * 
 * @member {date} [executionInfo.lastRequeueTime] Gets or sets the most recent
 * time at which the task has been requeued by the Batch service as the
 * result of a user request.
 * 
 */
export interface TaskInformation {
    taskUrl?: string;
    jobId?: string;
    taskId?: string;
    subtaskId?: number;
    taskState: string;
    executionInfo?: TaskExecutionInformation;
}

/**
 * @class
 * Initializes a new instance of the StartTaskInformation class.
 * @constructor
 * Information about a start task running on a compute node.
 * @member {string} state Gets or sets the state of the start task on the
 * compute node. Possible values include: 'running', 'completed'
 * 
 * @member {date} startTime Gets or sets the time at which the start task
 * started running.
 * 
 * @member {date} [endTime] Gets or sets the time at which the start task
 * stopped running.
 * 
 * @member {number} [exitCode] Gets or sets the exit code of the start task.
 * 
 * @member {object} [schedulingError] Gets or sets any error encountered
 * scheduling the start task.
 * 
 * @member {string} [schedulingError.category] Gets or sets the category of
 * the task scheduling error. Possible values include: 'usererror',
 * 'servererror', 'unmapped'
 * 
 * @member {string} [schedulingError.code] Gets or sets an identifier for the
 * task scheduling error.  Codes are invariant and are intended to be
 * consumed programmatically.
 * 
 * @member {string} [schedulingError.message] Gets or sets a message
 * describing the task scheduling error, intended to be suitable for display
 * in a user interface.
 * 
 * @member {array} [schedulingError.details] Gets or sets the list of
 * additional error details related to the scheduling error.
 * 
 * @member {number} retryCount Gets or sets the number of times the task has
 * been retried by the Batch service.
 * 
 * @member {date} [lastRetryTime] Gets or sets the most recent time at which a
 * retry of the task started running.
 * 
 */
export interface StartTaskInformation {
    state: string;
    startTime: Date;
    endTime?: Date;
    exitCode?: number;
    schedulingError?: TaskSchedulingError;
    retryCount: number;
    lastRetryTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeError class.
 * @constructor
 * An error encountered by a compute node.
 * @member {string} [code] Gets or sets an identifier for the compute node
 * error.  Codes are invariant and are intended to be consumed
 * programmatically.
 * 
 * @member {string} [message] Gets or sets a message describing the compute
 * node error, intended to be suitable for display in a user interface.
 * 
 * @member {array} [errorDetails] Gets or sets the list of additional error
 * details related to the compute node error.
 * 
 */
export interface ComputeNodeError {
    code?: string;
    message?: string;
    errorDetails?: NameValuePair[];
}

/**
 * @class
 * Initializes a new instance of the ComputeNode class.
 * @constructor
 * A compute node in the Batch service.
 * @member {string} [id] Gets or sets the id of the compute node.
 * 
 * @member {string} [url] Gets or sets the URL of the compute node.
 * 
 * @member {string} [state] Gets or sets the current state of the compute
 * node. Possible values include: 'idle', 'rebooting', 'reimaging',
 * 'running', 'unusable', 'creating', 'starting', 'waitingforstarttask',
 * 'starttaskfailed', 'unknown', 'leavingpool', 'offline'
 * 
 * @member {string} [schedulingState] Gets or sets whether the compute node
 * should be available for task scheduling. Possible values include:
 * 'enabled', 'disabled'
 * 
 * @member {date} [stateTransitionTime] Gets or sets the time at which the
 * compute node entered its current state.
 * 
 * @member {date} [lastBootTime] Gets or sets the time at which the compute
 * node was started.
 * 
 * @member {date} [allocationTime] Gets or sets the time at which this compute
 * node was allocated to the pool.
 * 
 * @member {string} [ipAddress] Gets or sets the IP address that other compute
 * nodes can use to communicate with this compute node.
 * 
 * @member {string} [affinityId] Gets or sets an identifier which can be
 * passed in the Add Task API to request that the task be scheduled close to
 * this compute node.
 * 
 * @member {string} [vmSize] Gets or sets the size of the virtual machine
 * hosting the compute node.
 * 
 * @member {number} [totalTasksRun] Gets or sets the total number of job tasks
 * completed on the compute node. This includes Job Preparation, Job Release
 * and Job Manager tasks, but not the pool start task.
 * 
 * @member {array} [recentTasks] Gets or sets the list of tasks that are
 * currently running on the compute node.
 * 
 * @member {object} [startTask] Gets or sets the task specified to run on the
 * compute node as it joins the pool.
 * 
 * @member {string} [startTask.commandLine] Gets or sets the command line of
 * the start task.
 * 
 * @member {array} [startTask.resourceFiles] Gets or sets a list of files that
 * Batch will download to the compute node before running the command line.
 * 
 * @member {array} [startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [startTask.runElevated] Gets or sets whether to run the
 * start task in elevated mode. The default value is false.
 * 
 * @member {number} [startTask.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried.
 * 
 * @member {boolean} [startTask.waitForSuccess] Gets or sets whether the Batch
 * Service should wait for the start task to complete successfully (that is,
 * to exit with exit code 0) before scheduling any tasks on the compute node.
 * 
 * @member {object} [startTaskInfo] Gets or sets runtime information about the
 * execution of the start task on the compute node.
 * 
 * @member {string} [startTaskInfo.state] Gets or sets the state of the start
 * task on the compute node. Possible values include: 'running', 'completed'
 * 
 * @member {date} [startTaskInfo.startTime] Gets or sets the time at which the
 * start task started running.
 * 
 * @member {date} [startTaskInfo.endTime] Gets or sets the time at which the
 * start task stopped running.
 * 
 * @member {number} [startTaskInfo.exitCode] Gets or sets the exit code of the
 * start task.
 * 
 * @member {object} [startTaskInfo.schedulingError] Gets or sets any error
 * encountered scheduling the start task.
 * 
 * @member {string} [startTaskInfo.schedulingError.category] Gets or sets the
 * category of the task scheduling error. Possible values include:
 * 'usererror', 'servererror', 'unmapped'
 * 
 * @member {string} [startTaskInfo.schedulingError.code] Gets or sets an
 * identifier for the task scheduling error.  Codes are invariant and are
 * intended to be consumed programmatically.
 * 
 * @member {string} [startTaskInfo.schedulingError.message] Gets or sets a
 * message describing the task scheduling error, intended to be suitable for
 * display in a user interface.
 * 
 * @member {array} [startTaskInfo.schedulingError.details] Gets or sets the
 * list of additional error details related to the scheduling error.
 * 
 * @member {number} [startTaskInfo.retryCount] Gets or sets the number of
 * times the task has been retried by the Batch service.
 * 
 * @member {date} [startTaskInfo.lastRetryTime] Gets or sets the most recent
 * time at which a retry of the task started running.
 * 
 * @member {array} [certificateReferences] Gets or sets the list of
 * certificates installed on the compute node.
 * 
 * @member {array} [errors] Gets or sets the list of errors that are currently
 * being encountered by the compute node.
 * 
 */
export interface ComputeNode {
    id?: string;
    url?: string;
    state?: string;
    schedulingState?: string;
    stateTransitionTime?: Date;
    lastBootTime?: Date;
    allocationTime?: Date;
    ipAddress?: string;
    affinityId?: string;
    vmSize?: string;
    totalTasksRun?: number;
    recentTasks?: TaskInformation[];
    startTask?: StartTask;
    startTaskInfo?: StartTaskInformation;
    certificateReferences?: CertificateReference[];
    errors?: ComputeNodeError[];
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeUser class.
 * @constructor
 * An user account on a compute node.
 * @member {string} name Gets or sets the user name of the account.
 * 
 * @member {boolean} [isAdmin] Gets or sets whether the account should be an
 * administrator on the compute node.
 * 
 * @member {date} [expiryTime] Gets or sets the time at which the account
 * should expire. If omitted, the default is 1 day from the current time.
 * 
 * @member {string} password Gets or sets the password of the account.
 * 
 */
export interface ComputeNodeUser {
    name: string;
    isAdmin?: boolean;
    expiryTime?: Date;
    password: string;
}

/**
 * @class
 * Initializes a new instance of the JobSchedulePatchParameter class.
 * @constructor
 * Parameters for a CloudJobScheduleOperations.Patch request.
 * @member {object} [schedule] Sets the schedule according to which jobs will
 * be created. If you do not specify this element, the existing schedule is
 * not modified.
 * 
 * @member {date} [schedule.doNotRunUntil] Gets or sets the earliest time at
 * which any job may be created under this job schedule. If you do not
 * specify a doNotRunUntil time, the schedule becomes ready to create jobs
 * immediately.
 * 
 * @member {date} [schedule.doNotRunAfter] Gets or sets a time after which no
 * job will be created under this job schedule.  The schedule will move to
 * the completed state as soon as this deadline is past and there is no
 * active job under this job schedule.
 * 
 * @member {moment.duration} [schedule.startWindow] Gets or sets the time
 * interval, starting from the time at which the schedule indicates a job
 * should be created, within which a job must be created.  If a job is not
 * created within the startWindow interval, then the 'opportunity' is lost;
 * no job will be created until the next recurrence of the schedule.
 * 
 * @member {moment.duration} [schedule.recurrenceInterval] Gets or sets the
 * time interval between the start times of two successive jobs under the job
 * schedule. A job schedule can have at most one active job under it at any
 * given time.
 * 
 * @member {object} [jobSpecification] Sets the details of the jobs to be
 * created on this schedule.
 * 
 * @member {number} [jobSpecification.priority] Gets or sets the priority of
 * jobs created under this schedule. Priority values can range from -1000 to
 * 1000, with -1000 being the lowest priority and 1000 being the highest
 * priority. The default value is 0.
 * 
 * @member {string} [jobSpecification.displayName] Gets or sets the display
 * name for jobs created under this schedule. It need not be unique and can
 * contain any Unicode characters up to a maximum length of 1024.
 * 
 * @member {boolean} [jobSpecification.usesTaskDependencies] Gets or sets the
 * flag that determines if this job will use tasks with dependencies.
 * 
 * @member {object} [jobSpecification.constraints] Gets or sets the execution
 * constraints for jobs created under this schedule.
 * 
 * @member {moment.duration} [jobSpecification.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the job may run, measured from
 * the time the job starts. If the job does not complete within the time
 * limit, the Batch service terminates it and any tasks that are still
 * running.
 * 
 * @member {number} [jobSpecification.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times each task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {object} [jobSpecification.jobManagerTask] Gets or sets the details
 * of a Job Manager task to be launched when a job is started under this
 * schedule.
 * 
 * @member {string} [jobSpecification.jobManagerTask.id] Gets or sets a string
 * that uniquely identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobSpecification.jobManagerTask.displayName] Gets or sets
 * the display name of the Job Manager task.
 * 
 * @member {string} [jobSpecification.jobManagerTask.commandLine] Gets or sets
 * the command line of the Job Manager task.
 * 
 * @member {array} [jobSpecification.jobManagerTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobManagerTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobSpecification.jobManagerTask.constraints] Gets or sets
 * constraints that apply to the Job Manager task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.retentionTime] Gets or sets
 * the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.killJobOnCompletion]
 * Gets or sets whether completion of the Job Manager task signifies
 * completion of the entire job.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runElevated] Gets or
 * sets whether to run the Job Manager task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runExclusive] Gets or
 * sets whether the Job Manager task requires exclusive use of the compute
 * node where it runs.
 * 
 * @member {object} [jobSpecification.jobPreparationTask] Gets or sets the Job
 * Preparation task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.id] Gets or sets a
 * string that uniquely identifies the job preparation task within the job.
 * The id can contain any combination of alphanumeric characters including
 * hyphens and underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.commandLine] Gets or
 * sets the command line of the Job Preparation task.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the Job
 * Preparation task.
 * 
 * @member {object} [jobSpecification.jobPreparationTask.constraints] Gets or
 * sets constraints that apply to the Job Preparation task..
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.retentionTime] Gets or
 * sets the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobPreparationTask.constraints.maxTaskRetryCount] Gets
 * or sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.waitForSuccess] Gets
 * or sets whether the Batch Service should wait for the Job Preparation task
 * to complete successfully before scheduling any other tasks of the job on
 * the compute node.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.runElevated] Gets or
 * sets whether to run the Job Preparation task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean}
 * [jobSpecification.jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobSpecification.jobReleaseTask] Gets or sets the Job
 * Release task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.id] Gets or sets a string
 * that uniquely identifies the Job Release task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.commandLine] Gets or sets
 * the command line of the Job Release task.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Release task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobReleaseTask.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the Job Release task may run on a given compute
 * node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobSpecification.jobReleaseTask.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the Job
 * Release task on the compute node.  After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobSpecification.jobReleaseTask.runElevated] Gets or
 * sets whether to run the Job Release task in elevated mode. The default
 * value is false.
 * 
 * @member {array} [jobSpecification.commonEnvironmentSettings] Gets or sets a
 * list of common environment variable settings.  These environment variables
 * are set for all tasks in jobs created under this schedule (including the
 * Job Manager, Job Preparation and Job Release tasks).
 * 
 * @member {object} [jobSpecification.poolInfo] Gets or sets the pool on which
 * the Batch service runs the tasks of jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.poolInfo.poolId] Gets or sets the id of
 * an existing pool. All the tasks of the job will run on the specified pool.
 * You must specify either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification] Gets or
 * sets characteristics for a temporary 'auto pool.' The Batch service will
 * create this auto pool and run all the tasks of the job on it, and will
 * delete the pool once the job has completed. You must specify either PoolId
 * or AutoPoolSpecification, but not both.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification.pool]
 * Gets or sets the pool specification for the auto pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetOSVersion]
 * Gets or sets the Azure Guest OS version to be installed on the virtual
 * machines in the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.maxTasksPerNode]
 * Gets or sets the maximum number of tasks that can run concurrently on a
 * single compute node in the pool.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets
 * or sets the timeout for allocation of compute nodes to the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetDedicated]
 * Gets or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval]
 * Gets or sets a time interval for the desired AutoScale evaluation period
 * in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication]
 * Gets or sets whether the pool permits direct communication between nodes.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.commandLine]
 * Gets or sets the command line of the start task.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.resourceFiles]
 * Gets or sets a list of files that Batch will download to the compute node
 * before running the command line.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.runElevated]
 * Gets or sets whether to run the start task in elevated mode. The default
 * value is false.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount]
 * Gets or sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess]
 * Gets or sets whether the Batch Service should wait for the start task to
 * complete successfully (that is, to exit with exit code 0) before
 * scheduling any tasks on the compute node.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.applicationPackageReferences]
 * Gets or sets the list of application packages to be installed on each
 * compute node in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.metadata] Gets or
 * sets a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [jobSpecification.metadata] Gets or sets a list of
 * name-value pairs associated with each job created under this schedule as
 * metadata.
 * 
 * @member {array} [metadata] Sets a list of name-value pairs associated with
 * the job schedule as metadata.
 * 
 */
export interface JobSchedulePatchParameter {
    schedule?: Schedule;
    jobSpecification?: JobSpecification;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the JobScheduleUpdateParameter class.
 * @constructor
 * Parameters for a CloudJobScheduleOperations.Update request.
 * @member {object} schedule Sets the schedule according to which jobs will be
 * created. If you do not specify this element, it is equivalent to passing
 * the default schedule: that is, a single job scheduled to run immediately.
 * 
 * @member {date} [schedule.doNotRunUntil] Gets or sets the earliest time at
 * which any job may be created under this job schedule. If you do not
 * specify a doNotRunUntil time, the schedule becomes ready to create jobs
 * immediately.
 * 
 * @member {date} [schedule.doNotRunAfter] Gets or sets a time after which no
 * job will be created under this job schedule.  The schedule will move to
 * the completed state as soon as this deadline is past and there is no
 * active job under this job schedule.
 * 
 * @member {moment.duration} [schedule.startWindow] Gets or sets the time
 * interval, starting from the time at which the schedule indicates a job
 * should be created, within which a job must be created.  If a job is not
 * created within the startWindow interval, then the 'opportunity' is lost;
 * no job will be created until the next recurrence of the schedule.
 * 
 * @member {moment.duration} [schedule.recurrenceInterval] Gets or sets the
 * time interval between the start times of two successive jobs under the job
 * schedule. A job schedule can have at most one active job under it at any
 * given time.
 * 
 * @member {object} jobSpecification Sets details of the jobs to be created on
 * this schedule.
 * 
 * @member {number} [jobSpecification.priority] Gets or sets the priority of
 * jobs created under this schedule. Priority values can range from -1000 to
 * 1000, with -1000 being the lowest priority and 1000 being the highest
 * priority. The default value is 0.
 * 
 * @member {string} [jobSpecification.displayName] Gets or sets the display
 * name for jobs created under this schedule. It need not be unique and can
 * contain any Unicode characters up to a maximum length of 1024.
 * 
 * @member {boolean} [jobSpecification.usesTaskDependencies] Gets or sets the
 * flag that determines if this job will use tasks with dependencies.
 * 
 * @member {object} [jobSpecification.constraints] Gets or sets the execution
 * constraints for jobs created under this schedule.
 * 
 * @member {moment.duration} [jobSpecification.constraints.maxWallClockTime]
 * Gets or sets the maximum elapsed time that the job may run, measured from
 * the time the job starts. If the job does not complete within the time
 * limit, the Batch service terminates it and any tasks that are still
 * running.
 * 
 * @member {number} [jobSpecification.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times each task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {object} [jobSpecification.jobManagerTask] Gets or sets the details
 * of a Job Manager task to be launched when a job is started under this
 * schedule.
 * 
 * @member {string} [jobSpecification.jobManagerTask.id] Gets or sets a string
 * that uniquely identifies the Job Manager task. A GUID is recommended.
 * 
 * @member {string} [jobSpecification.jobManagerTask.displayName] Gets or sets
 * the display name of the Job Manager task.
 * 
 * @member {string} [jobSpecification.jobManagerTask.commandLine] Gets or sets
 * the command line of the Job Manager task.
 * 
 * @member {array} [jobSpecification.jobManagerTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobManagerTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Manager task.
 * 
 * @member {object} [jobSpecification.jobManagerTask.constraints] Gets or sets
 * constraints that apply to the Job Manager task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobManagerTask.constraints.retentionTime] Gets or sets
 * the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobManagerTask.constraints.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.killJobOnCompletion]
 * Gets or sets whether completion of the Job Manager task signifies
 * completion of the entire job.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runElevated] Gets or
 * sets whether to run the Job Manager task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean} [jobSpecification.jobManagerTask.runExclusive] Gets or
 * sets whether the Job Manager task requires exclusive use of the compute
 * node where it runs.
 * 
 * @member {object} [jobSpecification.jobPreparationTask] Gets or sets the Job
 * Preparation task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.id] Gets or sets a
 * string that uniquely identifies the job preparation task within the job.
 * The id can contain any combination of alphanumeric characters including
 * hyphens and underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobPreparationTask.commandLine] Gets or
 * sets the command line of the Job Preparation task.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobPreparationTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the Job
 * Preparation task.
 * 
 * @member {object} [jobSpecification.jobPreparationTask.constraints] Gets or
 * sets constraints that apply to the Job Preparation task..
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.maxWallClockTime] Gets or
 * sets the maximum elapsed time that the task may run, measured from the
 * time the task starts. If the task does not complete within the time limit,
 * the Batch service terminates it.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobPreparationTask.constraints.retentionTime] Gets or
 * sets the minimum time to retain the working directory for the task on the
 * compute node where it ran. After this time, the Batch service may delete
 * the working directory and all its contents. The default is infinite.
 * 
 * @member {number}
 * [jobSpecification.jobPreparationTask.constraints.maxTaskRetryCount] Gets
 * or sets the maximum number of times the task may be retried. The Batch
 * service retries a task if its exit code is nonzero.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.waitForSuccess] Gets
 * or sets whether the Batch Service should wait for the Job Preparation task
 * to complete successfully before scheduling any other tasks of the job on
 * the compute node.
 * 
 * @member {boolean} [jobSpecification.jobPreparationTask.runElevated] Gets or
 * sets whether to run the Job Preparation task in elevated mode. The default
 * value is false.
 * 
 * @member {boolean}
 * [jobSpecification.jobPreparationTask.rerunOnNodeRebootAfterSuccess] Gets
 * or sets whether the Batch service should rerun the Job Preparation task
 * after a compute node reboots. The default value is true.
 * 
 * @member {object} [jobSpecification.jobReleaseTask] Gets or sets the Job
 * Release task for jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.id] Gets or sets a string
 * that uniquely identifies the Job Release task within the job. The id can
 * contain any combination of alphanumeric characters including hyphens and
 * underscores and cannot contain more than 64 characters.
 * 
 * @member {string} [jobSpecification.jobReleaseTask.commandLine] Gets or sets
 * the command line of the Job Release task.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.resourceFiles] Gets or
 * sets a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array} [jobSpecification.jobReleaseTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the Job Release task.
 * 
 * @member {moment.duration}
 * [jobSpecification.jobReleaseTask.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the Job Release task may run on a given compute
 * node, measured from the time the task starts. If the task does not
 * complete within the time limit, the Batch service terminates it. The
 * default value is 15 minutes.
 * 
 * @member {moment.duration} [jobSpecification.jobReleaseTask.retentionTime]
 * Gets or sets the minimum time to retain the working directory for the Job
 * Release task on the compute node.  After this time, the Batch service may
 * delete the working directory and all its contents. The default is infinite.
 * 
 * @member {boolean} [jobSpecification.jobReleaseTask.runElevated] Gets or
 * sets whether to run the Job Release task in elevated mode. The default
 * value is false.
 * 
 * @member {array} [jobSpecification.commonEnvironmentSettings] Gets or sets a
 * list of common environment variable settings.  These environment variables
 * are set for all tasks in jobs created under this schedule (including the
 * Job Manager, Job Preparation and Job Release tasks).
 * 
 * @member {object} [jobSpecification.poolInfo] Gets or sets the pool on which
 * the Batch service runs the tasks of jobs created under this schedule.
 * 
 * @member {string} [jobSpecification.poolInfo.poolId] Gets or sets the id of
 * an existing pool. All the tasks of the job will run on the specified pool.
 * You must specify either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification] Gets or
 * sets characteristics for a temporary 'auto pool.' The Batch service will
 * create this auto pool and run all the tasks of the job on it, and will
 * delete the pool once the job has completed. You must specify either PoolId
 * or AutoPoolSpecification, but not both.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [jobSpecification.poolInfo.autoPoolSpecification.pool]
 * Gets or sets the pool specification for the auto pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetOSVersion]
 * Gets or sets the Azure Guest OS version to be installed on the virtual
 * machines in the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.maxTasksPerNode]
 * Gets or sets the maximum number of tasks that can run concurrently on a
 * single compute node in the pool.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets
 * or sets the timeout for allocation of compute nodes to the pool.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.targetDedicated]
 * Gets or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval]
 * Gets or sets a time interval for the desired AutoScale evaluation period
 * in the pool.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication]
 * Gets or sets whether the pool permits direct communication between nodes.
 * 
 * @member {object}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.commandLine]
 * Gets or sets the command line of the start task.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.resourceFiles]
 * Gets or sets a list of files that Batch will download to the compute node
 * before running the command line.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.environmentSettings]
 * Gets or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.runElevated]
 * Gets or sets whether to run the start task in elevated mode. The default
 * value is false.
 * 
 * @member {number}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount]
 * Gets or sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess]
 * Gets or sets whether the Batch Service should wait for the start task to
 * complete successfully (that is, to exit with exit code 0) before
 * scheduling any tasks on the compute node.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.applicationPackageReferences]
 * Gets or sets the list of application packages to be installed on each
 * compute node in the pool.
 * 
 * @member {array}
 * [jobSpecification.poolInfo.autoPoolSpecification.pool.metadata] Gets or
 * sets a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [jobSpecification.metadata] Gets or sets a list of
 * name-value pairs associated with each job created under this schedule as
 * metadata.
 * 
 * @member {array} [metadata] Sets a list of name-value pairs associated with
 * the job schedule as metadata. If you do not specify this element, it takes
 * the default value of an empty list; in effect, any existing metadata is
 * deleted.
 * 
 */
export interface JobScheduleUpdateParameter {
    schedule: Schedule;
    jobSpecification: JobSpecification;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the JobDisableParameter class.
 * @constructor
 * Parameters for a CloudJobOperations.Disable request.
 * @member {string} disableTasks Sets what to do with active tasks associated
 * with the job. Possible values include: 'requeue', 'terminate', 'wait'
 * 
 */
export interface JobDisableParameter {
    disableTasks: string;
}

/**
 * @class
 * Initializes a new instance of the JobTerminateParameter class.
 * @constructor
 * Parameters for a CloudJobOperations.Terminate request.
 * @member {string} [terminateReason] Sets the text you want to appear as the
 * job's TerminateReason. The default is 'UserTerminate'.
 * 
 */
export interface JobTerminateParameter {
    terminateReason?: string;
}

/**
 * @class
 * Initializes a new instance of the JobPatchParameter class.
 * @constructor
 * Parameters for a CloudJobOperations.Patch request.
 * @member {number} [priority] Sets the priority of the job. Priority values
 * can range from -1000 to 1000, with -1000 being the lowest priority and
 * 1000 being the highest priority. If omitted, the priority of the job is
 * left unchanged.
 * 
 * @member {object} [constraints] Sets the execution constraints for the job.
 * If omitted, the existing execution constraints are left unchanged.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the job may run, measured from the time the job
 * starts. If the job does not complete within the time limit, the Batch
 * service terminates it and any tasks that are still running.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times each task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {object} [poolInfo] Sets the pool on which the Batch service runs
 * the job's tasks. If omitted, the job continues to run on its current pool.
 * 
 * @member {string} [poolInfo.poolId] Gets or sets the id of an existing pool.
 * All the tasks of the job will run on the specified pool. You must specify
 * either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [poolInfo.autoPoolSpecification] Gets or sets
 * characteristics for a temporary 'auto pool.' The Batch service will create
 * this auto pool and run all the tasks of the job on it, and will delete the
 * pool once the job has completed. You must specify either PoolId or
 * AutoPoolSpecification, but not both.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool] Gets or sets the
 * pool specification for the auto pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.targetOSVersion] Gets
 * or sets the Azure Guest OS version to be installed on the virtual machines
 * in the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.maxTasksPerNode] Gets
 * or sets the maximum number of tasks that can run concurrently on a single
 * compute node in the pool.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets or sets the
 * timeout for allocation of compute nodes to the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.targetDedicated] Gets
 * or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval] Gets or
 * sets a time interval for the desired AutoScale evaluation period in the
 * pool.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication] Gets or
 * sets whether the pool permits direct communication between nodes.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.startTask.commandLine] Gets or sets
 * the command line of the start task.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.resourceFiles] Gets or sets
 * a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.runElevated] Gets or sets
 * whether to run the start task in elevated mode. The default value is false.
 * 
 * @member {number}
 * [poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess] Gets or
 * sets whether the Batch Service should wait for the start task to complete
 * successfully (that is, to exit with exit code 0) before scheduling any
 * tasks on the compute node.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.applicationPackageReferences] Gets or
 * sets the list of application packages to be installed on each compute node
 * in the pool.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.metadata] Gets or sets
 * a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [metadata] Sets a list of name-value pairs associated with
 * the job as metadata. If omitted, the existing job metadata is left
 * unchanged.
 * 
 */
export interface JobPatchParameter {
    priority?: number;
    constraints?: JobConstraints;
    poolInfo?: PoolInformation;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the JobUpdateParameter class.
 * @constructor
 * Parameters for a CloudJobOperations.Update request.
 * @member {number} [priority] Sets the priority of the job. Priority values
 * can range from -1000 to 1000, with -1000 being the lowest priority and
 * 1000 being the highest priority. If omitted, the priority of the job is
 * left unchanged.
 * 
 * @member {object} [constraints] Sets the execution constraints for the job.
 * If omitted, the existing execution constraints are left unchanged.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the job may run, measured from the time the job
 * starts. If the job does not complete within the time limit, the Batch
 * service terminates it and any tasks that are still running.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times each task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 * @member {object} poolInfo Sets the pool on which the Batch service runs the
 * job's tasks. If omitted, the job continues to run on its current pool.
 * 
 * @member {string} [poolInfo.poolId] Gets or sets the id of an existing pool.
 * All the tasks of the job will run on the specified pool. You must specify
 * either PoolId or AutoPoolSpecification, but not both.
 * 
 * @member {object} [poolInfo.autoPoolSpecification] Gets or sets
 * characteristics for a temporary 'auto pool.' The Batch service will create
 * this auto pool and run all the tasks of the job on it, and will delete the
 * pool once the job has completed. You must specify either PoolId or
 * AutoPoolSpecification, but not both.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.autoPoolIdPrefix] Gets or
 * sets a prefix to be added to the unique identifier when a pool is
 * automatically created. The prefix can be up to 20 characters long.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.poolLifetimeOption] Gets
 * or sets the minimum lifetime of created auto pools, and how multiple jobs
 * on a schedule are assigned to pools. Possible values include:
 * 'jobschedule', 'job', 'unmapped'
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.keepAlive] Gets or sets
 * whether to keep an auto pool alive after its lifetime expires.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool] Gets or sets the
 * pool specification for the auto pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.displayName] Gets or
 * sets the display name for the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.vmSize] Gets or sets
 * the size of the virtual machines in the pool. All VMs in a pool are the
 * same size.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.osFamily] Gets or
 * sets the Azure Guest OS family to be installed on the virtual machines in
 * the pool.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.targetOSVersion] Gets
 * or sets the Azure Guest OS version to be installed on the virtual machines
 * in the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.maxTasksPerNode] Gets
 * or sets the maximum number of tasks that can run concurrently on a single
 * compute node in the pool.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy]
 * Gets or sets how tasks are distributed among compute nodes in the pool.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.taskSchedulingPolicy.nodeFillType]
 * Gets or sets how tasks should be distributed across compute nodes.
 * Possible values include: 'spread', 'pack', 'unmapped'
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.resizeTimeout] Gets or sets the
 * timeout for allocation of compute nodes to the pool.
 * 
 * @member {number} [poolInfo.autoPoolSpecification.pool.targetDedicated] Gets
 * or sets the desired number of compute nodes in the pool.
 * 
 * @member {boolean} [poolInfo.autoPoolSpecification.pool.enableAutoScale]
 * Gets or sets whether the pool size should automatically adjust over time.
 * 
 * @member {string} [poolInfo.autoPoolSpecification.pool.autoScaleFormula]
 * Gets or sets the formula for the desired number of compute nodes in the
 * pool.
 * 
 * @member {moment.duration}
 * [poolInfo.autoPoolSpecification.pool.autoScaleEvaluationInterval] Gets or
 * sets a time interval for the desired AutoScale evaluation period in the
 * pool.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.enableInterNodeCommunication] Gets or
 * sets whether the pool permits direct communication between nodes.
 * 
 * @member {object} [poolInfo.autoPoolSpecification.pool.startTask] Gets or
 * sets a task to run on each compute node as it joins the pool. The task
 * runs when the node is added to the pool or when the node is restarted.
 * 
 * @member {string}
 * [poolInfo.autoPoolSpecification.pool.startTask.commandLine] Gets or sets
 * the command line of the start task.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.resourceFiles] Gets or sets
 * a list of files that Batch will download to the compute node before
 * running the command line.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.startTask.environmentSettings] Gets
 * or sets a list of environment variable settings for the start task.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.runElevated] Gets or sets
 * whether to run the start task in elevated mode. The default value is false.
 * 
 * @member {number}
 * [poolInfo.autoPoolSpecification.pool.startTask.maxTaskRetryCount] Gets or
 * sets the maximum number of times the task may be retried.
 * 
 * @member {boolean}
 * [poolInfo.autoPoolSpecification.pool.startTask.waitForSuccess] Gets or
 * sets whether the Batch Service should wait for the start task to complete
 * successfully (that is, to exit with exit code 0) before scheduling any
 * tasks on the compute node.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.certificateReferences]
 * Gets or sets a list of certificates to be installed on each compute node
 * in the pool.
 * 
 * @member {array}
 * [poolInfo.autoPoolSpecification.pool.applicationPackageReferences] Gets or
 * sets the list of application packages to be installed on each compute node
 * in the pool.
 * 
 * @member {array} [poolInfo.autoPoolSpecification.pool.metadata] Gets or sets
 * a list of name-value pairs associated with the pool as metadata.
 * 
 * @member {array} [metadata] Sets a list of name-value pairs associated with
 * the job as metadata. If omitted, the existing job metadata is left
 * unchanged.
 * 
 */
export interface JobUpdateParameter {
    priority?: number;
    constraints?: JobConstraints;
    poolInfo: PoolInformation;
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the PoolEnableAutoScaleParameter class.
 * @constructor
 * Parameters for a CloudPoolOperations.EnableAutoScale request.
 * @member {string} [autoScaleFormula] Sets the formula for the desired number
 * of compute nodes in the pool.
 * 
 * @member {moment.duration} [autoScaleEvaluationInterval] Gets or sets a time
 * interval for the desired autoscale evaluation period in the pool.
 * 
 */
export interface PoolEnableAutoScaleParameter {
    autoScaleFormula?: string;
    autoScaleEvaluationInterval?: moment.Duration;
}

/**
 * @class
 * Initializes a new instance of the PoolEvaluateAutoScaleParameter class.
 * @constructor
 * Parameters for a CloudJobOperations.EvaluateAutoScale request.
 * @member {string} autoScaleFormula Sets a formula for the desired number of
 * compute nodes in the pool.
 * 
 */
export interface PoolEvaluateAutoScaleParameter {
    autoScaleFormula: string;
}

/**
 * @class
 * Initializes a new instance of the PoolResizeParameter class.
 * @constructor
 * Parameters for a CloudPoolOperations.Resize request.
 * @member {number} targetDedicated Sets the desired number of compute nodes
 * in the pool.
 * 
 * @member {moment.duration} [resizeTimeout] Sets the timeout for allocation
 * of compute nodes to the pool or removal of compute nodes from the pool.
 * The default value is 10 minutes.
 * 
 * @member {string} [nodeDeallocationOption] Sets when nodes may be removed
 * from the pool, if the pool size is decreasing. Possible values include:
 * 'requeue', 'terminate', 'taskcompletion', 'retaineddata'
 * 
 */
export interface PoolResizeParameter {
    targetDedicated: number;
    resizeTimeout?: moment.Duration;
    nodeDeallocationOption?: string;
}

/**
 * @class
 * Initializes a new instance of the PoolUpdatePropertiesParameter class.
 * @constructor
 * Parameters for a CloudPoolOperations.UpdateProperties request.
 * @member {object} [startTask] Sets a task to run on each compute node as it
 * joins the pool. If omitted, any existing start task is removed from the
 * pool.
 * 
 * @member {string} [startTask.commandLine] Gets or sets the command line of
 * the start task.
 * 
 * @member {array} [startTask.resourceFiles] Gets or sets a list of files that
 * Batch will download to the compute node before running the command line.
 * 
 * @member {array} [startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [startTask.runElevated] Gets or sets whether to run the
 * start task in elevated mode. The default value is false.
 * 
 * @member {number} [startTask.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried.
 * 
 * @member {boolean} [startTask.waitForSuccess] Gets or sets whether the Batch
 * Service should wait for the start task to complete successfully (that is,
 * to exit with exit code 0) before scheduling any tasks on the compute node.
 * 
 * @member {array} certificateReferences Sets a list of certificates to be
 * installed on each compute node in the pool. If you specify an empty
 * collection, any existing certificate references are removed from the pool.
 * 
 * @member {array} applicationPackageReferences Sets a list of application
 * packages to be installed on each compute node in the pool. If you specify
 * an empty collection, any existing application packages references are
 * removed from the pool.
 * 
 * @member {array} metadata Sets a list of name-value pairs associated with
 * the pool as metadata. If you specify an empty collection, any existing
 * metadata is removed from the pool.
 * 
 */
export interface PoolUpdatePropertiesParameter {
    startTask?: StartTask;
    certificateReferences: CertificateReference[];
    applicationPackageReferences: ApplicationPackageReference[];
    metadata: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the PoolUpgradeOSParameter class.
 * @constructor
 * Parameters for a CloudPoolOperations.UpgradeOS request.
 * @member {string} targetOSVersion Sets the Azure Guest OS version to be
 * installed on the virtual machines in the pool.
 * 
 */
export interface PoolUpgradeOSParameter {
    targetOSVersion: string;
}

/**
 * @class
 * Initializes a new instance of the PoolPatchParameter class.
 * @constructor
 * Parameters for a CloudPoolOperations.Patch request.
 * @member {object} [startTask] Sets a task to run on each compute node as it
 * joins the pool. If omitted, any existing start task is left unchanged.
 * 
 * @member {string} [startTask.commandLine] Gets or sets the command line of
 * the start task.
 * 
 * @member {array} [startTask.resourceFiles] Gets or sets a list of files that
 * Batch will download to the compute node before running the command line.
 * 
 * @member {array} [startTask.environmentSettings] Gets or sets a list of
 * environment variable settings for the start task.
 * 
 * @member {boolean} [startTask.runElevated] Gets or sets whether to run the
 * start task in elevated mode. The default value is false.
 * 
 * @member {number} [startTask.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried.
 * 
 * @member {boolean} [startTask.waitForSuccess] Gets or sets whether the Batch
 * Service should wait for the start task to complete successfully (that is,
 * to exit with exit code 0) before scheduling any tasks on the compute node.
 * 
 * @member {array} [certificateReferences] Sets a list of certificates to be
 * installed on each compute node in the pool. If omitted, any existing
 * certificate references are left unchanged.
 * 
 * @member {array} [applicationPackageReferences] Sets a list of application
 * packages to be installed on each compute node in the pool. If omitted, any
 * existing application package references are left unchanged.
 * 
 * @member {array} [metadata] Sets a list of name-value pairs associated with
 * the pool as metadata. If omitted, any existing metadata is left unchanged.
 * 
 */
export interface PoolPatchParameter {
    startTask?: StartTask;
    certificateReferences?: CertificateReference[];
    applicationPackageReferences?: ApplicationPackageReference[];
    metadata?: MetadataItem[];
}

/**
 * @class
 * Initializes a new instance of the TaskUpdateParameter class.
 * @constructor
 * Parameters for a CloudTaskOperations.Update request.
 * @member {object} [constraints] Sets constraints that apply to this task. If
 * omitted, the task is given the default constraints.
 * 
 * @member {moment.duration} [constraints.maxWallClockTime] Gets or sets the
 * maximum elapsed time that the task may run, measured from the time the
 * task starts. If the task does not complete within the time limit, the
 * Batch service terminates it.
 * 
 * @member {moment.duration} [constraints.retentionTime] Gets or sets the
 * minimum time to retain the working directory for the task on the compute
 * node where it ran. After this time, the Batch service may delete the
 * working directory and all its contents. The default is infinite.
 * 
 * @member {number} [constraints.maxTaskRetryCount] Gets or sets the maximum
 * number of times the task may be retried. The Batch service retries a task
 * if its exit code is nonzero.
 * 
 */
export interface TaskUpdateParameter {
    constraints?: TaskConstraints;
}

/**
 * @class
 * Initializes a new instance of the NodeUpdateUserParameter class.
 * @constructor
 * Parameters for a ComputeNodeOperations.UpdateUser request.
 * @member {string} password Sets the password of the account.
 * 
 * @member {date} [expiryTime] Sets the time at which the account should
 * expire. If omitted, the default is 1 day from the current time.
 * 
 */
export interface NodeUpdateUserParameter {
    password: string;
    expiryTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the NodeRebootParameter class.
 * @constructor
 * Parameters for a ComputeNodeOperations.Reboot request.
 * @member {string} [nodeRebootOption] Sets when to reboot the compute node
 * and what to do with currently running tasks. The default value is requeue.
 * Possible values include: 'requeue', 'terminate', 'taskcompletion',
 * 'retaineddata'
 * 
 */
export interface NodeRebootParameter {
    nodeRebootOption?: string;
}

/**
 * @class
 * Initializes a new instance of the NodeReimageParameter class.
 * @constructor
 * Parameters for a ComputeNodeOperations.Reimage request.
 * @member {string} [nodeReimageOption] Sets when to reimage the compute node
 * and what to do with currently running tasks. The default value is requeue.
 * Possible values include: 'requeue', 'terminate', 'taskcompletion',
 * 'retaineddata'
 * 
 */
export interface NodeReimageParameter {
    nodeReimageOption?: string;
}

/**
 * @class
 * Initializes a new instance of the NodeDisableSchedulingParameter class.
 * @constructor
 * Parameters for a ComputeNodeOperations.DisableScheduling request.
 * @member {string} [nodeDisableSchedulingOption] Gets or sets what to do with
 * currently running tasks when disable task scheduling on the compute node.
 * The default value is requeue. Possible values include: 'requeue',
 * 'terminate', 'taskcompletion'
 * 
 */
export interface NodeDisableSchedulingParameter {
    nodeDisableSchedulingOption?: string;
}

/**
 * @class
 * Initializes a new instance of the NodeRemoveParameter class.
 * @constructor
 * Parameters for a ComputeNodeOperations.Remove request.
 * @member {array} nodeList Sets a list containing the id of the compute nodes
 * to be removed from the specified pool.
 * 
 * @member {moment.duration} [resizeTimeout] Sets the timeout for removal of
 * compute nodes to the pool. The default value is 10 minutes.
 * 
 * @member {string} [nodeDeallocationOption] Sets when compute nodes may be
 * removed from the pool. Possible values include: 'requeue', 'terminate',
 * 'taskcompletion', 'retaineddata'
 * 
 */
export interface NodeRemoveParameter {
    nodeList: string[];
    resizeTimeout?: moment.Duration;
    nodeDeallocationOption?: string;
}

/**
 * @class
 * Initializes a new instance of the ErrorMessage class.
 * @constructor
 * An error message received in an Azure Batch error response.
 * @member {string} [lang] Gets or sets the language code of the error message
 * 
 * @member {string} [value] Gets or sets the text of the message.
 * 
 */
export interface ErrorMessage {
    lang?: string;
    value?: string;
}

/**
 * @class
 * Initializes a new instance of the BatchErrorDetail class.
 * @constructor
 * An item of additional information included in an Azure Batch error response.
 * @member {string} [key] Gets or sets an identifier specifying the meaning of
 * the Value property.
 * 
 * @member {string} [value] Gets or sets the additional information included
 * with the error response.
 * 
 */
export interface BatchErrorDetail {
    key?: string;
    value?: string;
}

/**
 * @class
 * Initializes a new instance of the BatchError class.
 * @constructor
 * An error response received from the Azure Batch service.
 * @member {string} [code] Gets or sets an identifier for the error. Codes are
 * invariant and are intended to be consumed programmatically.
 * 
 * @member {object} [message] Gets or sets a message describing the error,
 * intended to be suitable for display in a user interface.
 * 
 * @member {string} [message.lang] Gets or sets the language code of the error
 * message
 * 
 * @member {string} [message.value] Gets or sets the text of the message.
 * 
 * @member {array} [values] Gets or sets a collection of key-value pairs
 * containing additional details about the error.
 * 
 */
export interface BatchError {
    code?: string;
    message?: ErrorMessage;
    values?: BatchErrorDetail[];
}

/**
 * @class
 * Initializes a new instance of the ApplicationListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ApplicationListOptions {
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ApplicationGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ApplicationGetOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolListPoolUsageMetricsOptions class.
 * @constructor
 * Additional parameters for the listPoolUsageMetrics operation.
 * @member {date} [startTime] The earliest time from which to include metrics.
 * This must be at least two and a half hours before the current time.
 * 
 * @member {date} [endTime] The latest time from which to include metrics.
 * This must be at least two hours before the current time.
 * 
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolListPoolUsageMetricsOptions {
    startTime?: Date;
    endTime?: Date;
    filter?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolGetAllPoolsLifetimeStatisticsOptions class.
 * @constructor
 * Additional parameters for the getAllPoolsLifetimeStatistics operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolGetAllPoolsLifetimeStatisticsOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobGetAllJobsLifetimeStatisticsOptions class.
 * @constructor
 * Additional parameters for the getAllJobsLifetimeStatistics operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobGetAllJobsLifetimeStatisticsOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the CertificateAddOptions class.
 * @constructor
 * Additional parameters for the add operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface CertificateAddOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the CertificateListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface CertificateListOptions {
    filter?: string;
    select?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the CertificateCancelDeletionOptions class.
 * @constructor
 * Additional parameters for the cancelDeletion operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface CertificateCancelDeletionOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the CertificateDeleteMethodOptions class.
 * @constructor
 * Additional parameters for the deleteMethod operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface CertificateDeleteMethodOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the CertificateGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface CertificateGetOptions {
    select?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileDeleteFromTaskOptions class.
 * @constructor
 * Additional parameters for the deleteFromTask operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface FileDeleteFromTaskOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileGetFromTaskOptions class.
 * @constructor
 * Additional parameters for the getFromTask operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ocpRange] Specifies the byte range to be retrieved. The
 * default is to retrieve the entire file.  The format is startRange-endRange.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface FileGetFromTaskOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ocpRange?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileGetNodeFilePropertiesFromTaskOptions class.
 * @constructor
 * Additional parameters for the getNodeFilePropertiesFromTask operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface FileGetNodeFilePropertiesFromTaskOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileDeleteFromComputeNodeOptions class.
 * @constructor
 * Additional parameters for the deleteFromComputeNode operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface FileDeleteFromComputeNodeOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileGetFromComputeNodeOptions class.
 * @constructor
 * Additional parameters for the getFromComputeNode operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ocpRange] Specifies the byte range to be retrieved. The
 * default is to retrieve the entire file.  The format is startRange-endRange.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface FileGetFromComputeNodeOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ocpRange?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileGetNodeFilePropertiesFromComputeNodeOptions class.
 * @constructor
 * Additional parameters for the getNodeFilePropertiesFromComputeNode
 * operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface FileGetNodeFilePropertiesFromComputeNodeOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileListFromTaskOptions class.
 * @constructor
 * Additional parameters for the listFromTask operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface FileListFromTaskOptions {
    filter?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileListFromComputeNodeOptions class.
 * @constructor
 * Additional parameters for the listFromComputeNode operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface FileListFromComputeNodeOptions {
    filter?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleExistsOptions class.
 * @constructor
 * Additional parameters for the exists operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleExistsOptions {
    select?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleDeleteMethodOptions class.
 * @constructor
 * Additional parameters for the deleteMethod operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleDeleteMethodOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleGetOptions {
    select?: string;
    expand?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobSchedulePatchOptions class.
 * @constructor
 * Additional parameters for the patch operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobSchedulePatchOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleUpdateOptions class.
 * @constructor
 * Additional parameters for the update operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleUpdateOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleDisableOptions class.
 * @constructor
 * Additional parameters for the disable operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleDisableOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleEnableOptions class.
 * @constructor
 * Additional parameters for the enable operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleEnableOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleTerminateOptions class.
 * @constructor
 * Additional parameters for the terminate operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobScheduleTerminateOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleAddOptions class.
 * @constructor
 * Additional parameters for the add operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobScheduleAddOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobScheduleListOptions {
    filter?: string;
    select?: string;
    expand?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobDeleteMethodOptions class.
 * @constructor
 * Additional parameters for the deleteMethod operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobDeleteMethodOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobGetOptions {
    select?: string;
    expand?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobPatchOptions class.
 * @constructor
 * Additional parameters for the patch operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobPatchOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobUpdateOptions class.
 * @constructor
 * Additional parameters for the update operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobUpdateOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobDisableOptions class.
 * @constructor
 * Additional parameters for the disable operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobDisableOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobEnableOptions class.
 * @constructor
 * Additional parameters for the enable operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobEnableOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobTerminateOptions class.
 * @constructor
 * Additional parameters for the terminate operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface JobTerminateOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobAddOptions class.
 * @constructor
 * Additional parameters for the add operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobAddOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobListOptions {
    filter?: string;
    select?: string;
    expand?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobListFromJobScheduleOptions class.
 * @constructor
 * Additional parameters for the listFromJobSchedule operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobListFromJobScheduleOptions {
    filter?: string;
    select?: string;
    expand?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobListPreparationAndReleaseTaskStatusOptions class.
 * @constructor
 * Additional parameters for the listPreparationAndReleaseTaskStatus operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobListPreparationAndReleaseTaskStatusOptions {
    filter?: string;
    select?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolAddOptions class.
 * @constructor
 * Additional parameters for the add operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolAddOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolListOptions {
    filter?: string;
    select?: string;
    expand?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolDeleteMethodOptions class.
 * @constructor
 * Additional parameters for the deleteMethod operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolDeleteMethodOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolExistsOptions class.
 * @constructor
 * Additional parameters for the exists operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolExistsOptions {
    select?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolGetOptions {
    select?: string;
    expand?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolPatchOptions class.
 * @constructor
 * Additional parameters for the patch operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolPatchOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolDisableAutoScaleOptions class.
 * @constructor
 * Additional parameters for the disableAutoScale operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolDisableAutoScaleOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolEnableAutoScaleOptions class.
 * @constructor
 * Additional parameters for the enableAutoScale operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolEnableAutoScaleOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolEvaluateAutoScaleOptions class.
 * @constructor
 * Additional parameters for the evaluateAutoScale operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolEvaluateAutoScaleOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolResizeOptions class.
 * @constructor
 * Additional parameters for the resize operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolResizeOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolStopResizeOptions class.
 * @constructor
 * Additional parameters for the stopResize operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolStopResizeOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolUpdatePropertiesOptions class.
 * @constructor
 * Additional parameters for the updateProperties operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolUpdatePropertiesOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolUpgradeOSOptions class.
 * @constructor
 * Additional parameters for the upgradeOS operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolUpgradeOSOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolRemoveNodesOptions class.
 * @constructor
 * Additional parameters for the removeNodes operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface PoolRemoveNodesOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskAddOptions class.
 * @constructor
 * Additional parameters for the add operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface TaskAddOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface TaskListOptions {
    filter?: string;
    select?: string;
    expand?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskDeleteMethodOptions class.
 * @constructor
 * Additional parameters for the deleteMethod operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface TaskDeleteMethodOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {string} [expand] Sets an OData $expand clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface TaskGetOptions {
    select?: string;
    expand?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskUpdateOptions class.
 * @constructor
 * Additional parameters for the update operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface TaskUpdateOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskListSubtasksOptions class.
 * @constructor
 * Additional parameters for the listSubtasks operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface TaskListSubtasksOptions {
    select?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskTerminateOptions class.
 * @constructor
 * Additional parameters for the terminate operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 * @member {string} [ifMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag is an exact match as
 * specified.
 * 
 * @member {string} [ifNoneMatch] An ETag is specified. Specify this header to
 * perform the operation only if the resource's ETag does not match the
 * specified ETag.
 * 
 * @member {date} [ifModifiedSince] Specify this header to perform the
 * operation only if the resource has been modified since the specified
 * date/time.
 * 
 * @member {date} [ifUnmodifiedSince] Specify this header to perform the
 * operation only if the resource has not been modified since the specified
 * date/time.
 * 
 */
export interface TaskTerminateOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
    ifMatch?: string;
    ifNoneMatch?: string;
    ifModifiedSince?: Date;
    ifUnmodifiedSince?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeAddUserOptions class.
 * @constructor
 * Additional parameters for the addUser operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeAddUserOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeDeleteUserOptions class.
 * @constructor
 * Additional parameters for the deleteUser operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeDeleteUserOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeUpdateUserOptions class.
 * @constructor
 * Additional parameters for the updateUser operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeUpdateUserOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeGetOptions class.
 * @constructor
 * Additional parameters for the get operation.
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeGetOptions {
    select?: string;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeRebootOptions class.
 * @constructor
 * Additional parameters for the reboot operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeRebootOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeReimageOptions class.
 * @constructor
 * Additional parameters for the reimage operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeReimageOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeDisableSchedulingOptions class.
 * @constructor
 * Additional parameters for the disableScheduling operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeDisableSchedulingOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeEnableSchedulingOptions class.
 * @constructor
 * Additional parameters for the enableScheduling operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeEnableSchedulingOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeGetRemoteDesktopOptions class.
 * @constructor
 * Additional parameters for the getRemoteDesktop operation.
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeGetRemoteDesktopOptions {
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeListOptions class.
 * @constructor
 * Additional parameters for the list operation.
 * @member {string} [filter] Sets an OData $filter clause.
 * 
 * @member {string} [select] Sets an OData $select clause.
 * 
 * @member {number} [maxResults] Sets the maximum number of items to return in
 * the response.
 * 
 * @member {number} [timeout] Sets the maximum time that the server can spend
 * processing the request, in seconds. The default is 30 seconds.
 * 
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeListOptions {
    filter?: string;
    select?: string;
    maxResults?: number;
    timeout?: number;
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ApplicationListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ApplicationListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolListPoolUsageMetricsNextOptions class.
 * @constructor
 * Additional parameters for the listPoolUsageMetricsNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolListPoolUsageMetricsNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the CertificateListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface CertificateListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileListFromTaskNextOptions class.
 * @constructor
 * Additional parameters for the listFromTaskNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface FileListFromTaskNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the FileListFromComputeNodeNextOptions class.
 * @constructor
 * Additional parameters for the listFromComputeNodeNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface FileListFromComputeNodeNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobScheduleListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobScheduleListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobListFromJobScheduleNextOptions class.
 * @constructor
 * Additional parameters for the listFromJobScheduleNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobListFromJobScheduleNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the JobListPreparationAndReleaseTaskStatusNextOptions class.
 * @constructor
 * Additional parameters for the listPreparationAndReleaseTaskStatusNext
 * operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface JobListPreparationAndReleaseTaskStatusNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the PoolListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface PoolListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the TaskListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface TaskListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ComputeNodeListNextOptions class.
 * @constructor
 * Additional parameters for the listNext operation.
 * @member {string} [clientRequestId] Caller generated request identity, in
 * the form of a GUID with no decoration such as curly braces e.g.
 * 9C4D50EE-2D56-4CD3-8152-34347DC9F2B0.
 * 
 * @member {boolean} [returnClientRequestId] Specifies if the server should
 * return the client-request-id identifier in the response.
 * 
 * @member {date} [ocpDate] The time the request was issued. If not specified,
 * this header will be automatically populated with the current system clock
 * time.
 * 
 */
export interface ComputeNodeListNextOptions {
    clientRequestId?: string;
    returnClientRequestId?: boolean;
    ocpDate?: Date;
}
