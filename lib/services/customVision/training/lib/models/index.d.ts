/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

import * as moment from "moment";


/**
 * @class
 * Initializes a new instance of the Domain class.
 * @constructor
 * @property {uuid} [id]
 * @property {string} [name]
 * @property {string} [type] Possible values include: 'Classification',
 * 'ObjectDetection'
 * @property {boolean} [exportable]
 * @property {boolean} [enabled]
 */
export interface Domain {
  readonly id?: string;
  readonly name?: string;
  readonly type?: string;
  readonly exportable?: boolean;
  readonly enabled?: boolean;
}

/**
 * @class
 * Initializes a new instance of the ImageTagCreateEntry class.
 * @constructor
 * Entry associating a tag to an image.
 *
 * @property {uuid} [imageId] Id of the image.
 * @property {uuid} [tagId] Id of the tag.
 */
export interface ImageTagCreateEntry {
  imageId?: string;
  tagId?: string;
}

/**
 * @class
 * Initializes a new instance of the ImageTagCreateBatch class.
 * @constructor
 * Batch of image tags.
 *
 * @property {array} [tags] Image Tag entries to include in this batch.
 */
export interface ImageTagCreateBatch {
  tags?: ImageTagCreateEntry[];
}

/**
 * @class
 * Initializes a new instance of the ImageTagCreateSummary class.
 * @constructor
 * @property {array} [created]
 * @property {array} [duplicated]
 * @property {array} [exceeded]
 */
export interface ImageTagCreateSummary {
  created?: ImageTagCreateEntry[];
  duplicated?: ImageTagCreateEntry[];
  exceeded?: ImageTagCreateEntry[];
}

/**
 * @class
 * Initializes a new instance of the ImageRegionCreateEntry class.
 * @constructor
 * Entry associating a region to an image.
 *
 * @property {uuid} [imageId] Id of the image.
 * @property {uuid} [tagId] Id of the tag associated with this region.
 * @property {number} [left]
 * @property {number} [top]
 * @property {number} [width]
 * @property {number} [height]
 */
export interface ImageRegionCreateEntry {
  imageId?: string;
  tagId?: string;
  left?: number;
  top?: number;
  width?: number;
  height?: number;
}

/**
 * @class
 * Initializes a new instance of the ImageRegionCreateBatch class.
 * @constructor
 * Batch of image region information to create.
 *
 * @property {array} [regions]
 */
export interface ImageRegionCreateBatch {
  regions?: ImageRegionCreateEntry[];
}

/**
 * @class
 * Initializes a new instance of the ImageRegionCreateResult class.
 * @constructor
 * @property {uuid} [imageId]
 * @property {uuid} [regionId]
 * @property {string} [tagName]
 * @property {date} [created]
 * @property {uuid} [tagId] Id of the tag associated with this region.
 * @property {number} [left]
 * @property {number} [top]
 * @property {number} [width]
 * @property {number} [height]
 */
export interface ImageRegionCreateResult {
  readonly imageId?: string;
  readonly regionId?: string;
  readonly tagName?: string;
  readonly created?: Date;
  tagId?: string;
  left?: number;
  top?: number;
  width?: number;
  height?: number;
}

/**
 * @class
 * Initializes a new instance of the ImageRegionCreateSummary class.
 * @constructor
 * @property {array} [created]
 * @property {array} [duplicated]
 * @property {array} [exceeded]
 */
export interface ImageRegionCreateSummary {
  created?: ImageRegionCreateResult[];
  duplicated?: ImageRegionCreateEntry[];
  exceeded?: ImageRegionCreateEntry[];
}

/**
 * @class
 * Initializes a new instance of the ImageTag class.
 * @constructor
 * @property {uuid} [tagId]
 * @property {string} [tagName]
 * @property {date} [created]
 */
export interface ImageTag {
  readonly tagId?: string;
  readonly tagName?: string;
  readonly created?: Date;
}

/**
 * @class
 * Initializes a new instance of the ImageRegion class.
 * @constructor
 * @property {uuid} [regionId]
 * @property {string} [tagName]
 * @property {date} [created]
 * @property {uuid} [tagId] Id of the tag associated with this region.
 * @property {number} [left]
 * @property {number} [top]
 * @property {number} [width]
 * @property {number} [height]
 */
export interface ImageRegion {
  readonly regionId?: string;
  readonly tagName?: string;
  readonly created?: Date;
  tagId?: string;
  left?: number;
  top?: number;
  width?: number;
  height?: number;
}

/**
 * @class
 * Initializes a new instance of the Image class.
 * @constructor
 * Image model to be sent as JSON.
 *
 * @property {uuid} [id] Id of the image.
 * @property {date} [created] Date the image was created.
 * @property {number} [width] Width of the image.
 * @property {number} [height] Height of the image.
 * @property {string} [resizedImageUri] The URI to the (resized) image used for
 * training.
 * @property {string} [thumbnailUri] The URI to the thumbnail of the original
 * image.
 * @property {string} [originalImageUri] The URI to the original uploaded
 * image.
 * @property {array} [tags] Tags associated with this image.
 * @property {array} [regions] Regions associated with this image.
 */
export interface Image {
  readonly id?: string;
  readonly created?: Date;
  readonly width?: number;
  readonly height?: number;
  readonly resizedImageUri?: string;
  readonly thumbnailUri?: string;
  readonly originalImageUri?: string;
  readonly tags?: ImageTag[];
  readonly regions?: ImageRegion[];
}

/**
 * @class
 * Initializes a new instance of the ImageCreateResult class.
 * @constructor
 * @property {string} [sourceUrl] Source URL of the image.
 * @property {string} [status] Status of the image creation. Possible values
 * include: 'OK', 'OKDuplicate', 'ErrorSource', 'ErrorImageFormat',
 * 'ErrorImageSize', 'ErrorStorage', 'ErrorLimitExceed', 'ErrorTagLimitExceed',
 * 'ErrorRegionLimitExceed', 'ErrorUnknown',
 * 'ErrorNegativeAndRegularTagOnSameImage'
 * @property {object} [image] The image.
 * @property {uuid} [image.id] Id of the image.
 * @property {date} [image.created] Date the image was created.
 * @property {number} [image.width] Width of the image.
 * @property {number} [image.height] Height of the image.
 * @property {string} [image.resizedImageUri] The URI to the (resized) image
 * used for training.
 * @property {string} [image.thumbnailUri] The URI to the thumbnail of the
 * original image.
 * @property {string} [image.originalImageUri] The URI to the original uploaded
 * image.
 * @property {array} [image.tags] Tags associated with this image.
 * @property {array} [image.regions] Regions associated with this image.
 */
export interface ImageCreateResult {
  readonly sourceUrl?: string;
  readonly status?: string;
  readonly image?: Image;
}

/**
 * @class
 * Initializes a new instance of the ImageCreateSummary class.
 * @constructor
 * @property {boolean} [isBatchSuccessful] True if all of the images in the
 * batch were created successfully, otherwise false.
 * @property {array} [images] List of the image creation results.
 */
export interface ImageCreateSummary {
  readonly isBatchSuccessful?: boolean;
  readonly images?: ImageCreateResult[];
}

/**
 * @class
 * Initializes a new instance of the Region class.
 * @constructor
 * @property {uuid} [tagId] Id of the tag associated with this region.
 * @property {number} [left]
 * @property {number} [top]
 * @property {number} [width]
 * @property {number} [height]
 */
export interface Region {
  tagId?: string;
  left?: number;
  top?: number;
  width?: number;
  height?: number;
}

/**
 * @class
 * Initializes a new instance of the ImageFileCreateEntry class.
 * @constructor
 * @property {string} [name]
 * @property {buffer} [contents]
 * @property {array} [tagIds]
 * @property {array} [regions]
 */
export interface ImageFileCreateEntry {
  name?: string;
  contents?: Buffer;
  tagIds?: string[];
  regions?: Region[];
}

/**
 * @class
 * Initializes a new instance of the ImageFileCreateBatch class.
 * @constructor
 * @property {array} [images]
 * @property {array} [tagIds]
 */
export interface ImageFileCreateBatch {
  images?: ImageFileCreateEntry[];
  tagIds?: string[];
}

/**
 * @class
 * Initializes a new instance of the ImageUrlCreateEntry class.
 * @constructor
 * @property {string} [url]
 * @property {array} [tagIds]
 * @property {array} [regions]
 */
export interface ImageUrlCreateEntry {
  url?: string;
  tagIds?: string[];
  regions?: Region[];
}

/**
 * @class
 * Initializes a new instance of the ImageUrlCreateBatch class.
 * @constructor
 * @property {array} [images]
 * @property {array} [tagIds]
 */
export interface ImageUrlCreateBatch {
  images?: ImageUrlCreateEntry[];
  tagIds?: string[];
}

/**
 * @class
 * Initializes a new instance of the ImageIdCreateEntry class.
 * @constructor
 * @property {uuid} [id]
 * @property {array} [tagIds]
 * @property {array} [regions]
 */
export interface ImageIdCreateEntry {
  id?: string;
  tagIds?: string[];
  regions?: Region[];
}

/**
 * @class
 * Initializes a new instance of the ImageIdCreateBatch class.
 * @constructor
 * @property {array} [images]
 * @property {array} [tagIds]
 */
export interface ImageIdCreateBatch {
  images?: ImageIdCreateEntry[];
  tagIds?: string[];
}

/**
 * @class
 * Initializes a new instance of the BoundingBox class.
 * @constructor
 * @property {number} [left]
 * @property {number} [top]
 * @property {number} [width]
 * @property {number} [height]
 */
export interface BoundingBox {
  left?: number;
  top?: number;
  width?: number;
  height?: number;
}

/**
 * @class
 * Initializes a new instance of the RegionProposal class.
 * @constructor
 * @property {number} [confidence]
 * @property {object} [boundingBox]
 * @property {number} [boundingBox.left]
 * @property {number} [boundingBox.top]
 * @property {number} [boundingBox.width]
 * @property {number} [boundingBox.height]
 */
export interface RegionProposal {
  readonly confidence?: number;
  readonly boundingBox?: BoundingBox;
}

/**
 * @class
 * Initializes a new instance of the ImageRegionProposal class.
 * @constructor
 * @property {uuid} [projectId]
 * @property {uuid} [imageId]
 * @property {array} [proposals]
 */
export interface ImageRegionProposal {
  readonly projectId?: string;
  readonly imageId?: string;
  readonly proposals?: RegionProposal[];
}

/**
 * @class
 * Initializes a new instance of the ImageUrl class.
 * @constructor
 * @property {string} [url]
 */
export interface ImageUrl {
  url?: string;
}

/**
 * @class
 * Initializes a new instance of the Prediction class.
 * @constructor
 * @property {number} [probability]
 * @property {uuid} [tagId]
 * @property {string} [tagName]
 * @property {object} [boundingBox]
 * @property {number} [boundingBox.left]
 * @property {number} [boundingBox.top]
 * @property {number} [boundingBox.width]
 * @property {number} [boundingBox.height]
 */
export interface Prediction {
  readonly probability?: number;
  readonly tagId?: string;
  readonly tagName?: string;
  readonly boundingBox?: BoundingBox;
}

/**
 * @class
 * Initializes a new instance of the ImagePrediction class.
 * @constructor
 * @property {uuid} [id]
 * @property {uuid} [project]
 * @property {uuid} [iteration]
 * @property {date} [created]
 * @property {array} [predictions]
 */
export interface ImagePrediction {
  readonly id?: string;
  readonly project?: string;
  readonly iteration?: string;
  readonly created?: Date;
  readonly predictions?: Prediction[];
}

/**
 * @class
 * Initializes a new instance of the PredictionQueryTag class.
 * @constructor
 * @property {uuid} [id]
 * @property {number} [minThreshold]
 * @property {number} [maxThreshold]
 */
export interface PredictionQueryTag {
  readonly id?: string;
  readonly minThreshold?: number;
  readonly maxThreshold?: number;
}

/**
 * @class
 * Initializes a new instance of the PredictionQueryToken class.
 * @constructor
 * @property {string} [session]
 * @property {string} [continuation]
 * @property {number} [maxCount]
 * @property {string} [orderBy] Possible values include: 'Newest', 'Oldest',
 * 'Suggested'
 * @property {array} [tags]
 * @property {uuid} [iterationId]
 * @property {date} [startTime]
 * @property {date} [endTime]
 * @property {string} [application]
 */
export interface PredictionQueryToken {
  session?: string;
  continuation?: string;
  maxCount?: number;
  orderBy?: string;
  tags?: PredictionQueryTag[];
  iterationId?: string;
  startTime?: Date;
  endTime?: Date;
  application?: string;
}

/**
 * @class
 * Initializes a new instance of the StoredImagePrediction class.
 * @constructor
 * result of an image classification request.
 *
 * @property {string} [resizedImageUri] The URI to the (resized) prediction
 * image.
 * @property {string} [thumbnailUri] The URI to the thumbnail of the original
 * prediction image.
 * @property {string} [originalImageUri] The URI to the original prediction
 * image.
 * @property {uuid} [domain] Domain used for the prediction.
 * @property {uuid} [id]
 * @property {uuid} [project]
 * @property {uuid} [iteration]
 * @property {date} [created]
 * @property {array} [predictions]
 */
export interface StoredImagePrediction {
  readonly resizedImageUri?: string;
  readonly thumbnailUri?: string;
  readonly originalImageUri?: string;
  readonly domain?: string;
  readonly id?: string;
  readonly project?: string;
  readonly iteration?: string;
  readonly created?: Date;
  readonly predictions?: Prediction[];
}

/**
 * @class
 * Initializes a new instance of the PredictionQueryResult class.
 * @constructor
 * @property {object} [token]
 * @property {string} [token.session]
 * @property {string} [token.continuation]
 * @property {number} [token.maxCount]
 * @property {string} [token.orderBy] Possible values include: 'Newest',
 * 'Oldest', 'Suggested'
 * @property {array} [token.tags]
 * @property {uuid} [token.iterationId]
 * @property {date} [token.startTime]
 * @property {date} [token.endTime]
 * @property {string} [token.application]
 * @property {array} [results]
 */
export interface PredictionQueryResult {
  readonly token?: PredictionQueryToken;
  readonly results?: StoredImagePrediction[];
}

/**
 * @class
 * Initializes a new instance of the TagPerformance class.
 * @constructor
 * Represents performance data for a particular tag in a trained iteration.
 *
 * @property {uuid} [id]
 * @property {string} [name]
 * @property {number} [precision] Gets the precision.
 * @property {number} [precisionStdDeviation] Gets the standard deviation for
 * the precision.
 * @property {number} [recall] Gets the recall.
 * @property {number} [recallStdDeviation] Gets the standard deviation for the
 * recall.
 * @property {number} [averagePrecision] Gets the average precision when
 * applicable.
 */
export interface TagPerformance {
  readonly id?: string;
  readonly name?: string;
  readonly precision?: number;
  readonly precisionStdDeviation?: number;
  readonly recall?: number;
  readonly recallStdDeviation?: number;
  readonly averagePrecision?: number;
}

/**
 * @class
 * Initializes a new instance of the IterationPerformance class.
 * @constructor
 * Represents the detailed performance data for a trained iteration.
 *
 * @property {array} [perTagPerformance] Gets the per-tag performance details
 * for this iteration.
 * @property {number} [precision] Gets the precision.
 * @property {number} [precisionStdDeviation] Gets the standard deviation for
 * the precision.
 * @property {number} [recall] Gets the recall.
 * @property {number} [recallStdDeviation] Gets the standard deviation for the
 * recall.
 * @property {number} [averagePrecision] Gets the average precision when
 * applicable.
 */
export interface IterationPerformance {
  readonly perTagPerformance?: TagPerformance[];
  readonly precision?: number;
  readonly precisionStdDeviation?: number;
  readonly recall?: number;
  readonly recallStdDeviation?: number;
  readonly averagePrecision?: number;
}

/**
 * @class
 * Initializes a new instance of the ImagePerformance class.
 * @constructor
 * Image performance model.
 *
 * @property {array} [predictions]
 * @property {uuid} [id]
 * @property {date} [created]
 * @property {number} [width]
 * @property {number} [height]
 * @property {string} [imageUri]
 * @property {string} [thumbnailUri]
 * @property {array} [tags]
 * @property {array} [regions]
 */
export interface ImagePerformance {
  readonly predictions?: Prediction[];
  readonly id?: string;
  readonly created?: Date;
  readonly width?: number;
  readonly height?: number;
  readonly imageUri?: string;
  readonly thumbnailUri?: string;
  readonly tags?: ImageTag[];
  readonly regions?: ImageRegion[];
}

/**
 * @class
 * Initializes a new instance of the ProjectSettings class.
 * @constructor
 * Represents settings associated with a project.
 *
 * @property {uuid} [domainId] Gets or sets the id of the Domain to use with
 * this project.
 * @property {string} [classificationType] Gets or sets the classification type
 * of the project. Possible values include: 'Multiclass', 'Multilabel'
 */
export interface ProjectSettings {
  domainId?: string;
  classificationType?: string;
}

/**
 * @class
 * Initializes a new instance of the Project class.
 * @constructor
 * Represents a project.
 *
 * @property {uuid} [id] Gets The project id.
 * @property {string} [name] Gets or sets the name of the project.
 * @property {string} [description] Gets or sets the description of the
 * project.
 * @property {object} [settings] Gets or sets the project settings.
 * @property {uuid} [settings.domainId] Gets or sets the id of the Domain to
 * use with this project.
 * @property {string} [settings.classificationType] Gets or sets the
 * classification type of the project. Possible values include: 'Multiclass',
 * 'Multilabel'
 * @property {date} [created] Gets the date this project was created.
 * @property {date} [lastModified] Gets the date this project was last modifed.
 * @property {string} [thumbnailUri] Gets the thumbnail url representing the
 * project.
 */
export interface Project {
  readonly id?: string;
  name?: string;
  description?: string;
  settings?: ProjectSettings;
  readonly created?: Date;
  readonly lastModified?: Date;
  readonly thumbnailUri?: string;
}

/**
 * @class
 * Initializes a new instance of the Iteration class.
 * @constructor
 * Iteration model to be sent over JSON.
 *
 * @property {uuid} [id] Gets the id of the iteration.
 * @property {string} [name] Gets or sets the name of the iteration.
 * @property {boolean} [isDefault] Gets or sets a value indicating whether the
 * iteration is the default iteration for the project.
 * @property {string} [status] Gets the current iteration status.
 * @property {date} [created] Gets the time this iteration was completed.
 * @property {date} [lastModified] Gets the time this iteration was last
 * modified.
 * @property {date} [trainedAt] Gets the time this iteration was last modified.
 * @property {uuid} [projectId] Gets The project id. of the iteration.
 * @property {boolean} [exportable] Whether the iteration can be exported to
 * another format for download.
 * @property {uuid} [domainId] Get or sets a guid of the domain the iteration
 * has been trained on.
 * @property {string} [classificationType] Gets the classification type of the
 * project. Possible values include: 'Multiclass', 'Multilabel'
 */
export interface Iteration {
  readonly id?: string;
  name?: string;
  isDefault?: boolean;
  readonly status?: string;
  readonly created?: Date;
  readonly lastModified?: Date;
  readonly trainedAt?: Date;
  readonly projectId?: string;
  readonly exportable?: boolean;
  readonly domainId?: string;
  readonly classificationType?: string;
}

/**
 * @class
 * Initializes a new instance of the ExportModel class.
 * @constructor
 * @property {string} [platform] Platform of the export. Possible values
 * include: 'CoreML', 'TensorFlow', 'DockerFile', 'ONNX'
 * @property {string} [status] Status of the export. Possible values include:
 * 'Exporting', 'Failed', 'Done'
 * @property {string} [downloadUri] URI used to download the model.
 * @property {string} [flavor] Flavor of the export. Possible values include:
 * 'Linux', 'Windows', 'ONNX10', 'ONNX12'
 * @property {boolean} [newerVersionAvailable] Indicates an updated version of
 * the export package is available and should be re-exported for the latest
 * changes.
 */
export interface ExportModel {
  readonly platform?: string;
  readonly status?: string;
  readonly downloadUri?: string;
  readonly flavor?: string;
  readonly newerVersionAvailable?: boolean;
}

/**
 * @class
 * Initializes a new instance of the Tag class.
 * @constructor
 * Represents a Tag.
 *
 * @property {uuid} [id] Gets the Tag ID.
 * @property {string} [name] Gets or sets the name of the tag.
 * @property {string} [description] Gets or sets the description of the tag.
 * @property {string} [type] Gets or sets the type of the tag. Possible values
 * include: 'Regular', 'Negative'
 * @property {number} [imageCount] Gets the number of images with this tag.
 */
export interface Tag {
  readonly id?: string;
  name?: string;
  description?: string;
  type?: string;
  readonly imageCount?: number;
}
