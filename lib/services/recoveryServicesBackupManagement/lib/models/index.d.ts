/*
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for
 * license information.
 *
 * Code generated by Microsoft (R) AutoRest Code Generator.
 * Changes may cause incorrect behavior and will be lost if the code is
 * regenerated.
 */

import { BaseResource } from 'ms-rest-azure';
import { CloudError } from 'ms-rest-azure';

export { BaseResource } from 'ms-rest-azure';
export { CloudError } from 'ms-rest-azure';


/**
 * @class
 * Initializes a new instance of the Resource class.
 * @constructor
 * Base for all resources.
 *
 * @member {string} [id] Resource ID represents the complete path to the
 * resource.
 *
 * @member {string} [name] Resource name associated with the resource.
 *
 * @member {string} [type] Resource type represents the complete path of the
 * form Namespace/ResourceType/ResourceType/...
 *
 * @member {string} [location] Resource location.
 *
 * @member {object} [tags] Resource tags.
 *
 * @member {string} [eTag] Optional ETag.
 *
 */
export interface Resource extends BaseResource {
  id?: string;
  name?: string;
  type?: string;
  location?: string;
  tags?: { [propertyName: string]: string };
  eTag?: string;
}

/**
 * @class
 * Initializes a new instance of the Job class.
 * @constructor
 * Defines workload-agnostic properties for a job.
 *
 * @member {string} [entityFriendlyName] The friendly name of the entity on
 * which the current job is executing.
 *
 * @member {string} [backupManagementType] The backup management type for the
 * current job. Possible values include: 'Invalid', 'AzureIaasVM', 'MAB',
 * 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [operation] The operation name.
 *
 * @member {string} [status] The job status.
 *
 * @member {date} [startTime] The start time.
 *
 * @member {date} [endTime] The end time.
 *
 * @member {string} [activityId] ActivityId of job.
 *
 * @member {string} jobType Polymorphic Discriminator
 *
 */
export interface Job {
  entityFriendlyName?: string;
  backupManagementType?: string;
  operation?: string;
  status?: string;
  startTime?: Date;
  endTime?: Date;
  activityId?: string;
  jobType: string;
}

/**
 * @class
 * Initializes a new instance of the JobResource class.
 * @constructor
 * Defines the workload-agnostic properties for a job.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.entityFriendlyName] The friendly name of the
 * entity on which the current job is executing.
 *
 * @member {string} [properties.backupManagementType] The backup management
 * type for the current job. Possible values include: 'Invalid', 'AzureIaasVM',
 * 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [properties.operation] The operation name.
 *
 * @member {string} [properties.status] The job status.
 *
 * @member {date} [properties.startTime] The start time.
 *
 * @member {date} [properties.endTime] The end time.
 *
 * @member {string} [properties.activityId] ActivityId of job.
 *
 * @member {string} [properties.jobType] Polymorphic Discriminator
 *
 */
export interface JobResource extends Resource {
  properties?: Job;
}

/**
 * @class
 * Initializes a new instance of the ResourceList class.
 * @constructor
 * Base for all lists of resources.
 *
 * @member {string} [nextLink] The URI to GET the next page of resources. Call
 * ListNext() gets the next page of resources.
 *
 */
export interface ResourceList {
  nextLink?: string;
}

/**
 * @class
 * Initializes a new instance of the JobResourceList class.
 * @constructor
 * List of Job resources.
 *
 * @member {array} [value] List of resources.
 *
 */
export interface JobResourceList extends ResourceList {
  value?: JobResource[];
}

/**
 * @class
 * Initializes a new instance of the BackupEngineBase class.
 * @constructor
 * The base backup engine class. All workload-specific backup engines derive
 * from this class.
 *
 * @member {string} [friendlyName] The friendly name of the backup engine.
 *
 * @member {string} [backupManagementType] The type of backup management
 * associated with the backup engine. Possible values include: 'Invalid',
 * 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [registrationStatus] The status of the backup engine
 * registration with the Recovery Services vault.
 *
 * @member {string} [healthStatus] The backup status of the backup engine.
 *
 * @member {boolean} [canReRegister] The flag indicating whether the backup
 * engine be registered again, once the engine has been initially registered.
 *
 * @member {string} [backupEngineId] The ID of the backup engine.
 *
 * @member {string} backupEngineType Polymorphic Discriminator
 *
 */
export interface BackupEngineBase {
  friendlyName?: string;
  backupManagementType?: string;
  registrationStatus?: string;
  healthStatus?: string;
  canReRegister?: boolean;
  backupEngineId?: string;
  backupEngineType: string;
}

/**
 * @class
 * Initializes a new instance of the BMSBackupEngineQueryObject class.
 * @constructor
 * The query parameters used to GET the list of backup engines.
 *
 * @member {string} [backupManagementType] The backup management type
 * associated with the backup engine. Possible values include: 'Invalid',
 * 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 */
export interface BMSBackupEngineQueryObject {
  backupManagementType?: string;
}

/**
 * @class
 * Initializes a new instance of the OperationStatusError class.
 * @constructor
 * Error information associated with the operation status call.
 *
 * @member {string} [code] The error code for the failed operation.
 *
 * @member {string} [message] The error message for the failed operation.
 *
 */
export interface OperationStatusError {
  code?: string;
  message?: string;
}

/**
 * @class
 * Initializes a new instance of the OperationStatusExtendedInfo class.
 * @constructor
 * The base class for additional information about the operation status.
 *
 * @member {string} objectType Polymorphic Discriminator
 *
 */
export interface OperationStatusExtendedInfo {
  objectType: string;
}

/**
 * @class
 * Initializes a new instance of the OperationStatus class.
 * @constructor
 * Operation status.
 *
 * @member {string} [id] ID of the operation.
 *
 * @member {string} [name] Name of the operation.
 *
 * @member {string} [status] Operation status. Possible values include:
 * 'Invalid', 'InProgress', 'Succeeded', 'Failed', 'Canceled'
 *
 * @member {date} [startTime] The operation start time. The format of the time
 * is ISO-8601.
 *
 * @member {date} [endTime] The operation end time. The format of the time is
 * ISO-8601.
 *
 * @member {object} [error] Error information related to this operation.
 *
 * @member {string} [error.code] The error code for the failed operation.
 *
 * @member {string} [error.message] The error message for the failed operation.
 *
 * @member {object} [properties] Additional information associated with this
 * operation.
 *
 * @member {string} [properties.objectType] Polymorphic Discriminator
 *
 */
export interface OperationStatus {
  id?: string;
  name?: string;
  status?: string;
  startTime?: Date;
  endTime?: Date;
  error?: OperationStatusError;
  properties?: OperationStatusExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the OperationWorkerResponse class.
 * @constructor
 * The base class for operation result responses.
 *
 * @member {string} [statusCode] The HTTP status code of the operation.
 * Possible values include: 'Continue', 'SwitchingProtocols', 'OK', 'Created',
 * 'Accepted', 'NonAuthoritativeInformation', 'NoContent', 'ResetContent',
 * 'PartialContent', 'MultipleChoices', 'Ambiguous', 'MovedPermanently',
 * 'Moved', 'Found', 'Redirect', 'SeeOther', 'RedirectMethod', 'NotModified',
 * 'UseProxy', 'Unused', 'TemporaryRedirect', 'RedirectKeepVerb', 'BadRequest',
 * 'Unauthorized', 'PaymentRequired', 'Forbidden', 'NotFound',
 * 'MethodNotAllowed', 'NotAcceptable', 'ProxyAuthenticationRequired',
 * 'RequestTimeout', 'Conflict', 'Gone', 'LengthRequired',
 * 'PreconditionFailed', 'RequestEntityTooLarge', 'RequestUriTooLong',
 * 'UnsupportedMediaType', 'RequestedRangeNotSatisfiable', 'ExpectationFailed',
 * 'UpgradeRequired', 'InternalServerError', 'NotImplemented', 'BadGateway',
 * 'ServiceUnavailable', 'GatewayTimeout', 'HttpVersionNotSupported'
 *
 * @member {object} [headers] The HTTP headers associated with this operation.
 *
 */
export interface OperationWorkerResponse {
  statusCode?: string;
  headers?: { [propertyName: string]: string[] };
}

/**
 * @class
 * Initializes a new instance of the JobQueryObject class.
 * @constructor
 * The filters to list the jobs.
 *
 * @member {string} [status] Status of the job. Possible values include:
 * 'Invalid', 'InProgress', 'Completed', 'Failed', 'CompletedWithWarnings',
 * 'Cancelled', 'Cancelling'
 *
 * @member {string} [backupManagementType] Type of backup managmenent for the
 * job. Possible values include: 'Invalid', 'AzureIaasVM', 'MAB', 'DPM',
 * 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [operation] The type of operation. Possible values include:
 * 'Invalid', 'ConfigureBackup', 'Backup', 'Restore', 'DisableBackup',
 * 'DeleteBackupData'
 *
 * @member {string} [jobId] The ID of the job. Each jobID is unique.
 *
 * @member {date} [startTime] The time when the job starts. The value is in
 * UTC.
 *
 * @member {date} [endTime] The time when the job ends. The value is in UTC.
 *
 */
export interface JobQueryObject {
  status?: string;
  backupManagementType?: string;
  operation?: string;
  jobId?: string;
  startTime?: Date;
  endTime?: Date;
}

/**
 * @class
 * Initializes a new instance of the WorkloadProtectableItem class.
 * @constructor
 * The base class for backup item. Workload-specific backup items are derived
 * from this class.
 *
 * @member {string} [backupManagementType] The backup managemenent type.
 *
 * @member {string} [friendlyName] The friendly name of the backup item.
 *
 * @member {string} [protectionState] The state of the back up item. Possible
 * values include: 'Invalid', 'NotProtected', 'Protecting', 'Protected'
 *
 * @member {string} protectableItemType Polymorphic Discriminator
 *
 */
export interface WorkloadProtectableItem {
  backupManagementType?: string;
  friendlyName?: string;
  protectionState?: string;
  protectableItemType: string;
}

/**
 * @class
 * Initializes a new instance of the WorkloadProtectableItemResource class.
 * @constructor
 * The base class for the backup item. Workload-specific backup items are
 * derived from this class.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.backupManagementType] The backup managemenent
 * type.
 *
 * @member {string} [properties.friendlyName] The friendly name of the backup
 * item.
 *
 * @member {string} [properties.protectionState] The state of the back up item.
 * Possible values include: 'Invalid', 'NotProtected', 'Protecting',
 * 'Protected'
 *
 * @member {string} [properties.protectableItemType] Polymorphic Discriminator
 *
 */
export interface WorkloadProtectableItemResource extends Resource {
  properties?: WorkloadProtectableItem;
}

/**
 * @class
 * Initializes a new instance of the WorkloadProtectableItemResourceList class.
 * @constructor
 * The list of WorkloadProtectableItem resources.
 *
 * @member {array} [value] List of resources.
 *
 */
export interface WorkloadProtectableItemResourceList extends ResourceList {
  value?: WorkloadProtectableItemResource[];
}

/**
 * @class
 * Initializes a new instance of the OperationResultInfoBase class.
 * @constructor
 * The base class for operation result information.
 *
 * @member {string} objectType Polymorphic Discriminator
 *
 */
export interface OperationResultInfoBase {
  objectType: string;
}

/**
 * @class
 * Initializes a new instance of the ProtectedItem class.
 * @constructor
 * The base class for backup items.
 *
 * @member {string} [backupManagementType] The backup managemenent type
 * associated with the backup item. Possible values include: 'Invalid',
 * 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [workloadType] The workload type for this item. Possible
 * values include: 'Invalid', 'VM', 'FileFolder', 'AzureSqlDb', 'SQLDB',
 * 'Exchange', 'Sharepoint', 'DPMUnknown'
 *
 * @member {string} [sourceResourceId] The ID of the resource to be backed up.
 *
 * @member {string} [policyId] The ID of the backup policy associated with this
 * backup item.
 *
 * @member {date} [lastRecoveryPoint] The timestamp when the most recent backup
 * copy was created for this backup item.
 *
 * @member {string} protectedItemType Polymorphic Discriminator
 *
 */
export interface ProtectedItem {
  backupManagementType?: string;
  workloadType?: string;
  sourceResourceId?: string;
  policyId?: string;
  lastRecoveryPoint?: Date;
  protectedItemType: string;
}

/**
 * @class
 * Initializes a new instance of the ProtectedItemResource class.
 * @constructor
 * The base class for backup items.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.backupManagementType] The backup managemenent
 * type associated with the backup item. Possible values include: 'Invalid',
 * 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [properties.workloadType] The workload type for this item.
 * Possible values include: 'Invalid', 'VM', 'FileFolder', 'AzureSqlDb',
 * 'SQLDB', 'Exchange', 'Sharepoint', 'DPMUnknown'
 *
 * @member {string} [properties.sourceResourceId] The ID of the resource to be
 * backed up.
 *
 * @member {string} [properties.policyId] The ID of the backup policy
 * associated with this backup item.
 *
 * @member {date} [properties.lastRecoveryPoint] The timestamp when the most
 * recent backup copy was created for this backup item.
 *
 * @member {string} [properties.protectedItemType] Polymorphic Discriminator
 *
 */
export interface ProtectedItemResource extends Resource {
  properties?: ProtectedItem;
}

/**
 * @class
 * Initializes a new instance of the BMSPOQueryObject class.
 * @constructor
 * Filters the list of backup items.
 *
 * @member {string} [backupManagementType] The backup management type. Possible
 * values include: 'Invalid', 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer',
 * 'AzureSql'
 *
 * @member {string} [status] The backup status query parameter.
 *
 * @member {string} [friendlyName] The friendly name for the item.
 *
 */
export interface BMSPOQueryObject {
  backupManagementType?: string;
  status?: string;
  friendlyName?: string;
}

/**
 * @class
 * Initializes a new instance of the ProtectedItemResourceList class.
 * @constructor
 * The list of ProtectedItem resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface ProtectedItemResourceList extends ResourceList {
  value?: ProtectedItemResource[];
}

/**
 * @class
 * Initializes a new instance of the GetProtectedItemQueryObject class.
 * @constructor
 * This object filters the list of backup items.
 *
 * @member {string} [expand] Specifies if the additional information should be
 * provided for this item.
 *
 */
export interface GetProtectedItemQueryObject {
  expand?: string;
}

/**
 * @class
 * Initializes a new instance of the BackupRequest class.
 * @constructor
 * The base class for a backup request. Workload-specific backup requests are
 * derived from this class.
 *
 * @member {string} objectType Polymorphic Discriminator
 *
 */
export interface BackupRequest {
  objectType: string;
}

/**
 * @class
 * Initializes a new instance of the BackupRequestResource class.
 * @constructor
 * Base class for the backup request. Workload-specific backup requests are
 * derived from this class.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.objectType] Polymorphic Discriminator
 *
 */
export interface BackupRequestResource extends Resource {
  properties?: BackupRequest;
}

/**
 * @class
 * Initializes a new instance of the ProtectedItemQueryObject class.
 * @constructor
 * Filters the list of backup items.
 *
 * @member {string} [backupManagementType] The backup management type
 * associated with an item. Possible values include: 'Invalid', 'AzureIaasVM',
 * 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [itemType] The workload type associated with an item.
 * Possible values include: 'Invalid', 'VM', 'FileFolder', 'AzureSqlDb',
 * 'SQLDB', 'Exchange', 'Sharepoint', 'DPMUnknown'
 *
 * @member {string} [policyName] The name of the backup policy associated with
 * the item.
 *
 * @member {string} [containerName] The name of the container.
 *
 */
export interface ProtectedItemQueryObject {
  backupManagementType?: string;
  itemType?: string;
  policyName?: string;
  containerName?: string;
}

/**
 * @class
 * Initializes a new instance of the RecoveryPoint class.
 * @constructor
 * The base class for backup copies. Workload-specific backup copies are
 * derived from this class.
 *
 * @member {string} objectType Polymorphic Discriminator
 *
 */
export interface RecoveryPoint {
  objectType: string;
}

/**
 * @class
 * Initializes a new instance of the RecoveryPointResource class.
 * @constructor
 * The base class for backup copies. Workload-specific backup copies are
 * derived from this class.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.objectType] Polymorphic Discriminator
 *
 */
export interface RecoveryPointResource extends Resource {
  properties?: RecoveryPoint;
}

/**
 * @class
 * Initializes a new instance of the RecoveryPointResourceList class.
 * @constructor
 * The list of RecoveryPoint resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface RecoveryPointResourceList extends ResourceList {
  value?: RecoveryPointResource[];
}

/**
 * @class
 * Initializes a new instance of the ProtectionContainer class.
 * @constructor
 * The base class for a container with backup items. Containers with specific
 * workloads are derived from this class.
 *
 * @member {string} [friendlyName] Friendly name of the container.
 *
 * @member {string} [backupManagementType] The backup managemenent type for the
 * container. Possible values include: 'Invalid', 'AzureIaasVM', 'MAB', 'DPM',
 * 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [registrationStatus] The container's registration status
 * with the Recovery Services vault.
 *
 * @member {string} [healthStatus] The status of the container's health.
 *
 * @member {string} [containerType] The type assigned to the container. The
 * values to use for each of these propertes are:<br/> 1. Compute Azure VM is
 * Microsoft.Compute/virtualMachines<br/> 2. Classic Compute Azure VM is
 * Microsoft.ClassicCompute/virtualMachines<br/> 3. Windows machines (like
 * Azure Backup Server and DPM) is Windows<br/> 4. Azure SQL instance is
 * AzureSqlContainer.
 *
 * @member {string} protectableObjectType Polymorphic Discriminator
 *
 */
export interface ProtectionContainer {
  friendlyName?: string;
  backupManagementType?: string;
  registrationStatus?: string;
  healthStatus?: string;
  readonly containerType?: string;
  protectableObjectType: string;
}

/**
 * @class
 * Initializes a new instance of the ProtectionContainerResource class.
 * @constructor
 * Base class for a container with backup items. Containers with specific
 * workloads are derived from this class.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.friendlyName] Friendly name of the container.
 *
 * @member {string} [properties.backupManagementType] The backup managemenent
 * type for the container. Possible values include: 'Invalid', 'AzureIaasVM',
 * 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [properties.registrationStatus] The container's
 * registration status with the Recovery Services vault.
 *
 * @member {string} [properties.healthStatus] The status of the container's
 * health.
 *
 * @member {string} [properties.containerType] The type assigned to the
 * container. The values to use for each of these propertes are:<br/> 1.
 * Compute Azure VM is Microsoft.Compute/virtualMachines<br/> 2. Classic
 * Compute Azure VM is Microsoft.ClassicCompute/virtualMachines<br/> 3. Windows
 * machines (like Azure Backup Server and DPM) is Windows<br/> 4. Azure SQL
 * instance is AzureSqlContainer.
 *
 * @member {string} [properties.protectableObjectType] Polymorphic
 * Discriminator
 *
 */
export interface ProtectionContainerResource extends Resource {
  properties?: ProtectionContainer;
}

/**
 * @class
 * Initializes a new instance of the ProtectionContainerResourceList class.
 * @constructor
 * The list of ProtectionContainer resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface ProtectionContainerResourceList extends ResourceList {
  value?: ProtectionContainerResource[];
}

/**
 * @class
 * Initializes a new instance of the BMSRPQueryObject class.
 * @constructor
 * Filters the list of backup copies based on the property.
 *
 * @member {date} [startDate] Use backup copies created after this time.
 *
 * @member {date} [endDate] Use backup copies created before this time.
 *
 */
export interface BMSRPQueryObject {
  startDate?: Date;
  endDate?: Date;
}

/**
 * @class
 * Initializes a new instance of the ProtectionPolicy class.
 * @constructor
 * The base class for a backup policy. Workload-specific backup policies are
 * derived from this class.
 *
 * @member {number} [protectedItemsCount] The number of items associated with
 * this policy.
 *
 * @member {string} backupManagementType Polymorphic Discriminator
 *
 */
export interface ProtectionPolicy {
  protectedItemsCount?: number;
  backupManagementType: string;
}

/**
 * @class
 * Initializes a new instance of the ProtectionPolicyResource class.
 * @constructor
 * The base class for backup policy. Workload-specific backup policies are
 * derived from this class.
 *
 * @member {object} [properties]
 *
 * @member {number} [properties.protectedItemsCount] The number of items
 * associated with this policy.
 *
 * @member {string} [properties.backupManagementType] Polymorphic Discriminator
 *
 */
export interface ProtectionPolicyResource extends Resource {
  properties?: ProtectionPolicy;
}

/**
 * @class
 * Initializes a new instance of the ProtectionPolicyResourceList class.
 * @constructor
 * The list of ProtectionPolicy resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface ProtectionPolicyResourceList extends ResourceList {
  value?: ProtectionPolicyResource[];
}

/**
 * @class
 * Initializes a new instance of the BMSContainerQueryObject class.
 * @constructor
 * The query filters that can be used with the list containers API.
 *
 * @member {string} [backupManagementType] The backup management type for this
 * container. Possible values include: 'Invalid', 'AzureIaasVM', 'MAB', 'DPM',
 * 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [status] The status of the container's registration with
 * the Recovery Services vault.
 *
 * @member {string} [friendlyName] The friendly name of the container.
 *
 */
export interface BMSContainerQueryObject {
  backupManagementType?: string;
  status?: string;
  friendlyName?: string;
}

/**
 * @class
 * Initializes a new instance of the RestoreRequest class.
 * @constructor
 * The base class for restore requests. Workload-specific restore requests are
 * derived from this class.
 *
 * @member {string} objectType Polymorphic Discriminator
 *
 */
export interface RestoreRequest {
  objectType: string;
}

/**
 * @class
 * Initializes a new instance of the RestoreRequestResource class.
 * @constructor
 * The base class for a restore request. Workload-specific restore requests are
 * derived from this class.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.objectType] Polymorphic Discriminator
 *
 */
export interface RestoreRequestResource extends Resource {
  properties?: RestoreRequest;
}

/**
 * @class
 * Initializes a new instance of the ProtectionPolicyQueryObject class.
 * @constructor
 * This object filters the list of backup policies.
 *
 * @member {string} [backupManagementType] The backup management type
 * associated with the backup policy. Possible values include: 'Invalid',
 * 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 */
export interface ProtectionPolicyQueryObject {
  backupManagementType?: string;
}

/**
 * @class
 * Initializes a new instance of the ILRRequest class.
 * @constructor
 * Parameters to restore file or folders API.
 *
 * @member {string} objectType Polymorphic Discriminator
 *
 */
export interface ILRRequest {
  objectType: string;
}

/**
 * @class
 * Initializes a new instance of the ILRRequestResource class.
 * @constructor
 * The parameters to restore files or folders.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.objectType] Polymorphic Discriminator
 *
 */
export interface ILRRequestResource extends Resource {
  properties?: ILRRequest;
}

/**
 * @class
 * Initializes a new instance of the AzureBackupServerEngine class.
 * @constructor
 * The backup engine type when Azure Backup Server is used to manage the
 * backups.
 *
 */
export interface AzureBackupServerEngine extends BackupEngineBase {
}

/**
 * @class
 * Initializes a new instance of the DpmBackupEngine class.
 * @constructor
 * The backup engine type when Data Protection Manager (DPM) is used to manage
 * backups.
 *
 */
export interface DpmBackupEngine extends BackupEngineBase {
}

/**
 * @class
 * Initializes a new instance of the AzureSqlContainer class.
 * @constructor
 * Azure SQL workload-specific container.
 *
 */
export interface AzureSqlContainer extends ProtectionContainer {
}

/**
 * @class
 * Initializes a new instance of the IaaSVMContainer class.
 * @constructor
 * IaaS VM workload-specific container.
 *
 * @member {string} [virtualMachineId] The fully qualified Resource Manager URL
 * of the virtual machine represented by this Azure IaaS VM container.
 *
 * @member {string} [virtualMachineVersion] Specifies whether the container
 * represents a classic or a Resource Manager-deployed virtual machine.
 *
 * @member {string} [resourceGroup] The resource group name associated with the
 * Recovery Services vault.
 *
 */
export interface IaaSVMContainer extends ProtectionContainer {
  virtualMachineId?: string;
  virtualMachineVersion?: string;
  resourceGroup?: string;
}

/**
 * @class
 * Initializes a new instance of the MabContainerExtendedInfo class.
 * @constructor
 * Additional information for the container.
 *
 * @member {date} [lastRefreshedAt] The time stamp when this container was
 * refreshed.
 *
 * @member {string} [backupItemType] The type of backup items associated with
 * this container. Possible values include: 'Invalid', 'VM', 'FileFolder',
 * 'AzureSqlDb', 'SQLDB', 'Exchange', 'Sharepoint', 'DPMUnknown'
 *
 * @member {array} [backupItems] The list of backup items associated with this
 * container.
 *
 * @member {string} [policyName] The backup policy associated with this
 * container.
 *
 * @member {string} [lastBackupStatus] The latest backup status of this
 * container.
 *
 */
export interface MabContainerExtendedInfo {
  lastRefreshedAt?: Date;
  backupItemType?: string;
  backupItems?: string[];
  policyName?: string;
  lastBackupStatus?: string;
}

/**
 * @class
 * Initializes a new instance of the MabContainer class.
 * @constructor
 * The container associated with items backed up using Azure Backup Server.
 *
 * @member {boolean} [canReRegister] The container can be registered one more
 * time.
 *
 * @member {number} [containerId] The ID for the container.
 *
 * @member {number} [protectedItemCount] The number of backup items in the
 * container.
 *
 * @member {string} [agentVersion] The version of the agent used with this
 * container.
 *
 * @member {object} [extendedInfo] Additional information for the container.
 *
 * @member {date} [extendedInfo.lastRefreshedAt] The time stamp when this
 * container was refreshed.
 *
 * @member {string} [extendedInfo.backupItemType] The type of backup items
 * associated with this container. Possible values include: 'Invalid', 'VM',
 * 'FileFolder', 'AzureSqlDb', 'SQLDB', 'Exchange', 'Sharepoint', 'DPMUnknown'
 *
 * @member {array} [extendedInfo.backupItems] The list of backup items
 * associated with this container.
 *
 * @member {string} [extendedInfo.policyName] The backup policy associated with
 * this container.
 *
 * @member {string} [extendedInfo.lastBackupStatus] The latest backup status of
 * this container.
 *
 */
export interface MabContainer extends ProtectionContainer {
  canReRegister?: boolean;
  containerId?: number;
  protectedItemCount?: number;
  agentVersion?: string;
  extendedInfo?: MabContainerExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the IaaSVMProtectableItem class.
 * @constructor
 * This Azure VM workload-specific (also known as IaaS VM workload-specific)
 * backup item can be backed up.
 *
 * @member {string} [virtualMachineId] The fully qualified Resource Manager ID
 * of the virtual machine.
 *
 */
export interface IaaSVMProtectableItem extends WorkloadProtectableItem {
  virtualMachineId?: string;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMProtectedItemExtendedInfo class.
 * @constructor
 * Additional information for the Azure VM (also known as IaaS VM)-specific
 * backup item.
 *
 * @member {date} [oldestRecoveryPoint] The oldest backup copy available for
 * this backup item.
 *
 * @member {number} [recoveryPointCount] The number of backup copies available
 * for this backup item.
 *
 * @member {boolean} [policyInconsistent] Specifies if the backup policy
 * associated with the backup item is inconsistent.
 *
 */
export interface AzureIaaSVMProtectedItemExtendedInfo {
  oldestRecoveryPoint?: Date;
  recoveryPointCount?: number;
  policyInconsistent?: boolean;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMProtectedItem class.
 * @constructor
 * This Azure VM workload-specific (also known as IaaS VM workload-specific)
 * backup item has been backed up.
 *
 * @member {string} [friendlyName] The friendly name of the VM represented by
 * this backup item.
 *
 * @member {string} [virtualMachineId] The fully qualified Resource Manager ID
 * of the virtual machine represented by this item.
 *
 * @member {string} [protectionStatus] The backup status of this backup item.
 *
 * @member {string} [protectionState] The backup state of this backup item.
 * Possible values include: 'Invalid', 'IRPending', 'Protected',
 * 'ProtectionError', 'ProtectionStopped', 'ProtectionPaused'
 *
 * @member {string} [lastBackupStatus] The last backup operation status. The
 * possible values are: Healthy or Unhealthy.
 *
 * @member {date} [lastBackupTime] The timestamp of the last backup operation
 * for this backup item.
 *
 * @member {object} [extendedInfo] Additional information for this backup item.
 *
 * @member {date} [extendedInfo.oldestRecoveryPoint] The oldest backup copy
 * available for this backup item.
 *
 * @member {number} [extendedInfo.recoveryPointCount] The number of backup
 * copies available for this backup item.
 *
 * @member {boolean} [extendedInfo.policyInconsistent] Specifies if the backup
 * policy associated with the backup item is inconsistent.
 *
 */
export interface AzureIaaSVMProtectedItem extends ProtectedItem {
  friendlyName?: string;
  virtualMachineId?: string;
  protectionStatus?: string;
  protectionState?: string;
  lastBackupStatus?: string;
  lastBackupTime?: Date;
  extendedInfo?: AzureIaaSVMProtectedItemExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the MabFileFolderProtectedItemExtendedInfo class.
 * @constructor
 * Additional information for the backup item.
 *
 * @member {date} [lastRefreshedAt] The last day and time the agent synced with
 * the service.
 *
 * @member {date} [oldestRecoveryPoint] The oldest backup copy available.
 *
 * @member {number} [recoveryPointCount] The number of backup copies associated
 * with the backup item.
 *
 */
export interface MabFileFolderProtectedItemExtendedInfo {
  lastRefreshedAt?: Date;
  oldestRecoveryPoint?: Date;
  recoveryPointCount?: number;
}

/**
 * @class
 * Initializes a new instance of the MabFileFolderProtectedItem class.
 * @constructor
 * This is a file or folder workload-specific backup item.
 *
 * @member {string} [friendlyName] The friendly name of this backup item.
 *
 * @member {string} [computerName] The name of the computer associated with
 * this backup item.
 *
 * @member {string} [lastBackupStatus] The status of last backup operation.
 *
 * @member {string} [protectionState] The states for this property are:
 * Protected, ProtectionStopped, IRPending, or ProtectionError.
 *
 * @member {boolean} [isScheduledForDeferredDelete]
 *
 * @member {object} [extendedInfo] Additional information for this backup item.
 *
 * @member {date} [extendedInfo.lastRefreshedAt] The last day and time the
 * agent synced with the service.
 *
 * @member {date} [extendedInfo.oldestRecoveryPoint] The oldest backup copy
 * available.
 *
 * @member {number} [extendedInfo.recoveryPointCount] The number of backup
 * copies associated with the backup item.
 *
 */
export interface MabFileFolderProtectedItem extends ProtectedItem {
  friendlyName?: string;
  computerName?: string;
  lastBackupStatus?: string;
  protectionState?: string;
  isScheduledForDeferredDelete?: boolean;
  extendedInfo?: MabFileFolderProtectedItemExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the AzureSqlProtectedItemExtendedInfo class.
 * @constructor
 * Additional information for the Azure SQL specific backup item.
 *
 * @member {date} [oldestRecoveryPoint] The oldest backup copy available for
 * this item in the service.
 *
 * @member {number} [recoveryPointCount] The number of available backup copies
 * for this backup item.
 *
 * @member {string} [policyState] The state of the backup policy associated
 * with this backup item.
 *
 */
export interface AzureSqlProtectedItemExtendedInfo {
  oldestRecoveryPoint?: Date;
  recoveryPointCount?: number;
  policyState?: string;
}

/**
 * @class
 * Initializes a new instance of the AzureSqlProtectedItem class.
 * @constructor
 * This is an Azure SQL workload-specific backup item.
 *
 * @member {string} [protectedItemDataId] The internal ID of a backup item. The
 * internal ID is used by the Azure SQL Backup engine to contact Recovery
 * Services.
 *
 * @member {string} [protectionState] The backup state of the backup item.
 * Possible values include: 'Invalid', 'IRPending', 'Protected',
 * 'ProtectionError', 'ProtectionStopped', 'ProtectionPaused'
 *
 * @member {object} [extendedInfo] Additional information for this backup item.
 *
 * @member {date} [extendedInfo.oldestRecoveryPoint] The oldest backup copy
 * available for this item in the service.
 *
 * @member {number} [extendedInfo.recoveryPointCount] The number of available
 * backup copies for this backup item.
 *
 * @member {string} [extendedInfo.policyState] The state of the backup policy
 * associated with this backup item.
 *
 */
export interface AzureSqlProtectedItem extends ProtectedItem {
  protectedItemDataId?: string;
  protectionState?: string;
  extendedInfo?: AzureSqlProtectedItemExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the IaasVMBackupRequest class.
 * @constructor
 * This is an Azure VM (also known as IaaS VM) workload-specific backup
 * request.
 *
 * @member {date} [recoveryPointExpiryTimeInUTC] The backup copy will expire
 * after the time specified. The time is in UTC format.
 *
 */
export interface IaasVMBackupRequest extends BackupRequest {
  recoveryPointExpiryTimeInUTC?: Date;
}

/**
 * @class
 * Initializes a new instance of the SchedulePolicy class.
 * @constructor
 * The base class for backup schedules.
 *
 * @member {string} schedulePolicyType Polymorphic Discriminator
 *
 */
export interface SchedulePolicy {
  schedulePolicyType: string;
}

/**
 * @class
 * Initializes a new instance of the RetentionPolicy class.
 * @constructor
 * The base class for retention policy.
 *
 * @member {string} retentionPolicyType Polymorphic Discriminator
 *
 */
export interface RetentionPolicy {
  retentionPolicyType: string;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMProtectionPolicy class.
 * @constructor
 * Azure VM (also known as IaaS VM) workload-specific backup policy.
 *
 * @member {object} [schedulePolicy] The backup schedule specified as part of
 * backup policy.
 *
 * @member {string} [schedulePolicy.schedulePolicyType] Polymorphic
 * Discriminator
 *
 * @member {object} [retentionPolicy] The retention policy with the details on
 * backup copy retention ranges.
 *
 * @member {string} [retentionPolicy.retentionPolicyType] Polymorphic
 * Discriminator
 *
 */
export interface AzureIaaSVMProtectionPolicy extends ProtectionPolicy {
  schedulePolicy?: SchedulePolicy;
  retentionPolicy?: RetentionPolicy;
}

/**
 * @class
 * Initializes a new instance of the MabProtectionPolicy class.
 * @constructor
 * The backup policy for the file or folder container.
 *
 * @member {object} [schedulePolicy] The schedule specified in the backup
 * policy.
 *
 * @member {string} [schedulePolicy.schedulePolicyType] Polymorphic
 * Discriminator
 *
 * @member {object} [retentionPolicy] The details specified in the Retention
 * policy.
 *
 * @member {string} [retentionPolicy.retentionPolicyType] Polymorphic
 * Discriminator
 *
 */
export interface MabProtectionPolicy extends ProtectionPolicy {
  schedulePolicy?: SchedulePolicy;
  retentionPolicy?: RetentionPolicy;
}

/**
 * @class
 * Initializes a new instance of the AzureSqlProtectionPolicy class.
 * @constructor
 * The Azure SQL workload-specific backup policy.
 *
 * @member {object} [retentionPolicy] The retention policy details.
 *
 * @member {string} [retentionPolicy.retentionPolicyType] Polymorphic
 * Discriminator
 *
 */
export interface AzureSqlProtectionPolicy extends ProtectionPolicy {
  retentionPolicy?: RetentionPolicy;
}

/**
 * @class
 * Initializes a new instance of the KEKDetails class.
 * @constructor
 * The Key Encryption Key (KEK) is the encryption key for the Bitlocker
 * Encryption Key (BEK).
 *
 * @member {string} [keyUrl] Key refers to the Key Encryption Key (KEK). The
 * KEK is the Key to unlock the Secret.
 *
 * @member {string} [keyVaultId] Key Vault ID identifies where the KEK is
 * stored.
 *
 * @member {string} [keyBackupData] Key Backup Data refers to Key Encryption
 * Key (KEK) data.
 *
 */
export interface KEKDetails {
  keyUrl?: string;
  keyVaultId?: string;
  keyBackupData?: string;
}

/**
 * @class
 * Initializes a new instance of the BEKDetails class.
 * @constructor
 * BEK is Bitlocker Encrpytion Key.
 *
 * @member {string} [secretUrl] Secret refers to Bitlocker Encryption Key
 * (BEK). The Secret can be unlocked by the key (or KEK).
 *
 * @member {string} [secretVaultId] ID of the Key Vault where this Secret is
 * stored.
 *
 * @member {string} [secretData] Bitlocker Encryption Key (BEK) data.
 *
 */
export interface BEKDetails {
  secretUrl?: string;
  secretVaultId?: string;
  secretData?: string;
}

/**
 * @class
 * Initializes a new instance of the KeyAndSecretDetails class.
 * @constructor
 * BEK stands for Bitlocker Encryption Key.
 * KEK stands for Key Encryption Key. KEK is the encryption key used to protect
 * the Secret for the BEK
 * If the VM is encrypted, then the service stores the following details :
 * 1. Secret(BEK) - Url + Backup Data + vaultID.
 * 2. Key(KEK) - Url + Backup Data + vaultID.
 * It is possible for the BEK and KEK to have different vaultIDs.
 *
 * @member {object} [kekDetails] The Key Encryption Key (KEK) is the encryption
 * key for the Bitlocker Encryption Key (BEK).
 *
 * @member {string} [kekDetails.keyUrl] Key refers to the Key Encryption Key
 * (KEK). The KEK is the Key to unlock the Secret.
 *
 * @member {string} [kekDetails.keyVaultId] Key Vault ID identifies where the
 * KEK is stored.
 *
 * @member {string} [kekDetails.keyBackupData] Key Backup Data refers to Key
 * Encryption Key (KEK) data.
 *
 * @member {object} [bekDetails] BEK is Bitlocker Encrpytion Key.
 *
 * @member {string} [bekDetails.secretUrl] Secret refers to Bitlocker
 * Encryption Key (BEK). The Secret can be unlocked by the key (or KEK).
 *
 * @member {string} [bekDetails.secretVaultId] ID of the Key Vault where this
 * Secret is stored.
 *
 * @member {string} [bekDetails.secretData] Bitlocker Encryption Key (BEK)
 * data.
 *
 */
export interface KeyAndSecretDetails {
  kekDetails?: KEKDetails;
  bekDetails?: BEKDetails;
}

/**
 * @class
 * Initializes a new instance of the IaasVMRecoveryPoint class.
 * @constructor
 * Azure VM (also known as IaaS VM) workload-specific backup copy.
 *
 * @member {string} [recoveryPointType] Type of the backup copy.
 *
 * @member {date} [recoveryPointTime] The date and time when the backup copy
 * was created.
 *
 * @member {string} [recoveryPointAdditionalInfo] Additional information
 * associated with this backup copy.
 *
 * @member {string} [sourceVMStorageType] The storage type for the VM whose
 * backup copy was created.
 *
 * @member {boolean} [isSourceVMEncrypted] Identifies whether the VM was
 * encrypted when the backup copy is created.
 *
 * @member {object} [keyAndSecret] Required details for recovering an encrypted
 * VM. Applicable only when IsSourceVMEncrypted is true.
 *
 * @member {object} [keyAndSecret.kekDetails] The Key Encryption Key (KEK) is
 * the encryption key for the Bitlocker Encryption Key (BEK).
 *
 * @member {string} [keyAndSecret.kekDetails.keyUrl] Key refers to the Key
 * Encryption Key (KEK). The KEK is the Key to unlock the Secret.
 *
 * @member {string} [keyAndSecret.kekDetails.keyVaultId] Key Vault ID
 * identifies where the KEK is stored.
 *
 * @member {string} [keyAndSecret.kekDetails.keyBackupData] Key Backup Data
 * refers to Key Encryption Key (KEK) data.
 *
 * @member {object} [keyAndSecret.bekDetails] BEK is Bitlocker Encrpytion Key.
 *
 * @member {string} [keyAndSecret.bekDetails.secretUrl] Secret refers to
 * Bitlocker Encryption Key (BEK). The Secret can be unlocked by the key (or
 * KEK).
 *
 * @member {string} [keyAndSecret.bekDetails.secretVaultId] ID of the Key Vault
 * where this Secret is stored.
 *
 * @member {string} [keyAndSecret.bekDetails.secretData] Bitlocker Encryption
 * Key (BEK) data.
 *
 * @member {boolean} [isInstantILRSessionActive] Answer to the question - Is
 * the session to recover items from this backup copy still active.
 *
 */
export interface IaasVMRecoveryPoint extends RecoveryPoint {
  recoveryPointType?: string;
  recoveryPointTime?: Date;
  recoveryPointAdditionalInfo?: string;
  sourceVMStorageType?: string;
  isSourceVMEncrypted?: boolean;
  keyAndSecret?: KeyAndSecretDetails;
  isInstantILRSessionActive?: boolean;
}

/**
 * @class
 * Initializes a new instance of the GenericRecoveryPoint class.
 * @constructor
 * Generic backup copy.
 *
 * @member {string} [friendlyName] Friendly name of the backup copy.
 *
 * @member {string} [recoveryPointType] Type of the backup copy.
 *
 * @member {date} [recoveryPointTime] The time when this backup copy was
 * created.
 *
 * @member {string} [recoveryPointAdditionalInfo] Additional information
 * associated with this backup copy.
 *
 */
export interface GenericRecoveryPoint extends RecoveryPoint {
  friendlyName?: string;
  recoveryPointType?: string;
  recoveryPointTime?: Date;
  recoveryPointAdditionalInfo?: string;
}

/**
 * @class
 * Initializes a new instance of the EncryptionDetails class.
 * @constructor
 * Details needed if the VM was encrypted at the time of backup.
 *
 * @member {boolean} [encryptionEnabled] Identifies whether the backup copy
 * represents an encrypted VM at the time of backup.
 *
 * @member {string} [kekUrl] URL of the Key Encryption Key (KEK).
 *
 * @member {string} [secretKeyUrl] URL of the Bitlocker Encryption Key (BEK).
 *
 * @member {string} [kekVaultId] The ID of Key Vault where the Key Encryption
 * Key (KEK) is stored.
 *
 * @member {string} [secretKeyVaultId] The ID of Key Vault where the Bitlocker
 * Encryption Key (BEK), or Secret, is stored.
 *
 */
export interface EncryptionDetails {
  encryptionEnabled?: boolean;
  kekUrl?: string;
  secretKeyUrl?: string;
  kekVaultId?: string;
  secretKeyVaultId?: string;
}

/**
 * @class
 * Initializes a new instance of the IaasVMRestoreRequest class.
 * @constructor
 * IaaS VM workload-specific restore.
 *
 * @member {string} [recoveryPointId] The ID of the backup copy to be
 * recovered.
 *
 * @member {string} [recoveryType] The type of this recovery. Possible values
 * include: 'Invalid', 'OriginalLocation', 'AlternateLocation', 'RestoreDisks'
 *
 * @member {string} [sourceResourceId] The fully qualified Resource Manager ID
 * of the VM being recovered.
 *
 * @member {string} [targetVirtualMachineId] The complete Resource Manager ID
 * of the VM that will be created.
 * For example:
 * /subscriptions/{subId}/resourcegroups/{rg}/provider/Microsoft.Compute/virtualmachines/{vm}
 *
 * @member {string} [targetResourceGroupId] The Resource Manager ID of the
 * resource group you're creating for this VM and other artifacts.
 * For example: /subscriptions/{subId}/resourcegroups/{rg}
 *
 * @member {string} [storageAccountId] The fully qualified Resource Manager ID
 * of the storage account where the VM will be restored.
 *
 * @member {string} [virtualNetworkId] This is the virtual network ID of the
 * vnet that is attached to the virtual machine.
 * Your join action permissions are validated during the linked access.
 *
 * @member {string} [subnetId] Subnet ID is the identifier for the VM to be
 * restored. For Classic VMs the subnet ID would be
 * {VnetID}/Subnet/{SubnetName}, and for the Resource Manager VMs, the subnet
 * ID would be the Resource Manager resource ID used to represent the subnet.
 *
 * @member {string} [targetDomainNameId] The fully qualified Resource Manager
 * ID of the domain name to be associated with the VM being restored. Use the
 * Resource Manager ID to identify the domain, only for Classic-deployed
 * virtual machines.
 *
 * @member {string} [region] The region where the virtual machine is restored.
 *
 * @member {string} [affinityGroup] The affinity group associated with the VM
 * to be restored. Affinity groups are used only for Classic-deployed virtual
 * machines.
 *
 * @member {boolean} [createNewCloudService] Asks the question if a new cloud
 * service should be created while restoring the VM. If the answer is false,
 * the VM is restored to the same cloud service.
 *
 * @member {object} [encryptionDetails] If the VM was encrypted at the time of
 * backup, these details are needed.
 *
 * @member {boolean} [encryptionDetails.encryptionEnabled] Identifies whether
 * the backup copy represents an encrypted VM at the time of backup.
 *
 * @member {string} [encryptionDetails.kekUrl] URL of the Key Encryption Key
 * (KEK).
 *
 * @member {string} [encryptionDetails.secretKeyUrl] URL of the Bitlocker
 * Encryption Key (BEK).
 *
 * @member {string} [encryptionDetails.kekVaultId] The ID of Key Vault where
 * the Key Encryption Key (KEK) is stored.
 *
 * @member {string} [encryptionDetails.secretKeyVaultId] The ID of Key Vault
 * where the Bitlocker Encryption Key (BEK), or Secret, is stored.
 *
 */
export interface IaasVMRestoreRequest extends RestoreRequest {
  recoveryPointId?: string;
  recoveryType?: string;
  sourceResourceId?: string;
  targetVirtualMachineId?: string;
  targetResourceGroupId?: string;
  storageAccountId?: string;
  virtualNetworkId?: string;
  subnetId?: string;
  targetDomainNameId?: string;
  region?: string;
  affinityGroup?: string;
  createNewCloudService?: boolean;
  encryptionDetails?: EncryptionDetails;
}

/**
 * @class
 * Initializes a new instance of the IaasVMILRRegistrationRequest class.
 * @constructor
 * Restore files or folders from a backup copy, or recovery point, of an IaaS
 * (or Azure) VM.
 *
 * @member {string} [recoveryPointId] The ID of the IaaS VM recovery point used
 * to restore the files or folders.
 *
 * @member {string} [virtualMachineId] The fully qualified Resource Manager ID
 * of the VM used to restore the files or folders.
 *
 * @member {string} [initiatorName] The iSCSI initiator name.
 *
 * @member {boolean} [renewExistingRegistration] Whether to renew the existing
 * registration with the iSCSI server.
 *
 */
export interface IaasVMILRRegistrationRequest extends ILRRequest {
  recoveryPointId?: string;
  virtualMachineId?: string;
  initiatorName?: string;
  renewExistingRegistration?: boolean;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMErrorInfo class.
 * @constructor
 * Azure IaaS VM workload-specific error information.
 *
 * @member {number} [errorCode] Error code.
 *
 * @member {string} [errorTitle] Title: typically, the entity associated the
 * error.
 *
 * @member {string} [errorString] Localized error string.
 *
 * @member {array} [recommendations] List of localized recommendations for the
 * error string.
 *
 */
export interface AzureIaaSVMErrorInfo {
  errorCode?: number;
  errorTitle?: string;
  errorString?: string;
  recommendations?: string[];
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMJobTaskDetails class.
 * @constructor
 * Azure IaaS VM workload-specific job task details.
 *
 * @member {string} [taskId] The task display name.
 *
 * @member {date} [startTime] The start time.
 *
 * @member {date} [endTime] The end time.
 *
 * @member {string} [instanceId] The instance ID.
 *
 * @member {moment.duration} [duration] The time elapsed for the task.
 *
 * @member {string} [status] The status.
 *
 * @member {number} [progressPercentage] The progress of the task, as a
 * percentage.
 *
 */
export interface AzureIaaSVMJobTaskDetails {
  taskId?: string;
  startTime?: Date;
  endTime?: Date;
  instanceId?: string;
  duration?: moment.Duration;
  status?: string;
  progressPercentage?: number;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMJobExtendedInfo class.
 * @constructor
 * Additional information for the Azure IaaS VM workload-specific job.
 *
 * @member {array} [tasksList] List of tasks associated with this job.
 *
 * @member {object} [propertyBag] Job properties.
 *
 * @member {number} [progressPercentage] Indicates progress of the job. Null if
 * it has not started or completed.
 *
 * @member {string} [dynamicErrorMessage] Non-localized error message for job
 * execution.
 *
 */
export interface AzureIaaSVMJobExtendedInfo {
  tasksList?: AzureIaaSVMJobTaskDetails[];
  propertyBag?: { [propertyName: string]: string };
  progressPercentage?: number;
  dynamicErrorMessage?: string;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSVMJob class.
 * @constructor
 * The Azure IaaS VM workload-specifc job object.
 *
 * @member {moment.duration} [duration] The time that elapsed during the
 * execution of this job.
 *
 * @member {array} [actionsInfo] Gets or sets the state, or actions, applicable
 * on this job. Examples of the actions are: Cancel or Retry.
 *
 * @member {array} [errorDetails] Error details about this job.
 *
 * @member {string} [virtualMachineVersion] Specifies whether the backup item
 * is a Classic VM or a Resource Manager VM.
 *
 * @member {object} [extendedInfo] Additional information for this job.
 *
 * @member {array} [extendedInfo.tasksList] List of tasks associated with this
 * job.
 *
 * @member {object} [extendedInfo.propertyBag] Job properties.
 *
 * @member {number} [extendedInfo.progressPercentage] Indicates progress of the
 * job. Null if it has not started or completed.
 *
 * @member {string} [extendedInfo.dynamicErrorMessage] Non-localized error
 * message for job execution.
 *
 */
export interface AzureIaaSVMJob extends Job {
  duration?: moment.Duration;
  actionsInfo?: string[];
  errorDetails?: AzureIaaSVMErrorInfo[];
  virtualMachineVersion?: string;
  extendedInfo?: AzureIaaSVMJobExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the DpmErrorInfo class.
 * @constructor
 * DPM workload-specific error information.
 *
 * @member {string} [errorString] Localized error string.
 *
 * @member {array} [recommendations] The list of localized recommendations for
 * the error string.
 *
 */
export interface DpmErrorInfo {
  errorString?: string;
  recommendations?: string[];
}

/**
 * @class
 * Initializes a new instance of the DpmJobTaskDetails class.
 * @constructor
 * DPM workload-specific job task details.
 *
 * @member {string} [taskId] The task display name.
 *
 * @member {date} [startTime] The start time.
 *
 * @member {date} [endTime] The end time.
 *
 * @member {moment.duration} [duration] The time elapsed for task.
 *
 * @member {string} [status] The status.
 *
 */
export interface DpmJobTaskDetails {
  taskId?: string;
  startTime?: Date;
  endTime?: Date;
  duration?: moment.Duration;
  status?: string;
}

/**
 * @class
 * Initializes a new instance of the DpmJobExtendedInfo class.
 * @constructor
 * Additional information on the DPM workload-specific job.
 *
 * @member {array} [tasksList] List of tasks associated with this job.
 *
 * @member {object} [propertyBag] The job properties.
 *
 * @member {string} [dynamicErrorMessage] Non-localized error message on job
 * execution.
 *
 */
export interface DpmJobExtendedInfo {
  tasksList?: DpmJobTaskDetails[];
  propertyBag?: { [propertyName: string]: string };
  dynamicErrorMessage?: string;
}

/**
 * @class
 * Initializes a new instance of the DpmJob class.
 * @constructor
 * The DPM workload-specifc job object.
 *
 * @member {moment.duration} [duration] The time elapsed for the job.
 *
 * @member {string} [dpmServerName] DPM server name managing the backup item or
 * backup job.
 *
 * @member {string} [containerName] The name of the cluster or server
 * protecting the current backup item, if any.
 *
 * @member {string} [containerType] The type of container.
 *
 * @member {string} [workloadType] The type of backup item.
 *
 * @member {array} [actionsInfo] The state or actions applicable on this job,
 * such as Cancel or Retry.
 *
 * @member {array} [errorDetails] The errors.
 *
 * @member {object} [extendedInfo] Additional information for this job.
 *
 * @member {array} [extendedInfo.tasksList] List of tasks associated with this
 * job.
 *
 * @member {object} [extendedInfo.propertyBag] The job properties.
 *
 * @member {string} [extendedInfo.dynamicErrorMessage] Non-localized error
 * message on job execution.
 *
 */
export interface DpmJob extends Job {
  duration?: moment.Duration;
  dpmServerName?: string;
  containerName?: string;
  containerType?: string;
  workloadType?: string;
  actionsInfo?: string[];
  errorDetails?: DpmErrorInfo[];
  extendedInfo?: DpmJobExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the MabErrorInfo class.
 * @constructor
 * Azure Backup Server workload-specific error information.
 *
 * @member {string} [errorString] Localized error string.
 *
 * @member {array} [recommendations] List of localized recommendations.
 *
 */
export interface MabErrorInfo {
  errorString?: string;
  recommendations?: string[];
}

/**
 * @class
 * Initializes a new instance of the MabJobTaskDetails class.
 * @constructor
 * Azure Backup Server workload-specific job task details.
 *
 * @member {string} [taskId] The task display name.
 *
 * @member {date} [startTime] The start time.
 *
 * @member {date} [endTime] The end time.
 *
 * @member {moment.duration} [duration] Time elapsed for task.
 *
 * @member {string} [status] The status.
 *
 */
export interface MabJobTaskDetails {
  taskId?: string;
  startTime?: Date;
  endTime?: Date;
  duration?: moment.Duration;
  status?: string;
}

/**
 * @class
 * Initializes a new instance of the MabJobExtendedInfo class.
 * @constructor
 * Additional information for the Azure Backup Server workload-specific job.
 *
 * @member {array} [tasksList] List of tasks for this job.
 *
 * @member {object} [propertyBag] The job properties.
 *
 * @member {string} [dynamicErrorMessage] Non-localized error message specific
 * to this job.
 *
 */
export interface MabJobExtendedInfo {
  tasksList?: MabJobTaskDetails[];
  propertyBag?: { [propertyName: string]: string };
  dynamicErrorMessage?: string;
}

/**
 * @class
 * Initializes a new instance of the MabJob class.
 * @constructor
 * The Azure Backup Server workload-specific job.
 *
 * @member {moment.duration} [duration] The time required for the job to run.
 *
 * @member {array} [actionsInfo] The state or actions applicable on jobs such
 * as Cancel or Retry.
 *
 * @member {string} [mabServerName] The name of server protecting the data
 * store.
 *
 * @member {string} [mabServerType] Server type of the Azure Backup Server
 * container. Possible values include: 'Invalid', 'Unknown', 'IaasVMContainer',
 * 'IaasVMServiceContainer', 'DPMContainer', 'DPMVenusContainer',
 * 'MABContainer', 'ClusterResource', 'AzureSqlContainer', 'WindowsServer',
 * 'Windows'
 *
 * @member {string} [workloadType] Workload type of backup item. Possible
 * values include: 'Invalid', 'VM', 'FileFolder', 'AzureSqlDb', 'SQLDB',
 * 'Exchange', 'Sharepoint', 'DPMUnknown'
 *
 * @member {array} [errorDetails] The errors.
 *
 * @member {object} [extendedInfo] Additional information on the job.
 *
 * @member {array} [extendedInfo.tasksList] List of tasks for this job.
 *
 * @member {object} [extendedInfo.propertyBag] The job properties.
 *
 * @member {string} [extendedInfo.dynamicErrorMessage] Non-localized error
 * message specific to this job.
 *
 */
export interface MabJob extends Job {
  duration?: moment.Duration;
  actionsInfo?: string[];
  mabServerName?: string;
  mabServerType?: string;
  workloadType?: string;
  errorDetails?: MabErrorInfo[];
  extendedInfo?: MabJobExtendedInfo;
}

/**
 * @class
 * Initializes a new instance of the OperationResultInfo class.
 * @constructor
 * Information about the result of the operation.
 *
 * @member {array} [jobList] List of jobs created by this operation.
 *
 */
export interface OperationResultInfo extends OperationResultInfoBase {
  jobList?: string[];
}

/**
 * @class
 * Initializes a new instance of the ExportJobsOperationResultInfo class.
 * @constructor
 * This class is used to send blob details after exporting jobs.
 *
 * @member {string} [blobUrl] The URL of the blob. The serialized string, which
 * is the list of jobs, is exported to this URL.
 *
 * @member {string} [blobSasKey] The Shared Access Signatures (SAS) key used to
 * access the blob. The key expires after 15 minutes.
 *
 */
export interface ExportJobsOperationResultInfo extends OperationResultInfoBase {
  blobUrl?: string;
  blobSasKey?: string;
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSComputeVMContainer class.
 * @constructor
 * IaaS VM workload-specific backup item representing a Resource
 * Manager-deployed virtual machine.
 *
 */
export interface AzureIaaSComputeVMContainer extends IaaSVMContainer {
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSClassicComputeVMContainer class.
 * @constructor
 * IaaS VM workload-specific backup item representing a classic-deployed
 * virtual machine.
 *
 */
export interface AzureIaaSClassicComputeVMContainer extends IaaSVMContainer {
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSComputeVMProtectableItem class.
 * @constructor
 * IaaS VM workload-specific backup item representing a Resource Manager VM.
 *
 */
export interface AzureIaaSComputeVMProtectableItem extends IaaSVMProtectableItem {
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSClassicComputeVMProtectableItem class.
 * @constructor
 * IaaS VM workload-specific backup item representing a classic VM.
 *
 */
export interface AzureIaaSClassicComputeVMProtectableItem extends IaaSVMProtectableItem {
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSComputeVMProtectedItem class.
 * @constructor
 * IaaS VM workload-specific backup item representing the Resource Manager VM.
 *
 */
export interface AzureIaaSComputeVMProtectedItem extends AzureIaaSVMProtectedItem {
}

/**
 * @class
 * Initializes a new instance of the AzureIaaSClassicComputeVMProtectedItem class.
 * @constructor
 * IaaS VM workload-specific backup item representing the classic VM.
 *
 */
export interface AzureIaaSClassicComputeVMProtectedItem extends AzureIaaSVMProtectedItem {
}

/**
 * @class
 * Initializes a new instance of the OperationStatusJobExtendedInfo class.
 * @constructor
 * Extended information about the operation status job.
 *
 * @member {string} [jobId] ID of the job created for this backup item.
 *
 */
export interface OperationStatusJobExtendedInfo extends OperationStatusExtendedInfo {
  jobId?: string;
}

/**
 * @class
 * Initializes a new instance of the ClientScriptForConnect class.
 * @constructor
 * Client script details for the file or folder restore.
 *
 * @member {string} [scriptContent] File content of the client script for file
 * or folder restore.
 *
 * @member {string} [scriptExtension] File extension of the client script for
 * the file or folder restore. Some examples of the extension are: .ps1 and
 * .sh.
 *
 * @member {string} [osType] The operating system platform ("Windows" or
 * "Linux") for which this file or folder restore client script works.
 *
 */
export interface ClientScriptForConnect {
  scriptContent?: string;
  scriptExtension?: string;
  osType?: string;
}

/**
 * @class
 * Initializes a new instance of the InstantItemRecoveryTarget class.
 * @constructor
 * Target details for the file or folder restore.
 *
 * @member {array} [clientScripts] List of client scripts.
 *
 */
export interface InstantItemRecoveryTarget {
  clientScripts?: ClientScriptForConnect[];
}

/**
 * @class
 * Initializes a new instance of the OperationStatusProvisionILRExtendedInfo class.
 * @constructor
 * Extended information about the Item Level Recovery (ILR) provision action,
 * operation status.
 *
 * @member {object} [recoveryTarget] Target details for the file or folder
 * restore.
 *
 * @member {array} [recoveryTarget.clientScripts] List of client scripts.
 *
 */
export interface OperationStatusProvisionILRExtendedInfo extends OperationStatusExtendedInfo {
  recoveryTarget?: InstantItemRecoveryTarget;
}

/**
 * @class
 * Initializes a new instance of the OperationStatusJobsExtendedInfo class.
 * @constructor
 * Operation status extended info for the job list.
 *
 * @member {array} [jobIds] IDs of the jobs created for the backup item.
 *
 * @member {object} [failedJobsError] Stores all the failed jobs along with the
 * corresponding error codes.
 *
 */
export interface OperationStatusJobsExtendedInfo extends OperationStatusExtendedInfo {
  jobIds?: string[];
  failedJobsError?: { [propertyName: string]: string };
}

/**
 * @class
 * Initializes a new instance of the LongTermSchedulePolicy class.
 * @constructor
 * Long-term policy schedule.
 *
 */
export interface LongTermSchedulePolicy extends SchedulePolicy {
}

/**
 * @class
 * Initializes a new instance of the SimpleSchedulePolicy class.
 * @constructor
 * Simple policy schedule.
 *
 * @member {string} [scheduleRunFrequency] Defines the frequency interval
 * (daily or weekly) for the schedule policy. Possible values include:
 * 'Invalid', 'Daily', 'Weekly'
 *
 * @member {array} [scheduleRunDays] This list is the days of the week when the
 * schedule runs.
 *
 * @member {array} [scheduleRunTimes] List of times, during a day, when the
 * schedule runs.
 *
 * @member {number} [scheduleWeeklyFrequency] The number of times per week the
 * schedule runs.
 *
 */
export interface SimpleSchedulePolicy extends SchedulePolicy {
  scheduleRunFrequency?: string;
  scheduleRunDays?: string[];
  scheduleRunTimes?: Date[];
  scheduleWeeklyFrequency?: number;
}

/**
 * @class
 * Initializes a new instance of the RetentionDuration class.
 * @constructor
 * Retention duration.
 *
 * @member {number} [count] Count of the duration types. Retention duration is
 * determined by the combining the Count times and durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [durationType] The retention duration type of the retention
 * policy. Possible values include: 'Invalid', 'Days', 'Weeks', 'Months',
 * 'Years'
 *
 */
export interface RetentionDuration {
  count?: number;
  durationType?: string;
}

/**
 * @class
 * Initializes a new instance of the SimpleRetentionPolicy class.
 * @constructor
 * Simple policy retention.
 *
 * @member {object} [retentionDuration] Retention duration of the protection
 * policy.
 *
 * @member {number} [retentionDuration.count] Count of the duration types.
 * Retention duration is determined by the combining the Count times and
 * durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [retentionDuration.durationType] The retention duration
 * type of the retention policy. Possible values include: 'Invalid', 'Days',
 * 'Weeks', 'Months', 'Years'
 *
 */
export interface SimpleRetentionPolicy extends RetentionPolicy {
  retentionDuration?: RetentionDuration;
}

/**
 * @class
 * Initializes a new instance of the DailyRetentionSchedule class.
 * @constructor
 * Daily retention schedule.
 *
 * @member {array} [retentionTimes] The retention times of retention policy.
 *
 * @member {object} [retentionDuration] The retention duration of retention
 * policy.
 *
 * @member {number} [retentionDuration.count] Count of the duration types.
 * Retention duration is determined by the combining the Count times and
 * durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [retentionDuration.durationType] The retention duration
 * type of the retention policy. Possible values include: 'Invalid', 'Days',
 * 'Weeks', 'Months', 'Years'
 *
 */
export interface DailyRetentionSchedule {
  retentionTimes?: Date[];
  retentionDuration?: RetentionDuration;
}

/**
 * @class
 * Initializes a new instance of the WeeklyRetentionSchedule class.
 * @constructor
 * Weekly retention schedule.
 *
 * @member {array} [daysOfTheWeek] List of the days of the week for the weekly
 * retention policy.
 *
 * @member {array} [retentionTimes] Retention times of the retention policy.
 *
 * @member {object} [retentionDuration] Retention duration of retention policy.
 *
 * @member {number} [retentionDuration.count] Count of the duration types.
 * Retention duration is determined by the combining the Count times and
 * durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [retentionDuration.durationType] The retention duration
 * type of the retention policy. Possible values include: 'Invalid', 'Days',
 * 'Weeks', 'Months', 'Years'
 *
 */
export interface WeeklyRetentionSchedule {
  daysOfTheWeek?: string[];
  retentionTimes?: Date[];
  retentionDuration?: RetentionDuration;
}

/**
 * @class
 * Initializes a new instance of the Day class.
 * @constructor
 * Day of the week.
 *
 * @member {number} [date]
 *
 * @member {boolean} [isLast]
 *
 */
export interface Day {
  date?: number;
  isLast?: boolean;
}

/**
 * @class
 * Initializes a new instance of the DailyRetentionFormat class.
 * @constructor
 * Daily retention format.
 *
 * @member {array} [daysOfTheMonth] List of days of the month.
 *
 */
export interface DailyRetentionFormat {
  daysOfTheMonth?: Day[];
}

/**
 * @class
 * Initializes a new instance of the WeeklyRetentionFormat class.
 * @constructor
 * Weekly retention format.
 *
 * @member {array} [daysOfTheWeek] List of days of the week.
 *
 * @member {array} [weeksOfTheMonth] List of weeks of the month.
 *
 */
export interface WeeklyRetentionFormat {
  daysOfTheWeek?: string[];
  weeksOfTheMonth?: string[];
}

/**
 * @class
 * Initializes a new instance of the MonthlyRetentionSchedule class.
 * @constructor
 * The monthly retention schedule.
 *
 * @member {string} [retentionScheduleFormatType] Retention schedule format
 * type for monthly retention policy. Possible values include: 'Invalid',
 * 'Daily', 'Weekly'
 *
 * @member {object} [retentionScheduleDaily] Daily retention format for the
 * monthly retention policy.
 *
 * @member {array} [retentionScheduleDaily.daysOfTheMonth] List of days of the
 * month.
 *
 * @member {object} [retentionScheduleWeekly] Weekly retention format for the
 * monthly retention policy.
 *
 * @member {array} [retentionScheduleWeekly.daysOfTheWeek] List of days of the
 * week.
 *
 * @member {array} [retentionScheduleWeekly.weeksOfTheMonth] List of weeks of
 * the month.
 *
 * @member {array} [retentionTimes] Retention times of the retention policy.
 *
 * @member {object} [retentionDuration] Retention duration of the retention
 * policy.
 *
 * @member {number} [retentionDuration.count] Count of the duration types.
 * Retention duration is determined by the combining the Count times and
 * durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [retentionDuration.durationType] The retention duration
 * type of the retention policy. Possible values include: 'Invalid', 'Days',
 * 'Weeks', 'Months', 'Years'
 *
 */
export interface MonthlyRetentionSchedule {
  retentionScheduleFormatType?: string;
  retentionScheduleDaily?: DailyRetentionFormat;
  retentionScheduleWeekly?: WeeklyRetentionFormat;
  retentionTimes?: Date[];
  retentionDuration?: RetentionDuration;
}

/**
 * @class
 * Initializes a new instance of the YearlyRetentionSchedule class.
 * @constructor
 * Yearly retention schedule.
 *
 * @member {string} [retentionScheduleFormatType] Retention schedule format for
 * the yearly retention policy. Possible values include: 'Invalid', 'Daily',
 * 'Weekly'
 *
 * @member {array} [monthsOfYear] List of the months of year for the yearly
 * retention policy.
 *
 * @member {object} [retentionScheduleDaily] Daily retention format for the
 * yearly retention policy.
 *
 * @member {array} [retentionScheduleDaily.daysOfTheMonth] List of days of the
 * month.
 *
 * @member {object} [retentionScheduleWeekly] Weekly retention format for the
 * yearly retention policy.
 *
 * @member {array} [retentionScheduleWeekly.daysOfTheWeek] List of days of the
 * week.
 *
 * @member {array} [retentionScheduleWeekly.weeksOfTheMonth] List of weeks of
 * the month.
 *
 * @member {array} [retentionTimes] Retention times for the retention policy.
 *
 * @member {object} [retentionDuration] Retention duration for the retention
 * policy.
 *
 * @member {number} [retentionDuration.count] Count of the duration types.
 * Retention duration is determined by the combining the Count times and
 * durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [retentionDuration.durationType] The retention duration
 * type of the retention policy. Possible values include: 'Invalid', 'Days',
 * 'Weeks', 'Months', 'Years'
 *
 */
export interface YearlyRetentionSchedule {
  retentionScheduleFormatType?: string;
  monthsOfYear?: string[];
  retentionScheduleDaily?: DailyRetentionFormat;
  retentionScheduleWeekly?: WeeklyRetentionFormat;
  retentionTimes?: Date[];
  retentionDuration?: RetentionDuration;
}

/**
 * @class
 * Initializes a new instance of the LongTermRetentionPolicy class.
 * @constructor
 * Long-term retention policy.
 *
 * @member {object} [dailySchedule] Daily retention schedule of the backup
 * policy.
 *
 * @member {array} [dailySchedule.retentionTimes] The retention times of
 * retention policy.
 *
 * @member {object} [dailySchedule.retentionDuration] The retention duration of
 * retention policy.
 *
 * @member {number} [dailySchedule.retentionDuration.count] Count of the
 * duration types. Retention duration is determined by the combining the Count
 * times and durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [dailySchedule.retentionDuration.durationType] The
 * retention duration type of the retention policy. Possible values include:
 * 'Invalid', 'Days', 'Weeks', 'Months', 'Years'
 *
 * @member {object} [weeklySchedule] Weekly retention schedule of the backup
 * policy.
 *
 * @member {array} [weeklySchedule.daysOfTheWeek] List of the days of the week
 * for the weekly retention policy.
 *
 * @member {array} [weeklySchedule.retentionTimes] Retention times of the
 * retention policy.
 *
 * @member {object} [weeklySchedule.retentionDuration] Retention duration of
 * retention policy.
 *
 * @member {number} [weeklySchedule.retentionDuration.count] Count of the
 * duration types. Retention duration is determined by the combining the Count
 * times and durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [weeklySchedule.retentionDuration.durationType] The
 * retention duration type of the retention policy. Possible values include:
 * 'Invalid', 'Days', 'Weeks', 'Months', 'Years'
 *
 * @member {object} [monthlySchedule] Monthly retention schedule of the backup
 * policy.
 *
 * @member {string} [monthlySchedule.retentionScheduleFormatType] Retention
 * schedule format type for monthly retention policy. Possible values include:
 * 'Invalid', 'Daily', 'Weekly'
 *
 * @member {object} [monthlySchedule.retentionScheduleDaily] Daily retention
 * format for the monthly retention policy.
 *
 * @member {array} [monthlySchedule.retentionScheduleDaily.daysOfTheMonth] List
 * of days of the month.
 *
 * @member {object} [monthlySchedule.retentionScheduleWeekly] Weekly retention
 * format for the monthly retention policy.
 *
 * @member {array} [monthlySchedule.retentionScheduleWeekly.daysOfTheWeek] List
 * of days of the week.
 *
 * @member {array} [monthlySchedule.retentionScheduleWeekly.weeksOfTheMonth]
 * List of weeks of the month.
 *
 * @member {array} [monthlySchedule.retentionTimes] Retention times of the
 * retention policy.
 *
 * @member {object} [monthlySchedule.retentionDuration] Retention duration of
 * the retention policy.
 *
 * @member {number} [monthlySchedule.retentionDuration.count] Count of the
 * duration types. Retention duration is determined by the combining the Count
 * times and durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [monthlySchedule.retentionDuration.durationType] The
 * retention duration type of the retention policy. Possible values include:
 * 'Invalid', 'Days', 'Weeks', 'Months', 'Years'
 *
 * @member {object} [yearlySchedule] Yearly retention schedule of the backup
 * policy.
 *
 * @member {string} [yearlySchedule.retentionScheduleFormatType] Retention
 * schedule format for the yearly retention policy. Possible values include:
 * 'Invalid', 'Daily', 'Weekly'
 *
 * @member {array} [yearlySchedule.monthsOfYear] List of the months of year for
 * the yearly retention policy.
 *
 * @member {object} [yearlySchedule.retentionScheduleDaily] Daily retention
 * format for the yearly retention policy.
 *
 * @member {array} [yearlySchedule.retentionScheduleDaily.daysOfTheMonth] List
 * of days of the month.
 *
 * @member {object} [yearlySchedule.retentionScheduleWeekly] Weekly retention
 * format for the yearly retention policy.
 *
 * @member {array} [yearlySchedule.retentionScheduleWeekly.daysOfTheWeek] List
 * of days of the week.
 *
 * @member {array} [yearlySchedule.retentionScheduleWeekly.weeksOfTheMonth]
 * List of weeks of the month.
 *
 * @member {array} [yearlySchedule.retentionTimes] Retention times for the
 * retention policy.
 *
 * @member {object} [yearlySchedule.retentionDuration] Retention duration for
 * the retention policy.
 *
 * @member {number} [yearlySchedule.retentionDuration.count] Count of the
 * duration types. Retention duration is determined by the combining the Count
 * times and durationType.
 * For example, if Count = 3 and durationType = Weeks, then the retention
 * duration is three weeks.
 *
 * @member {string} [yearlySchedule.retentionDuration.durationType] The
 * retention duration type of the retention policy. Possible values include:
 * 'Invalid', 'Days', 'Weeks', 'Months', 'Years'
 *
 */
export interface LongTermRetentionPolicy extends RetentionPolicy {
  dailySchedule?: DailyRetentionSchedule;
  weeklySchedule?: WeeklyRetentionSchedule;
  monthlySchedule?: MonthlyRetentionSchedule;
  yearlySchedule?: YearlyRetentionSchedule;
}

/**
 * @class
 * Initializes a new instance of the BackupEngineBaseResource class.
 * @constructor
 * The base backup engine class. All workload-specific backup engines derive
 * from this class.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.friendlyName] The friendly name of the backup
 * engine.
 *
 * @member {string} [properties.backupManagementType] The type of backup
 * management associated with the backup engine. Possible values include:
 * 'Invalid', 'AzureIaasVM', 'MAB', 'DPM', 'AzureBackupServer', 'AzureSql'
 *
 * @member {string} [properties.registrationStatus] The status of the backup
 * engine registration with the Recovery Services vault.
 *
 * @member {string} [properties.healthStatus] The backup status of the backup
 * engine.
 *
 * @member {boolean} [properties.canReRegister] The flag indicating whether the
 * backup engine be registered again, once the engine has been initially
 * registered.
 *
 * @member {string} [properties.backupEngineId] The ID of the backup engine.
 *
 * @member {string} [properties.backupEngineType] Polymorphic Discriminator
 *
 */
export interface BackupEngineBaseResource extends Resource {
  properties?: BackupEngineBase;
}

/**
 * @class
 * Initializes a new instance of the BackupEngineBaseResourceList class.
 * @constructor
 * List of BackupEngineBase resources
 *
 * @member {array} [value] List of resources.
 *
 */
export interface BackupEngineBaseResourceList extends ResourceList {
  value?: BackupEngineBaseResource[];
}

/**
 * @class
 * Initializes a new instance of the OperationResultInfoBaseResource class.
 * @constructor
 * Base class for operation result information.
 *
 * @member {object} [properties]
 *
 * @member {string} [properties.objectType] Polymorphic Discriminator
 *
 */
export interface OperationResultInfoBaseResource extends OperationWorkerResponse {
  properties?: OperationResultInfoBase;
}

/**
 * @class
 * Initializes a new instance of the ProtectionPolicyResourceList class.
 * @constructor
 * The list of ProtectionPolicy resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface ProtectionPolicyResourceList extends ResourceList {
  value?: ProtectionPolicyResource[];
}

/**
 * @class
 * Initializes a new instance of the ProtectionContainerResourceList class.
 * @constructor
 * The list of ProtectionContainer resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface ProtectionContainerResourceList extends ResourceList {
  value?: ProtectionContainerResource[];
}

/**
 * @class
 * Initializes a new instance of the RecoveryPointResourceList class.
 * @constructor
 * The list of RecoveryPoint resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface RecoveryPointResourceList extends ResourceList {
  value?: RecoveryPointResource[];
}

/**
 * @class
 * Initializes a new instance of the ProtectedItemResourceList class.
 * @constructor
 * The list of ProtectedItem resources.
 *
 * @member {array} [value] The list of resources.
 *
 */
export interface ProtectedItemResourceList extends ResourceList {
  value?: ProtectedItemResource[];
}

/**
 * @class
 * Initializes a new instance of the WorkloadProtectableItemResourceList class.
 * @constructor
 * The list of WorkloadProtectableItem resources.
 *
 * @member {array} [value] List of resources.
 *
 */
export interface WorkloadProtectableItemResourceList extends ResourceList {
  value?: WorkloadProtectableItemResource[];
}

/**
 * @class
 * Initializes a new instance of the JobResourceList class.
 * @constructor
 * List of Job resources.
 *
 * @member {array} [value] List of resources.
 *
 */
export interface JobResourceList extends ResourceList {
  value?: JobResource[];
}

/**
 * @class
 * Initializes a new instance of the BackupEngineBaseResourceList class.
 * @constructor
 * List of BackupEngineBase resources
 *
 * @member {array} [value] List of resources.
 *
 */
export interface BackupEngineBaseResourceList extends ResourceList {
  value?: BackupEngineBaseResource[];
}


/**
 * @class
 * Initializes a new instance of the ProtectionPolicyResourceList class.
 * @constructor
 * The list of ProtectionPolicy resources.
 *
 */
export interface ProtectionPolicyResourceList extends Array<ProtectionPolicyResource> {
}

/**
 * @class
 * Initializes a new instance of the ProtectionContainerResourceList class.
 * @constructor
 * The list of ProtectionContainer resources.
 *
 */
export interface ProtectionContainerResourceList extends Array<ProtectionContainerResource> {
}

/**
 * @class
 * Initializes a new instance of the RecoveryPointResourceList class.
 * @constructor
 * The list of RecoveryPoint resources.
 *
 */
export interface RecoveryPointResourceList extends Array<RecoveryPointResource> {
}

/**
 * @class
 * Initializes a new instance of the ProtectedItemResourceList class.
 * @constructor
 * The list of ProtectedItem resources.
 *
 */
export interface ProtectedItemResourceList extends Array<ProtectedItemResource> {
}

/**
 * @class
 * Initializes a new instance of the WorkloadProtectableItemResourceList class.
 * @constructor
 * The list of WorkloadProtectableItem resources.
 *
 */
export interface WorkloadProtectableItemResourceList extends Array<WorkloadProtectableItemResource> {
}

/**
 * @class
 * Initializes a new instance of the JobResourceList class.
 * @constructor
 * List of Job resources.
 *
 */
export interface JobResourceList extends Array<JobResource> {
}

/**
 * @class
 * Initializes a new instance of the BackupEngineBaseResourceList class.
 * @constructor
 * List of BackupEngineBase resources
 *
 */
export interface BackupEngineBaseResourceList extends Array<BackupEngineBaseResource> {
}
